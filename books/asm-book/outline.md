# Программа курса (этап 2)

> Артефакт этапа 2 workflow из `books/self-education-book-create-plan.md` (далее «План», раздел 3).
> Самоучитель: **«Ассемблер x86-64 с нуля: от байта до языка C»** (`books/asm-book/`; название принято владельцем 2026-07-30).
> Нормативная база: План + требования этапа 0 `asm-book-requirements.md` (решения владельца Р1–Р6, FR-1…FR-7, NFR-1…NFR-5) + реестр источников этапа 1 `sources.md`.
> Статус: **v1.10 от 2026-08-03** — маковской сессией **погашен долг по прогону**: главы 1 и 2 прогнаны на обоих контурах, обе клетки трекера `[x]`, долг `Asm-learning.md` §9.4 пуст. **В17 закрыт целиком** (`lldb` на контуре M работает полностью, дословные сессии внесены во врезки 2.Ч1 и 2.Ч2); реестр добран до **25 источников** (T12 — man-страницы `xcode-select(1)` и `softwareupdate(1)`, повод — установка инструментов в 1.Ч3). Найдены и исправлены два расхождения: `stepi N` ≠ `si N` в карте T6 и внутреннее противоречие 2.Ч1 про точку останова. Перечень — §8. Ранее v1.9 от 2026-08-01 — решениями владельца закрыты оба вопроса захода 2: **В17** (отладчик выбирается по контуру — на L `gdb`, на M `lldb`; FR-6 переформулирован, 7.Ч2 даёт обе сессии) и **доступ к S2** (владелец кладёт тома 24592 и 24594 в `books/asm-book/extra-sources/`, читаются через `pdftotext`, — значит главы 3 и 4 пишутся с нормативной опорой). Перечень — §8. Ранее v1.8 того же дня — по итогам захода 2 (**глава 2 написана**, `chapters/02-data-and-addressing.md`, статус `[L]`, долг по контуру M — `Asm-learning.md` §9.4 пп. 12–19). Порождён **В17**: под WSL1 `lldb` не продолжает исполнение процесса, поэтому сессии отладчика контура L сняты в `gdb`; для FR-6 и 7.Ч2 нужно решение владельца. Записаны оговорка по недоступности текста S2 и превышение объёма главы; перечень изменений — §8. Ранее v1.7 того же дня — по итогам захода 1 уточнён состав исключения 1 (§5.4): образцы 1.Ч3 содержат строки глав 2 **и 5**, пролог функции обязателен из-за 16-байтового выравнивания стека (S1); перечень изменений — §8. **Этап 3 идёт: глава 1 написана** (`chapters/01-machine-and-tools.md`, статус `[L]`, долг по контуру M — `Asm-learning.md` §9.4). Ранее v1.6 того же дня — маковской сессией закрыты **В14** (исход Б: ассемблер контура M строку `.note.GNU-stack` не принимает, но и проблемы с правами стека на M нет — форма листингов книги зафиксирована окончательно, `Asm-learning.md` §2.1 п. 8) и **В15** (valgrind на M отсутствует; замена — ASan + `leaks(1)`, `libgmalloc` под Rosetta непригоден). **Блокер старта этапа 3 снят — глава 1 разблокирована.** Порождён **В16**: на M выход за границу блока кучи в *рукописной ассемблерной* программе не ловится ничем, ловит ли его valgrind на L — проба до захода 8. Перечень изменений — §8, протокол проб — `Asm-learning.md` §9.5. Ранее v1.5 от 2026-07-31 — закрыт **В13** добором реестра (T11 valgrind; S7 — арбитр по библиотечным функциям; T8 — деталям реализации L), порождён **В15** (диагностика кучи на контуре M). Ранее в тот же день v1.4: **В14 объявлен блокером старта этапа 3** (решение владельца), создан операционный документ `Asm-learning.md` (FR-7). Предыдущая v1.3 того же дня — правки по результатам прогонов на обоих контурах: закрыты **В10** (механизм ввода змейки), **В11** (форма листингов) и **В12** (форма таблицы переходов — кандидат Б, подтверждён на L и M); 8.Ч2 разблокирована, ограничение «до главы 4 закрыть В12» снято, `lldb` поставлен на контуре L; порождён **В14** (строка `.note.GNU-stack`). Перечень изменений — §8; протокол прогонов — требования §7.2, новые записи — `Asm-learning.md` §9. Этап 3 (написание глав) открывается по этой программе **после закрытия В14**.

---

## 1. Замысел программы

Книга ведёт читателя без формального бэкграунда по прямой линии: **байт → инструкция → программа → язык C**.

- **Главы 1–4 — машина без памяти и без функций.** Читатель учится думать регистрами: что такое байт и адрес, какие есть регистры, как переслать данные, посчитать, сравнить и разветвиться. К концу главы 4 он пишет законченные программы, в которых нет ничего, кроме регистров, флагов и переходов.
- **Главы 5–6 — программа как структура.** Стек и соглашение о вызове (центральная тема книги: без него не вызвать ни одной функции libc и не написать своей), затем данные в памяти — массивы, строки, структуры, куча, файлы. После главы 6 читатель располагает полным набором средств: вычисления, управление, вызовы, память.
- **Глава 7 — взгляд снаружи.** Тот же материал с другой стороны: что порождает компилятор из программы на C, как это читать в дизассемблере и отладчике, что делает оптимизатор. Здесь навык переворачивается: до сих пор читатель писал ассемблер, теперь он его читает.
- **Глава 8 — две итоговые работы.** Виртуальная машина байт-кода и змейка в терминале (решение Р6). Каждая задействует материал всех предыдущих глав сразу.
- **Глава 9 — мост в C.** Каждая конструкция C предъявляется как уже знакомая ассемблерная схема; сквозной пример — змейка из главы 8, переписанная на C. Книга заканчивается передачей эстафеты в `books/c-and-rust-book/`.

**Почему такой порядок.** Три зависимости жёстко задают последовательность и не допускают перестановок:

1. Адресация (гл. 2) нужна всему, что работает с памятью, поэтому идёт до арифметики над данными в памяти.
2. Флаги (гл. 3) — предпосылка ветвлений (гл. 4): условный переход читает результат предыдущего вычисления, а не сравнивает сам.
3. Соглашение о вызове (гл. 5) — предпосылка любой работы с libc «по правилам», а значит и всей главы 6 (`malloc`, `fopen`, `printf`) и обоих итоговых проектов.

Обратные зависимости отсутствуют: ни одна тема глав 1–7 не требует материала последующих глав. Исключения, которых потребовала сама дисциплина, объявлены в §5.4 — их два, и оба проговариваются читателю в тексте.

**Что читатель умеет в конце.** Написать, собрать, запустить и отладить на двух системах законченную программу на ассемблере x86-64 объёмом в несколько сотен строк, с собственными функциями, динамической памятью, файловым вводом-выводом и взаимодействием с терминалом; прочитать вывод компилятора и объяснить, во что превращается знакомая ему конструкция C.

---

## 2. Решения этапа 2

Черновой охват §4 требований принят с семью уточнениями. Каждое закрывает вопрос, явно оставленный этапу 2.

| # | Вопрос | Решение | Обоснование |
|---|---|---|---|
| **П1** | Вещественная арифметика — В6 требований §4.3 | **Минимум в главе 5 + обзорная часть 7.Ч4.** В 5.Ч4 вводится ровно столько, сколько нужно, чтобы напечатать `double`: правило `%al` для вариативных функций и передача аргумента в `%xmm0`. Полное изложение (регистры `%xmm`, скалярные SSE-инструкции, преобразования, класс SSE в psABI) — обзорная часть 7.Ч4 с явной пометкой о статусе | Ни один итоговый проект вещественной арифметики не требует, значит она не на критическом пути и по Плану §3 уместна как обзорная. Но `%al` не обзорный: без него `printf("%f")` — магия, а вызов вариативной функции нарушает ABI. Место 7.Ч4 отвечает требованию Плана «в конце»: это последняя машинная тема книги, главы 8–9 новых инструкций не вводят. Опоры есть: A1 §3.11, S1, S2 т. 1 (реестр §6 п. 4) |
| **П2** | Загрузка байт-кода для VM — варианты А/Б/В требований §4.1 | **Вариант А:** параграф про `fopen`/`fread`/`fclose` в части 6.Ч4, рядом с кучей | Файловый ввод-вывод нужен не только VM: чекпоинт 6 змейки — таблица рекордов в файле. Значит это не «требование одного проекта», а общая тема, и её место — в главе про данные, а не в главе-проекте. Буфер в куче и функция, которая его наполняет, — естественная пара |
| **П3** | Размещение промежуточных мини-проектов — FR-4 п. 2 | **Четыре мини-проекта**, замыкающие главы 4, 5, 6 и 7 (состав — §4) | Каждый ставится там, где впервые становится возможной программа нового класса: только логика (гл. 4), логика + собственные функции (гл. 5), + данные в памяти (гл. 6), + стыковка с C (гл. 7). Получается лестница в главу 8, а не четыре разрозненных упражнения |
| **П4** | Когда вводить отладчик — FR-6 | **Глава 2, часть 1** — минимальный набор (точка останова, шаг по инструкции, чтение регистров, чтение памяти) как инструмент наблюдения; систематически — часть 7.Ч2. **Выполнено заходом 2**; команды приводятся в написании того отладчика, который работает на контуре (FR-6 в редакции 2026-08-01): на L — `gdb`, на M — `lldb` | Глава 2 — про пересылку данных; увидеть, как меняется регистр, наглядно ровно тогда, когда изучаешь `mov`, а не через пять глав. Отложить отладчик до главы 7 значит четыре главы учить вслепую |
| **П5** | Где параграф о втором диалекте — NFR-3 | **Глава 2, часть 3** (последний параграф) | Раньше нечего переводить, позже поздно: с главы 2 читатель обращается к руководству AMD64 (S2) — нормативному арбитру по семантике инструкций, — а оно, как и руководства Intel, написано в Intel-синтаксисе (NFR-3). Без «переводчика» реестр источников для него наполовину нечитаем |
| **П6** | Форма листингов при двух контурах — NFR-2 | **Основная форма — контур L (ELF)**, отличия контура M — врезкой. Первая врезка (имена символов) — в 1.Ч3, вторая (директивы секций) — в 6.Ч3 | Дублировать каждый листинг в двух вариантах — удвоить объём книги ради одного символа подчёркивания. Врезка сохраняет одно каноническое написание и делает различие предметом изучения. Выбор ELF основной формой — потому что весь академический материал реестра (A1–A4) и вывод `gcc -S` на контуре L записаны так. **В11 закрыт прогоном 2026-07-31** (требования §7.2): ассемблер контура M принимает весь набор директив книги в написании GNU as, кроме секции неизменяемых данных, — решение остаётся в силе с двумя уточнениями. **(а)** `.section .rodata` на M отвергается, поэтому в листингах основного текста неизменяемые данные лежат в `.data`, а различие написаний (`.section .rodata` против `.const`/`.cstring`) разбирается врезкой № 2 в 6.Ч3. **(б)** Подчёркивание касается не одного символа, а всех глобальных имён и всех импортов libc (`call puts` → `call _puts`), поэтому врезка № 1 в 1.Ч3 даётся как правило перевода листинга, а не как замечание |
| **П7** | Дробление больших глав на заходы — NFR-5 | Главы **5, 6 и 7 — по два захода** (части 1–2, затем 3–4), глава **8 — четыре** (текст Ч1, текст Ч2, решения Ч1, решения Ч2) | Во всех трёх главах по 4 части при норме 800–1500 строк на главу; в главе 7 к этому добавляются мини-проект и обзорная часть. План §5 прямо допускает заход в одну часть для крупных глав; для главы 8 это уже зафиксировано решением по В8 |

---

## 3. Карта книги

Девять глав, 28 частей. Slug — имя файла в `chapters/` и `solutions/`.

| # | Slug | Название | Частей | Ключевой выход |
|---|---|---|---|---|
| 1 | `01-machine-and-tools` | Машина, число, инструменты | 3 | рабочее окружение и первая собранная программа |
| 2 | `02-data-and-addressing` | Данные и адресация | 3 | умение прочитать любой операнд |
| 3 | `03-arithmetic-and-flags` | Арифметика, биты, флаги | 3 | вычисления и понимание флагов |
| 4 | `04-control-flow` | Управление | 3 | вся логика C, но руками |
| 5 | `05-stack-and-calls` | Стек, функции, соглашение о вызове | 4 | центральная глава книги |
| 6 | `06-data-in-memory` | Данные в памяти | 4 | массивы, строки, структуры, куча, файлы |
| 7 | `07-reading-compiler-output` | Чтение вывода компилятора | 4 | навык, ради которого всё затевалось |
| 8 | `08-capstone` | Итоговые проекты: VM и змейка | 2 | два самостоятельных приложения |
| 9 | `09-bridge-to-c` | Мост в язык C | 2 | вход в самоучитель по C |

---

## 4. Главы

Идентификаторы источников — по реестру `sources.md` (A1…A5, S1…S8, T1…T11); в главах они проставляются в конце каждой части блоком «Источники» (правило реестра §5 п. 1). Курс CMU 15-213 (**A2**) в списках ниже не повторяется: он ведётся по A1 и служит не источником фактов, а методическим слоем — калибровкой порядка подачи и сложности заданий для глав 1–7. Каждая программа, попадающая в текст любой главы, предварительно прогоняется на обоих контурах (FR-5).

**Оговорка по libc.** T8 — руководство glibc, то есть документация реализации **контура L**; на контуре M библиотека другая. Поэтому арбитром по поведению библиотечных функций, которое книга объявляет верным на обеих системах, служит **S7 (POSIX.1-2024)**, где эти функции описаны нормативно, а T8 привлекается для деталей реализации L. **Реестр приведён в соответствие 2026-07-31** (закрытие В13): роли S7 и T8 разделены в `sources.md` §6 п. 8, страницы POSIX по `malloc()`, `fopen()`, `fread()`, `puts()`, `rand()`/`srand()` внесены в строку S7.

### Глава 1. Машина, число, инструменты — `01-machine-and-tools`

**Что даёт:** читатель понимает, из чего состоит память и что делает процессор, и получает работающее окружение на своей системе.
**Место в программе:** книга самодостаточна (FR-2, решение Р3) — ни двоичной системы, ни устройства памяти, ни установленного компилятора у читателя не предполагается. Без этой главы нельзя выполнить ни одного задания последующих.
**Источники:** A1 §2.1, A3 гл. 4–5, A5; T1, T2, T3; S2 т. 1.

- **Ч1. Числа, байты, память.** Двоичная и шестнадцатеричная записи и переводы между ними; бит, байт, разряды 8/16/32/64; знаковые и беззнаковые целые, дополнительный код, диапазоны; память как массив байтов, адрес и ячейка; коды символов (ASCII).
- **Ч2. Процессор и инструкции.** Цикл выборки-исполнения; регистры общего назначения x86-64 и их имена; счётчик инструкций `%rip`; машинная инструкция как последовательность байтов и зачем нужен язык ассемблера; чем ассемблер отличается от компилятора; почему у одной архитектуры три имени — x86-64, AMD64, Intel 64 (по титульным листам S2 и S3, без исторических отступлений).
- **Ч3. Инструменты и первая программа.** Установка тулчейна на обоих контурах; путь `.s` → исполняемый файл (`clang prog.s -o prog`), этапы «ассемблирование → компоновка», раздельное ассемблирование (`-c`) и компоновка; первая программа — вывод строки через `puts` (NFR-1: только libc, никаких сырых системных вызовов); **второй образец — печать числа через `printf`**, без которого не обойдётся ни одна программа глав 2–4; построчный разбор обоих шаблонов; чтение сообщений ассемблера и компоновщика. **Врезка L/M № 1:** имена символов — правило, а не замечание: на Mach-O ведущее подчёркивание несут и точка входа (`main` → `_main`), и каждый вызов libc (`call puts` → `call _puts`). Прогон 2026-07-31 (требования §7.2 п. 3) даёт материал для врезки: ELF-форма на маке **ассемблируется без замечаний** и падает только на компоновке с сообщением `Undefined symbols … "_main", referenced from: <initial-undefines>`, — то есть ошибка возникает не там, где написана, и это первый в книге случай такого рода. **Открытый вопрос В14 — той же части:** на контуре L листинг без завершающей строки `.section .note.GNU-stack,"",@progbits` компонуется с предупреждением `missing .note.GNU-stack section implies executable stack` и получает исполняемый стек (требования §7.2 п. 19). Предупреждение появится у читателя на самой первой сборке, поэтому часть обязана его объяснить; примет ли ту же строку ассемблер контура M — проверяется пробой §7.1 п. 7, и от ответа зависит, входит она в каноническую форму листинга или во врезку.

### Глава 2. Данные и адресация — `02-data-and-addressing`

**Что даёт:** умение прочитать и написать любой операнд — половина навыка чтения ассемблерного кода.
**Место в программе:** всё дальнейшее записывается операндами; арифметика (гл. 3) без них бессодержательна.
**Источники:** A1 §3.2–3.4, A3 §7.1–7.2, A4; S2 т. 1 и т. 3, S3; T1; T6, T7 (отладчик).

- **Ч1. Регистры и пересылка.** `mov` и суффиксы разрядности `b`/`w`/`l`/`q`; полные и частичные регистры (`%rax` / `%eax` / `%ax` / `%al`) и **правило записи в 32-битный регистр: старшая половина обнуляется, а при записи в `%ax`/`%al` — нет** (источник частых ошибок, арбитр S2); непосредственные операнды, пределы размера литерала и `movabs`; допустимые и недопустимые сочетания операндов (в частности, отсутствие пересылки «память → память»). **Отладчик как микроскоп** (решение П4): точка останова, шаг по инструкции, чтение регистров — каждая инструкция параграфа наблюдается в работе.
- **Ч2. Адресация памяти.** Косвенная адресация; полная форма `disp(base,index,scale)` и что означает каждая компонента; **минимальные директивы статических данных (`.quad`, `.byte`) — чтобы было что адресовать**; систематически данные разбираются в 6.Ч1, здесь они лишь материал для операндов; `lea` — вычисление адреса и попутно арифметическая инструкция; **RIP-относительная адресация как основная форма обращения к глобальным данным** (NFR-2: не продвинутая тема, а единственно правильная); просмотр памяти в отладчике.
- **Ч3. Размер, знак, порядок байтов.** Расширение при пересылке: `movz` и `movs`; знаковое и беззнаковое прочтение одних и тех же байтов; порядок байтов (little-endian) — наблюдение в памяти и в кодировании инструкций через `objdump -d`. **Параграф о втором диалекте** (NFR-3, решение П5): Intel-синтаксис — порядок операндов, префиксы, размерность; как переключить (`.intel_syntax noprefix`, ключи дизассемблера) и зачем это нужно при чтении S2/S3 и A5.

### Глава 3. Арифметика, биты, флаги — `03-arithmetic-and-flags`

**Что даёт:** умение вычислять — и понимание, что вычисление всегда оставляет след во флагах.
**Место в программе:** флаги — предпосылка главы 4: условный переход не сравнивает, а читает результат предыдущей инструкции.
**Источники:** A1 §2.2–2.3, §3.5, A3 §7.2–7.3; S2 т. 3, S3.

- **Ч1. Целочисленная арифметика.** `add`, `sub`, `inc`, `dec`, `neg`; умножение — двух- и трёхоперандный `imul` и однооперандные `mul`/`imul`; деление `idiv`/`div`, обязательное расширение делимого (`cqto`/`cltd`) и получение остатка; что происходит при переполнении и при делении на ноль.
- **Ч2. Биты: логика и сдвиги.** `and`, `or`, `xor`, `not`; логические и арифметические сдвиги `shl`/`shr`/`sar`; маски — установка, сброс, проверка и извлечение поля; типовые приёмы (обнуление через `xor`, умножение и деление сдвигом) и **их границы: для отрицательных чисел `sar` округляет вниз, а не к нулю, поэтому сдвигом нельзя заменить `idiv`** — обязательная рубрика «типичные ошибки» (A1 §2.3, S2 т. 3).
- **Ч3. Флаги.** Регистр RFLAGS: ZF, SF, CF, OF; какие инструкции их изменяют, а какие нет; знаковое переполнение против беззнакового переноса — почему это два разных флага; инструкции `set<cc>`; наблюдение флагов в отладчике.

### Глава 4. Управление — `04-control-flow`

**Что даёт:** вся управляющая логика языков высокого уровня, собранная руками из сравнений и переходов.
**Место в программе:** последняя глава «машины без памяти»: после неё читатель пишет законченные программы на одних регистрах, и естественным становится вопрос «а где хранить больше данных» — глава 5 и далее.
**Источники:** A1 §3.6 (включая §3.6.6 — мотивация условной пересылки), A3 §7.4, A4; S2 т. 3.

- **Ч1. Сравнение и ветвление.** `cmp` и `test`; безусловный `jmp` и семейство `j<cc>`; знаковые и беззнаковые условия — почему `ja` и `jg` не одно и то же; схемы `if`, `if-else`, вложенных условий; метки, в том числе локальные.
- **Ч2. Циклы.** Схемы `while`, `do-while`, `for` в переходах; счётчики и условия выхода; ранний выход и продолжение; вложенные циклы; почему компилятор предпочитает форму `do-while` (здесь как наблюдение, подтверждается прогоном в 7.Ч3).
- **Ч3. Без ветвлений и по таблице.** Условная пересылка `cmov`: чем она отличается от перехода и **почему вычисляются обе ветви — отсюда прямой запрет применять `cmov` там, где ветвь разыменовывает указатель или имеет побочный эффект**; мотивация — в объёме A1 §3.6.6, без собственной модели конвейера. `switch` через таблицу переходов: таблица в статических данных, косвенный переход, обязательная проверка диапазона. **Форма таблицы — кандидат Б: 32-битные смещения от базы самой таблицы** (`leaq tbl(%rip), %rax` → `movslq (%rax,%rcx,4), %rdx` → `addq %rax, %rdx` → `jmp *%rdx`), таблица лежит в неизменяемых данных. **В12 закрыт двухконтурно 2026-07-31** (требования §7.2, пп. 12–14 на M и 15–18 на L): форма работает в обоих размещениях на обеих системах, дополнительных релокаций не требует, и ровно её порождают `gcc -O2` и `clang -O2` на каждом контуре. Таблица абсолютных адресов `.quad` — материал рубрики «типичные ошибки», причём поучительна она именно тем, что **ведёт себя на контурах по-разному**: пока таблица лежит в изменяемых данных, вариант работает на обоих (ценой правки каждого элемента загрузчиком при старте), а стоит положить её туда, где ей место по смыслу, — в неизменяемую секцию, — как на M компоновка **отказывает** (`Illegal text-relocations`), а на L **проходит с предупреждением** (`relocation in read-only section`, `creating DT_TEXTREL in a PIE`) и оставляет в готовой программе флаг `TEXTREL`, то есть требование к загрузчику снять защиту записи. Отсюда вывод, который часть формулирует прямо: ошибка, пойманная на маке сразу, на Linux доживает до готовой программы. Происхождение варианта А тоже объясняется — его порождает непозиционно-независимая сборка (`gcc -O2 -fno-pie` даёт `jmp *.L4(,%rdi,8)` и таблицу `.quad`), которой книга не пользуется. Попутная выгода выбора: в 7.Ч3 читатель увидит в выводе компилятора ровно ту же конструкцию, что писал руками. **Мини-проект МП-1** — анализатор траекторий Коллатца: для диапазона 1…N (граница задана константой в исходном тексте — см. §5.4) найти число с самой длинной траекторией, её длину и максимум. Только регистры, флаги, вложенные циклы и `printf`; ни памяти, ни собственных функций — программа целиком из материала глав 2–4.

### Глава 5. Стек, функции, соглашение о вызове — `05-stack-and-calls`

**Что даёт:** умение писать и вызывать функции по правилам, действующим на обеих системах, — и понимать, почему нарушение этих правил ломает программу не там, где ошиблись.
**Место в программе:** центральная глава книги (требования §4). Всё, что дальше, — вызовы: `malloc`, `fopen`, `printf`, собственные подпрограммы обоих итоговых проектов.
**Заходов: два** (Ч1–Ч2, затем Ч3–Ч4) — решение П7.
**Источники:** A1 §3.7, A3 §7.5–7.6, A4; **S1 (нормативный арбитр)**, S4, S5; S7, T8 (libc).

- **Ч1. Стек и вызовы.** Стек как область памяти и регистр `%rsp`; в какую сторону растёт стек и что из этого следует для адресов (по описанию `push`/`pop` в S2); `call` и `ret`, адрес возврата на стеке; первая собственная функция; что именно ломается, если забыть `ret` или разбалансировать стек.
- **Ч2. Соглашение о вызове (System V AMD64 ABI).** Регистры аргументов `%rdi`, `%rsi`, `%rdx`, `%rcx`, `%r8`, `%r9` и возвращаемого значения `%rax`; передача седьмого и последующих аргументов через стек; регистры, сохраняемые вызывающим и вызываемым (caller/callee-saved); **выравнивание `%rsp` по 16 байт в точке `call` — универсальное требование ABI на обоих контурах**, а не особенность одной системы (S1); что именно ломается при нарушении и почему сбой возникает внутри чужой функции, а не в месте ошибки; red zone и её границы. Нормативный арбитр всей части — S1.
- **Ч3. Кадр стека и локальные переменные.** Пролог и эпилог; `%rbp` как база кадра и вариант работы без него; выделение места под локальные переменные; адрес локальной переменной через `lea` и передача указателя в функцию; типичная ошибка — возврат адреса локальной переменной.
- **Ч4. Рекурсия и библиотечные функции.** Рекурсия как цепочка кадров; глубина и переполнение стека; вызов функций libc по правилам части 2; вариативные функции и правило `%al` (число использованных векторных регистров), передача `double` в `%xmm0` — минимум для `printf("%f")`, решение П1. **Мини-проект МП-2** — ханойские башни с подсчётом ходов плюс числа Фибоначчи рекурсией и итерацией: несколько собственных функций с честным ABI, callee-saved регистры, значения, переживающие вызов.

### Глава 6. Данные в памяти — `06-data-in-memory`

**Что даёт:** раскладку данных: как в памяти лежат массив, строка и структура, откуда берётся динамическая память и как загрузить в неё содержимое файла.
**Место в программе:** обе итоговые работы — это программы над данными: байт-код в буфере и поле змейки. Без этой главы они невозможны.
**Заходов: два** (Ч1–Ч2, затем Ч3–Ч4) — решение П7.
**Источники:** A1 §3.8–3.9, A3 §7.7–7.9; **S1** (раскладка и выравнивание), **S7** (`malloc`/`free`, `fopen`/`fread`/`fclose` — нормативно на обоих контурах), T8 (детали реализации L), **T11** (диагностика кучи, только L); T1 (директивы).

- **Ч1. Массивы и индексная адресация.** Полный набор директив данных (`.byte`, `.word`, `.long`, `.quad`, `.zero`) — развитие минимума из 2.Ч2; адрес элемента как «база + индекс × размер» и почему множитель `scale` принимает именно значения 1/2/4/8; проход по массиву; двумерный массив и построчная раскладка.
- **Ч2. Строки.** Нуль-терминированная строка как массив байтов; собственные реализации длины, копирования и сравнения; строковые функции libc; форматный вывод `printf` с несколькими аргументами (теперь уже по правилам 5.Ч4, а не по образцу); арифметика над кодами символов.
- **Ч3. Структуры и выравнивание.** Структура как набор смещений от базового адреса; выравнивание полей и паддинг (арбитр — S1); массив структур и вычисление адреса поля; секции статических данных — изменяемые, неизменяемые, нулевые; **директивы выравнивания: `.align` толкуется по-разному в зависимости от цели, поэтому в книге используются `.p2align`/`.balign`** (T1; подтверждено прогоном 2026-07-31 — на Mach-O `.align 3` выровняло на 8 байт, то есть аргумент прочитан как степень двойки). **Врезка L/M № 2:** директивы секций (`.text`/`.data`/`.rodata` против формы `.section __TEXT,__text,…`). Конкретика по прогону (требования §7.2 п. 4): `.text` и `.data` пишутся одинаково на обоих контурах, а вот **секция неизменяемых данных — нет**: ELF-написание `.section .rodata` ассемблер контура M отвергает, там свои формы `.const`, `.cstring`, `.section __TEXT,__cstring`. Именно поэтому до этой части листинги книги держат неизменяемые данные в `.data` — здесь читателю объясняется, почему так и как записать то же самое на своём контуре. Часть опирается на документацию Apple и собственный прогон, оговорка реестра §6 п. 2 приводится в тексте.
- **Ч4. Куча и файлы.** `malloc`/`free`, время жизни объекта в куче, правило «кто выделил — тот освобождает»; типичные ошибки (утечка, освобождение дважды, обращение после освобождения) и их диагностика **valgrind/Memcheck на контуре L** (T11, внесён в реестр 2026-07-31): классы ошибок Memcheck и четыре категории утечек `--leak-check` — «definitely lost», «indirectly lost», «possibly lost», «still reachable» — дают готовую рубрику «типичные ошибки». Чем эта диагностика закрывается на контуре M, **не проверено** (в снимке тулчейна M valgrind не опрашивался) — открытый вопрос **В15**, проба до захода по главе 6; от ответа зависит, будет ли параграф одноконтурным с врезкой или двухконтурным; чтение файла в буфер: `fopen`, `fread`, `fclose`, обработка отказа (решение П2); аргументы командной строки как первый настоящий ввод программы. **Мини-проект МП-3** — частотный анализ файла: прочитать файл, имя которого взято из аргументов командной строки, в буфер кучи, посчитать частоты байтов в массиве счётчиков, отсортировать и напечатать таблицу. Задействует главы 3–6 разом и прямо готовит первый чекпоинт VM.

### Глава 7. Чтение вывода компилятора — `07-reading-compiler-output`

**Что даёт:** навык, ради которого книга затевалась: посмотреть, во что компилятор превратил программу, и объяснить каждую инструкцию.
**Место в программе:** после главы 6 у читателя есть все схемы, из которых состоит вывод компилятора, — значит его можно читать, а не угадывать. Одновременно это подготовка главы 9: мост в C строится на сопоставлении, которое здесь впервые предъявляется.
**Заходов: два** (Ч1–Ч2, затем Ч3–Ч4) — решение П7.
**Источники:** A1 §3.10, гл. 5, §3.11; A3 гл. 12; T2, T3 (`-S`, уровни оптимизации), T4, T5 (дизассемблирование), T6, T7 (отладчики), T10 (стенд, не источник); S1.

- **Ч1. От C к инструкциям: `gcc -S` и `clang -S`.** Короткие программы на C и их ассемблерный вывод; сопоставление с уже пройденными схемами (переменная, условие, цикл, вызов); **что в выводе служебное: директивы `.cfi_*`, метки вида `.L…`, `.type`/`.size`, суффикс `@PLT` у вызовов библиотечных функций** — иначе читатель тонет в шуме и не находит содержательные три строки. На контуре L к тому же списку добавляются `.section .note.GNU-stack` (та самая строка из главы 1), секция `.note.gnu.property` и префикс `notrack` у косвенного перехода — два последних порождает включённая по умолчанию защита потока управления (`-fcf-protection`); всё трое наблюдались в прогоне 2026-07-31 (требования §7.2 пп. 18–20). С этой части в тексте появляются фрагменты на C — знание языка не требуется, каждая конструкция тут же сводится к знакомой схеме (см. §5.4).
- **Ч2. Дизассемблирование и отладка чужого кода.** `objdump -d` на контуре L и `llvm-objdump` на контуре M — врезка о различиях вывода; символы, секции, соответствие «байты ↔ инструкция»; полная отладочная сессия — **по одной на каждый контур** (FR-6 в редакции 2026-08-01, решение владельца по В17): на L в `gdb`, на M в `lldb`, с одинаковым сценарием — точки останова, пошаговое исполнение, регистры, память, стек вызовов — и таблицей соответствия команд по T6. Материал 2.Ч1 здесь развивается, а не повторяется.
- **Ч3. `-O0` против `-O2`.** Что делает оптимизатор: свёртка констант, размещение переменных в регистрах, устранение лишних пересылок, инлайнинг, преобразования циклов (здесь же подтверждается наблюдение 4.Ч2 о форме `do-while`); почему оптимизированный код тяжелее отлаживать. Все утверждения — на прогонах обоих контуров, а не на общих словах. **Мини-проект МП-4** — собственная функция на ассемблере, скомпонованная с программой на C, против функции, порождённой компилятором из того же алгоритма: сравнение кода, размера и поведения.
- **Ч4 (обзорная — статус по Плану §3). Вещественная арифметика.** Регистры `%xmm`; скалярные SSE-инструкции пересылки и арифметики; преобразования «целое ↔ вещественное»; класс SSE в классификации аргументов psABI и связь с правилом `%al` из части 5.Ч4. Цель — уметь прочитать вещественный код компилятора; полнота изложения SSE не предполагается и оговаривается в тексте (решение П1). Это последняя машинная тема книги: главы 8 и 9 новых инструкций не вводят.

### Глава 8. Итоговые проекты: виртуальная машина и змейка — `08-capstone`

**Что даёт:** две законченные, работающие программы, каждая из которых требует материала всех предыдущих глав одновременно.
**Место в программе:** ставится **перед** мостом в C (требования §4): книга сначала закрывает ассемблер как самостоятельный навык.
**Форма:** проектная глава — проектные решения и их разбор вместо классических параграфов (допущение Плана §3, FR-4). Задания части — ступени проекта плюс расширения.
**Заходов: четыре** — текст Ч1, текст Ч2, решения Ч1, решения Ч2 (решение П7, вопрос В8 требований).
**Источники:** A1 §3.6.8 (таблицы переходов), A3 §7.9; **S7** (терминал, `poll`, `read`, библиотечные функции), **S8** и T9 (управляющие последовательности), T8 (детали реализации на L).

- **Ч1. Виртуальная машина байт-кода.** Восемь чекпоинтов по §4.1 требований: формат байт-кода и загрузка файла в память → стек операндов в куче с контролем границ → цикл выборки-декодирования-исполнения → диспетчер опкодов таблицей переходов (форма таблицы — по решению В12) → арифметика, сравнения и переходы VM → `CALL`/`RET` внутри VM с собственными кадрами в куче → ввод-вывод VM через libc → трассировка и диагностика ошибок байт-кода. Демонстрационная программа для VM — рекурсивный факториал или числа Фибоначчи.
- **Ч2. Змейка в терминале.** Шесть чекпоинтов по §4.2 требований: поле как двумерный массив и отрисовка кадра управляющими последовательностями → тело змейки кольцевым буфером в куче → сырой ввод без Enter (`stty raw -echo` плюс `poll(2)` с таймаутом кадра) и диспетчер клавиш таблицей переходов → движение, столкновения, поедание и рост → тайминг кадра по дедлайну и нарастание темпа, генерация еды через `rand`/`srand` → таблица рекордов в файле и восстановление терминала на всех путях выхода, включая аварийный. Проверка — сценарным прогоном: скрипт подаёт нажатия в псевдотерминал и сверяет кадры. ~~Заблокирована вопросом В10~~ — **разблокирована 2026-07-31**: механизм ввода подтверждён на обоих контурах (требования §7.2). Две поправки из маковского прогона, которые часть обязана учесть: нажатия, сделанные до включения сырого режима, приходят эхом и попадают в первый кадр (сброс входного буфера перед циклом), а под Rosetta от запуска до первого кадра проходит ≈0.13 с — заметная для игры задержка старта, но не кадра.

### Глава 9. Мост в язык C — `09-bridge-to-c`

**Что даёт:** понимание, что язык C — это способ записывать уже знакомые ассемблерные схемы короче, и готовность продолжать в `books/c-and-rust-book/`.
**Место в программе:** последняя глава, единственная точка связи с соседним самоучителем (FR-3, следствие Р2+Р3: связь односторонняя и только на выходе). **Пишется после 8.Ч2**, потому что её сквозной пример — та же змейка (§7).
**Источники:** A1 гл. 3 целиком, A3 гл. 6 и 10; S1, **S6** (Windows x64 ABI); реестр `books/c-and-rust-book/sources.md`.

- **Ч1. Конструкции C поверх знакомых схем.** Переменная ↔ регистр или ячейка; функция ↔ `call` и кадр стека; параметры ↔ регистры аргументов по S1; массив ↔ база и индексная адресация; указатель ↔ адрес; `struct` ↔ смещения полей и паддинг; автоматическая память ↔ стек, `malloc` ↔ куча; управляющие конструкции ↔ схемы главы 4. Каждая пара — не аналогия, а тот же самый вывод компилятора, прочитанный по правилам главы 7.
- **Ч2. Та же змейка на C и передача эстафеты.** Змейка из части 8.Ч2, переписанная на C: та же программа, которую читатель собрал руками, — в полутора экранах кода; построчное сопоставление с собственной ассемблерной версией. Формула «C — переносимый ассемблер» как вывод, а не лозунг: что именно в ней верно и в чём она обманывает. **Врезка про Windows x64 ABI** (S6) — для читателей, которые пойдут в C-книгу по её виндовому контуру: другие регистры аргументов, теневая область, другой список volatile-регистров. Передача читателя в `books/c-and-rust-book/` с указанием, что в её главе 1 можно пролистать.

---

## 5. Сквозные линии

### 5.1. Врезки о различиях контуров (NFR-2)

Каждая врезка попадает в текст только после прогона на обоих контурах (FR-5, правило реестра §5 п. 5). ✅ отмечает то, что уже установлено прогоном; остальное подлежит проверке при написании соответствующей части.

| Расхождение | Вводится | Арбитр и состояние |
|---|---|---|
| Имена символов: подчёркивание на Mach-O несут **все** глобальные имена и все импорты libc, а не только `main` | 1.Ч3 | S4, S5 + ✅ прогон на M 2026-07-31 (требования §7.2 п. 3): ассемблирование проходит, ошибка — на компоновке |
| Директивы секций (`.text`/`.data` против формы `.section __TEXT,__text,…`) | 6.Ч3, кратко упомянуто в 1.Ч3 | S5 + ✅ прогон на M 2026-07-31 (оговорка реестра §6 п. 2) |
| **Секция неизменяемых данных**: `.section .rodata` (L) против `.const` / `.cstring` / `.section __TEXT,__cstring` (M) — на M ELF-написание не принимается вовсе | 6.Ч3 (врезка № 2); до неё листинги держат такие данные в `.data` | T1 + ✅ прогон на M 2026-07-31 (требования §7.2 п. 4) |
| Толкование `.align`: на Mach-O аргумент — степень двойки, поэтому в книге всюду `.p2align`/`.balign` | 6.Ч3 | T1 + ✅ прогон на M 2026-07-31 (`.align 3` дало выравнивание на 8); ELF-сторона сверяется прогоном на L при написании части |
| RIP-относительная адресация как единственная форма доступа к глобальным данным | 2.Ч2 | S1, S4 |
| Как проявляется нарушение 16-байтового выравнивания стека: само требование универсально (S1), а сбой зависит от реализации libc и потому может выглядеть по-разному на L и M | 5.Ч2 | **S1**, различие — по результату прогона |
| Форма таблицы переходов при позиционно-независимой сборке: сама форма (кандидат Б) одна на оба контура, различается **поведение отвергнутого варианта А** в неизменяемой секции — на M отказ компоновки, на L предупреждение и `DT_TEXTREL` | 4.Ч3 | S1 + ✅ прогнан на обоих контурах 2026-07-31 (требования §7.2 пп. 12–18); **В12 закрыт** |
| Примечание `.section .note.GNU-stack,"",@progbits` в конце листинга: на L без него сборка даёт предупреждение и **исполняемый стек** (`GNU_STACK` с правами `RWE`); ассемблер M строку не принимает вовсе, и проблемы прав стека там нет | 1.Ч3 (врезка № 1) | T1 + ✅ прогон на L 2026-07-31 (требования §7.2 п. 19) + ✅ прогон на M 2026-08-01: **В14 закрыт**, исход Б (`Asm-learning.md` §9.2 пп. 11–13) |
| `objdump` (binutils) против `llvm-objdump`: различия вывода | 7.Ч2, частично 2.Ч3 | T4, T5 + ✅ прогон на M 2026-08-03: `-M intel` LLVM понимает, но печатает полные адреса, комментарий через `##`, секцию `__TEXT,__text` и вызов libc через `__stubs` без имени функции; мнемоника — `movabsq`, а не `movabs` (§9.2 п. 34) |

Ниже — расхождения, найденные при погашении долга по прогону 2026-08-03 (`Asm-learning.md` §9.2 пп. 28–34). Все уже внесены в главы 1 и 2 врезками.

| Расхождение | Вводится | Арбитр и состояние |
|---|---|---|
| **На M ассемблер один** (`as` — обёртка над `clang -cc1as`), поэтому пары «clang / GNU as» в рубриках ошибок вырождаются в первую строку; плата — потеря информативности (`incorrect register '%rax' used with 'l' suffix` порождает только GNU as) | 1.Ч3 (§1.3.6), 2.Ч1 (§2.1.4) | ✅ прогон на M 2026-08-03: проверено на всех ошибках глав 1–2 (§9.2 п. 28) |
| **Словарь компоновщика M**: `Undefined symbols for architecture x86_64:` вместо `undefined reference`; `<initial-undefines>` вместо `Scrt1.o`/`_start` | 1.Ч3 (§1.3.6) | ✅ прогон на M 2026-08-03 (§9.2 п. 29) |
| **Этап, на котором ловится абсолютная адресация**: на M — ассемблер (`error: 32-bit absolute addressing is not supported in 64-bit mode`), на L — компоновщик (`R_X86_64_32S … PIE`). Затрагивает дидактический тезис «ошибка не там, где написана» | 2.Ч2 (§2.2.4) | ✅ прогон на M 2026-08-03 (§9.2 п. 33). Побочно: при сборке драйвером сообщение называет временную копию файла в `/var/folders/…`, при вызове `as` — настоящий файл |
| **Имена секций в дампах**: `-j .data` на M не находит ничего, маковское имя — `__data` (`__DATA,__data`); адреса другие, **содержимое побайтово совпадает** | 2.Ч2 (§2.2.1), 2.Ч3 (§2.3.3), развивается в 6.Ч3 | ✅ прогон на M 2026-08-03 (§9.2 п. 34) |
| **Мелочи прогона, вошедшие в текст**: код возврата `main` после `puts` (L — 4, M — 10); `file` на объектнике (`Mach-O 64-bit object x86_64`); текст отказа при запуске `.o` печатает оболочка, а не система (bash и zsh говорят по-разному), код возврата 126 на обоих контурах | 1.Ч3 (§1.3.3, §1.3.5) | S7 (про `puts` обещано лишь неотрицательное значение) + ✅ прогоны на L 2026-08-01 и на M 2026-08-03 |
| **Отладчик выбирается по контуру: на L — `gdb`, на M — `lldb`** (редакция FR-6 от 2026-08-01, решение владельца по В17) | 2.Ч1 (минимум), 7.Ч2 (полно — **обе** сессии) | T6, T7 + ✅ состав контура M сверен 2026-07-31 (`lldb` есть, `gdb` отсутствует) + ✅ прогон на L 2026-08-01: `lldb` под WSL1 не продолжает исполнение, `gdb` работает (`Asm-learning.md` §9.2 п. 21) + ✅ прогон на M 2026-08-03: `lldb` там работает полностью, дословная сессия снята и внесена в 2.Ч1 и 2.Ч2 (§9.2 пп. 30–32). Уточнение к T6: `stepi N` — это `si -c N`, а не `si N` |

### 5.2. Линия инструментов (FR-6)

| Инструмент | Впервые | Систематически |
|---|---|---|
| Ассемблер и компоновщик (`clang`/`gcc`, `as`), раздельная сборка | 1.Ч3 | 7.Ч3 |
| Отладчик — по контуру: `gdb` на L, `lldb` на M (FR-6, ред. 2026-08-01) | 2.Ч1 | 7.Ч2 (обе сессии) |
| Дизассемблер (`objdump` / `llvm-objdump`) | 2.Ч3 (кодирование инструкций и порядок байтов) | 7.Ч2 |
| `gcc -S` / `clang -S` | 7.Ч1 | 7.Ч1, 7.Ч3 |
| Диагностика ошибок кучи (только контур L) | 6.Ч4 | 8.Ч1 |
| Compiler Explorer — **стенд, не источник** (реестр T10) | 7.Ч1 | — |

### 5.3. Практика: три уровня (FR-4)

1. **Задания в каждой части** — 6–15 штук с идентификаторами `Г{глава}.Ч{часть}.З{номер}` в собственном пространстве имён книги; распределение сложности 30/40/30 (План §2.2). Верхние ~30 % каждой части — законченная работающая программа с заданными входом, выходом и ограничениями, а не дописывание строки. **Исключение — части 1.Ч1 и 1.Ч2:** инструментов у читателя ещё нет (они ставятся в 1.Ч3), поэтому там верхняя треть — задачи на разбор и расчёт (переводы систем счисления, чтение дампов памяти, ручное декодирование), а программы начинаются с 1.Ч3. Ориентировочный итог по 28 частям — 250–300 заданий.
2. **Мини-проекты** (решение П3) — последние задания частей 4.Ч3, 5.Ч4, 6.Ч4 и 7.Ч3: МП-1 анализатор Коллатца (только регистры и управление), МП-2 ханойские башни и Фибоначчи (собственные функции, ABI, рекурсия), МП-3 частотный анализ файла, имя которого взято из аргументов командной строки (куча, файлы, массивы, сортировка), МП-4 ассемблерная функция в связке с программой на C.
3. **Итоговые работы** — глава 8, по проекту в каждой части.

**Покрытие материала итоговыми проектами** (проверка требования «составной», FR-4):

| Глава | VM (8.Ч1) | Змейка (8.Ч2) |
|---|---|---|
| 2. Данные и адресация | декодирование операндов байт-кода | двумерное поле, `disp(base,index,scale)` |
| 3. Арифметика, биты, флаги | извлечение полей опкода, арифметика VM | координаты, счёт, генерация еды |
| 4. Управление | диспетчер опкодов таблицей переходов | диспетчер клавиш таблицей переходов |
| 5. Стек и вызовы | собственные кадры VM в куче + настоящие кадры | вызовы libc, разбиение на функции |
| 6. Данные в памяти | буфер байт-кода, стек операндов, структуры | кольцевой буфер тела, файл рекордов |
| 7. Чтение вывода компилятора | отладка собственной VM | сценарная проверка, разбор сбоев |

Ни одна глава не остаётся незадействованной; ни один проект не собирается знанием половины книги.

### 5.4. Монотонность и два объявленных исключения

Правило Плана: глава N опирается только на главы 1…N−1. В программе выполняется, но у дисциплины есть петля, которую нельзя развязать полностью: **вывод нужен с первой страницы, а вывод — это вызов функции libc, то есть соглашение о вызове из главы 5**. Петля разрезается двумя исключениями, и оба объявляются читателю прямо в тексте, а не умалчиваются.

1. **Программы-образцы в 1.Ч3** (`puts` и `printf` с числом) содержат инструкции, разбираемые в главах 2 **и 5**. Они подаются именно как образцы с построчным комментарием и отсылкой «правила — дальше»; читатель их не сочиняет, а собирает и изменяет по заданию. Состав уточнён заходом 1 (2026-08-01), когда образец был написан целиком: к главе 2 относятся пересылка и адресация (`leaq msg(%rip), %rdi`, `movl $0, %eax`), к главе 5 — обрамление функции (`pushq %rbp` / `movq %rsp, %rbp` … `popq %rbp`), `call`/`ret` и правило `%al` для вариативных функций (`movl $0, %eax` перед `call printf`). Обрамление входит в образец не для красоты: без него в точке `call` нарушается 16-байтовое выравнивание стека, которого требует ABI (S1); на контуре L это нарушение ничем не проявляется (`Asm-learning.md` §9.2 п. 19), поэтому текст ссылается на требование ABI, а не на несуществующий сбой. В главе 1 состав образца выписан таблицей «что в образце из будущих глав» — новых исключений не появилось, назван точный состав имеющихся (обоснование — `Asm-learning.md` §2.1 п. 9).
2. **Вызовы `puts`/`printf` в главах 1–4 идут по объявленному правилу-образцу**: «первый аргумент кладётся в `%rdi`, второй в `%rsi`, третий в `%rdx`, затем `call`; почему именно так — глава 5». До главы 5 читатель не пишет собственных функций и не вызывает ничего, кроме функций вывода, поэтому образца достаточно, а полное соглашение (сохраняемые регистры, выравнивание стека, вариативные функции) вводится в 5.Ч2 и 5.Ч4 уже как объяснение знакомого приёма. Прямое следствие для практики: **до главы 5 программы не читают ввод** — данные задаются константами в исходном тексте; ввод появляется в 6.Ч2 (строки) и 6.Ч4 (файлы и аргументы командной строки).

Прочие потенциальные забегания сняты правкой программы, а не оговоркой: минимальные директивы статических данных введены в 2.Ч2 (иначе в 4.Ч3 таблица переходов опиралась бы на материал главы 6), а глава 9 переставлена в очередь после 8.Ч2, потому что её сквозной пример — змейка из этой части. Проверка отсутствия **других** забеганий вперёд — отдельный пункт чеклиста ревизии (этап 4), особенно для пар «адресация ↔ ABI» и «стек ↔ куча», взаимозависимых по существу дисциплины.

Отдельно оговаривается статус языка C в главах 7 и 9: фрагменты на C приводятся как **исходный материал для сопоставления**, а не как предмет изучения. Ни одно задание книги не требует писать на C.

### 5.5. Чего в книге нет

Границы объявляются в предисловии, чтобы читатель не искал ненаписанного:

- сырые системные вызовы (NFR-1) — весь ввод-вывод через libc, потому что номера и соглашения syscall на L и M различаются;
- 32-битный режим и 16-битное прошлое x86 — только упоминание в 1.Ч2;
- системное программирование: страницы, прерывания, кольца защиты, загрузчики;
- устройство процессора: конвейер, кеши, предсказание переходов — только в объёме A1 §3.6.6, необходимом, чтобы мотивировать `cmov` (4.Ч3);
- векторный SIMD (обработка нескольких значений одной инструкцией) — в 7.Ч4 только скалярный SSE, обзорно;
- x87 и `long double` — упоминание в 7.Ч4 без изложения;
- другие архитектуры (ARM64 и прочие) и нативная сборка под Windows: читатель на Windows работает через WSL — это контур L.

---

## 6. Открытые вопросы

| # | Вопрос | Влияние | Действие |
|---|---|---|---|
| ~~В14~~ | Примет ли ассемблер контура M строку `.section .note.GNU-stack,"",@progbits`. На L без неё каждый рукописный листинг книги собирается с предупреждением компоновщика и получает **исполняемый стек** (требования §7.2 п. 19); компилятор ставит эту строку в свой вывод всегда | — | **Закрыт 2026-08-01 пробой на маке, исход Б** (протокол — `Asm-learning.md` §9.5). Строка отвергается: `error: unexpected token in '.section' directive`; дефис ни при чём — отвергается сама ELF-форма (`.section` на Mach-O — «сегмент, секция», `@progbits` — тип секции ELF). Вторая половина пробы закрыта отрицательно: **на M проблемы с правами стека нет** — стек неисполняемый по умолчанию (проверено исполнением кода со стека: SIGSEGV), а опции `-allow_stack_execute` в `ld-1167.5` нет. Итог: строка входит в **канонический (L) листинг**, врезка № 1 в 1.Ч3 убирает её для M (`Asm-learning.md` §2.1 п. 8 — там же разобрано, почему направление врезки обратно первоначальной формулировке). **Блокер старта этапа 3 снят** |
| ~~В13~~ | Реестр источников не покрывает две вещи: инструмент диагностики кучи для 6.Ч4 (на контуре L — valgrind) и семантику библиотечных функций на **обоих** контурах (T8 — это glibc, то есть только L) | — | **Закрыт 2026-07-31 добором реестра** (`sources.md` §2, §3, §4, §5 п. 3, §6 пп. 7–8): внесён **T11** — Valgrind User Manual, гл. 4 Memcheck; нормативным арбитром по библиотечным функциям стал **S7**, T8 сужен до деталей реализации L. Ограничение «до главы 6» снято |
| ~~В15~~ | Чем на контуре M закрывается диагностика ошибок кучи. T11 (valgrind) внесён в реестр как инструмент контура L; наличие valgrind или замены на маке никогда не опрашивалось | — | **Закрыт 2026-08-01 пробой на маке** (`Asm-learning.md` §9.2 пп. 14–15, §9.5). Valgrind на M **отсутствует**. Из штатных замен под Rosetta не годится `libgmalloc` — вешает любой x86-64 процесс, даже исправный. Рабочая пара: **ASan** (`-fsanitize=address`) на переполнение и use-after-free — но `detect_leaks` на этой платформе не поддерживается — плюс **`leaks(1)`** с `MallocStackLogging=1` на утечки (работает и на ассемблерных программах; на транслированной цели отчёт полон, но процесс `leaks` сам не завершается). Диагностика в 6.Ч4 будет **двухконтурной, но разного состава** — на L valgrind, на M пара инструментов, и это оформляется врезкой |
| ~~В17~~ | **Отладчик контура L.** Заход 2 установил прогоном, что **под WSL1 `lldb` не продолжает исполнение процесса**: загрузка, точки останова, дизассемблирование и `process launch --stop-at-entry` работают, а `run`, `continue` и `si` не возвращают управления (пять сценариев, таймаут 25 с). `gdb` на той же программе отрабатывает полностью. Это расходилось с FR-6, назначавшим `lldb` основным отладчиком книги на обоих контурах | — | **Закрыт 2026-08-01 решением владельца в день порождения: отладчик выбирается по контуру** — на L `gdb`, на M `lldb`; FR-6 переформулирован (требования §FR-6, редакция того же дня), **7.Ч2 даёт обе сессии** с одинаковым сценарием и таблицей соответствия команд по T6. Главы 2.Ч1 и 2.Ч2 правки не потребовали — они изначально написаны по факту. Прогонная половина закрыта **2026-08-03**: `lldb` на маке работает полностью (весь сценарий `b main` → `run` → `si` → `register read` → `x/8xb` → `continue`), дословные сессии внесены во врезки 2.Ч1 и 2.Ч2, долг §9.4 пуст. Побочно уточнена карта T6: `stepi N` переводится как `si -c N`, а не `si N` — позиционный аргумент `si` означает номер потока (`Asm-learning.md` §9.2 пп. 30–32). **Вопрос закрыт целиком** |
| **В16** | На контуре M выход за границу блока кучи в **рукописной ассемблерной** программе не ловится ничем: ASan инструментирует только компилируемый код (в `.s` он ловит лишь ошибки уровня перехватчиков — двойное `free` ловит, запись за границу пропускает молча), `libgmalloc` под Rosetta нерабочий. Ловит ли это valgrind на контуре L — **не проверялось**: Memcheck работает по неинструментированному коду (T11), но на ассемблерной программе книги не прогонялся ни разу | **6.Ч4**, рубрика «типичные ошибки»: можно ли вообще показать читателю поимку такой ошибки, и на скольких контурах | Проба на **виндовой** машине до захода 8: собрать ассемблерный листинг с записью за границу `malloc`-блока и прогнать под valgrind. Постановка — `Asm-learning.md` §9.3, действие 6. Порождён 2026-08-01 при закрытии В15. Главы 1–5 не блокирует |
| В2 | Второй контур (Mac M3) не записан в журнал §10 C-книги | Следующий заход по любой книге начнёт с неверными предпосылками об окружении | Дописать при создании `Asm-learning.md` (FR-7) |
| В3 | Доступность цели `x86_64` в Xcode CLT зависит от версии SDK и Rosetta 2 | Контур M может отпасть на будущих macOS | Зафиксировать версии в журнале; запасной путь — Linux x86-64 в контейнере |
| В5 | Врезки-ссылки на ассемблерное дополнение внутри `c-and-rust-book` | Правка глав, уже закрытых ревизией | Согласовать отдельно; по умолчанию C-книгу не трогаем |
| ~~—~~ | ~~`lldb` не установлен на контуре L, а FR-6 назначает его основным отладчиком~~ | — | **Снято 2026-07-31**: поставлен `lldb` 18.1.3 (требования §7.1 п. 6). Отладчик книги есть на обоих контурах, на L дополнительно `gdb` |
| — | Intel SDM (S3) недоступен из рабочего окружения; оформление `.s` под Mach-O не покрыто двумя источниками | Формулировки частей 2.Ч3, 6.Ч3 | Действуют решения реестра §6 пп. 1–2: арбитр — S2, оговорка приводится в тексте |

Закрыт этапом 2: **В6** (вещественная арифметика) — решением П1.

Закрыты прогонами на контуре M 2026-07-31 (протокол — требования §7.2): **В10** — механизм ввода змейки подтверждён двухконтурно, 8.Ч2 разблокирована; **В11** — форма листингов определена, решение П6 сохранено с двумя уточнениями (§2). Заодно закрыт п. 3 реестра `sources.md` §6: `as` на контуре M — обёртка над `clang -cc1as`, а не GNU as.

Закрыт прогоном на контуре L 2026-07-31 (требования §7.2 пп. 15–18): **В12** — форма таблицы переходов подтверждена на обоих контурах, книга пользуется кандидатом Б, ограничение «до главы 4» снято. Тем же прогоном порождён **В14** (строка `.note.GNU-stack`) и снято ограничение перед главой 2 — `lldb` установлен.

Закрыты пробами на контуре M 2026-08-01 (протокол — `Asm-learning.md` §9.5): **В14** — форма листингов книги зафиксирована окончательно, **блокер старта этапа 3 снят**; **В15** — состав диагностики кучи на M определён. Тем же заходом порождён **В16**. Открытыми остались В2, В3, В5 и В16; из них ни один не блокирует главу 1.

---

## 7. Порядок работ и оценка

| Этап | Что | Заходов |
|---|---|---|
| 3. Написание глав | Главы 1–4 и 9 — по одному заходу; главы 5, 6, 7 — по два; глава 8 — два (текст Ч1, текст Ч2) | 13 |
| 4. Ревизия | Сквозная проверка: связность, забегания вперёд, соответствие источникам, нумерация, самодостаточность (FR-2) | 2 |
| 5. Решения | Зеркально главам: 1–4 и 9 — по одному; 5, 6, 7 — по два; 8 — два (решения Ч1, решения Ч2) | 13 |
| 6. Финальная сверка | Совпадение множеств ID «главы ↔ решения», прогон всего кода на обоих контурах, итоговый отчёт | 1 |

Итого ≈ 29 заходов. Порядок этапа 3 — строго по номерам глав, с двумя оговорками:

- ~~этап 3 не начинается, пока открыт **В14**~~ — **снято 2026-08-01**: проба выполнена на маке, форма листингов зафиксирована (`Asm-learning.md` §2.1 п. 8, §5.1). Этап 3 открыт, следующий заход — глава 1;
- ~~до главы 4 должен быть закрыт **В12**; до главы 6 — **В13**~~ — **оба сделано 2026-07-31**: форма таблицы переходов подтверждена двухконтурно, реестр добран (T11 valgrind, S7 как арбитр по libc). ~~До главы 6 — **В15**~~ — **сделано 2026-08-01**. Осталось одно предварительное условие внутри главы 6: **В16 закрывается до захода 8** (6.Ч3–6.Ч4) — проба на **виндовой** машине, ловит ли valgrind выход за границу кучи в ассемблерной программе; заход 7 (6.Ч1–6.Ч2) ею не блокирован;
- ~~8.Ч2 не начинается, пока открыт В10~~ — **ограничение снято 2026-07-31**: механизм ввода подтверждён на обоих контурах, обходной порядок с откладыванием 8.Ч2 не понадобится. Действующим осталось одно правило: **глава 9 не начинается, пока не написана 8.Ч2** (сквозной пример главы 9 — та самая змейка, и сопоставление строится по её готовому коду). Обходного пути, при котором глава 9 пишется раньше 8.Ч2, нет.

Правила захода, шаблоны главы и решений, трекер и журнал окружения — в `books/asm-book/Asm-learning.md` (FR-7), **созданном 2026-07-31**. Там же уточнено размещение двух ревизионных заходов, которое эта таблица не задавала: **Р1 после главы 5** (гейт на ABI — на нём стоит вся вторая половина книги), **Р2 сквозная** после главы 9. Норма объёма — 800–1500 строк на главу, файл решений 1.3–2× главы (NFR-5). Формат коммита — `book(asm): ch NN <slug> — <chapter|revision|solutions|check>`.

---

## 8. История правок программы

### v1.10 — погашение долга по прогону на контуре M (2026-08-03)

Состав и порядок глав не изменились. Изменилось состояние: **главы 1 и 2 прогнаны на обоих контурах**, долг пуст, обе клетки трекера — `[x]`.

1. **В17 закрыт целиком.** Оставалась его прогонная половина — работает ли `lldb` на контуре M вообще. Работает: полный сценарий (`b main` → `run` → `si` → `register read` → `x/8xb` → `continue`) снят и внесён во врезки 2.Ч1 и 2.Ч2 дословно. Решение П4 («отладчик вводится в 2.Ч1») выполнено на обоих контурах; план 7.Ч2 — две параллельные сессии — подтверждён как выполнимый.
2. **Уточнена карта соответствий T6.** Найдено единственное место, где она не переводится буквально: `stepi N` — это `si -c N`, а не `si N` (позиционный аргумент `si` означает номер потока). Затрагивает 2.Ч1 и 7.Ч2, где строится парная сессия; в реестре T6 остаётся рабочим инструментом, но в тексте книги команды сверяются прогоном, а не только картой.
3. **Реестр вырос до 25 источников: внесён T12** — man-страницы macOS `xcode-select(1)` и `softwareupdate(1)`. Повод — пункт 11 долга: §1.3.1 не мог описать установку инструментов на M, потому что процедуру не покрывал ни один источник. Особенность T12 — он читается локально на контуре M, а не по ссылке.
4. **Записано расхождение по существу, а не по форме:** абсолютную адресацию (2.Ч2) на M отвергает ассемблер, а на L — компоновщик. Для 7.Ч1, где разбирается вывод компилятора, и для рубрики «типичные ошибки» это означает, что «этап, на котором ловится ошибка» — свойство контура, а не архитектуры.
5. **Исправлено внутреннее расхождение главы 2**, найденное сверкой: пункт «типичных ошибок» 2.Ч1 утверждал, что точка останова встаёт на первую инструкцию функции, тогда как приведённая рядом сессия `gdb` показывала остановку после пролога. Верно для `lldb`, неверно для `gdb`; пункт переписан как различие отладчиков.

Протокол захода — `Asm-learning.md` §9.5, платформенные факты — там же §9.2 пп. 28–34.

### v1.9 — решения владельца по В17 и по доступу к S2 (2026-08-01)

Оба вопроса, поднятые заходом 2, закрыты владельцем в тот же день — до начала главы 3, где они оба сработали бы.

1. **В17 закрыт: отладчик выбирается по контуру.** На L — `gdb`, на M — `lldb`. FR-6 переформулирован в требованиях (прежняя редакция назначала `lldb` основным на обоих контурах и не могла быть выполнена на L). Правки: таблица врезок §5.1, линия инструментов §5.2, решение П4 §2, описание части 7.Ч2 в §4 — теперь она даёт **две** сессии, по одной на контур, с общим сценарием и таблицей соответствия команд по T6. Глава 2 переписывания не потребовала: она изначально написана по факту прогона. Открытым остался только прогон — дословная сессия `lldb` на маке (долг §9.4, п. 20).
2. **Доступ к S2 решается локальной копией.** Владелец скачивает тома 1 (24592) и 3 (24594) руководства AMD64 в `books/asm-book/extra-sources/` (в git не попадают — как закрытые издания книги о движке); читаются через `pdftotext`. Оговорка `sources.md` §6 п. 10 остаётся как история и как правило для сессий, где копии нет, но **главы 3 и 4 пишутся уже с нормативной опорой на S2** — семантика арифметических инструкций, правила `imul`/`idiv`, поведение флагов и сдвигов сверяются по нему.

### v1.8 — по итогам захода 2 (глава 2 написана, 2026-08-01)

Состав и порядок глав не изменились; изменилось одно средство и появился один вопрос.

1. **Порождён В17 (§6) — отладчик контура L.** Решение П4 («отладчик вводится в 2.Ч1») остаётся в силе и выполнено, но его практическая часть уточнена прогоном: под WSL1 `lldb` не продолжает исполнение процесса, поэтому сессии контура L в книге сняты в `gdb`, а `lldb` остаётся отладчиком контура M. Это расходится с FR-6, который назначает `lldb` **основным** отладчиком книги и планирует на нём полную сессию в 7.Ч2, — расхождение вынесено вопросом владельцу, а не решено внутри захода. Граница факта установлена узко: не «`lldb` не работает», а «не продолжает исполнение» (`Asm-learning.md` §9.2 п. 21).
2. **Записана оговорка по S2 (§6, последняя строка таблицы).** Текст нормативного арбитра по семантике инструкций из рабочего окружения не читается: портал `docs.amd.com` отдаёт JavaScript-оболочку, прямые PDF на `amd.com` не отвечают. Практика глав 1 и 2 одинакова — утверждения опираются на A1, A3, A4, T1 и прогон, а S2 упоминается как документ. Для глав 3 и 4, где S2 тоже назван арбитром, это означает тот же порядок работы. Оговорка внесена в `sources.md` §6 п. 10.
3. **Зафиксировано превышение объёма.** Глава 2 — 1715 строк при ориентире NFR-5 в 800–1500 (глава 1 была 1536). Причина структурная: 15 параграфов с шестью обязательными элементами и 39 заданий на три части. Дробление главы 2 на два захода программой не предусмотрено (решение П7 касается глав 5–8), поэтому превышение принято; отмечено к сверке на ревизии Р1.

### v1.7 — по итогам захода 1 (глава 1 написана, 2026-08-01)

Содержание программы не изменилось; изменилась одна формулировка, которую заход проверил на практике.

1. **Уточнён состав исключения 1 (§5.4).** Программа объявляла, что образцы 1.Ч3 содержат инструкции «главы 2». Когда образец был написан целиком и прогнан, выяснилось, что строк из главы 5 в нём столько же: пролог и эпилог функции, `call`/`ret` и правило `%al` перед вызовом `printf`. Пролог не декоративен — без него в точке `call` `%rsp` не выровнен по 16 байт, чего требует S1. Формулировка приведена в соответствие с фактом; **новых исключений не введено**, состав уже объявленных назван точно, и в самой главе он выписан таблицей.
2. **Записан факт, важный для 5.Ч2.** Нарушение выравнивания стека на контуре L не проявляется никак: программа без пролога собирается, печатает и возвращает 0 (`Asm-learning.md` §9.2 п. 19). Значит рубрика «типичные ошибки» части 5.Ч2 обязана говорить о требовании ABI, а не об ожидаемом сбое; поведение той же программы на M пока не проверено.
3. **Глава 1 закрыта статусом `[L]`** — прогнана на одном контуре, долг по контуру M из 11 пунктов записан в `Asm-learning.md` §9.4. Открытых вопросов заход не породил.

### v1.6 — пробы В14 и В15 на контуре M; старт этапа 3 разблокирован (2026-08-01)

Первая маковская сессия после 2026-07-31 закрыла оба открытых машинозависимых действия. Протокол — `Asm-learning.md` §9.5, платформенные факты — там же §9.2 пп. 11–15.

1. **В14 закрыт, исход Б.** Ассемблер контура M строку `.section .note.GNU-stack,"",@progbits` не принимает (`error: unexpected token in '.section' directive`); дело не в дефисе — отвергается сама ELF-форма записи. Вторая половина постановки закрыта отрицательно: **на M аналогичной проблемы с правами стека нет**, стек неисполняемый по умолчанию (проверено исполнением кода со стека — SIGSEGV), а сделать его исполняемым нечем — опции `-allow_stack_execute` в `ld-1167.5` не существует.
2. **Форма листингов книги зафиксирована окончательно, блокер старта снят.** Строка входит в канонический (L) листинг, врезка № 1 в 1.Ч3 убирает её для M. Глава 1 пишется сразу набело.
3. **Исправлено противоречие в постановке исхода Б (существенное).** И требования §7.1 п. 7, и эта программа описывали исход Б как «уходит во врезку как *на L дописать эту строку*» — то есть канонический листинг **без** неё. Это расходится с решением П6: каноническая форма — форма контура L, а листинг L без этой строки собирается с предупреждением и получает исполняемый стек, образцом для книги он быть не может. Направление врезки развёрнуто: строка в тексте есть, врезка снимает её для M. Обоснование — `Asm-learning.md` §2.1 п. 8.
4. **В15 закрыт.** Valgrind на контуре M отсутствует. `libgmalloc` заменой не служит — под Rosetta вешает любой x86-64 процесс, включая заведомо исправный (нативно на arm64 работает). Рабочая диагностика на M составная: **ASan** на переполнение и use-after-free (но `detect_leaks` на этой платформе не поддерживается) плюс **`leaks(1)`** на утечки. Следствие для 6.Ч4: диагностика двухконтурная, но **разного состава** по контурам, и это оформляется врезкой, а не общим текстом.
5. **Новый вопрос В16 (методически важный).** Пробы ставились не только на C, но и на рукописном `.s` — потому что программы главы 6 ассемблерные. Оказалось, что ASan инструментирует лишь компилируемый код: в `.s` он ловит ошибки уровня перехватчиков (двойное `free`), а запись за границу блока пропускает молча. Вместе с нерабочим `libgmalloc` это значит, что **на M выход за границу кучи в ассемблерной программе не ловится ничем**. Ловит ли его valgrind на L — вопрос с пробой до захода 8, а не предположение: Memcheck работает по неинструментированному коду, но на ассемблерной программе книги не прогонялся.

### v1.1 — самопроверка на связность и монотонность (2026-07-30)

Самопроверка версии v1.0 на связность, монотонность и запрет догадок дала восемь правок; две из них устраняли реальные дефекты программы.

1. **Забегание вперёд в 4.Ч3 (существенное).** Таблица переходов — это массив адресов в статических данных, а директивы данных вводились только в 6.Ч1. Исправлено переносом минимума (`.quad`, `.byte`) в 2.Ч2, где он и нужен: показать индексную адресацию не на чем, если в памяти ничего не лежит.
2. **Ошибка по существу в таблице врезок (существенное).** 16-байтовое выравнивание стека стояло в списке различий контуров — то есть читателя учили бы, что на Linux выравнивать необязательно. Это универсальное требование ABI (S1); в 5.Ч2 оно теперь дано как общее правило, а контурам оставлено лишь то, чем они действительно различаются, — как проявляется нарушение.
3. **Новый вопрос В12.** Таблица переходов при позиционно-независимой сборке требует особой формы; от неё зависят 4.Ч3 и оба диспетчера главы 8. Проверяется прогоном до главы 4.
4. **Новый вопрос В13.** Реестр не покрывает инструмент диагностики кучи (6.Ч4) и не даёт арбитра по libc, верного на обоих контурах: T8 — это glibc, то есть только контур L. Добор к этапу 1 до главы 6.
5. **Сломанный обходной порядок.** В v1.0 при открытом В10 предлагалось писать главу 9 раньше 8.Ч2 — но сквозной пример главы 9 и есть змейка из 8.Ч2. Порядок исправлен в §7.
6. **Глава 7 разделена на два захода** — как главы 5 и 6: четыре части, мини-проект и обзорная часть в один заход не укладываются.
7. **Невыполнимое правило практики.** Требование «верхние 30 % каждой части — законченная программа» неисполнимо в 1.Ч1 и 1.Ч2: инструменты ставятся только в 1.Ч3. Исключение оговорено явно.
8. **Уточнения, снимающие догадки и типичные ошибки:** обнуление старшей половины при записи в 32-битный регистр (2.Ч1); `movabs` и запрет пересылки «память → память» (2.Ч1); `sar` не заменяет `idiv` для отрицательных чисел (3.Ч2); `cmov` вычисляет обе ветви и потому запрещён там, где ветвь разыменовывает указатель (4.Ч3); `.align` толкуется по-разному — используем `.p2align`/`.balign` (6.Ч3); служебные элементы вывода компилятора `@PLT`, `.cfi_*`, `.L…` названы явно (7.Ч1); печать числа через `printf` объявлена вторым образцом главы 1, иначе главы 2–4 нечем вывести; «почему стек растёт вниз» заменено на фактическое «в какую сторону растёт и что из этого следует».

### v1.2 — по результатам прогонов на контуре M (2026-07-31)

Закрыты оба вопроса, которые могла закрыть только маковская сессия (протокол — требования §7.2). Программа поправлена в четырёх местах; замысел и порядок глав не изменились.

1. **Уточнено решение П6 (§2).** Ассемблер контура M принимает весь набор директив книги в написании GNU as — кроме секции неизменяемых данных: `.section .rodata` отвергается. Следствие для текста: до врезки в 6.Ч3 листинги держат неизменяемые данные в `.data`, и тогда одна каноническая форма листинга действительно собирается на обоих контурах — при переводе имён символов по врезке № 1.
2. **Врезка № 1 (1.Ч3) переформулирована из замечания в правило.** Подчёркивание касается не только `main`, но и каждого импорта libc; вдобавок получен точный вид ошибки компоновщика, который идёт в текст: ELF-форма на маке ассемблируется молча и падает только на компоновке.
3. **Врезка № 2 (6.Ч3) наполнена конкретикой** — принятые и отвергнутые написания секций, а также подтверждённая прогоном причина, по которой книга пользуется `.p2align`/`.balign`: на Mach-O `.align 3` выравнивает на 8 байт.
4. **Снято ограничение порядка работ (§7).** В10 закрыт, обходной путь «написать 8.Ч1, уйти в решения, вернуться к 8.Ч2 после мака» не понадобится; правило «глава 9 после 8.Ч2» остаётся.

### v1.3 — по результатам прогона на контуре L (2026-07-31)

Закрыт **В12** — последний вопрос, блокировавший порядок глав до главы 4; поставлен `lldb`. Замысел, карта книги и порядок глав не изменились; правок пять.

1. **4.Ч3 переписана в части таблицы переходов.** Форма (кандидат Б) подтверждена на обоих контурах и объявлена единственной. Разбор кандидата А стал содержательнее, чем планировалось: **предсказание не сбылось наполовину**. Перед прогоном ожидалось, что А в неизменяемой секции не скомпонуется и на L — как на M. На деле компоновщик L его принимает, печатая два предупреждения, и оставляет в готовой программе флаг `TEXTREL`. Это не помеха выбору (Б по-прежнему лучше по обоим контурам), а подарок рубрике «типичные ошибки»: одна и та же ошибка на маке видна сразу, на Linux доживает до готовой программы. Заодно в текст входит происхождение варианта А — его порождает сборка `-fno-pie`.
2. **Новый вопрос В14 и правка 1.Ч3.** Прогон вскрыл то, чего маковская сессия показать не могла: на L рукописный листинг без строки `.section .note.GNU-stack,"",@progbits` собирается с предупреждением и получает **исполняемый стек**. Читатель столкнётся с этим на первой же сборке, поэтому 1.Ч3 обязана предупреждение объяснить; входит ли строка в канонический листинг — решается пробой на маке (В14, требования §7.1 п. 7).
3. **7.Ч1 дополнена** двумя элементами служебного вывода, наблюдёнными на L: `notrack` у косвенного перехода и `.note.gnu.property` (оба — от `-fcf-protection`), плюс `.note.GNU-stack`.
4. **Таблица врезок §5.1**: строка про таблицу переходов переформулирована (различаются не формы, а поведение отвергнутого варианта) и помечена ✅ по обоим контурам; добавлена строка про `.note.GNU-stack` со статусом «L проверен, M нет».
5. **Сняты два ограничения порядка работ (§7)**: В12 закрыт — глава 4 свободна; `lldb` 18.1.3 установлен — глава 2 свободна. Действующих ограничений осталось три: В14 до **начала** главы 1 (с 2026-07-31 — блокер старта этапа 3, решение владельца), В13 до главы 6, глава 9 после 8.Ч2.

### v1.5 — добор реестра по В13 (2026-07-31)

Содержание глав не изменилось; изменились опоры.

1. **В13 закрыт.** Реестр добран до 24 источников: **T11** — Valgrind User Manual (гл. 4 Memcheck: классы ошибок и четыре категории утечек `--leak-check`), нормативным арбитром по семантике библиотечных функций стал **S7** (открыты страницы POSIX по `malloc()`, `fopen()`, `fread()`, `puts()`, `rand()`/`srand()`), **T8** сужен до деталей реализации контура L. Поправлены §4 (преамбула об идентификаторах, оговорка по libc, блоки «Источники» глав 5, 6 и 8, текст 6.Ч4) и §6.
2. **Порождён В15.** Заход едва не записал в 6.Ч4 догадку «на контуре M инструмента диагностики кучи нет» — на самом деле его наличие там никогда не опрашивалось. Вместо утверждения поставлен вопрос с пробой до главы 6; до ответа неизвестно, будет параграф двухконтурным или одноконтурным с врезкой.
3. **Практическое следствие для очереди:** заходов, выполнимых без мака, не осталось — и старт этапа 3 (В14), и подготовка главы 6 (В15) требуют маковской сессии.

### v1.4 — порядок работ: В14 стал блокером старта (2026-07-31)

Правок две, обе — про порядок, а не про содержание; замысел, карта книги и состав частей не изменились.

1. **В14 переведён из «до сдачи главы 1» в «до начала главы 1»** — решение владельца: «ждём мака обязательно, это блок для старта». Прежний мягкий порядок (писать главу 1, пометив форму листинга предварительной) отменён: от пробы зависит завершающая строка канонического листинга **всей** книги, а глава 1 — образец для остальных восьми, поэтому при любом исходе её пришлось бы перепроверять целиком. Поправлены §6 (строка В14), §7 (список ограничений порядка) и §9 (пп. 2 и 5). Следствие для планирования: пока блокер держится, на виндовой машине возможен единственный содержательный заход — добор реестра по **В13**.
2. **Создан операционный документ `Asm-learning.md`** (FR-7): правила захода и оформления кода книги, шаблоны главы и решений, чеклист ревизии, трекер по 13 заходам / 28 частям, журнал окружения, шаблон промпта. Там же уточнено то, чего эта программа не задавала: размещение двух ревизионных заходов (**Р1 после главы 5** как гейт на ABI, **Р2 сквозная**) и порядок работы, когда доступен только один контур (статусы `[L]`/`[M]` и «долг по прогону»).

---

## 9. Что дальше

1. ~~Создать `books/asm-book/Asm-learning.md`~~ — **сделано 2026-07-31**: жёсткие правила захода (§2 + оформление кода §2.1), шаблоны главы и решений, трекер по 13 заходам / 28 частям, журнал окружения (§9) и шаблон промпта. Журнал §7.2 требований при этом не переносился, а заморожен на месте — на его пункты ссылается эта программа в полутора десятках мест; новые записи идут в `Asm-learning.md` §9.
2. Этап 3, глава 1 — **после закрытия В14, не раньше** (решение владельца 2026-07-31; см. п. 5 и `Asm-learning.md` §5.1). ~~Перед заходом по главе 2 — поставить `lldb` на контуре L; перед главой 4 — закрыть **В12**; перед главой 6 — **В13**~~ — всё сделано 2026-07-31, главы 2, 4 и 6 источниками не блокированы. Осталось перед главой 6 одно: **В15** — проба на маке, чем там закрывается диагностика кучи (порождена добором реестра).
3. ~~В первой же маковской сессии — закрыть **В10** и **В11**~~ — **сделано 2026-07-31** (требования §7.1 пп. 3–4, протокол §7.2).
4. ~~Маковскую половину В12 прогнать в любую доступную маковскую сессию~~ — **сделано 2026-07-31**: выбран кандидат Б (требования §7.2 пп. 12–14). ~~На виндовой машине осталось прогнать ту же четвёрку вариантов на контуре L~~ — **сделано 2026-07-31** (требования §7.2 пп. 15–18): **В12 закрыт**. Сверка с ожиданием: про кандидата Б подтвердилось всё, про кандидата А — только половина (на L он в неизменяемой секции компонуется, с предупреждением и `DT_TEXTREL`).
5. **В ближайшую маковскую сессию — закрыть В14** одной пробой (требования §7.1 п. 7): принимает ли ассемблер M строку `.section .note.GNU-stack,"",@progbits` и есть ли на M та же проблема с правами стека. От ответа зависит завершающая строка канонического листинга книги, и с 2026-07-31 это **блокер старта этапа 3**: до пробы глава 1 не начинается вовсе.
