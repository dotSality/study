# Требования к самоучителю по ассемблеру (артефакт этапа 0)

> Результат разведки требований (`/sc:brainstorm`, 2026-07-30). Фиксирует **что** нужно сделать и **в каком формате**.
> Нормативная база — `books/self-education-book-create-plan.md` (далее «План»). При противоречии приоритет у Плана.
> Это **не** программа курса: программа (`outline.md`) — артефакт этапа 2 и составляется после этапа 1 (исследование источников).

---

## 1. Цель и место в экосистеме репозитория

Создать самоучитель по ассемблеру x86-64 — **«Ассемблер x86-64 с нуля: от байта до языка C»** (название принято владельцем 2026-07-30), — который:

1. Ведёт человека без формального бэкграунда от нуля до умения писать, собирать, запускать и отлаживать небольшие программы на ассемблере.
2. **Заканчивается плавным переходом к языку C**: последние главы показывают, что каждая конструкция C — это знакомая читателю ассемблерная схема, и передают эстафету самоучителю «C, затем Rust» (`books/c-and-rust-book/`).

Позиция в репозитории — третий самоучитель по общему Плану, наравне с `linear-algebra` (завершён) и `c-and-rust-book` (26 глав написаны, решения закрыты по гл. 18).

### 1.1. Принятые владельцем решения (2026-07-30)

| # | Решение | Выбор |
|---|---|---|
| Р1 | Архитектура и контур верификации | **x86-64, System V ABI**, синтаксис GAS, линковка с libc |
| Р2 | Размещение | **Отдельная книга** `books/asm-book/` со своими `sources.md` / `outline.md` / `chapters/` / `solutions/` |
| Р3 | Точка входа | **Полностью самодостаточная книга**: биты/байты/адреса/установка инструментов излагаются своими словами, без отсылки к главе 1 C-книги |
| Р4 | Очередь работ | **Начинать сейчас**, чередуя заходы с решениями глав 19–26 книги C→Rust |
| Р5 | Синтаксис ассемблера | **AT&T (GAS) — основной**; Intel-синтаксис вводится отдельным параграфом как второй диалект |
| Р6 | Финальная работа | **Два проекта** в одной итоговой главе: Ч1 — виртуальная машина стекового байт-кода, Ч2 — змейка в терминале (см. FR-4, §4.1, §4.2) |

Следствие Р2+Р3: книга читается независимо, а связь с C-книгой — односторонняя и только на выходе (финальная глава-мост). Обратная правка `c-and-rust-book` сводится к 2–3 строкам-врезкам «см. дополнение по ассемблеру» и требует отдельного согласования, потому что оба тома уже прошли ревизию.

---

## 2. Функциональные требования

### FR-1. Структура — по Плану, без отклонений

Глава → части → параграфы + практика. Каждый параграф содержит все 6 элементов Плана §2.3 (определения; интуиция; 2–3 разобранных примера; связь с практикой; типичные ошибки; ссылки на источники). В каждой части 6–15 заданий с ID `Г{глава}.Ч{часть}.З{номер}` в **собственном пространстве имён** книги (коллизий с `c-and-rust-book` нет — разные файлы). Решения — только в `solutions/`, зеркально главам, с вариативностью подходов по Плану §6 п. 5.

Специфика дисциплины для рубрики «интуиция»: ASCII-схемы регистров, кадра стека и раскладки байтов в памяти — норма, как схемы памяти в C-книге.

### FR-2. Самодостаточность (следствие Р3)

Книга не предполагает у читателя знания C, двоичной системы, устройства памяти или установленного компилятора. Всё это вводится в первой главе своими словами. Пересечение с главой 1 C-книги допустимо и ожидаемо; дословного копирования быть не должно (Правило Плана §4.3).

### FR-3. Мост в C

Финальный блок книги обязан:

- показать `gcc -S` / `clang -S` на программах, которые читатель уже понимает на уровне инструкций;
- сопоставить конструкции C с уже пройденными ассемблерными схемами (переменная ↔ ячейка/регистр, функция ↔ `call` + кадр стека, массив ↔ базовый адрес + индексная адресация, указатель ↔ адрес, `struct` ↔ смещения полей, стек ↔ куча через `malloc`);
- объяснить формулу «C — переносимый ассемблер» уже как вывод, а не как лозунг;
- явно передать читателя в `books/c-and-rust-book/` с указанием, что в его главе 1 можно пролистать.

Сквозной пример главы-моста — **змейка из главы 8, переписанная на C**: читатель видит собственную программу, которую он собрал из инструкций вручную, в виде полутора экранов кода на C, и понимает, во что именно компилятор её развернёт. Это самая наглядная стыковка двух книг, какая здесь возможна.

### FR-4. Прикладная линия и финальная работа

Книга учит писать **программы**, а не фрагменты. Три уровня практики:

1. **Задания в каждой части** — 6–15 штук по Плану §2.2, распределение сложности 30/40/30. Верхние ~30% каждой части — не «допишите строку», а законченная работающая программа с заданными входом, выходом и ограничениями.
2. **Промежуточные мини-проекты** — задания, собирающие материал нескольких предыдущих глав в одну программу (по образцу проектных чекпоинтов части 12.Ч2 C-книги). Где именно их разместить — решается на этапе 2.
3. **Финальная глава — два проекта** (решение Р6), по одному в каждой части: виртуальная машина байт-кода (§4.1) и змейка в терминале (§4.2). Прямая аналогия с главой 25 C-книги, где итоговая глава тоже состоит из двух управляемых проектов.

Требования к каждому из проектов (сформулированы владельцем 2026-07-30):

- **не типовой и не базовый**: не «калькулятор», не «отсортируйте массив», не набор упражнений под общим заголовком;
- **составной**: обязан задействовать одновременно арифметику и флаги, ветвления и таблицы переходов, стек и соглашение о вызове, работу с памятью (массивы/строки/структуры/куча) и вызовы libc — то есть провалить его, зная только часть книги, невозможно;
- **доведённый до работающей программы**, а не до эскиза; собирается и запускается на обоих контурах (FR-5);
- построен как **управляемый проект с чекпоинтами**: проектные решения и их разбор вместо классических параграфов (допущение Плана §3 для проектных глав, уже применённое в главах 25–26 C-книги);
- задания части — ступени этого проекта плюс вариации/расширения.

**Почему проектов два, а не один.** Они закрывают разные половины материала и почти не пересекаются:

| | VM (Ч1) | Змейка (Ч2) |
|---|---|---|
| Побитовые операции, декодирование | сильно | слабо |
| Таблица переходов | диспетчер опкодов | диспетчер клавиш |
| Собственные кадры вызовов, рекурсия | сильно | нет |
| Индексная адресация `disp(base,index,scale)` | средне | сильно — двумерное поле |
| Структуры, куча, кольцевой буфер | сильно | сильно — тело змейки |
| Реальное время, взаимодействие с терминалом | нет | сильно |

Порядок частей: **сначала VM, потом змейка**. VM — прямое продолжение главы 7 (как на самом деле исполняются инструкции), а змейка даёт видимый играбельный финал и служит сквозным примером для главы-моста: «а теперь та же змейка на C».

Место в порядке глав: итоговая глава идёт **перед** главой-мостом в C. Так книга сначала закрывает ассемблер как самостоятельный навык, а мост остаётся последним и действительно «перетекает» в C.

### FR-5. Двухконтурная верификация кода

Правило Плана §4.4 и §2.3 операционного документа C-книги («некомпилирующийся фрагмент в текст не попадает») действуют без послаблений. Каждая программа прогоняется **на обоих доступных контурах**:

| Контур | Машина | Тулчейн | Формат объектника |
|---|---|---|---|
| **L** (Linux) | винда, WSL1 Ubuntu 24.04 | gcc 13.3 / clang 18.1.3, `as`, `objdump`, valgrind 3.22 | ELF |
| **M** (macOS) | Mac M3 | Apple clang 17 (`-arch x86_64` + Rosetta 2), `as`, `objdump`, `lldb` | Mach-O |

Оба контура используют одну и ту же System V AMD64 ABI: те же регистры, то же соглашение о вызове, то же выравнивание стека. Расхождения — только в оформлении, и они выносятся во врезки (см. NFR-2).

Проверено 2026-07-30 прямым прогоном на маке: `clang -arch x86_64` собирает и `-S`, и исполняемый файл, программа отрабатывает (Rosetta 2 установлена).

### FR-6. Инструменты как часть предмета

Ассемблер без инструментов не изучается. Книга обязана научить:

- собирать: `as`/`clang`/`gcc` из `.s`, раздельное ассемблирование + компоновка;
- дизассемблировать: `objdump -d` (на маке это LLVM-версия, на WSL — binutils);
- отлаживать пошагово с просмотром регистров и памяти — **основной отладчик `lldb`** (есть на маке из коробки; на WSL ставится `apt`). `gdb` на маке отсутствует и требует подписи сертификатом — в основной путь не берётся, упоминается как альтернатива для контура L;
- пользоваться Compiler Explorer как справочным стендом (после проверки его пригодности на этапе 1).

### FR-7. Операционный документ и трекер

По образцу `c-and-rust-book/C-and-Rust-learning.md` создаётся `books/asm-book/Asm-learning.md`: жёсткие правила захода, шаблон главы, шаблон решений, таблица-трекер по главам (Глава / Ревизия / Решения / Сверка), журнал окружения и договорённостей, шаблон промпта для запуска захода.

---

## 3. Нефункциональные требования и технические ограничения

### NFR-1. Никаких сырых системных вызовов

Номера и соглашения системных вызовов Linux и macOS различаются — программа с `syscall`-номерами не переносится между контурами. Поэтому весь ввод-вывод и работа с кучей идут **через libc** (`puts`, `printf`, `malloc`, `free`), а программа линкуется компилятором (`clang prog.s -o prog`), а не голым `ld`. Побочная выгода: читатель с первой главы вызывает те самые функции, которые потом увидит в C, — мост начинает строиться сразу.

### NFR-2. Каталог различий контуров — ведётся с первой главы

Известные расхождения L/M, которые придётся оформить как сквозную врезку (перечень уточняется на этапе 1):

- **имена символов**: Mach-O добавляет ведущее подчёркивание (`_main`), ELF — нет. Прогон 2026-07-31 (§7.2) уточнил охват: подчёркивание несут **все** глобальные символы **и все имена импортируемых функций libc** (`call puts` → `call _puts`), поэтому листинг в ELF-форме на маке не компонуется целиком, а не в одной строке; ассемблирование при этом проходит без замечаний — расхождение вылезает на компоновке;
- **директивы секций**: `.text` / `.data` против `.section __TEXT,__text,...`. Прогон 2026-07-31: `.text`, `.data` и весь остальной набор директив книги ассемблер контура M принимает в написании GNU as, **кроме секции неизменяемых данных** — `.section .rodata` отвергается («unexpected token in '.section' directive»), голая `.rodata` — «unknown directive»; маковские написания: `.const`, `.cstring`, `.section __TEXT,__cstring`, `.section __DATA,__const`;
- **адресация глобальных данных**: macOS требует позиционно-независимого кода, поэтому RIP-относительная адресация (`lea sym(%rip), %rax`) вводится **сразу как единственно правильная**, а не как продвинутая тема;
- **выравнивание стека**: SysV требует 16-байтового выравнивания `%rsp` в точке вызова; нарушение на маке валит libc-функции. Тема обязана появиться в главе про функции, а не в примечании.

Каждый пункт подтверждается прогоном на обоих контурах и попадает в текст только после этого.

### NFR-3. Синтаксис — AT&T (GAS) как основной (решение Р5)

Обоснование: `-S` у gcc и clang по умолчанию выдаёт именно его, а вся книга построена на чтении вывода компилятора; каноническая академическая литература по x86-64 (CS:APP / CMU 15-213) написана в нём же. Intel-синтаксис вводится отдельным параграфом как «второй диалект» — потому что справочники Intel/AMD и большинство дизассемблеров используют его, и читатель обязан уметь переключаться. Параграф про Intel-синтаксис нужен ещё и потому, что часть источников реестра (руководства Intel/AMD) написана в нём: без явного «переводчика» ссылки на них читатель применить не сможет.

### NFR-4. Язык и терминология

Русский; устоявшийся английский термин в скобках при первом употреблении (План §7). Арбитр терминологии для этой дисциплины определяется на этапе 1 — предположительно спецификация psABI и руководства Intel/AMD, по аналогии с ролью стандарта C5 в томе I C-книги.

### NFR-5. Объём и ритм работ

Ориентир по существующим книгам репозитория: глава ≈ 800–1500 строк, файл решений ≈ 1.3–2× главы. Один заход = одна глава или один файл решений, с коммитом и обновлением трекера (План §5). При 9 главах (§4) это ≈ 20–24 захода плюс 2 подготовительных (этапы 1–2) и 2 ревизионных. Глава 8 несёт два полноценных проекта — её текст и решения пишутся по одной части за заход (Плана §5 это прямо допускает для крупных глав), то есть 4 захода вместо двух.

Формат коммита — по образцу C-книги: `book(asm): ch NN <slug> — <chapter|revision|solutions|check>`, тело в 1–3 строки.

---

## 4. Предварительный охват (черновик для этапа 2 — НЕ утверждён)

Приводится как оценка объёма и проверка на полноту цепочки «с нуля → до C». Окончательный состав глав и частей определяется на этапе 2 **после** исследования источников и утверждается владельцем.

| # | Slug | О чём | Ключевой выход |
|---|---|---|---|
| 1 | `01-machine-and-tools` | Биты, байты, шестнадцатеричная запись, память как массив байтов, процессор и регистры x86-64; сборка `.s` → исполняемый файл; первая программа (вывод через libc) | рабочее окружение на своём контуре |
| 2 | `02-data-and-addressing` | `mov` и разрядности (b/w/l/q), типы операндов, полная форма адресации `disp(base,index,scale)`, RIP-относительная адресация, `lea`, порядок байтов, знаковое/беззнаковое расширение | умение читать любой операнд |
| 3 | `03-arithmetic-logic` | Сложение/вычитание/умножение/деление, побитовые операции, сдвиги; регистр флагов как побочный эффект вычислений | вычисления без ветвлений |
| 4 | `04-control-flow` | `cmp`/`test`, условные переходы, ветвления, циклы, условная пересылка, таблицы переходов | вся логика из C, но руками |
| 5 | `05-stack-and-functions` | Стек, `push`/`pop`, `call`/`ret`, System V ABI (регистры аргументов, возврат, caller/callee-saved, выравнивание, red zone), кадр стека, локальные переменные, рекурсия, вызов функций libc | центральная глава книги |
| 6 | `06-data-in-memory` | Массивы и индексная адресация, нуль-терминированные строки, структуры как смещения полей, выравнивание и паддинг, секции статических данных, куча через `malloc`/`free` | раскладка данных в памяти |
| 7 | `07-reading-compiler-output` | `gcc -S` / `objdump -d` / отладчик; чтение чужого кода; `-O0` против `-O2` — что делает оптимизатор | навык, ради которого всё затевалось |
| 8 | `08-capstone` | **Итоговые проекты (FR-4, Р6)**, две части: Ч1 — виртуальная машина байт-кода (§4.1), Ч2 — змейка в терминале (§4.2) | два самостоятельных приложения на ассемблере |
| 9 | `09-bridge-to-c` | Конструкции C поверх уже пройденных схем; сквозной пример — змейка из гл. 8, переписанная на C; «переносимый ассемблер»; отличия Windows x64 ABI (для читателей, идущих по MinGW-контуру C-книги); передача эстафеты в `c-and-rust-book` | вход в самоучитель по C |

### 4.1. Итоговый проект Ч1: виртуальная машина байт-кода

Программа на ассемблере, которая читает файл байт-кода и исполняет его. Тематически замыкает книгу: читатель, разобравшийся, как процессор исполняет инструкции, пишет собственный процессор.

Предварительные чекпоинты проекта (уточняются на этапе 2):

1. формат байт-кода и чтение файла в память (`fopen`/`fread`/`malloc`);
2. стек операндов в куче — рост, границы, диагностика переполнения;
3. цикл выборки-декодирования-исполнения;
4. диспетчер по опкоду — таблица переходов;
5. арифметика, сравнения и условные переходы виртуальной машины;
6. `CALL`/`RET` внутри виртуальной машины — собственные кадры вызовов (двойная рекурсия: кадры VM живут в куче, кадры самого ассемблерного кода — на настоящем стеке);
7. ввод-вывод виртуальной машины через libc;
8. диагностика: режим трассировки исполнения, понятные сообщения об ошибках байт-кода.

Демонстрационная программа для VM — факториал или числа Фибоначчи через рекурсию: она проверяет чекпоинты 5–7 разом.

Покрытие материала книги (проверка требования «составная» из FR-4): гл. 2 — декодирование операндов и адресация, гл. 3 — арифметика и флаги, гл. 4 — таблица переходов, гл. 5 — соглашение о вызове и кадры, гл. 6 — куча, структуры и раскладка данных, гл. 7 — отладка собственной VM в отладчике. Ни одна глава не остаётся незадействованной.

#### Что VM требует от предыдущих глав

Единственное требование VM сверх уже запланированного материала — **загрузка байт-кода**: чтение файла в буфер. Куча, структуры, таблицы переходов, ABI и адресация в программе и так есть по существу дисциплины, а не ради проекта.

Причём в ассемблере чтение файла — не отдельная тема, а применение уже известного: это вызов библиотечных функций `fopen`/`fread`/`fclose` с указателями в аргументах. Как передавать указатель в функцию, читатель узнаёт в главе 5 (соглашение о вызове), как выделить буфер под данные — в главе 6 (куча). Дальше остаётся один параграф «вызов библиотечных функций, заполняющих буфер» в главе 6. Ни порядок глав, ни нарастание сложности это не нарушает (требование Плана §3 о монотонности выполняется).

Три способа закрыть вопрос, если и одного параграфа окажется много:

- **А (по умолчанию).** Параграф про `fopen`/`fread` в главе 6, рядом с кучей. Буфер в куче + функция, которая его наполняет, — естественная пара.
- **Б (без файлов вообще).** Байт-код кладётся в секцию статических данных или передаётся через аргументы командной строки. Тогда VM не нуждается в файловом вводе-выводе ни в каком виде, а загрузка из файла становится заданием-расширением в конце главы 8.
- **В.** Загрузка файла делается первым чекпоинтом самой главы 8 — как проектное решение с разбором, а не как теория предыдущей главы. Допустимо: главы-проекты по Плану §3 могут вводить прикладные детали по месту (так устроены главы 12 и 25 C-книги).

Требование порождено словом «составное», а не выбором VM: любое составное приложение откуда-то берёт вход.

### 4.2. Итоговый проект Ч2: змейка в терминале

Классическая змейка в текстовом терминале: поле, растущее тело, еда, столкновения, счёт, нарастающий темп.

Предварительные чекпоинты (уточняются на этапе 2):

1. поле как двумерный массив, отрисовка кадра ANSI-последовательностями через `printf`;
2. тело змейки как кольцевой буфер координат в куче — голова растёт, хвост отпускается;
3. ввод с клавиатуры без Enter, диспетчер клавиш таблицей переходов;
4. движение, столкновения со стеной и с собой, поедание и рост;
5. тайминг кадра и нарастание скорости; генерация еды через `rand`/`srand`;
6. таблица рекордов в файле, корректное восстановление терминала при выходе.

#### Ввод с клавиатуры: механизм и его проверка

Главный технический риск проекта — сырой (raw) ввод: чтение клавиши без Enter и без блокировки игрового цикла.

«Правильный» путь через `termios` для этой книги **непригоден**: структура `struct termios` и значения констант `ICANON`/`ECHO` различаются между Linux и macOS, а в ассемблере смещения полей и константы зашиваются числами — получилось бы два разных модуля ввода под два контура (нарушение духа FR-5).

**Кандидат 1 — таймаут средствами терминала. Отвергнут: на контуре L не работает.** Проверен на контуре M 2026-07-30 (x86-64 через Rosetta, в настоящем псевдотерминале):

```
system("stty raw -echo min 0 time 1");   // сырой режим + чтение с таймаутом
int c = getchar();                        // 'w' -> 119; клавиши нет -> -1, без блокировки
system("stty sane");                      // восстановление терминала
```

На M отработал как обещано: нажатие `w` пришло как `c=119` без Enter и без эха, на итерациях без нажатия `getchar` возвращал `-1`, не блокируя цикл. **Прогон на контуре L 2026-07-30 (WSL1 Ubuntu 24.04) это опроверг**: `stty` настройки принимает (`stty -a` внутри pty показывает `min = 0; time = 1`), первая клавиша приходит (`tick 0 c=119`), а следующее чтение **блокируется навсегда** — pty в WSL1 игнорирует `VMIN`/`VTIME`. Проверено и сырым `read(2)` в обход stdio: поведение то же, значит дело не в буферизации. Детали прогона и версии — §7.2.

**Кандидат 2 (действующий) — таймаут средствами программы.** Сырой режим без `min`/`time`, а неблокирующее ожидание даёт `poll(2)` с таймаутом длиной в кадр:

```
system("stty raw -echo");                    // только сырой режим и отключение эха
struct pollfd pfd = { .fd = 0, .events = POLLIN };
int r = poll(&pfd, 1, 100);                  // 100 мс — длительность кадра
int c = -1;
if (r > 0) { unsigned char b; if (read(0, &b, 1) == 1) c = b; }
system("stty sane");                         // восстановление терминала
```

Проверен на контуре L 2026-07-30: тики идут строго по таймауту (`poll=0 c=-1` на 0.10 / 0.20 / 0.30 с), нажатие приходит как `poll=1 c=119`, цикл ни на чём не блокируется, код возврата 0. `stty`, `poll` и `read` — обычные libc/POSIX-вызовы, одинаковые на обеих системах, поэтому развилки по контурам нет и запасной путь с двумя платформенными модулями `termios` не понадобился. **Подтверждён на контуре M 2026-07-31** тем же управляющим скриптом (протокол — §7.2): структура вывода совпала с эталоном L, три прогона подряд стабильны, терминал восстанавливается. **В10 закрыт**, механизм принят как единственный для обоих контуров, `termios`-резерв не понадобился.

Побочная выгода замены: таймаут `poll` — это и есть тайминг кадра, поэтому отдельная задержка в игровом цикле не нужна, а частота кадров задаётся программой, а не настройками терминала.

Два уточнения к тексту книги:

- тайминг кадра задавать **явно** — таймаутом `poll` либо `nanosleep`/`usleep`, — а не полагаться на таймаут терминала `time 1`: так частота кадров управляется программой;
- восстановление терминала (`stty sane`) обязано выполняться на всех путях выхода, включая аварийный, — это отдельный чекпоинт и хороший повод поговорить о дисциплине освобождения ресурсов (та же тема, что «кто выделил — тот освобождает» для кучи). Проверено на L 2026-07-30: после `stty sane` снимок `stty -a` совпадает с состоянием до запуска (`icanon` и `echo` включены, `min=1 time=0`).

#### Тайминг кадра: таймаут `poll` — не часы

`poll` ждёт **не более** таймаута и возвращается сразу, как только пришёл байт. Поэтому таймаут нельзя брать за часы кадра: пока клавиша нажата, кадры укорачиваются и змейка ускоряется от самого факта управления. В прогоне §7.2 п. 4 это видно в цифрах: `tick 2 poll=0 c=-1 total=0.30` и `tick 3 poll=1 c=119 total=0.30` — кадр с нажатием занял почти ноль.

Рабочая схема — держать момент следующего шага и отдавать в `poll` остаток времени до него:

```
дедлайн = сейчас + период
пока остаток R = дедлайн − сейчас > 0:
    poll(R): пришла клавиша — забрать, обработать, пересчитать R
шаг игры: сдвинуть змейку, отрисовать кадр, дедлайн += период
```

Тогда темп задаёт программа: движение идёт строго раз в период, а нажатие обрабатывается за миллисекунды, а не в следующем кадре. Нарастание скорости (чекпоинт 5) — уменьшение периода, например от 150 к 80 мс.

Для главы это ещё и наглядный урок о природе ассемблера: период — обычный аргумент вызова (у `poll` третий, значит `%edx` по SysV), то есть константа в инструкции или число в памяти. Ни процессор, ни ассемблер не знают, что это миллисекунды, и ждать не умеют — время отсчитывает ядро, смысл числу придаёт документация вызванной функции.

Наивная версия «таймаут = кадр» собирается, запускается и выглядит работающей: ошибка видна только глазом, при удержании клавиши. Готовая рубрика «типичные ошибки» для части 8.Ч2.

#### Как верифицируется интерактивная программа

Правило §2.3 «весь код прогоняется» для игры выполнимо: партия проигрывается скриптом, который подаёт нажатия в псевдотерминал и читает кадры вывода. Приём **проверен 2026-07-30 на обоих контурах** — на M и на L (§7.2): управляющий скрипт на `pty` подал нажатие и получил ожидаемый код клавиши; он же поймал зависание отвергнутого кандидата 1, то есть годится и как ловушка на регресс. Это прямой аналог того, чем в C-книге проверяли Win32-главу (автоматизация через P/Invoke).

### 4.3. Открытый вопрос охвата

Нужна ли вещественная арифметика (SSE, регистры `xmm`). Минимум её придётся задеть в главе 5 — вызов `printf` с `%f` в SysV требует передавать в `%al` число векторных аргументов, и без объяснения это выглядит магией. Решение — «минимально необходимое в гл. 5» или «отдельная часть» — принимается на этапе 2.

---

## 5. Кандидаты в реестр источников (этап 1 выполнен 2026-07-30)

> **Итог этапа 1:** реестр собран — `books/asm-book/sources.md` (23 источника: A1–A5 учебники и курсы, S1–S8 спецификации, T1–T10 документация инструментов), все ссылки открыты из сессии в дату проверки, матрица покрытия по главам приведена в его §4. Список ниже сохранён как исходный перечень кандидатов; расхождения между ним и реестром описаны сразу после таблицы.

Список для проверки, а не утверждённый реестр. Каждый кандидат проходит критерии Плана §4.2 с фиксацией даты проверки; в текст ничего не попадает до внесения в `sources.md`.

**Академические:** Computer Systems: A Programmer's Perspective (Bryant, O'Hallaron) — глава о машинном представлении программ, написана под контур x86-64/SysV/AT&T; открытый курс CMU 15-213 на её основе; Dive into Systems (свободный онлайн-учебник).

**Официальные спецификации:** Intel 64 and IA-32 Architectures Software Developer's Manuals; AMD64 Architecture Programmer's Manual; System V AMD64 psABI (соглашение о вызове, выравнивание, red zone); документация Apple по x86-64 на macOS (расхождения контура M).

**Документация инструментов:** GNU as (GAS) manual; LLVM/Clang; objdump (binutils и LLVM); LLDB.

**Под итоговую главу (§4.2):** спецификация POSIX на `stty` (нормативный арбитр по параметрам `raw`/`min`/`time`, одинаковым на обоих контурах); стандарт ECMA-48 на управляющие последовательности терминала (нормативный источник по ANSI-escape для отрисовки кадра) — оба нужны, потому что «работает у меня в терминале» правилом Плана §4.4 не считается.

**Стенды:** Compiler Explorer.

Пробел, который надо закрыть на этапе 1: правило Плана §4.3 требует **двух независимых источников на каждую часть**. Для тем «регистры/инструкции/ABI» это выполняется легко (учебник + спецификация). Для тем «как оформить `.s` под Mach-O» независимых авторитетных источников может не найтись — тогда часть помечается как опирающаяся на официальную документацию Apple + собственный прогон, по образцу того, как в C-книге решён вопрос с egui (глава 26).

### 5.1. Что этап 1 изменил в этом списке (2026-07-30)

Проверка кандидатов дала четыре расхождения с ожиданиями; все они зафиксированы в `sources.md` §6.

1. **Пробел по Mach-O подтвердился ровно в предсказанном виде.** Синтаксис `.section __TEXT,__text` описан только в архивной «OS X Assembler Reference» (покрывает PowerPC и i386, помечена Apple как не обновляемая), а действующая страница Apple о директивах не говорит. Принят запасной путь, описанный выше: официальная документация Apple + собственный прогон на контуре M, с оговоркой в тексте книги.
2. **Intel SDM недоступен из рабочего окружения** (403 на запросы агента; скачивается лишь сборка тома 2 от декабря 2022 при актуальной 092). Арбитром по семантике инструкций назначено **руководство AMD64** — открыто, актуально (тома 1 и 3 датированы 2025 годом), описывает ту же архитектуру. Intel SDM остаётся параллельным источником и цитируется только после сверки на доступной сборке.
3. **Найден дополнительный учебник — Р. Плантц, «Introduction to Computer Organization … x86-64»** (No Starch, 2022). Проверка кода книги показала, что она написана в **Intel-синтаксисе** (`.intel_syntax noprefix` поверх GNU as), поэтому в качестве образца листингов основного текста непригодна — зато закрывает параграф о втором диалекте (NFR-3) и даёт дидактику «пишем свои программы, а не читаем вывод компилятора».
4. **Для главы-моста добавлена официальная документация Microsoft по x64 calling convention** — врезка об отличиях Windows-контура (§4, глава 9) теперь опирается на первоисточник, а не на пересказ.

Подтвердилось без оговорок: CS:APP3e (гл. 2–3, оглавление сверено по PDF предисловия), открытые материалы CMU 15-213 (слайды скачиваются без логина, синтаксис — AT&T), Dive into Systems (гл. 7, дословное заявление об AT&T), Stanford CS107, System V AMD64 psABI (версия 1.0 от 12.03.2025), POSIX.1-2024 по `stty`/термина́лу/`poll`/`read`, ECMA-48, документация GNU as, GCC, clang, objdump/llvm-objdump, LLDB, GDB и glibc. Compiler Explorer проверен и оставлен **стендом, а не источником**: по FR-5 всё, что попадает в текст, прогоняется локально на обоих контурах.

---

## 6. Критерии готовности

- [x] `sources.md` собран (2026-07-30), все ссылки проверены с датой, каждая тема покрыта ≥2 независимыми источниками; единственное исключение — оформление `.s` под Mach-O — оговорено явно (реестр §6 п. 2, здесь §5.1 п. 1).
- [ ] `outline.md` утверждён владельцем до написания первой главы.
- [ ] Все главы соответствуют структуре Плана §2; в каждой части 6–15 заданий с нарастающей сложностью 30/40/30.
- [ ] **Каждая программа книги прогнана на обоих контурах (L и M)**, выводы в тексте дословные; расхождения контуров оформлены врезками.
- [ ] Нет догадок (Правило Плана §4.4): непроверенное либо подтверждено, либо не вошло и записано в «Открытые вопросы».
- [ ] Книга самодостаточна: не требует знаний, не введённых в ней самой (проверка отдельным пунктом ревизии — ассемблер особенно склонен к «забеганиям вперёд», потому что ABI, стек и адресация взаимозависимы).
- [ ] **Оба итоговых проекта** (FR-4: VM и змейка) написаны целиком, собираются и запускаются на обоих контурах; вместе задействуют материал всех предыдущих глав; эталонные реализации приведены в `solutions/` полностью, а не фрагментами.
- [ ] Змейка проверена сценарным прогоном (скрипт подаёт нажатия в псевдотерминал, кадры сверяются), терминал корректно восстанавливается на всех путях выхода, включая аварийный.
- [ ] Финальная глава реально стыкуется с `c-and-rust-book`: сквозной пример, доведённый от ассемблера до C.
- [ ] `solutions/` покрывает 100% ID, код прогнан, вариативность по Плану §6 п. 5.
- [ ] Пройдены ревизия (этап 4) и финальная сверка ID (этап 6).

---

## 7. Открытые вопросы и риски

| # | Вопрос / риск | Влияние | Предлагаемое действие |
|---|---|---|---|
| ~~В1~~ | Отсутствие `rustup` на маке | — | **Закрыт 2026-07-30 решением владельца: мак используется только для ассемблера.** Rust-заходы (решения глав 19–26 C-книги) ведутся на виндовой машине, где тулчейн уже настроен; ставить rustup на мак не нужно |
| В2 | Смена машины не отражена в журнале C-книги (§10 описывает только винду: MinGW GCC 15.2, WSL1, rustup gnu) | Следующий заход по любой книге начнёт с неверными предпосылками об окружении | Дописать в `C-and-Rust-learning.md` §10 запись о втором контуре (Mac M3, macOS 15.7.4, Apple clang 17, Rosetta 2, состав тулчейна) |
| В3 | Доступность цели `x86_64` в Xcode CLT зависит от версии SDK и наличия Rosetta 2 | Контур M может отвалиться на будущих macOS | Зафиксировать версии в журнале; запасной путь — Docker/Lima с Linux x86-64 либо переход на ARM64 (потребует пересмотра источников) |
| ~~В4~~ | Основной синтаксис ассемблера | — | **Закрыт 2026-07-30**: AT&T (GAS), решение Р5 |
| В5 | Врезки-ссылки на дополнение внутри `c-and-rust-book` | Правка глав, уже закрытых ревизией | Согласовать отдельно; по умолчанию **не трогаем** C-книгу вообще |
| ~~В6~~ | Объём вещественной арифметики (SSE/`xmm`) | — | **Закрыт 2026-07-30 на этапе 2** (решение П1, `outline.md` §2): минимум в части 5.Ч4 — правило `%al` для вариативных функций и передача `double` в `%xmm0`; полное изложение — обзорная часть 7.Ч4 с пометкой о статусе |
| ~~В7~~ | Тема финальной работы | — | **Закрыт 2026-07-30**: виртуальная машина байт-кода, решение Р6, состав в §4.1 |
| ~~В8~~ | Объём итоговой главы: два полноценных проекта | — | **Закрыт 2026-07-30 владельцем**: пишем по одной части за заход (Плана §5 это прямо допускает для крупных глав) — 4 захода на главу 8 вместо двух. При перерасходе расширения (новые опкоды VM, режимы игры) выносятся в задания вместо текста |
| ~~В9~~ | `stty raw` проверен только на контуре M | — | **Закрыт 2026-07-30 прогоном в WSL с отрицательным результатом**: вариант с `min 0 time 1` на контуре L неработоспособен (pty в WSL1 игнорирует `VMIN`/`VTIME`, чтение блокируется). Механизм заменён на `poll(2)` с таймаутом кадра и на L проверен — §4.2; протокол прогона — §7.2. Порождён В10 |
| ~~В10~~ | `poll(2)`-вариант ввода проверен только на контуре L | — | **Закрыт 2026-07-31 прогоном на контуре M** (§7.2, пп. 7–10): структура вывода совпала с эталоном L, три прогона подряд стабильны, `stty sane` восстанавливает пользовательские настройки терминала. **Ограничение на 8.Ч2 снято** |
| ~~В11~~ | Принимает ли ассемблер контура M директивы `.text`, `.data`, `.globl`, `.p2align`, `.asciz` в том же написании, что GNU as (открыт на этапе 2 решением П6 `outline.md` §2) | — | **Закрыт 2026-07-31 прогоном на контуре M** (§7.2, пп. 1–6). Решение П6 (одна каноническая форма листинга + врезки) сохраняется, но с двумя уточнениями: (1) `.section .rodata` на M не принимается, поэтому в листингах основного текста неизменяемые данные до 6.Ч3 кладутся в `.data`, а различие написаний разбирается врезкой; (2) подчёркивание касается всех глобальных символов и всех импортов libc, поэтому врезка № 1 в 1.Ч3 формулируется как правило, а не как замечание об одном символе |
| ~~В12~~ | Форма таблицы переходов, работающая на обоих контурах при сборке по умолчанию, то есть позиционно-независимой (открыт на этапе 2 самопроверкой программы). Кандидаты: таблица абсолютных адресов `.quad` с загрузкой базы через `lea tbl(%rip)`; таблица 32-битных смещений от базы | — | **Закрыт 2026-07-31 двухконтурно** (§7.2, пп. 12–14 на M и пп. 15–18 на L): форма книги — **кандидат Б**, 32-битные смещения от базы самой таблицы, таблица в неизменяемых данных. Обе реализации компилятора на обоих контурах порождают именно её. Кандидат А уходит в «типичные ошибки» с двумя разными исходами: на M — отказ компоновки, на L — предупреждение, `DT_TEXTREL` и правка адресов загрузчиком |
| В14 | Строка `.section .note.GNU-stack,"",@progbits`: на контуре L рукописный листинг **без неё** собирается с предупреждением компоновщика и получает **исполняемый стек** (§7.2 п. 19). Принимает ли её ассемблер контура M — неизвестно: `.section` на Mach-O требует формы «сегмент, секция», а `@progbits` — понятие ELF | Форма листингов всей книги (решение П6) и врезка № 1 в 1.Ч3 — то есть **глава 1** | Одна проба на маке (§7.1 **п. 7**). Принимается — строка входит в каноническую форму листинга; отвергается — уходит во врезку 1.Ч3 как «на L дописать эту строку». **Блокер старта этапа 3** (решение владельца 2026-07-31: «ждём мака обязательно, это блок для старта»): до ответа глава 1 **не начинается**. Прежняя формулировка — писать главу 1 с предварительной формой листинга — отменена: глава 1 задаёт образец для восьми остальных, и при любом исходе пробы её пришлось бы перепроверять целиком. Разбор — `Asm-learning.md` §5.1 |
| В13 | Реестр источников не покрывает инструмент диагностики кучи для 6.Ч4 (на L — valgrind) и не даёт арбитра по семантике libc, верного на **обоих** контурах: T8 — руководство glibc, то есть документация контура L | 6.Ч4, 8.Ч1 и блоки «Источники» всех частей, где вызывается libc | Добор к этапу 1 **до захода по главе 6**: внести документацию valgrind строкой реестра и расширить применение S7 (POSIX.1-2024) на семантику библиотечных функций, оставив T8 деталям реализации L. Правится `sources.md` §3 и матрица §4 |

### 7.1. Действия, привязанные к конкретной машине

Часть проверок нельзя выполнить с мака. Они выписаны отдельно, чтобы не потерялись между сессиями.

> **Раздел заморожен 2026-07-31**, как и §7.2: он остаётся историей выполненных проб (пп. 1–6 закрыты), а **действующий** список машинозависимых действий ведётся в `Asm-learning.md` §9.3. На дату заморозки открыт один пункт — п. 7 (проба `.note.GNU-stack` на маке, закрывает В14).

**На виндовой машине (распоряжение владельца 2026-07-30) — оба пункта выполнены 2026-07-30, протокол прогона и состав окружения в §7.2:**

1. **Прогон сырого ввода в WSL** (закрывал В9). ✅ Выполнен; **результат отрицательный** — из трёх утверждений ниже не подтвердилось второе в части «не блокируя цикл». Проверялось, что на контуре L ведёт себя так же, как подтверждено на маке:
   - `system("stty raw -echo min 0 time 1")` включает сырой режим без эха;
   - `getchar()` возвращает код клавиши без Enter, а при отсутствии нажатия — `-1`, не блокируя цикл;
   - `system("stty sane")` восстанавливает терминал.

   Прогонять в настоящем псевдотерминале (управляющий скрипт подаёт нажатия и читает вывод) — в обычном пайпе `stty` работать не будет: терминала нет. Результат — в журнал `Asm-learning.md` с датой и версиями.

   Проверочная программа (та же, что отработала на маке; на C, потому что проверяется механизм, а не ассемблерная его запись):

   ```c
   #include <stdio.h>
   #include <stdlib.h>
   int main(void) {
       system("stty raw -echo min 0 time 1");
       for (int i = 0; i < 10; i++) {
           int c = getchar();
           printf("tick %d c=%d\r\n", i, c);
           fflush(stdout);
       }
       system("stty sane");
       return 0;
   }
   ```

   Управляющий скрипт: открыть псевдотерминал (`pty.openpty()` в Python), запустить программу с этим pty на stdin/stdout, через ~0.3 с записать в мастер один байт `w`, дочитать вывод. Ожидаемый результат — строка с `c=119` (клавиша дошла без Enter и без эха) и строки с `c=-1` на итерациях без нажатия (чтение не заблокировалось). Вывод контура M для сверки: `tick 0 c=119`, далее `tick 1..9 c=-1`.

   *Поведение разошлось.* Фактический вывод на L: `tick 0 c=119`, затем зависание на втором чтении. В запасной путь с `termios` и двумя платформенными модулями ввода при этом идти не потребовалось: таймаут перенесён из терминала в программу (`poll(2)`, §4.2, кандидат 2), и модуль ввода остаётся один на оба контура. На утверждение владельцу выносится именно этот вариант — В10; `termios` остаётся резервом, если `poll` не подтвердится на маке.

2. Зафиксировать актуальный состав тулчейна WSL (версии gcc/clang/binutils, наличие `lldb`) — записи в журнале C-книги датированы 2026-07-05 и могли устареть. ✅ Выполнено, состав — в §7.2. Существенное: **`lldb` в WSL не установлен**, а FR-6 назначает его основным отладчиком — понадобится `apt install lldb` к главе 7; на контуре L есть `gdb`. *(Устранено 2026-07-31 — см. п. 6 ниже.)*

**На маке (контур M) — оба пункта выполнены 2026-07-31, протокол прогона и состав окружения в §7.2:**

3. **Прогон `poll(2)`-варианта ввода** (закрывал В10, разблокировал главу 8 Ч2). ✅ Выполнен; **результат положительный**: структура вывода совпала с эталоном L, три прогона подряд стабильны. Единственная поправка — к управляющему скрипту, а не к механизму: под Rosetta запуск программы вместе с `system("stty raw -echo")` занимает дольше 0.3 с, поэтому нажатие отсчитывается от первого вывода программы, а не от запуска процесса (§7.2 п. 7). Схема — та, что подтверждена на L: `stty raw -echo` без `min`/`time`, ожидание через `poll` по дескриптору 0 с таймаутом 100 мс, `read(0, &b, 1)` при готовности, `stty sane` на выходе. Управляющий скрипт — тот же, что в пункте 1 (`pty.openpty()`, нажатие `w` через ~0.3 с). Вывод контура L для сверки: `tick 0 poll=0 c=-1 total=0.10`, `tick 1 poll=0 c=-1 total=0.20`, `tick 2 poll=0 c=-1 total=0.30`, `tick 3 poll=1 c=119 total=0.30`, `tick 4 poll=0 c=-1 total=0.40`, код возврата 0. Результат — в §7.2 (позже — в журнал `Asm-learning.md`).

   Проверочная программа (прогнана на L 2026-07-30 дословно в этом виде):

   ```c
   #define _POSIX_C_SOURCE 200809L   /* под -std=c17 объявления POSIX скрыты */
   #include <stdio.h>
   #include <stdlib.h>
   #include <unistd.h>
   #include <poll.h>
   #include <time.h>

   static double now(void) {
       struct timespec ts;
       clock_gettime(CLOCK_MONOTONIC, &ts);
       return ts.tv_sec + ts.tv_nsec / 1e9;
   }

   int main(void) {
       system("stty raw -echo");          /* без min/time: таймаут даёт poll */
       double t0 = now();
       for (int i = 0; i < 5; i++) {
           struct pollfd pfd = { .fd = 0, .events = POLLIN };
           int r = poll(&pfd, 1, 100);    /* 100 мс — кадр игры */
           int c = -1;
           if (r > 0 && (pfd.revents & POLLIN)) {
               unsigned char b = 0;
               if (read(0, &b, 1) == 1) c = (int)b;
           }
           printf("tick %d poll=%d c=%d total=%.2f\r\n", i, r, c, now() - t0);
           fflush(stdout);
       }
       system("stty sane");
       return 0;
   }
   ```

   Грабли сборки, пойманные на L: без `#define _POSIX_C_SOURCE 200809L` строгий `-std=c17` не объявляет `clock_gettime` (`error: 'CLOCK_MONOTONIC' undeclared`). Либо macro, либо `-std=gnu17`.

4. **Прогон эталонного `.s` и опрос ассемблера** (закрывал В11 и п. 3 реестра `sources.md` §6; добавлено этапом 2). ✅ Выполнен; результат: `as` на маке — **не GNU as, а обёртка над `clang -cc1as`**; директивы книги приняты в написании GNU as все, кроме секции неизменяемых данных; расхождение имён символов проявляется на компоновке и охватывает все глобальные символы и импорты libc. Разбор — §7.2 пп. 1–6. Исходная постановка: выяснить, чем является `as` на маке (`as --version`) и принимает ли он директивы в том же написании, что GNU as. Минимальная программа главы 1 (вывод строки через `puts`) собирается двумя путями: `clang prog.s -o prog` и раздельно — `as prog.s -o prog.o` плюс компоновка через `clang prog.o -o prog`; в обоих случаях с `-arch x86_64`. Проверяются: имя точки входа (`main` против `_main`), директивы `.text`, `.data`, `.globl`, `.p2align`, `.asciz`, RIP-относительное обращение к строке (`lea msg(%rip), %rdi`). Результат определяет форму листингов всей книги — решение П6 в `outline.md` §2. Записать в §7.2 с версиями.

**На виндовой машине, вторая сессия — оба пункта выполнены 2026-07-31, протокол в §7.2 (пп. 15–20):**

5. **Половина L прогона по В12** — те же четыре варианта таблицы переходов, что на маке (кандидат А в изменяемых и в неизменяемых данных, кандидат Б в секции кода и в неизменяемых данных), каждый рабочей программой с диспетчером на четыре ветви, сборка по умолчанию. ✅ Выполнено; **В12 закрыт**: форма книги — кандидат Б. Ожидание, записанное перед прогоном («Б работает в обоих размещениях, А в неизменяемой секции не компонуется»), подтвердилось наполовину: про Б — полностью, а вот **А на контуре L компонуется**, в отличие от M, — с двумя предупреждениями и ценой в виде `DT_TEXTREL`. Расхождение контуров записано в §7.2 п. 16 и идёт в текст 4.Ч3.
6. **`apt install lldb` на контуре L** (предпосылка FR-6 и захода по главе 2). ✅ Выполнено: `lldb` 18.1.3 (`/usr/bin/lldb`, пакеты `lldb 1:18.0-59~exp2`, `lldb-18`, `liblldb-18`, `python3-lldb-18`). Отладчик книги теперь есть на обоих контурах; на L дополнительно остаётся `gdb` как альтернатива.

**На маке (контур M) — одно новое действие, порождённое прогоном 2026-07-31; с того же дня оно блокирует старт этапа 3:**

7. **Проба `.note.GNU-stack`** (закрывает **В14**; с 2026-07-31 — **блокер старта этапа 3**: до неё глава 1 не начинается). Собрать `as -arch x86_64` файл, в конце которого стоит строка `.section .note.GNU-stack,"",@progbits`, и записать реакцию ассемблера. Причина: на контуре L без этой строки любой рукописный листинг книги компонуется с предупреждением `missing .note.GNU-stack section implies executable stack` и получает сегмент `GNU_STACK` с правами `RWE` (§7.2 п. 19). Если ассемблер M строку принимает — она входит в каноническую форму листинга книги; если отвергает (что вероятно: `.section` на Mach-O требует формы «сегмент, секция») — уходит во врезку № 1 в 1.Ч3 рядом с правилом подчёркивания. Проверить заодно, есть ли у листингов на M аналогичная проблема с правами стека.

~~**Ограничение:** глава 8 (часть про змейку) не пишется, пока не закрыт **В10**~~ — **снято 2026-07-31**: механизм ввода подтверждён на обоих контурах, 8.Ч2 разблокирована. Действующих ограничений порядка три: **этап 3 не начинается, пока открыт В14** (блокер старта, решение владельца 2026-07-31 — п. 7 выше), **В13 закрывается до главы 6**, **глава 9 пишется после 8.Ч2**, потому что её сквозной пример — та самая змейка.

### 7.2. Журнал прогонов и окружения

> **Раздел заморожен 2026-07-31** — в день создания `Asm-learning.md` (FR-7). Записи ниже остаются здесь как исторический протокол: на их пункты ссылается программа (`outline.md`) в полутора десятках мест формой «требования §7.2 п. N», и перенос порвал бы эти ссылки. **Новые прогоны, замеры и договорённости пишутся в журнал `Asm-learning.md` §9**; сводка окружения и действующие платформенные факты — там же (§9.1–9.2), список открытых машинозависимых действий — §9.3.

**2026-07-30, контур L (виндовая машина, WSL1 Ubuntu 24.04). Выполнены пункты 1 и 2 §7.1.**

Состав тулчейна на дату прогона: ядро `4.4.0-19041-Microsoft` (WSL1; WSL2 на машине недоступен — виртуализация выключена в BIOS), gcc 13.3.0, clang 18.1.3, GNU as (binutils) 2.42, `objdump` — binutils, `gdb` — есть (`/usr/bin/gdb`), **`lldb` отсутствует** (не ставился), python3 3.12.3, coreutils `stty` 9.4.

Прогоны: сборка `gcc -std=c17 -Wall -Wextra` (без замечаний), исполнение — в настоящем псевдотерминале (`pty.openpty()` в Python), нажатие `w` подаётся в мастер через 0.3 с, стенд после прогона удалён.

1. **Программа §7.1 п. 1 дословно** (`stty raw -echo min 0 time 1` + `getchar`): напечатала `tick 0 c=119` и зависла — второй `getchar` не вернулся, процесс снят по таймауту 6 с. Ожидалось по контуру M: `tick 0 c=119`, далее `tick 1..9 c=-1`.
2. **Настройки терминала применяются**: `stty -a` внутри pty после `stty raw -echo min 0 time 1` даёт `min = 0; time = 1;`, `-icanon`, `-echo`. То есть `stty` не отказывает и не врёт.
3. **Причина не в буферизации stdio**: та же логика сырым `read(2)` дала `tick 0 n=1 c=119 dt=0.39`, затем зависание. Вывод: pty в WSL1 игнорирует `VMIN`/`VTIME` — чтение блокируется вместо возврата 0 байт по истечении таймаута.
4. **`poll(2)`-вариант** (`stty raw -echo` без `min`/`time`, `poll` с таймаутом 100 мс, затем `read`): `tick 0 poll=0 c=-1 total=0.10`, `tick 1 poll=0 c=-1 total=0.20`, `tick 2 poll=0 c=-1 total=0.30`, `tick 3 poll=1 c=119 total=0.30`, `tick 4 poll=0 c=-1 total=0.40`, код возврата 0. Тики ровно по таймауту, блокировки нет, клавиша доходит без Enter и без эха.
5. **`stty sane` восстанавливает терминал**: снимки `stty -a` до запуска и после совпадают — `icanon` включён, `echo` включён, `min=1`, `time=0`.

Итог: из трёх утверждений §7.1 п. 1 подтверждены сырой режим без эха, приход клавиши без Enter и восстановление терминала; **неблокирующее чтение по таймауту терминала на контуре L не работает**. Механизм ввода змейки заменён на `poll(2)` (§4.2, кандидат 2) — открыт В10 на подтверждение с мака.

**2026-07-31, контур M (Mac M3, macOS 15.7.4, сборка 24G517, ядро Darwin 24.6.0, хост arm64). Выполнены пункты 3 и 4 §7.1 — закрыты В10 и В11.**

Состав тулчейна на дату прогона: Apple clang 17.0.0 (clang-1700.0.13.5), Command Line Tools в `/Library/Developer/CommandLineTools` (`xcode-select -p`), компоновщик `ld-1167.5`, `objdump` — Apple LLVM 17.0.0 (`/usr/bin/objdump`), **`lldb` есть** (`/usr/bin/lldb`), **`gdb` отсутствует** — то есть предпосылка FR-6 («основной отладчик книги — `lldb`») на контуре M подтверждена; python3 3.14.6 (homebrew, только как управляющий скрипт); Rosetta 2 работает (`arch -x86_64 /usr/bin/true` — код 0, пакет `com.apple.pkg.RosettaUpdateAuto` установлен). Всё собиралось с `-arch x86_64`; `file` на каждом результате даёт «Mach-O 64-bit executable x86_64».

**В11 — ассемблер и форма листингов (пункт 4 §7.1).**

1. **Чем является `as` на маке.** `as --version` печатает баннер `Apple clang version 17.0.0 (clang-1700.0.13.5)`, а `as -v` показывает фактический вызов `clang -cc1as -triple …`. То есть `as` на контуре M — **обёртка над встроенным ассемблером LLVM, а не GNU as**; отдельного документа на него нет (п. 3 реестра `sources.md` §6 закрывается этим прогоном, а не источником).
2. **Эталонная программа главы 1** (вывод строки через `puts`, RIP-относительное `leaq msg(%rip), %rdi`) в маковской форме (`_main`, `call _puts`) собралась **обоими путями** и отработала: `clang -arch x86_64 prog.s -o prog` → «Hello from asm», код 0; `as -arch x86_64 prog.s -o prog.o` плюс компоновка `clang -arch x86_64 prog.o -o prog` → тот же результат.
3. **Та же программа в ELF-форме** (`main`, `call puts`) **ассемблируется без единого замечания** и падает только на компоновке: `Undefined symbols for architecture x86_64: "_main", referenced from: <initial-undefines>; "puts", referenced from: main in …o`. Отсюда точная формулировка расхождения: дело не в директивах, а в именах символов, и оно касается **и точки входа, и каждой вызываемой функции libc**.
4. **Директивы по одной** (каждая проба ассемблировалась отдельным вызовом `as -arch x86_64`). Приняты: `.text`, `.data`, `.globl`, `.p2align 4, 0x90`, `.balign`, `.align`, `.byte`, `.word`, `.long`, `.quad`, `.zero`, `.space`, `.ascii`, `.asciz`, `.string`, `.comm`, `.set`, `.equ`, локальные метки вида `.Lname` и числовые `1f`/`1b`, `.intel_syntax noprefix`. **Отвергнуты**: `.section .rodata` («unexpected token in '.section' directive») и голая `.rodata` («unknown directive»). Маковские написания секции неизменяемых данных приняты все: `.const`, `.cstring`, `.section __TEXT,__cstring`, `.section __DATA,__const`.
5. **Семантика `.align` действительно различается** — то, о чём предупреждает T1, подтверждено прогоном, а не только документацией: на Mach-O аргумент толкуется как степень двойки. Раскладка `a: .byte 1` / `.align 3` / `b: .quad 2` / `.balign 8` / `c: .quad 3` дала по `nm -n` смещения `a=0`, `b=8`, `c=16`, то есть `.align 3` выровняло на 8. Правило книги (`.p2align`/`.balign` вместо `.align`) остаётся в силе как проверенное.
6. **Полный образец с данными всех типов** (`.long`, `.byte`, `.zero`, `.balign`, `.quad`, `.word`, `.ascii`) и вызовом `printf` с тремя аргументами собрался обоими путями после единственной правки — `.section .rodata` → `.const` — и напечатал `num=42 tag=7 big=1234567890123`.
7. **Попытка сделать листинг буквально переносимым алиасами отвергнута.** `.set _main, main` закрывает точку входа, но `puts` остаётся неразрешённым; добавление `.set puts, _puts` линкуется и программа работает, однако компоновщик печатает `warning: … has malformed LC_DYSYMTAB, expected 2 undefined symbols to start at index 3, found 1` — объектник формально некорректен. Приём в книгу не берётся; в тексте остаётся врезка с правилом подчёркивания.

**В10 — `poll(2)`-ввод (пункт 3 §7.1).** Программа §7.1 п. 3 собрана дословно: `clang -arch x86_64 -std=c17 -Wall -Wextra` — без замечаний (макрос `_POSIX_C_SOURCE` нужен и здесь, как на L). Прогон — тем же управляющим скриптом на `pty.openpty()`.

8. **Первый прогон по букве протокола L** (нажатие через 0.3 с после запуска процесса) дал расхождение: `tick 0 poll=1 c=119 total=0.00`, причём сам символ `w` попал в вывод эхом. Причина не в `poll`: запуск x86-64 бинарника под Rosetta вместе с `system("stty raw -echo")` занимает дольше 0.3 с, поэтому клавиша легла в очередь терминала **до** входа в цикл, когда сырой режим ещё не был включён. Замер: первый вывод программы появляется через **0.13 с** после запуска (на L задержка незаметна).
9. **После переноса отсчёта** на «0.25 с от первого вывода программы» вывод совпал с эталоном L по структуре, три прогона подряд: `tick 0..2 poll=0 c=-1 total=0.10/0.20/0.30`, `tick 3 poll=1 c=119 total=0.35…0.38`, `tick 4 poll=0 c=-1 total=0.45…0.48`, код возврата 0. Тики идут ровно по таймауту, цикл ни на чём не блокируется, клавиша приходит без Enter и без эха.
10. **Наблюдение §4.2 «таймаут `poll` — не часы» воспроизвелось и на M**: тик с нажатием занял 0.05–0.08 с вместо 0.10. Рубрика «типичные ошибки» части 8.Ч2 подтверждена на обоих контурах.
11. **Восстановление терминала.** Снимки `stty -a` до запуска и после выхода расходятся ровно в одном флаге: до — `-pendin`, после — `pendin`. Локализовано отдельной пробой: флаг выставляет **переход «raw → sane»** (ни `stty raw -echo`, ни `stty sane` по отдельности его не дают) и снимает **первое же обычное чтение строки в каноническом режиме**. Пользовательские настройки восстанавливаются полностью: `icanon`, `echo`, `min = 1`, `time = 0` совпадают со снимком «до». На L снимки совпадали целиком — различие контура M записано как факт; на пригодность механизма оно не влияет, потому что бит транзитный.

Итог: **оба вопроса закрыты**. Механизм ввода змейки подтверждён двухконтурно (FR-5) — 8.Ч2 разблокирована; форма листингов определена — решение П6 сохраняется с двумя уточнениями (§7 таблица, `outline.md` §2 и §5.1). Материал, полученный попутно и годный в текст книги: точный вид ошибки компоновщика при ELF-форме на маке (1.Ч3), смещения из пробы `.align`/`.balign` (6.Ч3), задержка старта под Rosetta и эхо клавиши, нажатой до включения сырого режима (8.Ч2, «типичные ошибки»).

**2026-07-31, контур M — половина прогона по В12 (форма таблицы переходов).** Окружение то же. Обе формы прогонялись сборкой по умолчанию, то есть позиционно-независимой; каждая — рабочей программой с диспетчером на четыре ветви, а не только сборкой.

12. **Кандидат А — таблица абсолютных адресов `.quad`, база через `leaq tbl(%rip)`.** В изменяемых данных (`.data`) собирается и работает: диспетчер напечатал `case 0`…`case 3`, код возврата 0. В **неизменяемой** секции (`.const`) компоновка отказывает: `Illegal text-relocations: text-relocation in 'tbl'+0x18 … to '.Lc3'`. Цена варианта видна и в готовом файле: `dyld_info -fixups` показывает по записи `rebase` на каждый элемент таблицы (`__DATA __data 0x1000020… rebase`) — адреса правит загрузчик при старте.
13. **Кандидат Б — таблица 32-битных смещений от собственной базы** (`movslq (%rax,%rcx,4), %rdx` → `addq %rax, %rdx` → `jmp *%rdx`). Работает в **обоих** размещениях: и в секции кода, и в неизменяемой `.const`; релокаций времени загрузки не требует. Разность меток между разными секциями Mach-O представляет нормально — опасение, что это не пройдёт, не подтвердилось.
14. **Эталон компилятора.** `clang -arch x86_64 -O2 -S` из `switch` с шестью несливаемыми ветвями порождает ровно кандидата Б: проверка диапазона `cmpl $5, %edi` / `ja`, затем `leaq LJTI0_0(%rip), %rdx` → `movslq (%rdx,%rcx,4), %rcx` → `addq %rdx, %rcx` → `jmpq *%rcx`; сама таблица — `.long` со смещениями от `LJTI0_0`, оформленными через `.set L0_0_set_N, LBB0_N-LJTI0_0`.

Итог половины: **на контуре M выбор — кандидат Б**. Он единственный ложится в неизменяемую память, не требует правки адресов загрузчиком и совпадает с тем, что читатель увидит в главе 7 в выводе компилятора, — то есть 4.Ч3 и 7.Ч3 будут говорить об одной и той же конструкции. Кандидат А идёт в рубрику «типичные ошибки»: он работает, пока таблица лежит в изменяемых данных, и отказывает ровно тогда, когда её пытаются положить туда, где ей место по смыслу. **В12 закрывается после такого же прогона на контуре L** — по FR-5 форма объявляется единственной только после двух контуров.

**2026-07-31, контур L (виндовая машина, WSL1 Ubuntu 24.04) — половина L по В12 и установка `lldb`. Выполнены пункты 5 и 6 §7.1; В12 закрыт, порождён В14.**

Состав тулчейна — тот же, что в снимке 2026-07-30 (ядро `4.4.0-19041-Microsoft`, gcc 13.3.0, clang 18.1.3, GNU as 2.42, `gdb` есть), с одним изменением: **установлен `lldb` 18.1.3** (`/usr/bin/lldb`; пакеты `lldb 1:18.0-59~exp2`, `lldb-18`, `liblldb-18`, `python3-lldb-18`). FR-6 обеспечен на обоих контурах.

Прогон по В12 повторяет маковский по составу: четыре варианта, каждый — **рабочей программой** с диспетчером на четыре ветви (печатает `case 0`…`case 3`), сборка по умолчанию, то есть позиционно-независимая. Каждый вариант собран и `clang`, и `gcc`: результаты и сообщения компоновщика совпали, поэтому ниже они не разделяются. Стенд после прогона удалён.

15. **Кандидат А — таблица абсолютных адресов `.quad` в изменяемых данных (`.data`).** Собирается, работает, `case 0`…`case 3`, код возврата 0. Цена видна в готовом файле: `readelf -r` даёт **7** записей `R_X86_64_RELATIVE` против **3** у кандидата Б — то есть ровно по одной на элемент таблицы. Это прямой аналог маковских записей `rebase` (§7.2 п. 12): адреса правит загрузчик при старте.
16. **Кандидат А в неизменяемых данных (`.section .rodata`) — главное расхождение контуров.** На M этот вариант **не компонуется** (`Illegal text-relocations`). **На L он компонуется и работает**, но с двумя предупреждениями: `warning: relocation in read-only section '.rodata'` и `warning: creating DT_TEXTREL in a PIE`. В `.dynamic` готового файла появляются `TEXTREL` и `FLAGS TEXTREL` — то есть загрузчик обязан снять защиту записи со страниц, куда легла таблица. Формулировка «типичных ошибок» в 4.Ч3 должна называть **оба** исхода: на маке ошибка видна сразу, на Linux она молчит и остаётся в готовой программе.
17. **Кандидат Б — таблица 32-битных смещений от собственной базы.** Оба размещения — в секции кода и в `.rodata` — собираются и работают; **дополнительных релокаций нет** (3 записи `R_X86_64_RELATIVE`, столько же, сколько у программы без таблицы), `DT_TEXTREL` не появляется. Разность меток из разных секций (`.long .Lc0-tbl`, где метки в `.text`, а таблица в `.rodata`) компоновщик разрешает статически — как и на Mach-O. Размещение подтверждено `nm`: `d tbl` для А/`.data`, `r tbl` для А/`.rodata`, `t tbl` для Б/код, `r tbl` для Б/`.rodata`.
18. **Эталон компилятора на L — тот же кандидат Б.** `gcc -O2 -S` из `switch` с шестью ветвями: `cmpl $5, %edi` / `ja`, затем `leaq .L4(%rip), %rdx` → `movslq (%rdx,%rdi,4), %rax` → `addq %rdx, %rax` → `notrack jmp *%rax`; таблица — `.long .L9-.L4` в `.rodata`. `clang -O2 -S` даёт ту же конструкцию с меткой `.LJTI0_0`, буквально совпадающую с выводом Apple clang на M (§7.2 п. 14). Обратная проверка объясняет происхождение кандидата А: `gcc -O2 -S -fno-pie` порождает именно его — `notrack jmp *.L4(,%rdi,8)` и таблицу `.quad`. Значит А не ошибочен сам по себе, он принадлежит непозиционно-независимой сборке, которой книга не пользуется. *Замечание к постановке эксперимента:* если все шесть ветвей отличаются только строкой-аргументом `puts`, clang 18 сворачивает `switch` не в таблицу переходов, а в таблицу **адресов строк** (`.Lreltable.f`, те же 32-битные смещения) с хвостовым вызовом; чтобы получить настоящую таблицу переходов, ветви должны выполнять разные действия.
19. **Побочная находка, которой не было в маковском прогоне: рукописный `.s` на L даёт исполняемый стек.** Каждая сборка листинга без завершающей строки `.section .note.GNU-stack,"",@progbits` сопровождается предупреждением `missing .note.GNU-stack section implies executable stack`, и это не косметика: `readelf -l` показывает у полученного файла сегмент `GNU_STACK` с правами **`RWE`**. Со строкой предупреждение исчезает, права становятся `RW`, поведение программы не меняется. Компилятор ставит эту строку в свой вывод всегда — она видна в конце любого `gcc -S`/`clang -S`. Касается **всех** листингов книги на контуре L, начиная с первой программы главы 1, поэтому оформлено отдельным вопросом **В14**: примет ли эту строку ассемблер контура M.
20. **Мелочь в копилку 7.Ч1** (список служебного в выводе компилятора): на L к `.cfi_*`, `.L…`, `.type`/`.size` и `@PLT` добавляются префикс `notrack` у косвенного перехода и секция `.note.gnu.property` — оба следствия включённой по умолчанию защиты потока управления (`-fcf-protection`), — а также сама `.note.GNU-stack`.

Итог: **В12 закрыт двухконтурно** (FR-5). Форма таблицы переходов в книге одна — **кандидат Б**, таблица лежит в неизменяемых данных; 4.Ч3, оба диспетчера главы 8 и разбор вывода компилятора в 7.Ч3 говорят об одной и той же конструкции. Кандидат А остаётся в «типичных ошибках» с двумя исходами по контурам. Снято ограничение «до главы 4 закрыть В12»; установкой `lldb` снято и ограничение перед главой 2. Порождён В14 — единственное машинозависимое действие, оставшееся в §7.1.

---

## 8. Что дальше

1. ~~**Утвердить или поправить** этот документ~~ — сделано; на момент записи (2026-07-30) открытыми оставались В2, В3, В5, В6, В10 и охват §4. **Актуальный список открытых вопросов — таблица §7**: после этапа 2, маковской сессии и виндовой сессии 2026-07-31 в нём остались В2, В3, В5, В13 и новый В14.
2. ~~**Этап 1** — исследование источников → `books/asm-book/sources.md`~~ — **сделано 2026-07-30**: реестр собран, ссылки проверены, матрица покрытия глав — в §4 реестра, оговорки и пробелы — в §6 реестра и в §5.1 этого документа.
3. ~~**Этап 2** — программа курса → `books/asm-book/outline.md`~~ — **закрыт 2026-07-30**: 9 глав, 28 частей, семь решений этапа 2 (§2 программы) закрывают в том числе В6, размещение мини-проектов и загрузку байт-кода. Приёмку владелец делегировал агенту (распоряжение 2026-07-30: «я ничего не смыслю в ассемблере, исправляй всё, что ошибочно») — программа доведена до v1.1 самопроверкой на связность, монотонность и запрет догадок; перечень исправленного — §8 программы, порождённые вопросы — В12 и В13.
4. ~~Создать `books/asm-book/Asm-learning.md` (операционный документ + трекер), перенести в его журнал список §7.1~~ — **сделано 2026-07-31**: FR-7 закрыт, документ содержит жёсткие правила захода, шаблоны главы и решений, трекер по 13 заходам / 28 частям, журнал окружения (§9) и шаблон промпта. Разделы §7.1–7.2 этого файла заморожены как исторический протокол и остались на месте из-за перекрёстных ссылок программы; действующий список машинозависимых действий — `Asm-learning.md` §9.3. Дальше — этап 3, глава 1, **но только после закрытия В14** (решение владельца 2026-07-31: проба на маке — блокер старта; разбор — `Asm-learning.md` §5.1). До неё на виндовой машине возможен один заход — добор реестра по В13.
5. ~~В первой сессии на виндовой машине — выполнить §7.1~~ — **сделано 2026-07-30**, протокол в §7.2. ~~Осталось одно машинозависимое действие: §7.1 п. 3 на маке~~ — **сделано 2026-07-31** (§7.1 пп. 3–4, протокол в §7.2): закрыты В10 и В11. Виндовой сессией 2026-07-31 выполнены пп. 5–6 §7.1: **закрыт В12** (форма таблицы переходов — кандидат Б) и поставлен `lldb` на контуре L. Машинозависимое действие снова одно, и оно на маке: **§7.1 п. 7** — проба `.note.GNU-stack`, закрывающая порождённый этим прогоном **В14**. Решением владельца от 2026-07-31 эта проба — **блокер старта этапа 3**: глава 1 не начинается до ответа. Открытые вопросы, не привязанные к машине: В13 (до главы 6 — и единственный заход, возможный на виндовой машине, пока держится блокер), В2, В3, В5.
