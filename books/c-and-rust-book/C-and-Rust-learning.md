# C-and-Rust-learning — программа написания самоучителя

> **Операционный документ для код-агента (фреймворк SuperClaude).**
> Определяет, как наполнять самоучитель «C, затем Rust: системное программирование с нуля».
> Нормативная база: `books/self-education-book-create-plan.md` (далее — «План»). При противоречии этого файла и Плана приоритет у Плана.

---

## 1. Контекст и артефакты

| Артефакт | Путь | Состояние |
|---|---|---|
| План (методология) | `books/self-education-book-create-plan.md` | утверждён |
| Реестр источников (этап 1) | `books/c-and-rust-book/sources.md` | ✅ готов (проверен 2026-07-05) |
| Программа курса (этап 2) | `books/c-and-rust-book/outline.md` | ✅ утверждена владельцем 2026-07-05 (v1.1, 26 глав) |
| Главы самоучителя (этап 3) | `books/c-and-rust-book/chapters/NN-<slug>.md` | ⏳ наполняются по этой программе |
| Сборник решений (этап 5) | `books/c-and-rust-book/solutions/NN-<slug>.md` | ⏳ после этапа 4 |
| Эта программа + трекер | `books/c-and-rust-book/C-and-Rust-learning.md` | живой документ: агент обновляет трекер (§7) после каждого захода |

Язык материалов — **русский**; устоявшиеся английские термины в скобках при первом употреблении (План, §7).

## 2. Жёсткие правила (действуют в каждом заходе)

1. **Никаких догадок** (План, §4.4): каждое фактическое утверждение подтверждено источниками из `sources.md`; не удалось подтвердить — не пишем, фиксируем в «Открытых вопросах» `outline.md`.
2. **Минимум два независимых источника на часть** (План, §4.3); идентификаторы (C1…C12, R1…R11) — в блоке «Источники» в конце части. Арбитры при расхождениях: C5 (стандарт C) для тома I, R7 (Rust Reference) для тома II.
3. **Весь код проверяется запуском**: C — компиляция `gcc -std=c17 -Wall -Wextra` (для глав ≥7 дополнительно прогон под `-fsanitize=address` там, где уместно); Rust — `cargo build && cargo test` (или `rustc` для одиночных файлов). Некомпилирующийся фрагмент в текст не попадает, кроме специально помеченных примеров ошибок («⛔ не компилируется — и вот почему»). GUI-главы (12 и 26): агент проверяет компиляцию и smoke-запуск (окно открывается без ошибок); функциональную проверку интерфейса выполняет владелец на ревизии — это фиксируется в отчёте захода как открытый пункт.
4. **Только пройденный материал**: глава N ссылается лишь на главы 1…N−1. Проверка «забеганий вперёд» — обязательный пункт ревизии.
5. **Решений в главах нет** — только условия заданий. Решения — исключительно в `solutions/`.
6. **Прямое копирование текста источников запрещено** — только пересказ/синтез со ссылкой.
7. Новый источник — сначала в `sources.md` (с проверкой по критериям §4.2 Плана и датой), потом в текст.

## 3. Шаблон главы (`chapters/NN-<slug>.md`)

```markdown
# Глава N. <Название>

## Введение
<зачем тема, где применяется, что читатель будет уметь — План §2.1>

## Часть N.M. <Название части>

### §N.M.1. <Название параграфа>
<каждый параграф ОБЯЗАН содержать все 6 элементов Плана §2.3:
 1) определения/формулировки, 2) интуиция (ASCII-схемы памяти/стека — норма для этой дисциплины),
 3) 2–3 полностью разобранных примера с выкладками, 4) связь с практикой (рабочий код),
 5) типичные ошибки и подводные камни, 6) ссылки на источники>

### §N.M.k. ...

### Практика части N.M
<6–15 заданий, по возрастанию сложности:
 ~30% механика → ~40% понимание (объяснить/найти ошибку/сравнить) → ~30% прикладные.
 Формат каждого: **ГN.ЧM.Зk.** <условие>. Для кодовых — точная постановка: вход/выход/ограничения.>

**Источники части:** C2 (гл. …), C3 (…), …

## Часть N.M+1. ...

## Резюме главы
<краткое резюме + связка со следующей главой — План §2.1>
```

Специфика курса: в томе II каждая часть, где это осмысленно, содержит рубрику-сопоставление **«Как это было в C»** (задано в `outline.md`); в главах 25 и 26 (проекты) и части 12.Ч2 вместо классических параграфов допустимы проектные чекпоинты.

## 4. Шаблон файла решений (`solutions/NN-<slug>.md`)

Для каждого задания по его ID (План §6): 1) полное условие (продублировано); 2) подробное пошаговое решение с объяснением каждого шага; 3) явно выделенный итоговый ответ; 4) для кодовых — полный рабочий код с комментариями (код реально запущен, вывод приведён); 5) где уместно — альтернативные решения и разбор типичных неверных подходов.

## 5. Порядок работ и команды SuperClaude

| Этап | Команда(ы) | Что делает агент |
|---|---|---|
| 0. Вход в заход | `/sc:load` | Загрузить контекст проекта (Serena), прочитать этот файл, План, `sources.md`, `outline.md`; найти в трекере (§7) первую незакрытую клетку |
| 1. Источники | `/sc:research` | ✅ выполнен. Точечно повторять при написании глав: уточнение фактов, добавление источников в реестр |
| 2. Программа курса | — | ✅ выполнен (`outline.md`). Перед этапом 3 получить утверждение владельца |
| 3. Написание глав | `/sc:task` / `/sc:implement` | Одна глава за заход (глава с 3 частями — допустимо 2 захода по частям). Строго по порядку 1→26. Код примеров и заданий прогонять (§2.3) |
| 4. Ревизия | `/sc:reflect` + `/sc:analyze` | Два прохода: (а) после каждого тома — главы 1–13, затем 14–26; (б) сквозной по завершении. Чеклист в §6 |
| 5. Сборник решений | `/sc:task` / `/sc:implement` | Один файл решений за заход, зеркально главам, тоже по порядку. Весь код — запущен, вывод сверен |
| 6. Финальная сверка | `/sc:reflect` | Автопроверка соответствия ID (скрипт: собрать все `ГN.ЧM.Зk` из chapters/ и solutions/, сравнить множества), итоговый отчёт |
| Выход из захода | `/sc:save` + `git commit` | Обновить трекер §7, зафиксировать прогресс (§8) |

### Протокол одного захода (этап 3, глава N)

1. `/sc:load`; прочитать резюме предыдущей главы (N−1) — для связки.
2. Свериться с `outline.md`: состав частей, источники, пометки сложности.
3. Написать теорию по частям (шаблон §3), сверяя каждую часть минимум с двумя источниками; всё неподтверждённое — в «Открытые вопросы».
4. Написать практику: 6–15 заданий на часть, ID сплошной нумерацией внутри части, распределение сложности 30/40/30.
5. Прогнать весь код (C: `gcc -std=c17 -Wall -Wextra`; Rust: `cargo build && cargo test`); в репозитории есть скрипт запуска C — использовать при удобстве.
6. Самопроверка по чеклисту §6 (мини-ревизия главы).
7. Обновить трекер §7 (клетка «Глава»), `/sc:save`, commit (§8). **Остановиться** — следующий заход начнётся заново с `/sc:load` (не терять качество на длинном контексте — План §5).

Протокол захода этапа 5 аналогичен: условия берутся из готовой главы, решения пишутся по шаблону §4, код запускается, трекер («Решения») и commit.

## 6. Чеклист ревизии (этапы 4 и 6)

- [ ] Структура каждой главы соответствует §3 (все 6 элементов в каждом параграфе).
- [ ] Нет «забеганий вперёд»: глава N использует только материал глав 1…N−1 (или явная пометка).
- [ ] В каждой части 6–15 заданий; ID уникальны, формат `ГN.ЧM.Зk`, сложность нарастает.
- [ ] У каждой части блок «Источники» с ≥2 источниками из реестра.
- [ ] Нет маркеров догадок: «вероятно», «скорее всего», «обычно считается» без ссылки (искать grep-ом).
- [ ] Весь код компилируется/запускается (перепрогнать выборочно).
- [ ] Терминология согласована между главами (единый глоссарий терминов «владение», «заимствование» и т.д.).
- [ ] Решения: 100% покрытие ID, условия совпадают с главами дословно, ответы выделены.

## 7. Трекер прогресса (агент обновляет после каждого захода)

Статусы: `[ ]` не начато · `[~]` в работе · `[x]` готово.

### Этапы 1–2

- [x] Этап 1. Реестр источников (`sources.md`) — 2026-07-05
- [x] Этап 2. Программа курса (`outline.md`) — 2026-07-05
- [x] Утверждение программы курса владельцем репозитория — утверждена v1.1 (26 глав), 2026-07-05

### Этапы 3–6 по главам

| Глава | Файл | Глава (этап 3) | Ревизия (этап 4) | Решения (этап 5) | Сверка (этап 6) |
|---|---|---|---|---|---|
| 1 | 01-intro-and-tools | [x] 2026-07-05 | [x] 2026-07-11 | [ ] | [ ] |
| 2 | 02-basics | [x] 2026-07-05 | [x] 2026-07-11 | [ ] | [ ] |
| 3 | 03-control-flow | [x] 2026-07-05 | [x] 2026-07-11 | [ ] | [ ] |
| 4 | 04-functions | [x] 2026-07-05 | [x] 2026-07-11 | [ ] | [ ] |
| 5 | 05-arrays-strings | [x] 2026-07-05 | [x] 2026-07-11 | [ ] | [ ] |
| 6 | 06-pointers | [x] 2026-07-05 | [x] 2026-07-11 | [ ] | [ ] |
| 7 | 07-memory | [x] 2026-07-05 | [x] 2026-07-11 | [ ] | [ ] |
| 8 | 08-structs | [x] 2026-07-08 | [x] 2026-07-11 | [ ] | [ ] |
| 9 | 09-data-structures | [x] 2026-07-09 | [x] 2026-07-11 | [ ] | [ ] |
| 10 | 10-files-io | [x] 2026-07-09 | [x] 2026-07-11 | [ ] | [ ] |
| 11 | 11-preprocessor-build | [x] 2026-07-09 | [x] 2026-07-11 | [ ] | [ ] |
| 12 | 12-gui-c-win32 | [x] 2026-07-09 | [x] 2026-07-11 | [ ] | [ ] |
| 13 | 13-ub-and-bridge | [x] 2026-07-10 | [x] 2026-07-11 | [ ] | [ ] |
| — | **Ревизия тома I (главы 1–13)** | — | [x] 2026-07-11 | — | — |
| 14 | 14-rust-start | [x] 2026-07-11 | [x] 2026-07-22 | [ ] | [ ] |
| 15 | 15-ownership | [x] 2026-07-12 | [x] 2026-07-22 | [ ] | [ ] |
| 16 | 16-structs-enums | [x] 2026-07-12 | [x] 2026-07-22 | [ ] | [ ] |
| 17 | 17-collections-strings | [x] 2026-07-14 | [x] 2026-07-22 | [ ] | [ ] |
| 18 | 18-error-handling | [x] 2026-07-16 | [x] 2026-07-22 | [ ] | [ ] |
| 19 | 19-generics-traits | [x] 2026-07-18 | [x] 2026-07-22 | [ ] | [ ] |
| 20 | 20-modules-cargo-tests | [x] 2026-07-18 | [x] 2026-07-22 | [ ] | [ ] |
| 21 | 21-functional | [x] 2026-07-18 | [x] 2026-07-22 | [ ] | [ ] |
| 22 | 22-smart-pointers | [x] 2026-07-18 | [x] 2026-07-22 | [ ] | [ ] |
| 23 | 23-concurrency | [x] 2026-07-19 | [x] 2026-07-22 | [ ] | [ ] |
| 24 | 24-unsafe-ffi | [x] 2026-07-19 | [x] 2026-07-22 | [ ] | [ ] |
| 25 | 25-capstone | [x] 2026-07-19 | [x] 2026-07-22 | [ ] | [ ] |
| 26 | 26-gui-rust-egui | [x] 2026-07-20 | [x] 2026-07-22 | [ ] | [ ] |
| — | **Ревизия тома II (главы 14–26)** | — | [x] 2026-07-22 | — | — |
| — | **Сквозная финальная ревизия** | — | [ ] | — | — |
| — | **Итоговый отчёт этапа 6** | — | — | — | [ ] |

Последовательность закрытия клеток: сначала столбец «Глава» сверху вниз (1→26, с ревизией тома I после главы 13 — она же гейт на том II); затем «Решения» 1→26; затем сквозная ревизия и сверка. Допустимое отклонение: начинать «Решения» тома I параллельно написанию тома II (условия глав 1–13 к этому моменту заморожены ревизией тома I).

## 8. Фиксация прогресса

- **Коммит после каждого захода**: `book(c-rust): ch NN <slug> — <chapter|revision|solutions|check>`; тело — **кратко и лаконично** (1–3 строки сути: что за глава/изменение, ключевой факт, обновление трекера), без подробного поблочного перечисления сделанного (договорённость с владельцем, 2026-07-05).
- **Serena-память** (`/sc:save`): ключ `c-rust-book-progress` — последняя закрытая клетка трекера, решения/договорённости, на чём остановились. Работа возобновляема с любого места: источник истины — трекер §7 + git-история.

## 9. Шаблон запуска захода (копировать в промпт агенту)

```
/sc:load
/sc:task Продолжить написание самоучителя C→Rust по программе
books/c-and-rust-book/C-and-Rust-learning.md: прочитать программу, План
(books/self-education-book-create-plan.md), sources.md и outline.md; найти в
трекере (§7) первую незакрытую клетку с учётом порядка §7; выполнить ровно один
заход по протоколу §5; прогнать весь код; обновить трекер; /sc:save и commit.
Жёсткие правила §2 обязательны, приоритет у Плана.
```

## 10. Окружение и договорённости (журнал; агент дополняет по мере работы)

- 2026-07-05: компилятор для верификации C на Windows-машине владельца — **GCC 15.2.0 (MinGW-w64), установлен через scoop**; в свежих терминалах доступен как `gcc`, в терминалах, открытых до установки, — только по полному пути `C:\Users\Max\scoop\apps\gcc\current\bin\gcc.exe`.
- `run-c.sh` в корне репозитория рассчитан на clang + `-fsanitize=address` — под MinGW-gcc не работает; владелец пользуется им в WSL. Примеры главы 7 (санитайзеры) прогонять в WSL.
- Верификация кода выполняется во временной папке (scratchpad) вне репозитория; сборки и бинарники после захода удаляются — в репозиторий попадает только markdown (договорённость с владельцем, 2026-07-05).
- Вся рабочая память проекта ведётся в файлах репозитория (этот журнал, трекер §7, git-история), а не во внешних хранилищах (договорённость с владельцем, 2026-07-05).
- 2026-07-05: инструмент Bash код-агента (Git Bash) **не видит `gcc` на PATH** — верифицировать код через полный путь `/c/Users/Max/scoop/apps/gcc/current/bin/gcc.exe` (в PowerShell-терминалах `gcc` доступен как обычно). Программы с вводом проверяются подачей stdin через конвейер: `printf '7 4\n' | ./prog.exe`.
- 2026-07-05: подтверждено сборкой (GCC 15.2.0, MinGW-w64, LLP64): `int`=4, `long`=4 (⇒ `LONG_MAX==INT_MAX`), `long long`=8 байт; спецификатор `%zu` для `size_t` работает (UCRT). Использовано в главе 2 как иллюстрация непереносимости размеров.
- 2026-07-05: подтверждено сборкой (GCC 15.2.0, MinGW-w64) для главы 4: аргументы одного `printf` вычисляются **справа налево** (пример `static`-vs-regular в §4.2.2 печатает `3 2 1`); вызов необъявленной функции — **ошибка** компиляции (`-Wimplicit-function-declaration` как error в C17). Порядок вычисления аргументов не гарантирован стандартом — в тексте оговорён как платформенная деталь (тема неуказанного поведения — глава 13). Все 22 примера главы 4 + эталонные решения прикладных заданий прогнаны `-std=c17 -Wall -Wextra`, вывод сверён.
- 2026-07-05: глава 6 (указатели) написана и верифицирована (GCC 15.2.0, MinGW-w64, `-std=c17 -Wall -Wextra`), 3 части (6.1 адреса/`&`/`*`/`NULL`/выходные параметры; 6.2 массивы/распад/арифметика/строки; 6.3 `const`/`void*`/`**`/указатели на функции+`qsort`), 9 параграфов, 30 заданий (10 на часть, 30/40/30). Все 24 теоретических примера + эталонные решения прикладных заданий прогнаны, вывод сверён (комбинированный прогон — все совпало). Подтверждённые платформенные факты (LLP64): `sizeof(int*)`=`sizeof(&x)`=8; массив `int a[10]` → `sizeof` 40 в `main` против 8 у параметра `int a[]` (параметр-массив == `int*`); разность указателей печатается `%td` (UCRT), считается в элементах (`q-p`==3 при `(char*)q-(char*)p`==12 для `int`). Использовано: (1) gcc с `-Wall` выдаёт `-Wsizeof-array-argument` на `sizeof` параметра-массива — прямое подтверждение распада, вынесено в §6.2.1; (2) два ⛔-примера подтверждены как ошибки компиляции — запись через `const int *` (`assignment of read-only location '*p'`) и переприсваивание `int *const` (`assignment of read-only variable 'q'`), §6.3.1. Изменение строкового литерала — UB, в текст как рантайм-ошибка не выносится (не прогонялось). Глава использует только материал глав 1–5 + новые указатели; `malloc` (гл. 7) и структуры (гл. 8) не задействованы; `qsort`/`void*`/указатели на функции самодостаточны внутри §6.3. Адреса (`%p`) в §6.1.1 показаны как представительный прогон с явной оговоркой «на вашей машине будут другими».
- 2026-07-05: глава 5 (массивы и строки) написана и верифицирована (GCC 15.2.0, MinGW-w64, `-std=c17 -Wall -Wextra`), все 24 примера прогнаны, вывод сверён. Ключевые подтверждённые факты: (1) выход за границу массива (`for i<=3` по `int a[3]`) компилируется **без единого предупреждения** даже с `-Wall -Wextra` — использовано в §5.1.2 как иллюстрация того, что C не проверяет границы (детерминированный вывод у UB-примера в текст не выносится); (2) `sizeof "hello"`-массива = 6, `strlen` = 5 (терминатор `'\0'` занимает ячейку); (3) `scanf("%9s", …)` усекает длинный ввод `Alexandragreat` → `Alexandra` (защита от переполнения); (4) `strncpy(dst, src, 3)` при `strlen(src)>3` `'\0'` не дописывает; (5) `strcmp` возвращает знак (на платформе −1/0/+1), в тексте оговорено, что гарантирован лишь знак. Глава использует только материал глав 1–4; указатели/`&`/`sizeof` параметра-массива вынесены вперёд к главе 6 с явными пометками. Массивы как аргументы функций поданы «наивно» (параметр `int a[]`, длина отдельным аргументом) — механизм распада в указатель отложен на главу 6 (задумано `outline.md`).
- 2026-07-05: настроен **прямой доступ код-агента к санитайзерам через WSL1**. Исходная WSL2-Ubuntu не стартовала (на Ryzen 5900X флаг SVM/виртуализации выключен в BIOS → `HCS_E_HYPERV_NOT_INSTALLED`); перевод существующего дистрибутива в WSL1 невозможен (конверсия `--set-version 1` сама требует запуска WSL2-ВМ). Решение (с владельцем): снесли WSL2-дистрибутив, `wsl --set-default-version 1`, поставили свежую **Ubuntu-24.04 как WSL1** (аппаратная виртуализация не нужна). Тулчейн (apt, как root): **gcc 13.3.0, clang 18.1.3, valgrind 3.22.0**. Проверено прогоном: `-fsanitize=address` (и gcc, и clang) даёт полные отчёты под WSL1; **valgrind memcheck работает** (`--leak-check=full`; безвредное предупреждение `error calling PR_SET_PTRACER, vgdb might block`). Запуск из код-агента: `wsl -d Ubuntu-24.04 -u root bash -c '...'`; файлы Windows видны как `/mnt/c/...` (компилировал в `/root/ch7`). Это **снимает** прежнее ограничение «санитайзеры прогоняет только владелец в WSL» — агент запускает их сам. (Windows-сторона без изменений: корректный код по-прежнему сверяется на MinGW-gcc 15.2, ASan под MinGW не линкуется — `libasan` нет.)
- 2026-07-05: глава 7 (динамическая память) написана и верифицирована, 2 части, 5 параграфов, 20 заданий (12 + 8, распределение ~30/40/30). §7.1 (модель памяти стек/куча и время жизни; `malloc`/`free` + дисциплина владения + выделение через выходной параметр `int **`; `calloc`/`realloc` + растущий массив с удвоением ёмкости) — весь корректный код прогнан на **Windows GCC 15.2 `-std=c17 -Wall -Wextra`** (эталон курса): 7 демо-примеров + 3 эталонных прикладных решения (`dup_array`, `str_dup`, `fill_range`) собираются чисто, вывод сверён (три региона памяти различны; `make_squares`→`0 1 4 9 16`; out-param→`1 2 3 4`; `calloc`→нули; `realloc`→`1..8`; растущий буфер→`len=5 cap=8`). §7.2 (каталог ошибок + инструменты) — **реальные отчёты из WSL1**: ASan на use-after-free / heap-buffer-overflow / double-free (`gcc -fsanitize=address`), Valgrind на утечке / UAF / чистом прогоне после исправления; номера строк в отчётах сверены с листингами §7.2.1 (uaf 6/8/9, overflow 6/8, dfree 6/7, leak 6). Подтверждённые факты (использованы в тексте): Windows GCC `-Wall -Wextra` **молчит** на утечке (`leak.c`) и переполнении кучи (`overflow.c`), но выдаёт `-Wuse-after-free` на тривиальных внутрифункциональных UAF/double-free и `-Wreturn-local-addr` на возврате адреса локальной — подано как довод «статика ловит очевидное, для остального нужны рантайм-инструменты». В реестр добавлены **C13 (Valgrind)** и **C14 (AddressSanitizer)**, URL проверены веб-запросом. Забеганий вперёд нет: использованы только главы 1–6 (структуры — глава 8 — не задействованы; `int **` — из §6.3.2). Отчёты в тексте — реальный вывод с сокращением путей и служебных кадров (оговорено во врезке §7.2.2), как `%p`-адреса в главе 6.

- 2026-07-08: глава 8 (пользовательские типы) написана и верифицирована (GCC 15.2.0, MinGW-w64, `-std=c17 -Wall -Wextra`), 2 части, 6 параграфов, 20 заданий (10 + 10, ~30/40/30). Прогнаны 18 теоретических примеров, 3 ⛔-примера, эталонные решения прикладных заданий и факт-проверки условий — вывод сверён. Подтверждённые платформенные факты (в тексте): `sizeof(enum)`=4; `enum color c = 42;` компилируется **без предупреждений** даже с `-Wall -Wextra`; `-Wall` (`-Wswitch`) предупреждает о непокрытом перечислителе в `switch` без `default`; донумерация `{A=3,B,C=1,D}` → 3,4,1,2; `union{int;float;char[8]}`=8; паддинг: `{char,int,char}`=12 против `{int,char,char}`=8, `{char,int}`=8 при сумме полей 5; порядок байтов little-endian (`0x01020304` → `04 03 02 01`); присваивание структур копирует и поле-массив. ⛔ подтверждены: `==` для структур (`invalid operands to binary ==`), присваивание между одинаковыми по составу структурами с разными тегами (`invalid initializer`), поле-значение собственного типа (`field 'next' has incomplete type`). Нормативные ссылки на C5 сверены по PDF N3220 (pdftotext в WSL1): 6.5.3.4 члены, 6.5.4.4 sizeof, 6.5.10 equality, 6.7.3.2 структуры/union/паддинг, 6.7.3.3 enum, 6.7.3.4 теги, 6.7.9 typedef, 6.7.11 инициализация. Забеганий вперёд нет (только гл. 1–7; узлы в куче §8.1.3 — заготовка гл. 9, явно анонсировано).

- 2026-07-09: глава 9 (структуры данных) написана и верифицирована, 3 части, 7 параграфов, 30 заданий (10+10+10, ~30/40/30 в каждой части; Ч3 помечена «повышенная сложность» по outline). Прогнаны 23 программы (14 примеров + 9 эталонных решений прикладных) **двумя контурами**: Windows GCC 15.2 `-std=c17 -Wall -Wextra` (эталон, вывод сверён) и WSL1 gcc 13.3 `-fsanitize=address` — все 23 чисты (ни утечек, ни ошибок памяти); впервые ASan прогнан по всему коду главы (правило §2.3 для глав ≥7). Реализовано и проверено: вектор (len/cap, удвоение 2→4→8, вставка со сдвигом), список (push_front через `Node **`, insert_after, remove с prev, remove_all), стек на растущем массиве + проверка скобок, очередь head+tail + считалка (n=7, k=3 → выбывают 3 6 2 7 5 1, уцелел 4), кольцевой буфер (заворот по `% CAP` подтверждён), хеш-таблица с цепочками (K&R-хеш `31h+c`: "cat"=6, "tac"=6 — учебная коллизия, "act"=2, "dog"=4; владение копиями ключей) и BST (рекурсивные insert через `TNode **`/search, inorder `1 3 4 5 7 8 9`, вырожденное дерево 1..5 → высота 5). Асимптотика подана неформально («число шагов») — big-O курс не вводил; BFS — обзорное упоминание (по outline). Техническое: многострочные bash-скрипты через heredoc в Bash-инструменте и через `wsl bash -c '...'` ломаются (кавычки/CRLF) — файлы писать инструментом Write; скрипт для WSL класть в файл и запускать `tr -d '\r' < script > /root/s.sh && bash /root/s.sh`.

- 2026-07-09: глава 10 (файлы и ввод-вывод) написана и верифицирована, 2 части, 6 параграфов, 20 заданий (10 + 10, ~30/40/30). Прогнаны 25 программ (17 примеров, включая 2 ⚠️-примера неверного поведения, + 8 эталонных решений/факт-проверок условий) **двумя контурами**: Windows GCC 15.2 `-std=c17 -Wall -Wextra` (эталон, вывод сверён, ни одного предупреждения) и WSL1 gcc 13.3 `-fsanitize=address` — все 25 чисты. Подтверждённые платформенные факты (в тексте с оговорками): текстовый режим UCRT транслирует `\n` → `\r\n` (файл `"one\ntwo\n"` — 10 байт против 8 в `"wb"`; байты `0d 0a` видны hex-дампом), в бинарном режиме на Linux/WSL разницы нет; `perror` при отсутствии файла печатает `No such file or directory`; `char`-приёмник `fgetc` на знаковом `char` обрывает чтение на байте 0xFF (0 итераций из 2) — GCC молчит даже с `-Wall -Wextra`; `while (!feof)` дублирует последнюю строку (гарантия C5 §7.23.7.2: `fgets` при EOF-без-прочитанного не меняет буфер); `SEEK_END`+`ftell` = 20 для 5 `int` (работает, но в тексте помечено непереносимым — FIO19-C); паддинг пишется в файл (`{char;int}` → 8 байт при 5 байтах полей; 12 vs 8 при перестановке полей); little-endian в файле (`0x01020304` → `04 03 02 01`); структура с `char *name` даёт 16-байтовый файл без байтов строки. Нормативные ссылки сверены по N3220 (pdftotext, PDF сохранён в WSL `/root/n3220.pdf`): §7.23.2 Streams, §7.23.3 Files, §7.23.5.1/5.3, §7.23.6.1–6.3, §7.23.7.1–7.2, §7.23.8.1–8.2, §7.23.9.2/9.4/9.5, §7.23.10.2–10.4, §7.5 errno. Реестр: добавлен **C15** (Microsoft Learn, UCRT — текстовый/бинарный режим), у C10 зафиксирован новый канонический URL (cmu-sei.github.io; вики редиректит), правила FIO34-C/FIO42-C/FIO19-C проверены по нему. Забеганий вперёд нет (только гл. 1–9; `Vec` §9.1.1 — в задании З9 Ч2; ссылки на §2.2.4, §6.2.2, §6.3.2, §7.1.2–7.1.3, §8.2.2–8.2.3 сверены grep-ом по главам).

- 2026-07-09: глава 11 (препроцессор, многофайловые проекты, сборка) написана и верифицирована, 2 части, 7 параграфов (4 + 3), 20 заданий (10 + 10, ~30/40/30). Прогнаны ~45 сборок/запусков **двумя контурами**: Windows GCC 15.2 `-std=c17 -Wall -Wextra` (эталон, вывод сверён) и WSL (make 4.3, ar 2.42; все heap-примеры — vec-модуль, stack+brackets, libds-клиент — под ASan, чисто). Подтверждённые факты (в тексте): `gcc -E hello.c` → 1057 строк из 6; `__STDC_VERSION__`=201710 при `-std=c17`; точные ошибки GCC для `#define N = 100`/`#define M 100;` (с `note: in expansion of macro`), а `int x = M;` с хвостовой `;` компилируется молча; `SQUARE_BAD(3+1)`=7, `100/(x)*(x)`=100; двойное вычисление: `SQUARE(next())`=2 (2 вызова), `MAX(next(),next())`=3 (3 вызова); GCC `-Wall` ловит двухоператорный макрос под `if` (`-Wmultistatement-macros`), `{}`-макрос с `;` ломает `else` («'else' without a previous 'if'»); неопределённый идентификатор в `#if` → 0 (норматив §6.10.2 «pp-number 0»; `-Wundef` предупреждает); двойная вклейка без стража → `redefinition of 'struct Point'`; `const int N` как размер массива на файловом уровне → `variably modified at file scope`; линкерные ошибки: `undefined reference`, `multiple definition` (функция; переменная без `extern` — предварительное определение; определение функции в `.h` на два TU); `static int counter` в клиенте линкуется, но печатает 0 (две переменные); make: `up to date`, точечная пересборка по `touch vec.c`, оба `.o` по `touch vec.h`, `*** missing separator` при пробелах вместо TAB; порядок `-l` после объектников (обратный → `undefined reference`, проверено на MinGW и WSL). Реестр: добавлен **C16 (GNU Make Manual)**; сверены оглавления C2 (гл. 14–15, knking.com), C4 2-е изд. (гл. 9–10), C3 C23-изд. (§5.6, гл. 10, гл. 17 — по свободному PDF с HAL), C8 (лекция 9: `ar rcs`, `-L`/`-l`, linkage). Норматив N3220: §5.1.1.1–.2, §6.2.2, §6.7.2 (constexpr), §6.9/.3, §6.10.2/.3/.5/.5.5/.7/.10, §7.23.7.5 (`getc`). Окружение: в WSL доустановлен **make** (apt, 4.3); на Windows поставлен **GNU Make 4.4.1 через scoop** (ezwinports) — работает из Git Bash (PATH дополнить MinGW-gcc: `export PATH="/c/Users/Max/scoop/apps/gcc/current/bin:$PATH"`); нюанс: цель Makefile на Windows должна называться `app.exe`, иначе make перелинковывает вечно (отражено во врезке §11.2.3). Забеганий вперёд нет (гл. 1–10; vec §9.1.1 разложен в модуль дословно, стек — по образцу §9.2.1; `assert` из текста убран как непройденный).

- 2026-07-09: глава 12 (GUI на C: Win32; обзорная, контрольная работа тома I) написана и верифицирована, 2 части, 7 параграфов (3 + 4), 18 заданий (8 + 10, ~30/40/30). Прогнаны 11 программ (10 примеров + мини-блокнот из 3 файлов с Makefile) на Windows GCC 15.2 `-std=c17 -Wall -Wextra` — ни одного предупреждения; **GUI-поведение проверено агентом автоматизацией** (PowerShell + P/Invoke: BM_CLICK по кнопкам, WM_GETTEXT/WM_SETTEXT для чтения/записи текста контролов, PostMessage WM_CLOSE): счётчики (обе версии — глобали и GWLP_USERDATA) дают 0→3→0 по кликам; блокнот — полный E2E (Load без файла → «load failed», ввод → Save → файл побайтно `hello from test\r\nsecond line`, внешняя правка → Load → текст в EDIT, выход 0); MessageBox: закрытие X при MB_OK → IDOK=1, клики Yes/No → 6/7 (сверено со справочником). ⛔-примеры подтверждены: return 0 на всё (WM_NCCREATE) → CreateWindowEx NULL, exit 1; без PostQuitMessage → окно закрыто, процесс-зомби жив. Лог сообщений: 40 сообщений от создания до разрушения (№1 — 0x0024 WM_GETMINMAXINFO **до** WM_NCCREATE — использовано во врезке §12.2.2 о NULL-состоянии). Платформенные факты (в тексте с оговорками): MinGW default-спеки линкуют user32 (gdi32 — нет), `-mwindows` добавляет `-lgdi32 -lcomdlg32` (gcc -dumpspecs) и ставит PE Subsystem 2 (GUI) против 3 (CUI) — objdump; без gdi32 — `undefined reference to __imp_TextOutA` (связка с §11.2.1). storage-модуль блокнота прогнан консольным тестом под ASan в WSL — чист (ASan для GUI-кода неприменим — Windows-only). Факт-сверка: 15 страниц Microsoft Learn (C11: учебный цикл learnwin32 + справочники MessageBox/GetMessage/CreateWindowEx/WNDCLASS/WM_COMMAND/WM_NCCREATE/BN_CLICKED/EN_CHANGE/GetWindowText), оглавление Petzold (C12, гл. 1/3/4/5/9 — подтверждено оф. страницей Microsoft Press), GCC Manual. Реестр: добавлен **C17 (GCC Manual)**. Новая функция стандартной библиотеки snprintf введена явно в §12.2.1 (в гл. 1–11 не встречалась — проверено grep). UI-строки примеров — латиница (A/W-нюанс кодировок оговорён в §12.1.1). **Открытый пункт для владельца** (правило §2.3 для GUI-глав): ручная приёмка интерфейса — прокрутка/ввод в EDIT, поведение на больших файлах, внешний вид. Автоматизационный нюанс для будущих GUI-прогонов: PowerShell маршалит `$null` в `""` для string-параметров P/Invoke — передавать `[NullString]::Value`; `Start-Process -WindowStyle Hidden` перекрывает первый ShowWindow (STARTUPINFO) — окно остаётся скрытым, smoke-прогоны вести без Hidden.

- 2026-07-10: глава 13 (UB и мост к Rust; финал тома I) написана и верифицирована, 2 части, 7 параграфов (4 + 3), 17 заданий (9 + 8, ~30/40/30). C-примеры прогнаны на Windows GCC 15.2 `-std=c17 -Wall -Wextra` (эталон; всё чисто, unseq.c — намеренное `-Wsequence-point`), санитайзеры и «чистые» UB-прогоны — в WSL. Верифицированные демо: wraps.c (`x+1>x` → 1 даже при `-O0`; `-fwrapv` → 0); loop31.c — n=31 при `-O0`, **вечный цикл** при `-O2` (подтверждён asm `.L2: jmp .L2`); first.c — проверка `p==NULL` после `*p` удалена при `-O2` (тело — 2 инструкции), возвращается с `-fno-delete-null-pointer-checks`; oob (индекс 4 в int[4]) без санитайзера — 3 запуска → 3 разных мусорных значения, exit 0; nullref — SIGSEGV 139; divzero — SIGFPE 136; UBSan-отчёты по всем четырём; TSan-отчёт по гонке (Read/Previous write, `Location is global 'counter'`); race.c (pthread) собирается и на MinGW (`-pthread`, libwinpthread; потери инкрементов: 1165767/1050465) и в WSL (991268/1041592/1022163). MinGW: libubsan НЕТ (`-fsanitize=undefined` не линкуется, как ранее ASan), но `-fsanitize=undefined -fsanitize-trap=undefined` работает (крах Illegal instruction без отчёта) — отражено в §13.1.4. Норматив N3220 сверен pdftotext (/root/n3220.pdf): §3.5.1/3.5.3/3.5.4 (+примечания 1–3), §3.28, §5.1.2.5¶35, §6.2.4¶2, §6.5.1¶2/¶5, §6.5.4.2+сноска, §6.5.6¶6, §6.5.7, §6.5.8¶3, §7.24.3.3, J.2 (221 пункт; пп. 1, 5, 46). Rust-витрина части 13.2 верифицирована **rustc 1.75 (apt в WSL, установлен в этом заходе)**: E0382, E0106, паника `index out of bounds: the len is 4 but the index is 4`, отказ компиляции `deny(unconditional_panic)` при константном индексе, паника `attempt to add with overflow` (debug) / −2147483648 (release `-O`), E0373+E0503 на гонке, паника `attempt to divide by zero`, `Rust has no postfix increment operator` (+help `+= 1`), Option-пример работает. Факты R1 (гл. 3/4/6/15.6/16 — переполнение debug/release, правила владения/ссылок, Хоар/Option, «утечки memory safe», deadlock не предотвращается, Rc vs Arc), R7 (Behavior considered undefined), CERT MSC15-C — сверены WebFetch. Реестр: добавлены **C18 (UBSan)** и **C19 (TSan)**, расширено применение C17 (флаги оптимизатора/санитайзеров). Нюанс: пересказ GCC-мануала веб-выжимкой утверждал, что bounds не входит в `-fsanitize=undefined`, — противоречит нашему прогону (oob пойман без явного `-fsanitize=bounds`); в текст главы состав проверок вошёл по фактическим прогонам, без спорного утверждения о составе флага. Открытых вопросов не добавилось. Rust-сниппеты — только чтение для читателя (пометка «⏳ том II» в главе); полноценная установка rustup — глава 14. **Столбец «Глава» тома I закрыт полностью → следующий заход: ревизия тома I (главы 1–13), гейт на том II.**

- 2026-07-11: **ревизия тома I (главы 1–13) выполнена** (этап 4а, чеклист §6) — гейт на том II открыт. Механика (скрипт по всем главам): 311 уникальных ID заданий, нумерация сплошная, 6–15 на часть, сложность везде нарастает (мех → понимание → прикладные); у всех 30 частей блок «Источники» с ≥2 ID из реестра; структурные заголовки и связки «Дальше» на месте; 158 упоминаний будущих глав проверены — все являются явными анонсами с номером главы (использования непройденного материала нет). Найдено и исправлено: (1) в главах 5, 6, 7 блоки «Практика части» были вынесены в конец главы — возвращены внутрь своих частей по шаблону §3; задублированные блоки «Источники части» в гл. 5 сведены к одному (оставлены полные версии); (2) маркер догадки в §1.1.2 («вероятно, главный источник ошибок») переформулирован без догадки; (3) добавлены недостававшие блоки «Интуиция» в §1.2.2 и §7.2.2; (4) в 12 кодовых блоков заданий/примеров добавлены отсутствовавшие `#include` (гл. 4: Ч1.З5, Ч2.З3–З7; гл. 5: Ч1.З6; гл. 6: Ч1.З6; гл. 7: Ч2.З8 — `<stdlib.h>`; гл. 8: Ч1.З5; гл. 11: оба блока примера 3 §11.1) — без них C17 даёт implicit-declaration, у ⛔-заданий посторонняя ошибка маскировала намеренную; (5) убраны случайные бэктики после заголовка §2 этого файла (досессионная правка в рабочем дереве). Перепрогон кода: извлечены все 212 блоков с `main` из глав — 188 самостоятельных программ собираются чисто (MinGW GCC 15.2, `-std=c17 -Wall -Wextra`; гл. 12 с `-mwindows`, pthread-пример с `-pthread`), 7 ⛔-примеров падают ровно по своей причине, остальные — фрагменты по объявленным конвенциям (гл. 9) и многофайловые демо (гл. 11); 8 детерминированных прогонов сверены с текстом (make_squares, растущий буфер len=5 cap=8, little-endian 04 03 02 01, вектор 2→4→8, очередь FIFO, LLP64-размеры, strlen/sizeof, wraps `-O0` → 1); ASan-смоук в WSL на 4 heap-примерах — чисто. Параграфы с примерами вне канонической разметки (§1.2.1/§1.2.3 «Разобранный пример», §12.2.4 проектные чекпоинты, гл. 13 «Препараты»/«Демонстрации»/«Витрины») признаны соответствующими Плану §2.3 по существу. Новых «Открытых вопросов» нет. **Следующий заход: глава 14 (том II)**; допустимо параллельно начинать решения тома I (условия глав 1–13 заморожены этой ревизией).

- 2026-07-11: **глава 14 (знакомство с Rust; вход в том II) написана и верифицирована**, 2 части, 6 параграфов (2 + 4), 20 заданий (8 + 12, ~30/40/30 в каждой части). **Окружение тома II настроено в этом заходе**: на Windows-машине владельца установлен rustup (официальный rustup-init) с host-триплетом **`x86_64-pc-windows-gnu`** — MSVC Build Tools на машине нет (нет cl.exe/vswhere; каталоги «Microsoft Visual Studio» — пустые остатки), gnu-тулчейн самодостаточен (компонент rust-mingw) и согласован с MinGW-GCC тома I. Эталон верификации тома II: **rustc/cargo 1.97.0 stable, издание 2024** (`cargo new` генерирует `edition = "2024"`); rustc 1.75 в WSL остаётся запасным контуром. Прогнано и сверено ~40 программ/сборок: 24 работающих примера и эталона (все выводы в тексте дословные), 12 ⛔-примеров (E0384, E0308 ×6 видов, E0423, E0277, `missing type for const`, `expected expression found let`, deny(unconditional_panic), deny(arithmetic_overflow)), 2 паники (oob: `len is 5 but the index is 10`; переполнение u8 в dev-профиле), полный cargo-цикл (new/build/run/повторный run без Compiling/правка→пересборка/clean/check без exe/--release/--verbose с командой rustc) и u8-переполнение в двух профилях (dev — паника, release — 0). **Витрины §13.2 перепроверены на 1.97** — E0382/E0106/E0373+E0503 (плюс новый E0502 сверх текста гл. 13), паники oob/divzero, i32::MAX+step (dev-паника/release −2147483648), find/Option — поведение то же, текст гл. 13 не требует правок. Платформенные факты в тексте: gnu-тулчейн не создаёт .pdb (только .exe); формат паники 1.97 включает служебный номер потока `thread 'main' (NNNN)` (в 1.75 его не было); rustc 1.97 отклоняет статически видимое переполнение литералов через `deny(arithmetic_overflow)` даже сквозь let-цепочку (в гл. 13/1.75 такого не было) — для честной демонстрации рантайм-поведения значение прячется за вызовом функции (приём в §14.2.2). Реестр без изменений (R1, R2, R6, R7, R10 — все страницы сверены WebFetch в этот заход). Забеганий вперёд нет: String/&str/Option/match/enumerate/`as` — только как явные анонсы с номерами глав или ссылки на витрины §13.2. Техническое: PowerShell 5.1 читает `.ps1` без BOM как ANSI — скрипты прогона с кириллицей в комментариях ломаются на парсинге; писать скрипты чисто ASCII, а файлы с кириллицей класть инструментом Write и копировать. **Следующий заход: глава 15 `15-ownership` (этап 3); параллельно допустимы решения тома I (этап 5, с главы 1).**

- 2026-07-12: **глава 15 (владение и заимствование; центральная глава тома II) написана и верифицирована**, 3 части (владение / ссылки и заимствование / срезы), 8 параграфов (3 + 3 + 2), 29 заданий (10 + 10 + 9, ~30/40/30). Прогнано **46 программ** на эталоне rustc/cargo 1.97.0 (Windows gnu): 29 рабочих (все выводы в тексте дословные, предупреждений ноль), 14 ⛔-примеров (E0382 ×4 — move/drop/функция/кортеж-с-String, E0596, E0499 ×2 — включая `swap(&mut x, &mut x)`, E0502 ×3 — включая скрытого писателя `push_str` и first_word+clear, E0106, E0597, E0308 — `&[i32; 3]` vs `&[i32; 5]`, E0599 — `push_str` у `&str`), 2 паники срезов, 1 C-пример (`dangle_c.c`: gcc `-Wreturn-local-addr` **предупреждает, но собирает**, rc=0 — контраст с отказом Rust). Ключевые платформенные факты 1.97 (в тексте): E0382 даёт машинную подсказку `+ .clone()` и note «consider changing this parameter type … to borrow instead» на move-в-функцию; **срез с константными границами `&a[2..7]` компилируется молча и паникует в рантайме** («range end index 7 out of range for slice of length 5») — в отличие от константного индекса `a[7]` (deny(unconditional_panic), гл. 14) — контраст вынесен в §15.3.1 и З4 Ч3; авто-коэрция `sum(&a)` (`&[i32; N]` → `&[i32]`) и `first_word(&s)` (`&String` → `&str`, анонс deref coercion → гл. 22) подтверждены прогоном; NLL: последовательные `&mut` легальны, «неиспользуемый писатель» даёт только warning unused. Источники: R1 гл. 4.1–4.3, R7 (Pointer types / Destructors — обратный порядок drop / Slice types — DST, границы всегда проверяются), R6 (move/borrowck/slices/copy-types), R9 (`std::mem::drop` — пустое тело, drop у Copy — no-op; Copy у массивов `[T; N]`) — сверены WebFetch в этот заход. **R4 (Programming Rust) сверить оглавлением не удалось** (O'Reilly отдаёт 403 фетчеру; официальный репозиторий примеров глав ownership/references не содержит) — R4 цитируется по формулировке outline («гл. об Ownership/Moves», справочно), все факты его цитирований покрыты R1/R6/R7. Терминология: «разделяемая (shared) / изменяющая (mutable) ссылка» — по R7, согласовано с «изменяющая ссылка» из гл. 13. Забеганий вперёд нет: Option/match → гл. 16, устройство String/Vec и UTF-8 → гл. 17, трейты и лайфтайм-аннотации → гл. 19, Deref/Box и наблюдение drop → гл. 22, потоки → гл. 23, unsafe → гл. 24 — только явные анонсы. Открытых вопросов не добавилось. **Следующий заход: глава 16 `16-structs-enums` (этап 3); параллельно допустимы решения тома I (этап 5, с главы 1).**

- 2026-07-12: **глава 16 (структуры, перечисления, сопоставление с образцом; зеркало гл. 8) написана и верифицирована**, 2 части, 7 параграфов (3 + 4), 20 заданий (10 + 10, ~30/40/30). Прогнано **48 программ** на эталоне rustc/cargo 1.97.0 (Windows gnu): 29 рабочих (выводы в тексте дословные, предупреждений ноль), 2 намеренные демонстрации (паника `Option::unwrap()` на `None`; warning `unreachable pattern` — рукав после `_`), 17 ⛔-примеров: E0594 (поле без `mut`), E0382 ×3 (move структуры — с новой подсказкой 1.97 «consider implementing `Clone` for this type»; `..`-update перемещает поле — ошибка называет `s1.name` пополевно; метод-`self` — note «takes ownership of the receiver»), E0106 (`&str`-поле), E0308 ×3 (newtype-несовместимость Color/Point3; целое в enum; нерасходящийся else у `let...else` — «expected `!`, found `()`»), E0204 (Copy при String-поле), E0277 ×3 (Display/Debug без derive; `i32 + Option<i32>`), E0609 (поле у enum), E0732 (явный дискриминант при non-unit вариантах), E0004 ×2 (`Light::Green`/`None` not covered), E0599 (ассоциированная функция через точку, подсказка `Rect::square(3)`). Платформенные факты (в тексте с оговорками): `size_of` enum `{Dot, Circle(f64), Rect{f64,f64}}` = 24; `Option<i32>` = 8, `Option<&i32>` = 8 = `&i32` (NPO — гарантия по R9). Новое введено явно: `#[derive(Debug/Clone/Copy)]`, `{:?}`/`{:#?}`, `dbg!` (stderr, файл:строка, возвращает владение), `format!` (параллель с snprintf §12.2.1); явные дискриминанты unit-only enum компилируются (прогон). Нюанс: поля, читаемые только `derive(Debug)`-печатью, дают warning dead_code «fields are never read» — во все такие примеры добавлено прямое чтение полей (правило «предупреждений ноль»). Источники: R1 (гл. 5.1–5.3, 6.1–6.3), R2 (structs/enums/if let), R6 (named structs/methods/enums/let control flow/niche), R7 (Enumerations: нумерация с 0, явные дискриминанты только у unit-only; match: порядок рукавов, согласование типов), R9 (std::option: unwrap/unwrap_or, NPO) — сверены WebFetch в этот заход; у R6 сменилась структура URL (user-defined-types/structs.html — 404, актуален named-structs.html). Забеганий вперёд нет: Vec/HashMap/UTF-8/while let → гл. 17, Result → гл. 18, трейты/лайфтаймы/PartialEq/дженерики → гл. 19, итераторы → гл. 21 — только явные анонсы. Открытых вопросов не добавилось. **Следующий заход: глава 17 `17-collections-strings` (этап 3); параллельно допустимы решения тома I (этап 5, с главы 1).**

- 2026-07-14: **глава 17 (коллекции и строки; зеркало гл. 9) написана и верифицирована**, 2 части, 7 параграфов (4 + 3), 20 заданий (10 + 10, ~30/40/30). Прогнано **33 программы** на эталоне rustc/cargo 1.97.0 (Windows gnu): 23 рабочих (выводы в тексте дословные, предупреждений ноль; включая файл микропроверок утверждений текста — идиома `*m.entry(k).or_insert(0) += 1`, `let x = v[0]` для Copy-элементов, NLL-варианты ⛔-примеров), 2 паники (`v[10]` — «index out of bounds: the len is 3 but the index is 10»; `&s[0..1]` по «привет» — «**end** byte index 1 is not a char boundary; it is inside 'п' (bytes 0..2 **of string**)» — формулировка 1.97 отличается от цитируемой в R1: добавлено «end», сама строка не приводится), 8 ⛔-примеров: E0502 ×2 (`push` при живой `&v[0]` — физика перевыделения; `insert` при живом `get`), E0382 ×4 (`push(s)`; обход `for` по значению — в тексте ошибки «implicit call to `.into_iter()`»; `s1` после `s1 + &s2`; ключ-`String` после `insert`), E0507 (вынос `v[0]` из `Vec<String>`, два help — borrow/clone), E0277 (`s[0]` у `String`: 1.97 подсказывает `.chars().nth()`/`.bytes().nth()` и ссылается на гл. 8 The Book прямо в note). Механическая сверка: все 19 полных листингов главы извлечены скриптом и перекомпилированы — 12 чистых, 7 падают ровно заявленными кодами. Платформенные факты (в тексте с оговорками «наблюдение, не гарантия»): рост `Vec<i32>` cap 0→4→8 (R9 гарантирует только амортизированный O(1) `push`, стратегия не специфицирована — наше C-«удвоение» §9.1.1 больше не опора); порядок обхода HashMap различен в каждом запуске (SipHash 1-3 со случайным зерном, R9). Байты «привет»: 208 191 209 128 208 184 208 178 208 181 209 130 (диаграмма §17.1.4). Введено явно: `Vec` (new/`vec!`/push/pop/len/capacity/with_capacity/get/индексация), `while let` (обещание §16.2.4 закрыто; десахаризация в `loop`+`match` по R7 — прогнана), `String` как `Vec<u8>` с инвариантом UTF-8 (push/push_str/`+`-семантика/`format!`), UTF-8 (chars/bytes/срезы по байтовым границам/char = 32-битное скалярное значение, 1–4 байта в кодировке — R9 len_utf8), `HashMap` (первый `use` курса — с анонсом гл. 20; insert→Option со старым/get→Option<&V>/contains_key/remove/обход/entry/or_insert→&mut V), E0507. Источники сверены WebFetch в этот заход: R1 (гл. 8.1–8.3), R9 (Vec/String/HashMap/char), R2 (std/vec, std/str, std/hash, flow_control/while_let), R6 (std-types/{vec,string,hashmap} — URL-структура актуальна), R7 (types/str — str=[u8], нарушение инварианта UTF-8 — UB «now or in the future»; types/char — диапазоны скалярных значений; expressions/loop-expr — while let). R4 — O'Reilly по-прежнему 403; цитируется справочно по outline, факты покрыты R1/R9. Забеганий вперёд нет: Result → гл. 18, трейты/Eq+Hash/механика get по &str → гл. 19, use/модули → гл. 20, итераторы/into_iter/split_whitespace → гл. 21, deref-коэрция → гл. 22 — только явные анонсы с номерами глав. Открытых вопросов не добавилось. **Следующий заход: глава 18 `18-error-handling` (этап 3); параллельно допустимы решения тома I (этап 5, с главы 1).**

- 2026-07-16: **глава 18 (обработка ошибок) написана и верифицирована**, 2 части, 6 параграфов (3 + 3), 20 заданий (10 + 10, ~30/40/30). Прогнано **32 программы** на эталоне rustc/cargo 1.97.0 (Windows gnu): 17 рабочих примеров, 10 эталонных решений прикладных заданий, 5 ⛔; затем механическая сверка — все **19 полных листингов** извлечены скриптом из готового текста и перекомпилированы: 15 чистых (единственное намеренное предупреждение — must_use в `ignored.rs`), 4 ⛔ падают ровно заявленными кодами (E0308 «спроси компилятор»; E0004 на non_exhaustive `ErrorKind` без `_`; E0277 «? в fn -> ()» с подсказкой Box<dyn Error>; E0277 «? couldn't convert» без From — с прямым текстом «implicitly performs a conversion using the `From` trait»). **Критичное открытие окружения: голый `rustc` собирает в издании 2015** — `panic!("{t}")` там не форматирует плейсхолдер (warning non_fmt_panics); эталон верификации отныне **`rustc --edition 2024 файл.rs`** (в текст главы внесено с объяснением; выводы глав 14–17 от издания не зависят — использованные там конструкции эквивалентны в 2015/2024, — но их прогоны велись голым rustc: при ревизии тома II заменить «rustc файл.rs» из §14.1.1/введений на форму с флагом). Платформенные факты 1.97/Windows (в тексте с оговорками): паника → exit code 101 (наблюдение, не гарантия); `main -> Result` при Err → `Error: {Debug}` + exit 1; Display io::Error англоязычный от ОС «The system cannot find the file specified. (os error 2)»; Debug File показывает handle и `\\?\`-путь (в тексте сокращён); формат паники с номером потока — как в гл. 14. dead_code-нюанс гл. 16 подтверждён для error-enum: payload, читаемый только Debug-печатью (в т.ч. через Termination), даёт «field `0` is never read» — во всех листингах поля читаются match-ем, а вариант `main -> Result<(), AppError>` с предупреждениями сознательно не листинг (материал заданий). Введено явно: panic!/размотка/RUST_BACKTRACE, Result/unwrap/expect/unwrap_or/#[must_use], `?` (по R7 — try propagation), `main -> Result` (Termination), eprintln!, File::open/create/metadata (File — не Copy/Clone, drop закрывает: анонс §16.1.3 закрыт), fs::read_to_string, ErrorKind/non_exhaustive, собственные error-enum, **impl From как рецепт — первый трейт курса** (пометка «механика — гл. 19»), char::to_digit/is_whitespace, str::is_empty, String::clear. Не введено (только анонсы): Box<dyn Error> → гл. 22, Display/трейты/parse → гл. 19, ok_or/map_err → позже, тесты → гл. 20. Источники сверены WebFetch в заход: R1 (гл. 9.1–9.3), R9 (std::result, io::Error/ErrorKind, fs — File/read_to_string, convert::From, process::Termination, prelude), R7 (expressions/operator-expr — «The try propagation expression», арбитр), R2 (error/panic, error/result/enter_question_mark, error/multiple_error_types/define_error_type — критерии типа ошибки), R6 (error-handling/{panics,result,try-operator,try-conversions} — URL-структура актуальна). R4 — O'Reilly стабильно 403; цитируется справочно по outline, факты покрыты R1/R9/R7/R2. Забеганий вперёд нет; открытых вопросов не добавилось. **Следующий заход: глава 19 `19-generics-traits` (этап 3); параллельно допустимы решения тома I (этап 5, с главы 1).**

- 2026-07-18: **глава 19 (обобщённые типы, трейты, времена жизни; самый теоретический блок тома II) написана и верифицирована**, 3 части, 8 параграфов (3 + 3 + 2; часть 19.3 помечена «повышенная сложность» по outline), 30 заданий (10 + 11 + 9, ~30/40/30). Прогнано **44 программы** на эталоне rustc/cargo 1.97.0 (Windows gnu, `rustc --edition 2024`): 24 рабочих (выводы в тексте дословные, предупреждений ноль) и 20 ⛔/проверочных отказов; затем механическая сверка — все 21 листинг с `fn main` извлечены скриптом из готового текста и перекомпилированы: 14 полных чистые, 5 ⛔ падают ровно заявленными кодами (E0369, E0308, E0282, E0277, E0106), 2 — помеченные в тексте частичные листинги (`notify.rs`, `longest_scope_bad.rs` — «типы/функция из предыдущего примера»). Ключевые факты 1.97 (в тексте дословно): «cannot find type `T`» — **E0425** (не E0412) с подсказкой «you might be missing a type parameter»; E0369 у необобщённого `>` возникает **и без единого вызова** функции (проверка в точке определения — контраст с макросами §11.1.3); E0599 у `Point<{integer}>` с note «the method was found for `Point<f64>`»; `derive(PartialOrd)` без `PartialEq` — E0277 «required by a bound in `PartialOrd`» (супертрейт, R9); orphan rule — E0117 с note-ссылкой на R7 items/implementations и советом «define and implement a trait or new type instead» (newtype); возврат двух типов под `impl Trait` — E0308 с двумя help про `Box<dyn Summary>` (анонс гл. 22); `HashMap` с ключом без derive — E0277×2 (Eq, Hash) + E0599 разом; метод, возвращающий ссылку чужого лайфтайма вместо `self`, — ошибка «lifetime may not live long enough» без E-кода; `ParseIntError` kinds `Empty`/`InvalidDigit`/`PosOverflow` — прямое зеркало нашего `ParseError` из гл. 18 (использовано в тексте). Закрыты обещания прошлых глав: механика трейтов и прозрачность `impl From<X> for Y` (§18.2.2), ручной `Display`/`write!` для `{e}` (§18.1.2), `PartialEq`/`==` у enum (§16.2.1), E0106 в функциях (§15.2.3) и `&str`-полях (`Excerpt<'a>`, §16.1.1 и Г16.Ч1.З5 — «второй путь починки»), turbofish (§14.2.2), сигнатура `parse`/`FromStr` («parse_u32 на пенсию», резюме гл. 18), `Eq`+`Hash` и `get("...")` у HashMap (§17.2.1, Borrow — только справочная врезка), newtype+`From` против `typedef` (§8.2.3), единичная структура с трейтом (`Divider`, §16.1.1). Правила умолчаний лайфтаймов даны по R1 с арбитром R7 (lifetime-elision). Источники сверены WebFetch в заход: R1 гл. 10.0–10.3 (в актуальной редакции пример `Tweet` переименован в `SocialPost`), R7 (items/traits, items/implementations — orphan, lifetime-elision, items/generics), R9 (fmt::Display, cmp::PartialEq/Eq/PartialOrd, convert::From, str::FromStr, collections::HashMap), R2 (generics, trait, scope/lifetime — URL актуальны), R6 (generics/* — актуальны). R4 — O'Reilly стабильно 403, R5 — печатное: цитируются справочно по outline, факты покрыты R1/R7/R9. Кросс-ссылки на том I выверены по заголовкам глав (typedef — §8.2.3, `==` структур — §8.1.1, макросы — §11.1.3, хеш-таблица — §9.3.1, литералы — §5.2.1 + C5 §6.4.5). Забеганий вперёд нет: dyn Trait/`Box` → гл. 22, замыкания/итераторы/`impl Iterator` → гл. 21, модули/тесты → гл. 20, `TryFrom`/`Into`/`Borrow` — помечены «за рамками» — только явные анонсы. Открытых вопросов не добавилось. **Следующий заход: глава 20 `20-modules-cargo-tests` (этап 3); параллельно допустимы решения тома I (этап 5, с главы 1).**

- 2026-07-18 (2): **глава 20 (организация кода, тесты, документация; зеркало гл. 11) написана и верифицирована**, 2 части, 7 параграфов (4 + 3), 20 заданий (10 + 10, ~30/40/30 в каждой части). Прогнано **~40 программ/сборок** на эталоне rustc/cargo 1.97.0 (Windows gnu, издание 2024): 10 одиночных rustc-файлов (5 рабочих, 5 ⛔ — E0603 ×2, E0616, E0433, E0428) и cargo-проекты: **textstat** (сквозной пример: lib+bin, модули по файлам, реэкспорт, 10 юнит- + 2 интеграционных + 2 doc-теста; плюс «плоская» версия из листингов §20.1.4 — вывод совпал), units (эталон З8 обеих частей: 4 секции зелёные), tdd_words (эталон TDD-задания З9), words_ws (workspace: единственные target/ и Cargo.lock в корне, `-p` — проверено), fmt_demo (до/после cargo fmt), clippy_demo, dice_demo (rand), geo (реальный вывод для Ч2.З4). Механическая сверка: из готового текста извлечены **35 rust-блоков**, 17 с `fn main` перекомпилированы — 9 чистых, 8 падают ровно заявленно (⛔ либо листинги cargo-контекста, сверенные в проектах). Ключевые факты 1.97/издания 2024 (в тексте дословно): `cargo test` пакета lib+bin печатает **4 секции** (включая пустую «Running unittests src\main.rs» бинарного crate); doc-тесты собираются одним пакетом — «merged doctests», в панике doc-теста путь `doctest_bundle_2024.rs`; **E0433 переформулирован** («cannot find module or crate ... unresolved module or unlinked crate» вместо цитируемого R1 «failed to resolve»), при этом даёт пару «ошибка + warning unused_imports»; E0603 у приватного модуля дополнен пометкой «function ... is not publicly re-exported»; имя потока в панике теста = полное имя теста; неверный `expected` у should_panic → «panic did not contain expected string» с parой panic message / expected substring; фильтр по имени отключает секцию Doc-tests; clippy: `approx_constant` — **deny по умолчанию** (ошибка), `len_zero` — warn с готовой заменой; **rand 0.10.2: `random_range` переехал в трейт `RngExt`** (E0599 прямо советует `use rand::RngExt;` — пример R1 с `use rand::Rng` устарел, в тексте — актуальный API), `cargo add rand` лочит 8 пакетов; `cargo new` внутри git-репозитория вложенный .git не создаёт (проверено экспериментом); E0761 при clean.rs+clean/mod.rs; литерал с приватным полем — **E0451** (отдельный код от E0616-доступа), rustc показывает по одной privacy-ошибке за прогон (nuance для решений Ч1.З7). Инструменты поставки подтверждены: rustfmt 1.9.0-stable, clippy 0.1.97, rustdoc 1.97.0; `cargo doc` строит титульную с секцией Re-exports (проверено по HTML). Введено явно: mod/pub/pub(crate)/пути/super/self/use/as/pub use/глоб, пакет vs crate/crate root/src/bin, [dependencies]/cargo add/Cargo.lock/SemVer-требование, workspace (обзорно), #[test]/assert!/assert_eq!/assert_ne!/#[should_panic(expected)]/Result-тесты/#[cfg(test)]/#[ignore]/фильтры/--test-threads/--show-output/--lib/--test/--doc, tests/ и common/mod.rs, ///+//!/# Examples/# Panics/doc-тесты, cargo doc/fmt/clippy (+cargo fix упоминание), std::prelude, `std::f64::consts::PI`. Кросс-ссылки выверены по заголовкам глав (в ходе ревизии исправлены §19.2.3, §14.1.2, §11.1.1, `Rect::square`). Источники сверены WebFetch в заход: R1 (гл. 7.0–7.5, 11.1–11.3, 14.2–14.3, прил. D), R10 (guide: dependencies, project-layout), R7 (items/modules, visibility-and-privacy — арбитр), R6 (modules/*, testing/lints), R9 (std::prelude). R4 — O'Reilly стабильно 403; факты покрыты R1/R7/R10. Забеганий вперёд нет: замыкания/итераторы → гл. 21, Box/dyn → гл. 22, потоки → гл. 23 — только явные анонсы; Iterator/Box в списке прелюдии помечены номерами глав. Открытых вопросов не добавилось. **Следующий заход: глава 21 `21-functional` (этап 3); параллельно допустимы решения тома I (этап 5, с главы 1).**

- 2026-07-18 (3): **глава 21 (замыкания и итераторы) написана и верифицирована**, 2 части, 6 параграфов (3 + 3), 20 заданий (10 + 10, ~30/40/30 в каждой части). Прогнано **~55 программ/сборок** на эталоне rustc/cargo 1.97.0 (Windows gnu, `rustc --edition 2024`): 34 одиночных рабочих файла (включая факт-проверки условий), 17 ⛔/предупреждений, эталоны прикладных заданий в 4 cargo-пакетах (applyn; textstat21 — textstat из гл. 20, переписанный на цепочки под теми же 7 тестами; countdown; topwords — итого 19 тестов, все зелёные). Механическая сверка: из готового текста извлечён 41 rust-блок, 36 с `fn main` перекомпилированы — 27 чистых, 1 намеренное предупреждение (must_use у ленивого `Map`), 8 ⛔ падают ровно заявленными кодами (E0434, E0308 ×3, E0502, E0382 ×2, E0594), 5 — помеченные фрагменты. Ключевые факты 1.97/издания 2024 (в тексте дословно): вложенная `fn` не видит окружение — E0434 с help «use the `|| { ... }` closure form instead»; `collect` без аннотации — **E0283** (не E0282, вопреки старым материалам) с note про FromIterator и help `Vec<_>`; E0507 при выносе из FnMut-замыкания дополнен новой help-строкой «`Fn` and `FnMut` closures require captured values to be able to be consumed multiple times, but `FnOnce` closures may consume them only once»; FnMut-замыкание на Fn-bound — E0594 с двусторонней подсказкой «change this to accept `FnMut` instead of `Fn`»; повторный вызов FnOnce — E0382 с note «closure cannot be invoked more than once»; два замыкания в одном Vec — E0308 «no two closures, even if identical, have the same type» + «consider boxing» (анонс гл. 22); двойная ссылка в filter-предикате — E0308 «expected `&&_`, found integer»; «спроси компилятор» печатает луковицу `Filter<Map<Iter<...>, {closure@...}>>` и выгружает полное имя типа в файл; **точный захват полей издания 2024 подтверждён экспериментом** (`|| p.x += 1` сосуществует с `&p.y`); `sort_by_key` на 3 элементах зовёт ключ 6 раз (наблюдение, в тексте с оговоркой); Debug-печать `Option<&i32>` не показывает ссылочность (`Some(1)`). Введено явно: замыкания (формы, фиксация типов E0308, три режима захвата + move + Copy-нюанс, Fn/FnMut/FnOnce, тип `fn(i32) -> i32` и коэрция незахватывающих, E0373 при возврате без move, `-> impl Fn`), sort/sort_by/sort_by_key + `std::cmp::Ordering` (f64 — через partial_cmp().unwrap()), unwrap_or_else, Iterator/next/Item (ассоциированный тип — рабочее определение), iter/iter_mut/into_iter, десахаризация for (раскрытие прогнано руками), адаптеры map/filter/enumerate/zip/rev/take/skip/chain, потребители collect/sum/count/fold/max/min/all/any/find/position/for_each, свой Iterator (Counter с суммой 18, бесконечный Fib + take/find), `-> impl Iterator`. Векселя закрыты: §6.3.2 (указатели на функции → замыкания), §14.2.4 (enumerate), §16.2.1 (None = «поток кончился»), §17.1.1–17.1.2 (механика into_iter/for-по-значению), §17.1.4/§17.2.1 (chars/split_whitespace — итераторы), §17.2.1 (порядок HashMap: Vec + sort_by), §19.2.2 (`-> impl Iterator` для непроизносимых типов), гл. 20 (word_count → `split_whitespace().count()` под старыми тестами). Производительность — пересказ R1 гл. 13.4 (бенчмарк «Шерлок Холмс»: цикл ≈ итератор) с механикой через мономорфизацию §19.1.3; собственные бенчмарки не ставились. Источники сверены WebFetch в заход: R1 (гл. 13.1/13.2/13.4 — заголовки секций и примеры актуальны), R7 (types/closure — арбитр по режимам захвата и трейтам вызова; expressions/closure-expr; expressions/loop-expr «Iterator loops» — десахаризация), R9 (std::iter, Iterator — контракты методов, ops::FnOnce — иерархия супертрейтов), R2 (fn/closures, trait/iter — URL живые). R4 — печатное (O'Reilly 403), цитируется по outline, факты покрыты R1/R7/R9. Забеганий вперёд нет: Box/dyn/Deref/Rc → гл. 22, потоки → гл. 23 — только явные анонсы с номерами. Открытых вопросов не добавилось. **Следующий заход: глава 22 `22-smart-pointers` (этап 3); параллельно допустимы решения тома I (этап 5, с главы 1).**

- 2026-07-18 (4): **глава 22 (умные указатели) написана и верифицирована**, 2 части, 7 параграфов (4 + 3; часть 22.2 «повышенная сложность» по outline), 20 заданий (10 + 10, ~30/40/30 в каждой части). Прогнано **~50 программ/сборок** на эталоне rustc/cargo 1.97.0 (Windows gnu, `rustc --edition 2024`): 26 одиночных рабочих файлов (примеры + эталоны решений + факт-проверки условий), 3 паникующих, 8 ⛔, эталоны прикладных заданий в **5 cargo-пакетах** (shapes22, iterpick, mocklog, treeweak, staff — 19 тестов, все зелёные). Механическая сверка: из готового текста извлечён **31 rust-блок**, 30 с `fn main` перекомпилированы — 20 полных чистых (предупреждений ноль), 3 паники exit 101, 7 ⛔ падают ровно заявленными кодами (E0072, E0614, E0040, E0308, E0277, E0382, E0596), 1 помеченный фрагмент (определение трейта `Deref`). Ключевые факты 1.97/издания 2024 (в тексте дословно): **паника RefCell переформулирована** — «RefCell already borrowed» (второй писатель или писатель при читателе) и «RefCell already mutably borrowed» (читатель при писателе) — вместо цитируемого R1 «already borrowed: BorrowMutError» (тип ошибки тот же, текст новый; отражено в §22.2.1); E0072 — help «insert some indirection (e.g., a `Box`, `Rc`, or `&`) to break the cycle» с готовым диффом; E0382 на Box-списке — note «if `List` implemented `Clone`, you could clone the value»; E0596 на `push` сквозь Rc — help «trait `DerefMut` is required to modify through a dereference, but it is not implemented for `Rc<Vec<i32>>`»; E0277 при `Box<String>` в `Vec<Box<dyn Shape>>` — note «required for the cast from `Box<String>` to `Box<dyn Shape>`»; E0040 — дифф-замена `c.drop()` → `drop(c)`. Платформенные размеры (в тексте): `Box<i32>` = 8, `Option<Box<Node>>` = 8 (NPO как у `Option<&T>` §16.2.3), `Node{i32, Option<Box>}` = 16, `&dyn`/`Box<dyn>` = 16 против 8 обычных. Подтверждено прогоном: порядок drop (переменные — обратный объявлению, поля — по объявлению, ранний `drop()`); цикл Rc — Drop не вызывается (strong 2/2 на выходе), контрольная программа без цикла — вызывается; дерево с Weak — счётчики дословно (branch 1/1, leaf 2/0, после смерти родителя `upgrade` → None, leaf 1/0); ручное размыкание цикла (`*a.next.borrow_mut() = None`) возвращает оба drop. Введено явно: умный указатель (определение), Box + рекурсивные типы + **type-псевдоним** (родство с typedef §8.2.3 — раньше не вводился), Deref/Target/deref-коэрция (3 правила + места), Drop/drop glue/функция drop, dyn Trait как третья форма DST, широкий указатель/vtable, static vs dynamic dispatch, dyn-совместимость (кратко), `Box<dyn Error>` (+`fs::write`), Rc/strong_count/конвенция `Rc::clone(&a)`, RefCell/Ref/RefMut (+`try_borrow`, `Cell` — упоминания), `Rc<RefCell<T>>`, циклы/Weak/downgrade/upgrade, формат `{:.2}` (наследник `%.2f` §2.3.1 — раньше не вводился). Векселя закрыты: §8.1.3+§9.1.2 (рекурсивный тип и список без malloc), §15.1.1 (наблюдение порядка drop), §15.3.1 (третья форма DST), §15.3.2+§17.1.3 (механика коэрции `&String`→`&str`), §18.1.3 (`Box<dyn Error>`), §19.2.2+§21.2.3 («или-или» под `Box<dyn Iterator>`), §21.1.3 (`Vec<Box<dyn Fn>>` — «consider boxing» исполнен), §13.2.3 (утечка через цикл Rc собрана руками и вылечена), гл. 20 (Box в прелюдии). Источники сверены WebFetch в заход: R1 (гл. 15.0–15.6 и 18.2 — актуальны), R7 (destructors, types/trait-object, dynamically-sized-types, type-coercions, interior-mutability — арбитры; UnsafeCell как единственный легальный фундамент внутренней изменяемости), R9 (boxed, ops::Deref/Drop, mem::drop, rc — включая тонкость «Weak держит аллокацию, не значение», cell, error::Error), R2 (std/box, std/rc, trait/dyn — живые), R6 (smart-pointers/*, borrowing/interior-mutability — живые). R4/R5 — печатные, цитируются справочно по outline. Забеганий вперёд нет: Arc/Mutex/Send/Sync/потоки — только анонсы с номером «глава 23» (проверено grep). Открытых вопросов не добавилось. **Следующий заход: глава 23 `23-concurrency` (этап 3); параллельно допустимы решения тома I (этап 5, главы 1→13 по порядку).**

- 2026-07-19: **глава 23 (многопоточность) написана и верифицирована**, 2 части, 7 параграфов (3 + 4), 20 заданий (10 + 10, ~30/40/30 в каждой части). Прогнано **~50 программ/сборок** на эталоне rustc/cargo 1.97.0 (Windows gnu, `rustc --edition 2024`): 23 рабочих одиночных файла (недетерминированные — по 2–3 прогона, в текст выводы конкретных прогонов с пометкой), 10 ⛔ (E0373, E0382 ×3, E0499, E0277 ×5), 2 паникующих сценария (паника в потоке — join даёт Err, exit 0; unwrap на отравленном замке — exit 101), **3 подтверждённых зависания** (сняты по таймауту: двойной lock в одном потоке, перекрёстные замки двух потоков, цикл for по каналу без drop(tx)), 3 cargo-пакета эталонов прикладных (parsum 4, bank23 4, wordfreq23 3 теста — все зелёные). Механическая сверка: из готового текста извлечены **24 rust-блока**, 20 с `fn main` перекомпилированы — 15 чистых (предупреждений ноль), 5 ⛔ ровно заявленными кодами, 4 помеченных фрагмента (withdraw_racy дополнительно проверен компиляцией в обёртке). Ключевые факты 1.97 (в тексте дословно): E0373 у spawn — note «function requires argument type to outlive `'static`»; E0382 на move-в-замыкание — help «consider cloning the value before moving it into the closure»; два писателя в thread::scope — **E0499** (тот же код, что в §15.2.2, — отдельного «многопоточного» кода нет); Rc в spawn — E0277 «cannot be sent between threads safely» + «required by a bound in `spawn`»; Arc<RefCell> — E0277 «cannot be shared between threads safely» + note «required for `Arc<RefCell<i32>>` to implement `Send`» + совет RwLock; Mutex Debug — `Mutex { data: 6, poisoned: false, .. }`; RecvError Display — «receiving on a closed channel»; паника на отравленном — «called `Result::unwrap()` on an `Err` value: PoisonError { .. }»; двойной lock на Windows — молчаливое зависание (R9: unspecified, «вызов не вернётся»); MutexGuard !Send (R9: pthreads требуют отпирать в потоке взятия) / Sync — доказано компиляцией is_send/is_sync-каталога. Поведенческое: обрыв потоков при смерти main; **ловушка Copy** (два move-замыкания с u64 — компилируется чисто, потоки рапортуют по 1000000, main видит 0); оба ремейка race.c — канал частных сумм и Arc<Mutex> — дают ровно 2000000 в каждом прогоне. Введено явно: spawn/JoinHandle/join/sleep, **Duration** (ранее не вводился), thread::scope, **slice::split_at** (ранее не вводился), mpsc (channel/send/recv/try_recv/iter/clone отправителя/drop(tx)), Mutex/lock/MutexGuard/отравление/into_inner, Arc, Arc<Mutex<T>>, Send/Sync/авто-трейты, приём-доказательство `is_send::<T>()`. Векселя закрыты: §13.1.4 (бесстрашная версия — дважды), §15.2.2 (XOR-правило как фундамент), §21.1.2–21.1.3 (move для потоков), §22.1.2 (замок отпирается в Drop), §22.1.4 (Rc→Arc с показанным отказом), §22.2.1 (RefCell→Mutex), §22.2.2 (Arc<Mutex>, миграция Sensor — З10), §20.2.2 (тесты в потоках). Источники сверены WebFetch в заход: R1 (гл. 16.0–16.4), R9 (thread, mpsc, Mutex, MutexGuard — обоснование !Send, Arc — условия Send/Sync, RwLock, marker::Send), R7 (special-types-and-traits — авто-трейты, арбитр), R6 (concurrency/threads+channels+shared-state+send-sync — URL живые), R2 (std_misc/threads, std_misc/channels). R4/R5 — печатные (O'Reilly стабильно 403), по outline. C-рубрика: N3220 §7.28 `<threads.h>` (thrd_/mtx_/cnd_, `__STDC_NO_THREADS__`) сверен pdftotext. Забеганий вперёд нет: unsafe/UnsafeCell/FFI — только анонсы «глава 24» (grep). Самопроверка выловила и исправила две неверные кросс-ссылки (§15.1.4→§15.1.3 Copy; §19.1.2→§19.1.3 ограничения). Открытых вопросов не добавилось. **Следующий заход: глава 24 `24-unsafe-ffi` (этап 3); параллельно допустимы решения тома I (этап 5, главы 1→13 по порядку).**

- 2026-07-19 (2): **глава 24 (unsafe Rust и FFI; обзорно-продвинутая, замыкает пару C↔Rust) написана и верифицирована**, 2 части, 7 параграфов (3 + 4), 18 заданий (9 + 9, ~30/40/30 в каждой части). Прогнано **~65 программ/сборок** двумя компиляторами: Rust — эталон rustc/cargo 1.97.0 (Windows gnu, `rustc --edition 2024`), C — MinGW GCC 15.2 `-std=c17 -Wall -Wextra`: ~24 рабочих rust-файла (примеры, эталоны решений всех 18 заданий, факт-проверки), 10 ⛔ (E0133 ×3 вида, E0499, E0277, static_mut_refs-deny, «extern blocks must be unsafe», «unsafe attribute used without unsafe» и др.), 2 намеренных UB-прогона, 4 C-библиотеки + 3 смешанных бинарника (gcc-клиент + rustc-staticlib), cargo-проект mixed_demo (build.rs + cc 1.3.0, 3 пакета в замок). Механическая сверка: из готового текста извлечено **29 rust-блоков** (16 с `fn main`: 12 чистых — предупреждений ноль, 2 ⛔ ровно заявленными, 2 листинга cargo-контекста — сверены в проекте; блок rustlib собран staticlib чисто) и 5 C-блоков (все компилируются; клиент слинкован с Rust-библиотекой). **Окружение: установлен nightly-тулчейн с Miri** (`rustup toolchain install nightly --profile minimal --component miri`; rustc 1.99.0-nightly, miri 0.1.0 от 2026-07-18) — в тексте два подлинных UB-отчёта (dangling: «alloc has been freed, so this pointer is dangling» + места выделения/смерти; oob: «attempting to access 4 bytes, but got alloc+0xc which is at or beyond the end of the allocation of size 12 bytes»). Ключевые факты 1.97/издания 2024 (в тексте дословно): E0133 — общий код сверхспособностей с разными note (deref «raw pointers may be null, dangling or unaligned…», union «field may not be properly initialized», вызов «consult the function's documentation»); unsafe_op_in_unsafe_fn — warn («an unsafe function restricts its caller, but its body is safe by default»); **static_mut_refs — deny** («creating a shared reference to mutable static» — ловит даже `println!` от static mut; help «use `&raw const`»); новое предупреждение dangling_pointers_from_locals («a dangling pointer is safe, but dereferencing one is undefined behavior»); E0499 наивного split_at_mut — help «use `.split_at_mut(position)`»; **null-deref: dev — вставленная проверка** «null pointer dereference occurred» → non-unwinding abort (exit −1073740791), **release −O — мусор 32764 и exit 0** (3 прогона; зеркало §13.1.3); **ловушка точного захвата**: `unsafe impl Send` бессилен, если замыкание захватило поле `*mut` вместо структуры (E0277 на поле; лечится методом по self — врезка в §24.1.3); «extern blocks must be unsafe» / «unsafe attribute used without unsafe» + help «wrap the attribute in `unsafe(...)`»; safe/unsafe-квалификаторы в unsafe extern работают (safe fn abs — вызов без unsafe); **repr(Rust) реально переупорядочил** {c_char,c_int,c_char}: 8 байт (b=0,a=4,c=5) против 12 (0/4/8) у repr(C) == C (offset_of!/offsetof сверены с обеих сторон; Point3 24/0/8/16 совпал и без сюрпризов); `--print native-static-libs`: -lkernel32 -lntdll -luserenv -lws2_32 -ldbghelp (staticlib ~15 МБ — std внутри; без них undefined reference to NtReadFile); nm: v0-манглинг `_RNv…11mangled_add` против `T plain_add`; паника через extern "C" — «panic in a function that cannot unwind» → abort 0xC0000409; qsort из CRT с Rust-компаратором сортирует Rust-массив; CString::new с внутренним нулём → Err(NulError(позиция, байты)); wrong-signature extern-объявление линкуется молча (линкер сверяет только имена — прогнано без запуска). Введено явно: пять сверхспособностей, `*const`/`*mut`, `&raw const/mut`, add/is_null/from_raw_parts_mut, SAFETY-конвенция, static mut, union, unsafe trait/unsafe impl Send, UnsafeCell (+самодельный MyCell), Miri, ABI/`unsafe extern "C"`/safe fn/#[link], алиасы c_*, #[repr(C)]/offset_of!, CString/CStr, байтовые литералы b'x', #[unsafe(no_mangle)], staticlib, Box::into_raw/from_raw, opaque-типы, extern "C" fn-коллбэки, build.rs + crate cc (+bindgen обзорно). Векселя закрыты: §8.2.3 (repr(C) — «договор о раскладке»), §13.2.3 («грепни unsafe» + перенос ответственности), §15.2.1/§15.2.3 (сырые указатели — «круг замкнулся», dangle_c против предупреждения rustc), §22.2.1 (UnsafeCell — фундамент RefCell/Mutex, показан руками), §23.2.3 (unsafe impl Send под подпись), резюме гл. 23 (два языка в одной программе — в обе стороны). Источники сверены WebFetch в заход: R1 (гл. 20.1 — актуальна: unsafe extern, safe fn, #[unsafe(no_mangle)], Miri), R7 (unsafety, behavior-considered-undefined, external-blocks — семантика 2024, type-layout — арбитры), R8 (what-unsafe-does, ffi — приёмы владения/коллбэков/unwinding), R9 (std::ffi — CString/CStr/c_*), R6 (unsafe-rust/*, android/interoperability/with-c — URL живые). R4/R5 — печатные (O'Reilly стабильно 403), по outline. Самопроверка §6 выловила и исправила **~25 неверных кросс-ссылок** на параграфы томов I–II (номера сверялись по заголовкам: размеры типов — §2.1.2, union — §8.2.2, паддинг/offsetof — §8.2.3, «грепни unsafe» — §13.2.3, scope/split_at — §23.1.2 и др.) и NLL-дефект задания Ч1.З7 (два неиспользуемых `&mut` компилируются — фрагмент дополнен использованием, E0499 подтверждён прогоном). Открытых вопросов не добавилось. **Следующий заход: глава 25 `25-capstone` (этап 3); параллельно допустимы решения тома I (этап 5, главы 1→13 по порядку).**

- 2026-07-19 (3): **глава 25 (итоговые проекты) написана и верифицирована** — первая глава в формате проектных чекпоинтов (§3: допустимо для гл. 25/26), 2 части, 9 чекпоинтов + ретроспектива, 20 заданий (10 + 10, ~30/40/30 в каждой части). **Ч1 minigrep** (R1 гл. 12 + 13.3, адаптировано): 4 стадии эволюции (наивная → lib+`Config`/`Result` → TDD-поиск+регистр → итераторы без `clone`), каждая собрана и прогнана отдельно (cargo 1.97, edition 2024), 3 юнит-теста; пакет — «библиотека + тонкий main» с прицелом на переиспользование в гл. 26. C-эскиз (27 строк, MinGW чисто, ASan в WSL без отчётов); **подтверждён прогоном и внесён в текст** факт тихой потери совпадения на границе буфера `fgets` 1024 (слово, рассечённое границей: C — 1 из 2 строк, Rust — 2 из 2). **Ч2 ThreadPool + pargrep** (R1 гл. 21.2–21.3): стадия A без Drop (dead_code-предупреждения процитированы; реальный обрыв воркера «на полуслове» при смерти процесса — использован как мотивация Drop), ⛔ `while let` подтверждён прогоном (полная сериализация: 3 задачи из 8 против 8 из 8, замок живёт всю итерацию — R7 Destructors), финал: `Option::take` + `Vec::drain` + join (613 мс на 8×300 мс при 4 воркерах), тест пула (счётчик 100 под `Arc<Mutex>`); pargrep: path-зависимость на minigrep, обратный канал результатов, ошибки как значения. Бенч (release, 8 файлов×33 МБ, регистронезависимый поиск, 5900X): 1 поток 2.04 с → 4 потока 683 мс → 8 потоков 368 мс; счёт детерминирован (3200). Новые API главы: `process::exit`, `env::var`, `str::contains`/`lines`/`to_lowercase`, `is_ok`, `Option::take`, `Vec::drain`, `Instant`, `fs::write`; в C — `argc`/`argv` (в томе I не вводились!) и `strstr`. Мех. сверка: 27 код-блоков (25 rust + 1 c + 1 toml) — полные листинги diff-идентичны прогнанным файлам, фрагменты проверены на дословное вхождение, промежуточные стадии пересобраны из блоков и воспроизводят поведение; E0515 подтверждён компилятором. **Сверка перекрёстных ссылок скриптом** (node: все `§N.M(.k)` из главы против реальных заголовков глав 1–24): 68 ссылок, 0 битых после исправления ~15 промахов первой редакции — приём стоит повторить на ревизии тома II для глав 14–24.
- 2026-07-19 (3), инфраструктура: **wsl.exe из Git Bash теряет кавычки и коды возврата** (аргументы склеиваются и переразбираются оболочкой WSL: `$?` раскрывается не там, вывод длинных прогонов перемешивается/усекается). Рабочая схема: скрипт-файл на `/mnt/c/...`, запуск `wsl -d Ubuntu-24.04 -u root bash -c "bash /mnt/c/.../script.sh > /root/.../log 2>&1"`, чтение лога отдельным вызовом `cat` (лог — внутри WSL-ФС, не через конвейер). MSYS-конвертация путей (`/mnt/...` → `C:\Program Files\Git\mnt\...`) отключается `MSYS_NO_PATHCONV=1`. Попутно: MinGW разворачивает `argv[0]` в полный путь (в главе процитировано с пометкой «путь сокращён»).

- 2026-07-20: **глава 26 (GUI на Rust: egui; обзорная, контрольная работа тома II, финал этапа 3) написана и верифицирована**, 2 части: Ч1 — 3 классических параграфа + 8 заданий, Ч2 — 4 проектных чекпоинта + 9 заданий (17 всего, ~30/40/30 в каждой части). **Окружение: eframe/egui 0.35.0** (июнь 2026; `cargo add eframe` — 418 пакетов в замке, первая сборка 1 м 45 с). **API 0.35 отличается от старых материалов**: обязательный метод App — `ui(&mut self, &mut egui::Ui, &mut Frame)` (не `update(ctx, …)`); панели — единый `egui::Panel::top/bottom(id)` + `CentralPanel::default()`, у всех `.show(ui, …)` (бывший `show_inside` помечен «Renamed to `show`»); факты сверялись **по исходникам крейта в `~/.cargo/registry/src/`** — WebFetch-пересказы docs.rs дважды ошиблись, приём запомнить. Прогнано: 3 примера Ч1 + 2 стадии проекта (sync/фон) + release — 6 дымовых GUI-прогонов (окно → PrintWindow-скриншот → WM_CLOSE → exit 0; кириллица в default_fonts ок), счётчик 0→3→0 автокликами (зеркало прогона §12.2.1), полный E2E проекта (SendInput: «rust»/«big2.txt», флажок, кнопка): спиннер + «ищем…» пойманы снимком, итог «найдено строк: 2400 за 2.5058376s», ошибки путей с экрана («os error 2»; «os error 3» на пустом пути). **Ключевой замер главы**: WM_CLOSE во время поиска по 193-МБ файлу (2,4 млн строк, генератор §25.2.4) обслужен sync-версией через **2723 мс**, фоновой — через **191–199 мс**; сам поиск (без чтения): dev 2,48 с (без регистра)/1,07 с, release 1,39 с/52 мс; 33-МБ файл: 394/173 мс dev, 226/8,4 мс release. Библиотека minigrep реконструирована по листингам гл. 25 (99 строк — сошлось с заявленным в гл. 25), 3 теста зелёные, **изменений — ноль** (вексель §25.1.4 оплачен). ⛔ подтверждены: E0597 «поле-ломтики» («assignment requires that `contents` is borrowed for `'a`») и E0597 «Vec<&str> в канал» («lifetime `'1` appears in the type of `tx`»); вариант опроса `if let Some(rx) = &self.search { … self.search = None }` **компилируется** (NLL) — Option::take не нужен (в главе простая форма, take — задание Ч2.З7); `contains("")` == true (прогнано). windows_subsystem (R7 сверен): objdump Subsystem 3 (CUI) → 2 (GUI) — парный факт к `-mwindows` §12.1.2. Новое введено явно: `Default`/derive (нигде ранее не вводился!), `Option::is_some`, `try_recv`/`TryRecvError` (вексель «справочно» §23.1.3), `.cargo/config.toml` (R10 сверен: поиск вверх по каталогам, `[target] rustflags`, `CARGO_TARGET_DIR`), `egui::Context` (дёшев клон/подсчёт ссылок/Send+Sync — в исходниках egui дословно наш приём-доказательство §23.2.3), `request_repaint` из потока. Мех. сверка скриптом (node): 28 код-блоков — полные листинги diff-идентичны прогнанным файлам (hello 24 строки, счётчик 35, greet 48, sync 84, финал 131), ⛔ — префиксы/строки прогнанных bin; §-ссылки: 51 уникальная, 126 всего, **0 битых** (приём гл. 25 повторён). **Столбец «Глава» закрыт полностью — этап 3 завершён. Следующий заход: ревизия тома II (главы 14–26, этап 4а); параллельно допустимы решения тома I (этап 5, главы 1→13).** Напоминание для ревизии из захода 18: заменить «rustc файл.rs» в §14.1.1/введениях на форму «rustc --edition 2024».

- 2026-07-20, инфраструктура: (1) **gnu-тулчейн + eframe не линкуется из коробки**: `ld: cannot find -lshlwapi` — rust-mingw несёт 44 import-библиотеки (self-contained), shlwapi среди них нет, а clipboard-win (зависимость eframe) её требует; лечение (в тексте главы): `.cargo/config.toml` → `[target.x86_64-pc-windows-gnu] rustflags = ["-L", "<MinGW>\x86_64-w64-mingw32\lib"]`; путь `linker = "gcc"` НЕ работает — в scoop-MinGW нет `libgcc_eh.a` (проверено). Аномалия: самый первый прогон слинковался и без фикса (артефакт остался), все последующие стабильно падали; причина расхождения не установлена, фикс детерминирован. (2) **Коллизия отпечатков cargo**: два пакета с одинаковым `name` в общем `CARGO_TARGET_DIR` считают друг друга актуальными («сборка» за 0,4 с без Compiling — в target бинарник чужой стадии); стадии проекта верифицировать в раздельных target-каталогах. (3) **GUI-автоматизация winit/egui-окон**: у процесса несколько видимых окон — хелперы и консоль; консольное окно приписывается процессу, и WM_CLOSE ему убивает процесс с 0xC000013A — фильтровать класс `ConsoleWindowClass` и брать окно наибольшей площади; скриншоты — `PrintWindow` с PW_RENDERFULLCONTENT (CopyFromScreen снимает область экрана — в кадр попадает перекрывающее чужое окно; такие снимки удалены и пересняты); SendInput на x64: `INPUT` = 40 байт, union добивать до 32 (иначе молча 0 событий); **перед синтетическим вводом — обязательная проверка `GetForegroundWindow() == наше окно`** (без неё клики/текст уходят в постороннее приложение — случилось один раз; лечение: HWND_TOPMOST + клик по собственному заголовку + проверка, курсор восстанавливать).

- Открытый пункт владельцу (правило §2.3, к ревизии тома II): ручная приёмка GUI главы 26 — живая прокрутка 2400 результатов, поиск по своим файлам, внешний вид (плюс аналогичный пункт главы 12, открытый с 2026-07-09). **Ревизией тома II не закрыт — остаётся за владельцем (агент проверяет только компиляцию и дымовой запуск).**

- 2026-07-22: **ревизия тома II (главы 14–26) выполнена** (этап 4а, чеклист §6). Механика (скрипты node по всем 13 главам): 274 задания, ID уникальны, нумерация сплошная, 6–15 на часть (17–30 на главу); у всех 28 частей блок «Источники части» с ≥2 ID реестра; §-ссылок на курс битых нет (три «битые» — §6.4.5, §6.7.3, §7.28 — оказались ссылками на стандарт C, все атрибутированы C5); маркеры догадок (11 срабатываний grep) — все ложные (в т.ч. «не о**кажется,**») либо цитата R11 и обращения к читателю; 109 упоминаний будущих глав проверены — все явные анонсы с номером главы. **Перепрогон кода**: 379 rust-блоков извлечены скриптом и собраны на эталоне (rustc 1.97.0, `--edition 2024`): 224 блока собрались самостоятельно (199 чисто, 25 с предупреждением: 23 — артефакт сборки фрагментов как lib, 2 — намеренные демонстрации `#[must_use]`: unused `Result` §18.1.2 и unused `Map` §21.2.2), ещё 16 — в связке с предыдущим блоком (объявленная конвенция «продолжение файла»); **68 упали ровно с той ошибкой, что напечатана в книге** (первая строка сверена дословно: 59 одиночно, 9 в связке); 9 разобраны вручную реконструкцией файлов — 6 совпали дословно, включая строку и колонку (`c4_sort_fnonce_bad.rs:18:30`, `rc_mutex.rs:11:36`), `rand`-пример собран cargo-проектом офлайн (E0599 + обе help-строки), оба E0597 главы 26 воспроизведены с локальным `minigrep` (`borrowed_results.rs:9:48`); оставшиеся 62 — иллюстративные фрагменты, у которых в книге вывода ошибки нет (куски листингов, части cargo-проектов, FFI-демо с C-объектником). **Найдено и исправлено:** (1) гл. 20, §20.1.2, `fields_private.rs` — в листинге вывода стоял `error[E0603]`, компилятор даёт **E0616** (проза двумя абзацами ниже называла E0616 верно) — единственная фактическая ошибка ревизии; (2) гл. 17 — единственная глава тома II без параграфных рубрик «Связь с практикой» и «Источники»: дописаны во все 7 параграфов (источники — сводка уже стоявших в тексте ссылок, новых утверждений нет); (3) §14.1.1 закрыт вексель захода 18 — `rustc hello.rs` → `rustc --edition 2024 hello.rs` (3 места) с объяснением, зачем флаг (проверено: без флага действует издание 2015 — `async` компилируется как имя переменной); согласованы введения глав 15–17 (стояло `rustc файл.rs` при заявленном издании 2024) и формулировки глав 18–19, где флаг подавался как «новый»; (4) гл. 23, `move_then_use.rs` — некомпилирующийся пример без пометки ⛔ (правило §2.3): пометка добавлена в код, ошибка перепроверена (та же строка 10:32); (5) терминология: в гл. 25 «мутабельного» → «изменяемого» (единственное употребление на весь том), «ломтики» при первом появлении привязаны к срезам §15.3.1; (6) в гл. 23 ссылка «(§7.28 …)» в таблице сравнения атрибутирована как «C5, §7.28». **Признано соответствующим Плану §2.3 по существу** (правка не требуется): проектные чекпоинты глав 25 и 26 (разрешены §3), сквозной проект §18.2.3 и параграфы главы 24, где прикладная часть подана именованными рубриками («Гигиена unsafe», «Инструменты: Miri», «Типы-мостики», «Строки», «Владение через границу») вместо канонической «Связь с практикой». Открытых вопросов не добавилось. **Следующая клетка: решения (этап 5), главы 1→26 по порядку; затем сквозная финальная ревизия.**

## 11. Критерии готовности (Definition of Done — из Плана §8)

- [ ] Реестр источников существует; каждая часть сверена минимум с двумя авторитетными источниками.
- [ ] В теории нет догадок и непроверенных утверждений (правило 4.4).
- [ ] Все 26 глав написаны и соответствуют структуре §2 Плана (GUI-главы 12 и 26 — с явной пометкой «обзорная»).
- [ ] В каждой части — 6–15 заданий с уникальными ID, упорядоченных по сложности.
- [ ] Материал самодостаточен: не требует не введённых ранее знаний.
- [ ] Сборник решений покрывает 100% заданий; решения подробные, пошаговые, код проверен запуском.
- [ ] Финальная ревизия связности и корректности проведена (этапы 4 и 6).
