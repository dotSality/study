# custom-three-based-engine-book — программа написания самоучителя

> **Операционный документ для код-агента (фреймворк SuperClaude).**
> Определяет, как создавать самоучитель «Свой 3D-движок для веб-игр на three.js».
> Нормативная база: `books/self-education-book-create-plan.md` (далее — «План»). При противоречии этого файла и Плана приоритет у Плана.
>
> **Статус: этапы 0, 1 и 2 пройдены** (0 — 2026-07-27, 1 и 2 — 2026-07-28), программа утверждена владельцем 2026-07-28, **идёт этап 3 — написание глав**. Все решения раздела 6 приняты. Технический эксперимент выполнен — схема верификации раздела 4 подтверждена измерением, см. `spike-report.md`. Реестр источников составлен — `sources.md` (37 источников), все семь закрытых изданий получены локально. Программа курса — `outline.md`: 24 главы, 86 частей, 5 контрольных точек; там же закрыты инструментарий, формат журнала проектных решений и перечень форматов ассетов. **Закрыты главы 1–4** (последняя — 2026-08-01); следующая клетка — глава 5 `05-checkpoint-core.md`, первая контрольная точка (трекер, раздел 8.1).

---

## 1. Контекст и артефакты

| Артефакт | Путь | Состояние |
|---|---|---|
| План (методология) | `books/self-education-book-create-plan.md` | утверждён |
| Этот документ (требования + трекер) | `books/three-based-custom-engine/custom-three-based-engine-book.md` | 🔄 черновик от 2026-07-27 |
| Технический эксперимент (этап 0) | `books/three-based-custom-engine/spike-report.md` | [x] готов 2026-07-27 |
| Реестр источников (этап 1) | `books/three-based-custom-engine/sources.md` | [x] готов 2026-07-28 |
| Программа курса (этап 2) | `books/three-based-custom-engine/outline.md` | [x] составлена 2026-07-28, ждёт утверждения владельцем |
| Главы (этап 3) | `books/three-based-custom-engine/chapters/NN-<slug>.md` | [~] 4 из 24 (гл. 4 — 2026-08-01) |
| Сборник решений (этап 5) | `books/three-based-custom-engine/solutions/NN-<slug>.md` | [ ] |
| Эталонные снимки движка | `books/three-based-custom-engine/engine/checkpoint-NN/` | [ ] коммитятся (решение 6.3) |
| Стенд проверок и эталонные скриншоты | `books/three-based-custom-engine/harness/` | [ ] коммитится (решение 6.3) |

Язык материалов — **русский**; устоявшиеся английские термины в скобках при первом употреблении (План, §7). Код — **TypeScript** (решение 6.1), комментарии в коде русские, печатаемые строки латиницей (конвенция, перенесённая из книги C→Rust).

Книга **самостоятельная**: свой этап 1, свой реестр источников, своя программа. Готовый самоучитель по линейной алгебре (`books/linear-algebra/`) — не источник и не зависимость: книга не ссылается на него и вводит нужную ей прикладную 3D-математику заново из собственных источников. Пересечение тем допустимо и ожидаемо.

## 2. Замысел

**Что создаётся.** Самоучитель, ведущий читателя от простого к продвинутому через **создание собственного движка** для веб-игр поверх three.js. Движок растёт вместе с книгой: от минимального ядра до продвинутого — со своими оптимизациями, своими фичами и спроектированным публичным API.

**Роль three.js.** Рендер-бэкенд движка, а не предмет книги. Теория рендеринга даётся в объёме, достаточном, чтобы понимать, что происходит под капотом, и осмысленно оптимизировать; всё остальное — архитектура движка и игровые системы.

**Чем книга не является.** Не курс JavaScript/TypeScript. Не курс линейной алгебры. Не переизобретение рендерера на сыром WebGL (обзорные фрагменты допустимы, полноценная линия — нет).

**Обязательные возможности движка.** Требование владельца от 2026-07-27: движок должен уметь работать со всеми видами ассетов — модели, текстуры, материалы, анимации, звук, шрифты, данные. Чтобы требование было выполнимым и проверяемым (п. 4.4 требует реально прогнать каждый заявленный формат), оно раскрывается так:

- **Основной путь — полностью поддержан, проверен, разобран в тексте:** glTF 2.0 / GLB как формат моделей и сцен (PBR-материалы, скелетная анимация, морф-цели), сжатие геометрии, GPU-сжатые текстуры, HDR/EXR для окружения и освещения по изображению, обычные растровые форматы, звук через WebAudio, атласы и шрифты, JSON и бинарные данные. Точный перечень фиксируется на этапе 2 — после проверки фактической поддержки в зафиксированной версии three.js, а не по памяти агента.
- **Расширяемость важнее длины списка.** Ядро — реестр загрузчиков с единым контрактом: движок умеет не «эти N форматов», а «любой формат, для которого написан загрузчик». Добавление своего загрузчика — отдельный урок книги; один-два дополнительных формата читатель подключает в заданиях (простой учебный и legacy-формат «который приходится поддерживать»).
- **Ассеты — это конвейер, а не загрузчик.** Импорт → преобразование → упаковка → кэш; граф зависимостей между ассетами, асинхронная загрузка с приоритетами, подсчёт ссылок и выгрузка, бюджет памяти GPU, горячая перезагрузка в разработке, стриминг. Это одна из крупнейших подсистем движка и один из главных ответов на вопрос, чем движок отличается от «three.js плюс скрипт».

**Следствие для программы:** одна контрольная точка целиком отводится ресурсам и ассетам; бюджеты памяти и стриминг возвращаются в контрольной точке оптимизации. Оценку объёма (ниже) считать нижней границей.

**Оценка объёма** (по калибровке на двух готовых книгах репозитория, 2026-07-27): ~18–22 главы, ~450–600 заданий, ~45–60 заходов агента. Класс трудозатрат — как книга C→Rust (585 заданий, 46 заходов за 22 дня и не закончена), а не как линейная алгебра (348 заданий, 15 заходов). Оценка уточняется на этапе 2.

## 3. Форма книги

Иерархия Плана сохраняется без изменений: **глава → часть → параграфы + 6–15 заданий**, идентификаторы `ГN.ЧM.Зk`, распределение сложности 30/40/30.

**Главы-модернизации.** Примерно каждая 4–5-я глава — прикладная **контрольная точка**: читатель развивает свой движок в соответствии с заданиями главы. Это обычная глава по структуре Плана (части, параграфы, задания), просто все её задания прикладные. Никакого нового структурного уровня не вводится.

Зачем именно так (обоснование, чтобы не пересматривать решение в середине работы):

- Сквозной движок с главы 1 даёт цепочку из ~20 звеньев: читатель, застрявший в главе 7, теряет всю книгу. Контрольные точки сокращают цепочку до 5–6 звеньев, каждое — документированное состояние, с которого можно войти заново.
- Эталонных состояний движка нужно 5–6, а не по одному на главу — вчетверо меньше кода, который надо вести, собирать и проверять при каждой правке книги.
- Механические задания (первые ~30% каждой части) остаются независимыми от движка: посчитать матрицу, разобрать формат, найти ошибку в шейдере. Зависимость от предыдущего состояния движка возникает только в прикладных заданиях.

**Лестница контрольных точек** (иллюстрация замысла; фактический состав определяется на этапе 2): минимальное ядро (цикл кадра, ресурсы, сцена) → рендер-возможности (материалы, свет, тени, постобработка) → игровые системы (ввод, физика, звук, анимация) → оптимизация (батчинг, инстансинг, LOD, отсечение, воркеры, стриминг) → продакшн (сборка, профилирование, инструменты).

## 4. Схема верификации (три слоя)

Ключевое требование Плана — п. 4.4 «никаких догадок», распространяющийся и на решения: каждое утверждение о поведении кода подтверждено запуском. Для книги, половина утверждений которой — про картинку и производительность, это требует трёх разных механизмов.

### 4.1. Слой 1 — метрики

**Детерминированные счётчики — проверяет агент, точные числа входят в книгу.** Draw calls; число шейдерных программ; переключения материалов и состояний; треугольники и вершины в кадре; число геометрий и текстур в памяти GPU; число проходов рендера; обходов графа сцены за кадр; аллокации в горячем цикле (целевое значение — ноль); размеры буферов. Эти величины воспроизводятся один в один на любой машине, поэтому формулировки вида «после инстансинга draw calls: 5000 → 3» — факт, пригодный и для текста книги, и для автопроверки.

**Относительные показатели — меряет читатель у себя.** Не абсолютные значения, а **отношение до/после в одном прогоне на одной машине**: «кадровое время падает не менее чем втрое на сцене из 5000 объектов»; «пик потребления памяти не растёт при удвоении числа объектов». Бюджет кадра задаётся в долях, не в миллисекундах.

**Жёсткое правило.** Абсолютные тайминги (FPS, длительность кадра/rAF в миллисекундах, абсолютный объём памяти) **никогда** не входят в критерий приёмки задания. Они зависят от GPU, драйвера, версии браузера, фоновой нагрузки и троттлинга, а в headless-контуре рендер идёт программным растеризатором, где абсолютные цифры не значат ничего. Такие числа допустимы в тексте только как иллюстрация с пометкой «на эталонной машине» (приём перенесён из книги C→Rust).

### 4.2. Слой 2 — рантайм-проверка водителем

Агент проверяет работающий рендер по **заранее написанному исполняемому сценарию**: поднять страницу контрольной точки, прокрутить N кадров, снять счётчики (`renderer.info` и собственные счётчики движка), сделать скриншоты, сверить с эталоном.

**Условие:** сценарий — это файл в `harness/` (имя — `harness/checkpoint-NN.test.mjs`; расширение `.test.mjs` обязательно: `node --test` файлы с `.spec.` не подхватывает — проверено 2026-07-28, см. `outline.md`, 3.1), а не проза для агента. Инструкция в виде текста («проверь, что тени выглядят правильно») означает импровизацию агента, невоспроизводимость через полгода и, формально, догадку. Прецедент в репозитории — водитель `drv.ps1` для GUI-глав 12 и 26 книги C→Rust: скриптованный драйвер, который шлёт программе сообщения и читает результат.

**Что в этом слое проверяемо:** сверка с эталонным скриншотом с допуском; цвет пикселя в заданной точке; габариты силуэта объекта; кадр не пустой; отсутствие NaN в матрицах и позициях; число проходов и счётчики после N кадров; отсутствие ошибок консоли и потерь контекста.

**Эталонные скриншоты снимаются тем же контуром, которым потом сверяются.** Программный растеризатор и реальный GPU расходятся на сглаживании и фильтрации, поэтому эталон с «настоящей» видеокарты непригоден как база сравнения для headless-прогона.

### 4.3. Слой 3 — тесты и архитектурные проверки

**Автотесты на важные части движка** (Node, без GPU): математика, граф сцены и мировые матрицы, ECS/менеджер сущностей, менеджер ресурсов, пул объектов, отсечение, сериализация, игровой цикл с фиксированным шагом, загрузчики и разбор ассетов из файла. Прямой потомок `verify_*.js` из книги по линейной алгебре.

Книга **отдаёт этот набор тестов читателю**: «мой движок правильный» = «тесты проходят».

**Адаптер соответствия — обязателен.** Тесты фиксируют вызовы, а значит фиксируют публичный API; это противоречит обещанию «свой движок со своим API». Развязка: тесты обращаются не к движку напрямую, а к тонкому **адаптеру соответствия** (10–20 строк), который читатель пишет сам, отображая своё API в ожидания стенда. Свобода проектирования сохраняется, проверка остаётся механической. Сам адаптер — отдельный сюжет книги: урок про границы API.

**Архитектурные проверки — только механизируемые, бинарные:**

- границы импортов (модуль рендера не тянет геймплей) и отсутствие циклов в зависимостях;
- публичная поверхность — только через точку входа, остальное внутреннее;
- отсутствие глобального состояния и побочных эффектов при импорте модуля;
- ноль аллокаций в горячем цикле — проверяется тестом, а не осмотром;
- контрактный тест с подменённым рендером: если игровая логика работает с заглушкой вместо рендерера, развязка **доказана**, а не заявлена.

**Граница.** Оценка «соблюдён ли SOLID», «хорош ли дизайн» — суждение, а не факт; в критерий приёмки задания оно не входит (иначе нарушается п. 4.4 Плана). Всё, что тоньше перечисленных механических проверок, живёт в тексте главы как разбор проектного решения — см. раздел 5, п. 6.

### 4.4. Что агент не проверяет (ручная приёмка владельца)

Ведётся отдельным списком, как открытый пункт по GUI-главам 12 и 26 книги C→Rust: эстетика кадра, плавность, «ощущение» управления, артефакты, заметные только в движении, поведение на реальном GPU и на слабом железе, удобство итогового API с точки зрения пользователя движка.

## 5. Правила книги (действуют в каждом заходе)

1. **Никаких догадок** (План, п. 4.4). Каждое фактическое утверждение подтверждено источниками из `sources.md` либо прогоном. Не удалось подтвердить — не пишем, фиксируем в «Открытых вопросах» `outline.md`.
2. **Минимум два независимых источника на часть** (План, п. 4.3). Особенность дисциплины: у API three.js независимого второго источника не существует физически — библиотека одна. Поэтому связка для API-утверждений: **теория** (учебник по рендерингу/архитектуре движков) + **первоисточник** (официальная документация, manual, исходный код three.js), где источники подтверждают разные слои утверждения, плюс прогон. Это методическое решение фиксируется в `sources.md` в момент его создания, иначе на ревизии получится системное нарушение.
3. **Версия three.js зафиксирована** и объявлена в журнале окружения (раздел 10). three.js выпускает релизы часто и ломает API между ними; без фиксации листинги перестают собираться. Смена версии по ходу работы — отдельное решение владельца с ревизией затронутых глав.
4. **Весь код проверяется запуском** по трёхслойной схеме раздела 4. Несобирающийся фрагмент в текст не попадает, кроме специально помеченных примеров ошибок.
5. **Только пройденный материал**: глава N ссылается лишь на главы 1…N−1. Проверка «забеганий вперёд» — обязательный пункт ревизии.
6. **Вариативность решений (План, п. 6.5) работает иначе, чем в предыдущих книгах.** В сквозном движке альтернативы не могут свободно расходиться — кодовая база одна. Поэтому «вариант 2» превращается в **журнал проектных решений**: «взяли подход A, не B, вот почему, вот при каких условиях B выиграл бы». Формат фиксируется на этапе 2 и далее не пересматривается.
7. **Решений в главах нет** — только условия. Решения — в `solutions/`.
8. **Прямое копирование текста источников запрещено** — пересказ/синтез со ссылкой. Новый источник — сначала в `sources.md`, потом в текст.
9. **Тестовые ассеты — только со свободной лицензией**, с указанием источника, автора и лицензии в `harness/assets/README.md`. Размер минимальный: низкополигональные модели, маленькие текстуры. Крупные сцены для нагрузочных проверок **генерируются скриптом**, а не коммитятся — иначе репозиторий распухнет на бинарниках.

## 6. Решения владельца

**6.1. Язык — TypeScript.** ✅ Принято 2026-07-27. Обоснование: слой 3 на TS заметно сильнее — границы модулей, публичная поверхность API и контракты адаптера соответствия проверяются типами, а не соглашениями; кульминация книги — проектирование собственного API движка, для которого типы являются основным инструментом. Следствия для работы: в книге появляется шаг сборки (инструментарий определяется на этапе 2), снимки движка ведутся на TS, часть архитектурных проверок раздела 4.3 опирается на типы.

**6.2. Графический контур — WebGL 2, без TSL.** ✅ Принято 2026-07-27. Движок работает поверх WebGL-рендерера three.js; шейдеры пишутся на GLSL ES напрямую, узловой слой TSL не используется.

Существо выбора (зафиксировано 2026-07-27, чтобы не переобсуждать):

- **WebGL 2** — привязка к OpenGL ES 3.0, модель «машина состояний», дорогой вызов отрисовки, шейдеры на GLSL ES, вычислительных шейдеров нет. Спецификация заморожена, поддержка повсеместная, экосистема three.js исторически рассчитана на этот рендерер.
- **WebGPU** — стандарт W3C по образцу Vulkan/Metal/D3D12: явные конвейеры и группы привязок, дешёвый вызов отрисовки, запись команд вне главного потока, полноценные вычислительные шейдеры, язык WGSL.
- **TSL** — уровень three.js, а не платформы: шейдерная логика описывается узлами на JavaScript и компилируется библиотекой в GLSL или в WGSL, то есть материалы переносимы между бэкендами.
- **Почему это не косметика:** выбор меняет состав контрольной точки «оптимизация». На WebGL главный урок — сокращение draw calls и переключений состояний (инстансинг, батчинг, сортировка); на WebGPU эти вызовы дёшевы, и урок смещается к организации конвейеров и групп привязок, отсечению и частицам на вычислительных шейдерах, GPU-driven рендерингу.

Следствия принятого решения:

- Контрольная точка «оптимизация» строится вокруг модели WebGL: сокращение вызовов отрисовки и переключений состояний — инстансинг, батчинг, сортировка по материалам, атласы, отсечение на CPU.
- Вычислительные шейдеры недоступны; там, где нужны вычисления на GPU, применяются приёмы с отрисовкой в текстуру — с явной оговоркой, что это обход ограничения WebGL, а не общепринятый способ.
- Шейдеры в книге — GLSL ES, единым языком на всю книгу; узловой слой не вводится.
- Этап 0 упрощается: проверять надо только контур WebGL 2 (раздел 7).
- **Обзорная глава о WebGPU и TSL в конце книги сохраняется** — как обзор с явной пометкой о статусе, без использования в движке (План, раздел 3, прямо разрешает обзорные темы в конце программы). Фактический статус устанавливается поиском в момент написания этой главы, а не сейчас. Если обзорная глава не нужна вовсе — скажите, уберу из программы на этапе 2.

**6.3. В репозиторий коммитится всё.** ✅ Принято 2026-07-27. Эталонные снимки движка с тестами (`engine/checkpoint-NN/`), стенд проверок и эталонные скриншоты (`harness/`) — часть артефактов книги и коммитятся наравне с markdown. Без них относительные метрики не с чем сравнивать, а листинги средних глав нечем проверять после правок. Конвенция репозитория «в репо только markdown» касалась бинарников и сборок; исходный код и эталонные изображения под неё не подпадают. Не коммитятся: сборки, кэши, `node_modules`.

**6.4. Входной уровень.** ✅ Подтверждено 2026-07-27. JavaScript читателю знаком; 3D-математика — на уровне узнавания: книга объясняет её прикладную часть заново из собственных источников, но не начинает с «что такое вектор». Вводных глав по языку в книге нет.

## 7. Порядок работ

| Этап | Результат | Артефакт | Состояние |
|---|---|---|---|
| 0. Технический эксперимент | Проверено, что схема верификации физически работает | `spike-report.md` | [x] 2026-07-27 |
| 1. Исследование источников | Реестр проверенных авторитетных источников | `sources.md` | [x] 2026-07-28 |
| 2. Программа курса | Главы, части, контрольные точки; утверждение владельцем | `outline.md` | [x] составлена 2026-07-28; утверждение — за владельцем |
| 3. Написание глав | Главы по одной, строго по порядку | `chapters/NN-<slug>.md` | [ ] |
| 4. Ревизия | Связность, забегания вперёд, соответствие источникам | правки в главах | [ ] |
| 5. Сборник решений | Решения всех заданий, весь код прогнан | `solutions/NN-<slug>.md` | [ ] |
| 6. Финальная сверка | 100% покрытие ID, итоговый отчёт | отчёт | [ ] |

**Этап 0 (новый, до исследования источников).** Один заход, снимает главный технический риск книги. Проверить фактически, а не предположением:

1. Headless-рендер работает: поднять Playwright + Chrome, отрендерить сцену, снять скриншот и `renderer.info`, получить те же числа при повторном прогоне.
2. Детерминированность: те же счётчики и та же картинка (в пределах допуска) на двух прогонах подряд и после перезапуска.
3. Ассеты в Node-контуре: разобрать модель из файла без GPU и снять с неё ассерты (структура графа, число вершин, материалы, анимационные клипы) — это фундамент проверки всей подсистемы ассетов.
4. Зафиксировать версии: three.js, TypeScript и инструментарий сборки, Playwright, браузер — с записью в журнал (раздел 10).
5. Какие механизмы измерения памяти доступны и стабильны в этом контуре.

Результат — короткий отчёт `spike-report.md` с командами, версиями и полученными числами. Если пункт 1 или 2 не выполняется, схема раздела 4 пересматривается **до** написания программы курса.

## 8. Трекер прогресса

Статусы: `[ ]` не начато · `[~]` в работе · `[x]` готово.

- [x] Решение 6.1 (язык — TypeScript) — 2026-07-27
- [x] Решение 6.2 (WebGL 2, без TSL) — 2026-07-27
- [x] Решение 6.3 (коммитим код, стенд и эталонные скриншоты) — 2026-07-27
- [x] Решение 6.4 (входной уровень) — 2026-07-27
- [x] Этап 0. Технический эксперимент (`spike-report.md`) — 2026-07-27
- [x] Этап 1. Реестр источников (`sources.md`) — 2026-07-28
- [x] Этап 2. Программа курса (`outline.md`) — 2026-07-28: 24 главы, 86 частей, 5 контрольных точек; закрыты инструментарий (3.1), формат журнала проектных решений (3.3), перечень форматов ассетов (3.4)
- [x] **Утверждение программы владельцем — 2026-07-28**, все пять вопросов раздела 9 `outline.md` закрыты
- [~] Этап 3. Написание глав — таблица ниже; закрыты главы 1 (2026-07-28), 2 (2026-07-30), 3 (2026-07-31), 4 (2026-08-01) и 5 ★ (2026-08-01, вместе со снимком `engine/checkpoint-01/` и первым эталонным кадром `harness/reference/`), следующая клетка — глава 6 `06-asset-pipeline.md`

### 8.1. Главы (этап 3) и решения (этап 5)

Статусы: `[ ]` не начато · `[~]` в работе · `[x]` готово. Строки с ★ — главы-контрольные точки: закрываются только вместе с прошедшим стендом и снимком движка `engine/checkpoint-NN/`.

| № | Файл главы | Частей | Глава | Решения |
|---|---|---|---|---|
| 1 | `01-engine-scope.md` | 3 | [x] 2026-07-28 | [ ] |
| 2 | `02-frame-loop.md` | 4 | [x] 2026-07-30 | [ ] |
| 3 | `03-scene-graph.md` | 4 | [x] 2026-07-31 | [ ] |
| 4 | `04-world-model.md` | 4 | [x] 2026-08-01 | [ ] |
| 5 ★ | `05-checkpoint-core.md` | 3 | [x] 2026-08-01 | [ ] |
| 6 | `06-asset-pipeline.md` | 4 | [ ] | [ ] |
| 7 | `07-gltf-models.md` | 4 | [ ] | [ ] |
| 8 | `08-textures.md` | 4 | [ ] | [ ] |
| 9 ★ | `09-checkpoint-assets.md` | 3 | [ ] | [ ] |
| 10 | `10-render-pipeline.md` | 4 | [ ] | [ ] |
| 11 | `11-lighting-shadows.md` | 4 | [ ] | [ ] |
| 12 | `12-camera-postfx.md` | 4 | [ ] | [ ] |
| 13 ★ | `13-checkpoint-render.md` | 3 | [ ] | [ ] |
| 14 | `14-input.md` | 3 | [ ] | [ ] |
| 15 | `15-animation.md` | 4 | [ ] | [ ] |
| 16 | `16-physics.md` | 4 | [ ] | [ ] |
| 17 | `17-audio.md` | 3 | [ ] | [ ] |
| 18 ★ | `18-checkpoint-gameplay.md` | 3 | [ ] | [ ] |
| 19 | `19-profiling.md` | 4 | [ ] | [ ] |
| 20 | `20-draw-calls.md` | 4 | [ ] | [ ] |
| 21 | `21-visibility-lod.md` | 4 | [ ] | [ ] |
| 22 ★ | `22-checkpoint-optimization.md` | 3 | [ ] | [ ] |
| 23 | `23-engine-api.md` | 4 | [ ] | [ ] |
| 24 | `24-whats-next.md` | 2 | [ ] | [ ] |

Особые пометки: глава 14 Ч1 не пишется, пока в реестр не внесены спецификации платформы по вводу (правило 9); глава 16 планируется как двухзаходная (решение владельца о полных твёрдых телах); глава 24 — обзорная, источники по WebGPU и TSL устанавливаются в момент её написания (решение 6.2).

## 9. Фиксация прогресса

- **Один заход — один коммит** (указание владельца 2026-08-01): `book(engine): <этап или ch NN <slug>> — <spike|sources|outline|chapter|revision|solutions|check>`; тело — 1–3 строки сути, без поблочного перечисления (конвенция репозитория, договорённость 2026-07-05). В этот же коммит входят глава, трекер и журнал, рабочий проект `code/` и обновлённая Serena-память. Отдельных коммитов «очистка code», «память», «мелкая правка» быть не должно.
- **Каталог `code/` очищается в конце захода, и его содержимое в репозиторий не попадает** (решение владельца 2026-08-01, отменяет порядок «очистка в начале следующего»). Код рабочего места — леса для стройки текста; хранится только тот код, где это имеет смысл, то есть снимки контрольных точек `engine/checkpoint-NN/`. Правило закреплено механически: `code/*` закрыт `.gitignore`, отслеживается только `code/README.md`. Следующий заход разворачивает проект из последнего снимка и набирает поверх него листинги глав, написанных после снимка (листинги сверены дословно). Коммиты `163d732`, `fc623d7`, `290dc18`, `7c32c3f` с кодом глав 1–4 остаются в истории как след прежнего порядка.
- Источник истины — этот документ (трекер, раздел 8) и git-история. Serena-память — только указатель для быстрого входа в заход.
- Коммитится исходный код снимков движка, стенд проверок и эталонные скриншоты (решение 6.3). Не коммитятся: сборки, кэши, `node_modules`.

## 10. Журнал окружения и договорённостей

**2026-07-27 — замысел зафиксирован.** Обсуждение с владельцем: книга самостоятельная (свои источники, своя программа), движок — сквозной растущий артефакт с контрольными точками каждые ~4 главы, проверка трёхслойная (раздел 4). Отдельного документа о вариативности решений не заводим — действует п. 6 раздела 5.

**2026-07-27 — решения владельца 6.1–6.4, все закрыты.** Язык — TypeScript. Графический контур — WebGL 2, TSL не используется; обзорная глава о WebGPU/TSL в конце книги сохраняется. В репозиторий коммитится всё, включая снимки движка, стенд и эталонные скриншоты. Входной уровень: JS знаком, 3D-математика на уровне узнавания.

**2026-07-27 — требование по ассетам.** Движок обязан работать со всеми видами ассетов (модели, текстуры, анимации, звук, шрифты, данные). Раскрытие требования — раздел 2: основной путь на glTF/GLB плюс расширяемый реестр загрузчиков; подсистема ассетов трактуется как конвейер, а не набор загрузчиков, и получает отдельную контрольную точку. Тестовые ассеты — только свободные лицензии, минимальный размер (правило 9 раздела 5).

**2026-07-27 — окружение машины (проверено командами).** Node.js v24.15.0, npm 11.12.1. Google Chrome установлен: `C:\Program Files\Google\Chrome\Application\chrome.exe`. Playwright **не установлен** — при вызове `npx playwright` npm пытался загрузить 1.62.0, то есть сеть доступна и установка возможна. Каталог `node_modules` в корне репозитория пуст. Прочее окружение (Git Bash, WSL1 Ubuntu-24.04, MinGW GCC) описано в журнале книги C→Rust и здесь не дублируется.

**2026-07-27 — калибровка объёма по готовым книгам репозитория.** Линейная алгебра: 348 заданий, ~8 800 строк (главы + решения), 15 коммитов. C→Rust: 585 заданий, ~48 300 строк, 46 коммитов за 22 дня, этап 5 закрыт наполовину. Отсюда оценка раздела 2.

**2026-07-27 — этап 0 пройден, схема раздела 4 подтверждена.** Полный отчёт — `spike-report.md`. Существо: headless-рендер и детерминированность работают, пересматривать раздел 4 не требуется. Кадр 640×360 воспроизводится **побайтово** и между прогонами, и после перезапуска браузера. Счётчики (`renderer.info` и собственные) совпадают точно. Разбор ассетов в Node без GPU работает.

**2026-07-27 — версии зафиксированы (правило 3 раздела 5).** three **0.185.1**, TypeScript **7.0.2**, esbuild 0.28.1, Playwright **1.62.0**, Node.js 24.15.0, npm 11.12.1, системный Google Chrome **150.0.7871.182** (запускается через `channel: 'chrome'`, свой chromium Playwright не скачивается); pngjs 7.0.0 и pixelmatch 7.2.0 — сверка кадров. Смена любой из этих версий — отдельное решение владельца с ревизией затронутых глав.

**2026-07-27 — эталонный контур рендера включает флаги запуска.** Измерено: без `--use-angle=swiftshader --enable-unsafe-swiftshader` headless-Chrome молча уходит на настоящую видеокарту (в прогоне — `ANGLE (NVIDIA, NVIDIA GeForce RTX 4070 …, D3D11)` вместо SwiftShader), и кадр расходится с эталонным на 104 пикселях при максимальном отклонении канала 189. Счётчики при этом идентичны. Следствие: флаги — часть эталона, а не деталь запуска; слой 1 не зависит от видеокарты, слой 2 зависит. Ограничения программного контура: `MAX_TEXTURE_SIZE = 8192`, `MAX_SAMPLES = 4` — задания не должны требовать большего.

**2026-07-27 — требования к сценариям `harness/`, вытекающие из этапа 0.** Кадр — функция номера кадра (ни `performance.now`, ни `Date`, ни `Math.random`). Кадры сверяются **по пикселям, а не по хешу файла**: `canvas.toDataURL()` и композитный скриншот Playwright дают одинаковые пиксели, но разные PNG-файлы. Сервер стенда обязан отдавать `Cross-Origin-Opener-Policy: same-origin` и `Cross-Origin-Embedder-Policy: require-corp` (иначе недоступен `performance.measureUserAgentSpecificMemory()`) и обслуживать `/favicon.ico` (иначе критерий «ноль ошибок консоли» срабатывает ложно).

**2026-07-27 — ловушка атрибутов контекста (сюжет для книги).** Контекст создаётся у холста один раз; повторный `getContext` возвращает уже созданный и **молча игнорирует новые атрибуты**. «Безобидный» пробник поддержки WebGL 2 на рабочем холсте отменил у рендерера `preserveDrawingBuffer: true` и `antialias: false` — замер: запрошено `{antialias:false, preserveDrawingBuffer:true}`, получено `{antialias:true, preserveDrawingBuffer:false}`. Проявилось двумя способами сразу: снятый кадр полностью прозрачен и включилось сглаживание (уникальных цветов кадра 405 против 140 после исправления). Лечение — опрашивать поддержку на отдельном холсте.

**2026-07-27 — границы Node-контура для ассетов.** GLB разбирается `GLTFLoader.parse()` **без единой заглушки** (проверено 25 ассертами: граф, скиннинг, морф-цели, материалы, клипы). `GLTFExporter` в Node требует полифилл `FileReader.readAsArrayBuffer`. Для glTF с картинкой в `data:`-адресе нужны `self`, `ProgressEvent` и `createImageBitmap`; для GLB со встроенной текстурой дополнительно — собственный реестр `blob:`-адресов с подменой `fetch` (Node такой адрес создаёт, но скачать по нему не умеет) и обрезка выравнивающей добивки после маркера `IEND` (иначе строгий PNG-декодер отвечает `unrecognised content at end of stream`). С этим набором проверяются даже цвета отдельных пикселей текстуры. Следствие: контрольная точка по ресурсам опирается на быстрые Node-тесты, а не на браузер.

**2026-07-27 — измерение памяти: что стабильно.** Точно воспроизводимы только счётчики объектов (`renderer.info.memory.geometries` и `.textures` — разброс 0). Байтовые показатели гуляют: `usedJSHeapSize` ≈ 6,2 %, `totalJSHeapSize` ≈ 2,5 %, `performance.measureUserAgentSpecificMemory()` ≈ 0,1 % (требует кросс-происхожденческой изоляции; самый ровный из байтовых). Время прогона одинаковой работы гуляет на ≈ 32 % (12,9–17,1 с) — прямое подтверждение жёсткого правила раздела 4.1 о запрете абсолютных таймингов в критериях приёмки. Способа измерить память на стороне GPU в этом контуре не найдено: `WEBGL_debug_renderer_info` даёт только строку описания устройства, считать надо своими счётчиками движка.

**2026-07-27 — приём измерения (пригодится в стенде).** Ступенчатые опыты с заглушками надо гонять **каждый в своём процессе**: `FileLoader` в three склеивает одинаковые адреса в общую очередь ожидания, и после сорванной попытки следующая подписывается на колбэк, который уже никогда не вызовется. Первая версия опыта дала три ложных «зависания» подряд и едва не привела к неверному выводу «текстуры в Node не разбираются».

**2026-07-28 — этап 1 пройден, реестр источников создан.** `sources.md`: 32 источника в шести разделах (теория графики T1–T6, архитектура движков E1–E5, первоисточники three.js J1–J5, веб-платформа и графический контур W1–W8, форматы ассетов G1–G6, инструментарий I1–I2). Все ссылки и выходные данные проверены веб-поиском в эту дату.

**2026-07-28 — методическое решение по правилу 5.2 зафиксировано (раздел 1 `sources.md`).** Утверждение об API three.js подтверждается тройной связкой: теория (независима от библиотеки) + первоисточник проекта + прогон на зафиксированной версии; часть, опирающаяся только на документацию three.js и прогон, требование п. 4.3 Плана не закрывает. Установлены две границы независимости, обе — по проверке 2026-07-28: (1) документация, руководство, примеры и исходный код three.js — **один** источник, а не четыре; (2) руководство three.js выросло из threejsfundamentals того же автора (Gregg Tavares), что и webgl2fundamentals, поэтому пара «manual + WebGL2 Fundamentals» независимой не является. Арбитр при расхождении документации и поведения — исходный код плюс прогон; расхождение попадает в текст главы как факт с версией и датой.

**2026-07-28 — обнаружено при проверке изданий.** Game Engine Architecture вышла **4-м изданием** (двухтомник: Vol. I «Foundations and Core Engine Systems», Vol. II «Graphics, Motion, and Sound», CRC Press; ISBN комплекта 978-1-041-16259-9, тома продаются и по отдельности — 978-1-032-44308-9 и 978-1-041-16257-5) — на официальном сайте `gameenginebook.com` объявлено текущим. Существенно для нас: том разделён надвое, а изложение 3D-рендеринга и фотореалистичного освещения переписано под изменения в GPU — под эти темы добавлены две новые главы. Поэтому 4-е издание предпочтительнее 3-го (2018, 978-1-138-03545-4) именно в той половине программы, которая про рендеринг; для ядра, ресурсов и игрового цикла 3-е издание равноценно. Real-Time Rendering на эту дату остаётся в 4-м издании (2018), анонса пятого поиском не найдено. Foundations of Game Engine Development: изданы тома 1 (2016) и 2 (2019), тома 3 и 4 объявлены, но не вышли. Physically Based Rendering 4-е изд. (2023) доступно целиком бесплатно на `pbr-book.org`.

**2026-07-28 — открытый вопрос к владельцу: доступ к закрытым изданиям.** Семь опорных источников (T1, T3, T4, T5, E1, E4, E5) не имеют открытого полного текста, то есть агент физически не может по ним сверяться. В реестре они помечены 📕. Закрыт в тот же день: владелец предоставил **все семь**. Копии лежат в `books/three-based-custom-engine/extra-sources/` (восемь файлов — у Game Engine Architecture два тома; ~230 МБ) и **в репозиторий не коммитятся** — добавлено правило в `.gitignore`. Перечень файлов, объёмы и подтверждённые издания — раздел 8 `sources.md`. Пометок 📕 в реестре не осталось: любая часть программы может опираться на любой источник.

**Важное следствие для физики.** E5 (Millington) оказался **1-м изданием (© 2007)**, где главы об обнаружении столкновений ещё не переработаны — переписаны они именно во 2-м (2010). Пробел закрывает E4 (Ericson, © 2005, 633 с.): ограничивающие объёмы, примитивные тесты, иерархии BV, пространственное разбиение, BSP-деревья, методы на выпуклости. Связка E4 + E5 по этой теме равноценна 2-му изданию Millington.

**Два издания на руках — не последние, и это зафиксировано правилом.** T3 — Fundamentals of Computer Graphics **4-е** изд. (© 2016), а не 5-е (2021); E5 — **1-е** изд. (2007), а не 2-е (2010). Правило 6 раздела 9 `sources.md`: ссылка на раздел книги проставляется по изданию, которое реально на руках. Разделы между изданиями смещаются, и ссылка «по последнему вышедшему» — та же догадка, что и утверждение по памяти.

**2026-07-28 — этап 2 пройден, программа курса составлена.** `outline.md`: 24 главы в шести блоках, 86 частей, 5 контрольных точек (главы 5, 9, 13, 18, 22), оценка ~650 заданий и 54–71 захода. Превышение оценки раздела 2 (18–22 главы) обосновано там же и складывается ровно из трёх добавлений: отдельный блок ресурсов с собственной контрольной точкой, пятая контрольная точка на оптимизацию, обзорная глава 24. Структура опирается на разобранные оглавления опорных изданий (Game Engine Architecture т. I гл. 1–10 и т. II гл. 11–18, Real-Time Rendering гл. 1–24, FGED т. 1 гл. 1–4 и т. 2 гл. 5–10, Ericson гл. 1–13, Millington 1-е изд. гл. 1–15, Fundamentals of Computer Graphics 4-е изд. гл. 1–26) — номера разделов в ссылках программы сверены по локальным копиям, а не по памяти. **Главы не пишутся до утверждения программы владельцем** (пять вопросов в разделе 9 `outline.md`).

**2026-07-28 — инструментарий книги закрыт** (решение, отложенное отчётом этапа 0, §9 п. 6). Подробности — `outline.md`, 3.1; всё проверено запуском в эту дату. Существо: типы — TypeScript 7.0.2 (`tsc --noEmit`, флаг `--erasableSyntaxOnly` в 7.0.2 есть); тесты слоя 3 — встроенный `node:test`; сценарии слоя 2 — Playwright 1.62.0 **внутри того же `node --test`** (проверено: Chrome стартует из-под тест-раннера, контур SwiftShader поднимается, `MAX_TEXTURE_SIZE` 8192 и `MAX_SAMPLES` 4 те же, что в этапе 0); сборка страницы — esbuild 0.28.1 в режиме `context()` + `watch()`; **сервер стенда остаётся собственным** — в `ServeOptions` esbuild 0.28.1 нет произвольных заголовков (`port, host, servedir, keyfile, certfile, fallback, cors, onRequest`), `onRequest` вызывается уже после ответа, а документация esbuild для своих заголовков советует ставить перед ним прокси; стенду же нужны COOP/COEP.

**2026-07-28 — Node стирает типы сам, и это меняет конвенцию кода.** На Node 24.15.0 тест `.test.mjs` импортирует `./vec.ts` напрямую и проходит — **сборка для слоя 3 не нужна**; условие одно: `"type": "module"` в ближайшем `package.json`, иначе `.ts` читается как CommonJS и падает на `export`. Плата: strip-only режим отвергает нестираемый синтаксис — `enum` и параметры-свойства конструктора дают `SyntaxError [ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX]` (проверено обоими). Поэтому код движка пишется только в стираемом синтаксисе, а `tsc --erasableSyntaxOnly` превращает это в механическую проверку — первая из архитектурных проверок раздела 4.3.

**2026-07-28 — `node --test` не видит файлы `.spec.`** Проверено на трёх файлах (`a.spec.mjs`, `b.test.mjs`, `checkpoint-01.spec.js`): запустился только `b.test.mjs`. Следствие: все тесты книги — `*.test.mjs`, включая сценарии слоя 2. Пример имени `harness/checkpoint-NN.spec.js` в разделе 4.2 исправлен.

**2026-07-28 — системный Chrome обновился сам: 150.0.7871.182 → 150.0.7871.187.** За сутки после этапа 0, без участия владельца; зафиксировать браузер как зависимость нельзя. Счётчиков слоя 1 это не касается (они не зависят даже от видеокарты — измерено в этапе 0), но эталонные кадры слоя 2 привязаны к патч-версии. Принятая конвенция (`outline.md`, 3.1): сценарий слоя 2 записывает версию браузера рядом с кадром; расхождение кадров **при той же версии** — ошибка движка, расхождение **вместе с версией** — переснятие эталона с записью в этот журнал.

**2026-07-28 — перечень форматов ассетов зафиксирован по three 0.185.1** (`outline.md`, 3.4). Снят с установленного пакета и с исходного кода `GLTFLoader.js` (арбитр J3), а не по памяти. Основной путь — glTF 2.0/GLB с расширениями `KHR_draco_mesh_compression`, `EXT_meshopt_compression`, `KHR_mesh_quantization`, `KHR_texture_basisu`, `KHR_texture_transform`, `KHR_lights_punctual` и одиннадцатью `KHR_materials_*`; текстуры — PNG/JPEG, KTX2/Basis, HDR (RGBE) и EXR; звук — `AudioLoader`; шрифты — `TTFLoader`/`FontLoader` плюс SDF/MSDF; данные — JSON и бинарные буферы. В программу намеренно **не** входят FBX, Collada, USD/USDZ, VRML, LDraw и вокселы: новых понятий сверх glTF и OBJ они не дают. OBJ+MTL остаётся как учебный legacy-формат, свой формат читатель пишет сам (требование расширяемости из раздела 2).

**2026-07-28 — реестр пополнен четырьмя источниками по правилу 9** (до использования, все проверены онлайн в эту дату): **W9** Fetch Standard и **W10** Streams Standard — под главу 6 (асинхронная загрузка, отмена, потоковое чтение); **I3** документация esbuild и **I4** документация `node:test` — под инструментарий, работающий с главы 1. В реестре стало 36 источников.

**2026-07-28 — этап 3 начат, глава 1 закрыта.** `chapters/01-engine-scope.md`: 3 части, 11 параграфов, 28 заданий (8/10/10), четыре решения журнала Р1.1–Р1.4. Весь код прогнан в одноразовом каталоге; листинги сверены скриптом на непрерывность фрагментов файлов стенда (17 листингов кода). Числа первого кадра (пустая сцена 640×360, плотность 1): вызовов отрисовки 0, треугольников 0, `render.frame` 1, геометрий 0, текстур 0, программ 0, центральный пиксель `[16, 24, 32, 255]` при фоне `0x101820` — преобразование цвета туда-обратно точное. С одним кубом: 1 / 12 / 1 / 1 и пиксель `[255, 128, 64, 255]`.

**2026-07-28 — арбитраж по исходнику (правило 5.2, три факта для будущих глав).** В `WebGLRenderer` 0.185.1: `setSize` вычисляет буфер как `canvas.width = Math.floor(width * _pixelRatio)`; `this.info.render.frame ++` и следом `if (this.info.autoReset === true) this.info.reset()` стоят **внутри** `render()` до отрисовки — поэтому после вызова `calls`/`triangles` описывают ровно последний кадр, а `frame` копит число вызовов; `WebGLInfo.reset()` обнуляет `calls/triangles/points/lines`, но **не** `frame`. Замер плотности: CSS-размер холста не меняется, буфер растёт как квадрат плотности (640×360 = 230 400 пикселей при 1 против 1280×720 = 921 600 при 2).

**2026-07-28 — COOP/COEP измерены, а не приняты на веру.** Одна и та же страница на своём сервере с заголовками даёт `crossOriginIsolated === true`, на сервере без них — `false`. Это и есть довод решения Р1.2 (сервер стенда собственный), теперь подтверждённый числом, а не только документацией esbuild.

**2026-07-28 — детерминированность подтверждена на контуре главы 1.** Два прогона подряд дали снимок холста 1456 байт с совпадающей SHA-256 (первые 16 знаков `8741646730365f7c`); при плотности 2 — 4259 байт, другой файл, что правильно (изменился размер буфера). Соответствует измерению этапа 0, но снято уже на инструментарии `outline.md` 3.1.

**2026-07-28 — `node --test` с каталогом в аргументе не работает.** `node --test tests` (и `tests/`, и `./tests/`) падает с `Error: Cannot find module '…\tests'`: Node принимает аргумент за файл-модуль. Воспроизведено и в Git Bash, и в PowerShell, то есть это поведение Node 24.15.0, а не оболочки. Документация (I4) обещает **шаблоны**, а не каталоги, — расхождения с ней нет. Рабочие формы: `node --test` (обход по умолчанию), `node --test "tests/*.test.mjs"` (в кавычках), явный файл. Дополнение к записи о `.spec.`: наборы шаблонов по умолчанию включают и `.ts`-файлы, если не передан `--no-strip-types`.

**2026-07-28 — две ловушки сценария слоя 2, обе стоили прогона.** (1) Чтение пикселя `gl.readPixels()` заставляет драйвер написать в консоль `GL Driver Message (OpenGL, Performance, …): GPU stall due to ReadPixels`. Критерий «ноль сообщений в консоли» на этом падает, поэтому в стенде разведены **ошибки** (роняют тест) и **предупреждения** (сверяются со списком ожидаемых); текст предупреждения содержит изменчивый идентификатор контекста, сверять его можно только вхождением подстроки. (2) Если закрывать браузер и сервер после ассертов, первый же упавший ассерт оставляет их работать и `node --test` не завершается вовсе — прогон висел 130 с до принудительной остановки. Закрытие обязано быть в `finally`, а отчёт пишется на диск до проверок. Обе конвенции внесены в главу 1 и действуют для всех будущих сценариев `harness/`.

**2026-07-28 — TypeScript 7.0.2: две мелочи, экономящие время.** Если файлы перечислены в командной строке, компилятор отказывается работать при наличии `tsconfig.json` — ошибка **TS5112**, лечится флагом `--ignoreConfig` (нужно только для разовых проб, проект проверяется целиком). Полная картина по запрету нестираемого синтаксиса: `const enum`, параметр-свойство и `namespace` с содержимым дают **TS1294** у компилятора и внятное `SyntaxError [ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX]: TypeScript <конструкция> is not supported in strip-only mode` у Node; декоратор компилятор пропускает молча, а Node сообщает безликое `SyntaxError: Invalid or unexpected token`. Отсюда и правило двух проверок.

**2026-07-28 — отступление от конвенции печатаемых строк, нужно решение владельца.** Раздел 1 требует «печатаемые строки латиницей» (приём из книги C→Rust). В главе 1 названия тестов написаны по-русски (`test('плотность выше потолка обрезается', …)`), потому что они читаются как описание проверки, а контур выводит их корректно — проверено прогоном `node --test` в этой сессии. Исходная причина конвенции (кодировка консоли Windows у программ на C) к Node и Chrome не относится. Если владелец предпочитает букву конвенции, названия тестов переводятся на латиницу правкой одной главы; чем позже, тем дороже.

**2026-07-28 — открытый вопрос: где живёт код движка между контрольными точками.** Конвенция `outline.md` 3.2 предусматривает каталоги `engine/checkpoint-NN/` (снимки на главах 5, 9, 13, 18, 22) и `harness/`, но не предусматривает места для исходников движка в главах между ними. Глава 1 фактически заводит рабочий проект (`package.json`, `tsconfig.json`, `src/`, `tests/`, `web/`, `harness/`), и решение 6.3 велит код коммитить. В этом заходе закоммичена **только глава**: листинги в тексте полны и сверены скриптом, а заводить новый каталог верхнего уровня без решения владельца я не стал. Варианты: (а) завести `src/` и `tests/` в корне книги как «живой» движок, снимки контрольных точек оставить замороженными копиями; (б) коммитить код только снимками, между точками он живёт у читателя и восстанавливается из листингов. Нужен выбор.

**2026-07-28 — решение владельца по главе 16: полные твёрдые тела.** Из двух предложенных вариантов (вопрос 2 раздела 9 `outline.md`) выбран больший: интеграторы → формы и пересечения → широкая фаза → контакты с трением и покоящиеся контакты, по E5 гл. 3–7 и 13–15 плюс E4 гл. 4–7 и 11. Вариант «частицы и масс-агрегатные тела, вращающиеся тела обзорно» отвергнут. Следствие: глава 16 — самая тяжёлая в книге, планируется как двухзаходная; оценка раздела 10 `outline.md` это учитывает. Остальные четыре вопроса раздела 9 (объём программы, инструментарий, обзорная глава 24, формат журнала решений) на 2026-07-28 ещё не отвечены — главы не пишутся.

**2026-07-28 — программа утверждена владельцем целиком, этап 2 закрыт.** Приняты все пять вопросов раздела 9 `outline.md`: объём программы (24 главы, 6 блоков, 5 контрольных точек), объём физики главы 16 (полные твёрдые тела), инструментарий, обзорная глава 24 остаётся, формат журнала проектных решений (пересмотру не подлежит). В трекер добавлена таблица по главам (раздел 8.1). Этап 3 открыт, порядок — строго по номерам глав.

**2026-07-28 — инструментарий: альтернативы измерены до решения, а не отвергнуты по умолчанию.** Владелец спросил, что мешает взять tsx или собирать TypeScript через esbuild. Проверено на одном коде (`enum` + параметр-свойство + декоратор): `node` по умолчанию не берёт ни того, ни другого; **`node --experimental-transform-types`** берёт `enum`, `namespace` и параметры-свойства **без единой новой зависимости** (флаг экспериментальный, печатает `ExperimentalWarning`, декораторов не даёт); **tsx 4.23.1** берёт всё, включая декораторы; **сборка esbuild 0.28.1** — тоже всё. Вывод: запрет держался бы не на нехватке инструментов, и довод «меньше зависимостей» неверен — esbuild в контуре и так стоит. Оставлен стираемый синтаксис по двум другим доводам: (1) тесты слоя 3 книга отдаёт читателю, и путь прогона `node --test` без посредников минимизирует расхождения «у автора работает, у читателя нет»; (2) декораторную регистрацию компонентов обычно делают побочным эффектом импорта, а запрет побочных эффектов при импорте уже стоит в архитектурных проверках раздела 4.3. Владелец подтвердил: `enum` и декораторы не нужны.

**2026-07-28 — уточнение по стираемому синтаксису (моя вчерашняя формулировка была неполной).** Проверено `tsc --erasableSyntaxOnly` 7.0.2 на восьми файлах и прогоном на Node 24.15.0. Флаг ловит (ошибка **TS1294**): `enum`, `const enum`, `namespace` с рантайм-содержимым, параметры-свойства конструктора, `import =`. Не ловит **декораторы** — компилятор их пропускает молча, а Node падает уже в рантайме (`SyntaxError: Invalid or unexpected token`: V8 декораторы не поддерживает, стирание типов их не трогает). Следствие: механическая проверка запрета — это флаг компилятора **плюс** прогон тестов, по отдельности ни одна не полна. Стираются полностью и разрешены: интерфейсы, `type`, дженерики, `as const`, `satisfies`, `abstract`, `readonly`, `declare namespace` (последнее проверено запуском). Замена `enum` — объект с `as const` плюс одноимённый тип через `keyof typeof`.

**2026-07-28 — окружение: poppler для чтения PDF.** Установлен `poppler 26.02.0` через scoop (`scoop install poppler`). Без него PDF в этом окружении **не читаются вовсе**: инструмент чтения требует `pdftoppm` для рендера страниц. Попутно появились `pdftotext` и `pdfinfo` — ими дешевле искать нужный раздел книги и подтверждать издание по копирайту, чем листать страницы. Приём применён сразу: издание Millington определено извлечением копирайта и проверкой отсутствия глав-маркеров 2-го издания. Темы, теоретический слой которых опирается только на закрытые издания, на этапе 2 либо перестраиваются на открытые источники (PBR, MIT OCW 6.837, Game Programming Patterns, Data-Oriented Design, спецификации Khronos/W3C/WHATWG), либо выносятся в открытые вопросы программы. Заполнять пробел знаниями агента по памяти запрещено (п. 4.4 Плана).

**2026-07-30 — решение владельца: код движка живёт в репозитории, в `code/`.** Закрыт открытый вопрос от 2026-07-28. Устройство выбрано в обсуждении: **один проект** (`code/package.json`, один `node_modules`, одна сборка), а состояния движка лежат по главам — `code/chapters/NN-<slug>/` с полной копией проекта читателя того момента (`src/`, `tests/`, `harness/`, `web/`). Сверху надстроена оболочка `code/shell/`: собирает все состояния в одну страницу и даёт шапку с кнопками «1 · Движок и его границы», «2 · Цикл кадра и время». Причина отказа от единого растущего каталога — сверяемость: листинг главы 2 должен оставаться сверяемым и после того, как главы 7 и 20 перепишут те же файлы. Конвенция 3.2 `outline.md` обновлена, `.gitignore` пополнен (`code/web/`, `chapters/*/web/bundle.js*`, `chapters/*/harness/out/`).

**2026-07-30 (уточнение того же дня, отменяет постоянное хранение) — код главы удаляется при её закрытии.** Владелец уточнил замысел: `code/` нужен как рабочее место захода — собрать проект, снять числа, сверить листинги, — а не как хранилище состояний движка. Поэтому по завершении главы каталог очищается; в репозитории остаётся только `code/README.md` с описанием приёма и способом достать код закрытой главы из истории (`git show <коммит>:books/three-based-custom-engine/code/…`). Зафиксированные состояния: глава 1 — коммит `163d732`, глава 2 — `fc623d7`. Что **не** отменено: снимки контрольных точек `engine/checkpoint-NN/` и эталонные кадры `harness/reference/` (решение 6.3) — перед контрольной точкой проект пересобирается в `code/` по листингам закрытых глав. Цена решения названа и принята: между главами нет регрессионных прогонов (правка в главе 12 не проверяется тестами главы 2 автоматически), а база для относительных метрик блока оптимизации собирается заново в момент нужды.

**2026-07-30 — граница, отделяющая авторский код от работы читателя (следствие предыдущего решения).** В каталог главы попадает **ровно то, что напечатано в главе**: разобранные примеры, тесты и стенд. Реализации заданий туда не попадают никогда — они пишутся на этапе 5 в `solutions/NN-<slug>.md` и прогоняются в одноразовом каталоге. Правило записано в `code/README.md`, чтобы не размывалось со временем: иначе «живой проект» незаметно превратится в сборник ответов.

**2026-07-30 — решение владельца: имена тестов латиницей.** Закрыт второй открытый вопрос от 2026-07-28: действует буква конвенции раздела 1. Пять тестов главы 1 и её браузерный сценарий переведены (`pixel ratio 1: drawing buffer equals the CSS size` и далее), вывод прогонов в §1.3.3 и §1.3.4 переснят целиком, ссылка на тест в решении Р1.1 обновлена. Все тесты главы 2 написаны сразу по-английски.

**2026-07-30 — дрейф браузера: 150.0.7871.187 → 151.0.7922.71, кадр не изменился.** Проект главы 1 развёрнут заново и прогнан на новом Chrome: все шесть тестов зелёные, счётчики совпали (вызовов 0, треугольников 0, `render.frame` 1, центральный пиксель `[16,24,32,255]`), снимок холста — те же 1456 байт и тот же SHA-256 `8741646730365f7c`, что и 2026-07-28. То есть правило 3.1 `outline.md` о переснятии эталона при смене патч-версии сработало вхолостую — эталон переснимать не потребовалось. Версия в шапке главы 1 обновлена на 151.0.7922.71, потому что вывод прогонов в главе теперь с этого прогона.

**2026-07-30 — эталонный контур не воспроизводит фоновую вкладку.** Спецификация (W4) прямо говорит: документ со состоянием видимости `hidden` выбрасывается из шага обновления отрисовки, то есть обратных вызовов кадра не получает. Проверить это прогоном не удалось ни одним из трёх способов: вторая вкладка на переднем плане (`bringToFront`) оставляет первую `visible` и кадры продолжают идти (68 → 108 за полсекунды); CDP-команды `Emulation.setVisibilityState` в Chrome 151 больше нет (`wasn't found`); `Page.setWebLifecycleState` со значением `frozen` команду принимает, но кадры не останавливает. Решение для книги: факт держится на спецификации, ограничение контура закреплено тестом слоя 2 (если Chrome это изменит — тест упадёт), а фактическая проверка вынесена в задание ручной приёмки Г2.Ч1.З8.

**2026-07-30 — частота кадров в контуре ≈165 в секунду, а не 60.** Два окна по секунде дали 165 и 166 кадров: дисплея в headless нет, синхронизировать не с чем, а спецификация обязывает только «не чаще, чем позволяет оборудование». Число машинно-зависимое и в критерии приёмки не входит (§1.3.1), но в главе приведено — оно наглядно опровергает привычку рассчитывать на 16,7 мс между кадрами.

**2026-07-30 — плавающая точка съедала по шагу симуляции.** Аккумулятор сравнивался с шагом напрямую (`accumulator >= stepMs`), и тесты немедленно упали: 61 кадр ровно по шагу давал 59 шагов вместо 60 (`59 !== 60`), кадр в 50 мс — два шага вместо трёх. Причина: шаг 16,666666666666668 мс, а дельта, полученная вычитанием двух отметок времени, оказывается на ~10⁻¹⁵ мс меньше, и недобор копится. Введён допуск сравнения `EPSILON_MS = 1e-9` плюс отсечение отрицательного остатка. Приём общий: **любое сравнение накопленного времени с шагом требует допуска**, и это стоит помнить в главах о физике и анимации.

**2026-07-30 — измерение аллокаций: `PerformanceObserver` и анализ убегания.** Две ловушки, обе стоили прогона. Первая: записи типа `'gc'` доставляются наблюдателю **асинхронно**, а измеряемый цикл блокирует поток — при наивном счёте получаются нули; лечится сбором записей с отметками времени и разнесением по окнам замеров. Вторая: объект, не покидающий итерацию, V8 почти не создаёт (10 сборок мусора против 188 у точно такого же объекта, попадающего в кольцо живых), поэтому измерять «цену аллокации» на несбегающем объекте бессмысленно. Итог измерения на 5 млн итераций: несбегающий объект — 10 сборок, сбегающий — 188, пул на 64 объекта — 0 сборок и 64 созданных объекта. Реестр пополнен источником **I5** (`node:perf_hooks`) по правилу 9.

**2026-07-30 — Playwright: неявный контекст не принимает вторую страницу.** `browser.newPage()` создаёт контекст сам, и попытка добавить в него ещё одну страницу падает с `Error: Please use browser.newContext()`. Сценариям, которым нужны две вкладки (проверка фона), контекст нужно создавать явно через `browser.newContext()`.

**2026-07-30 — заход по главе 2 завершён.** Глава `chapters/02-frame-loop.md`: 4 части, 13 параграфов, **31 задание** (8/9/7/7), пять решений журнала **Р2.1–Р2.5** (свой цикл поверх rAF; фиксированный шаг 60 Гц с двумя потолками; интерполяция вместо экстраполяции; собственные часы вместо `THREE.Timer`; пул с постоянной ёмкостью и отказом вместо роста). Код состояния — `code/chapters/02-frame-loop/`: шесть модулей движка, 40 тестов слоя 3, 4 сценария слоя 2, измерение аллокаций; полный прогон — 44 теста, все зелёные. Сверочный скрипт захода (`scratchpad/verify_ch02.mjs`, не коммитится) даёт 0 проблем и подтверждает, что все 25 листингов главы — дословные фрагменты реальных файлов.

**2026-07-31 — грязный флаг в three при умолчаниях не экономит ничего.** Главная техническая находка захода по главе 3, проверена прогоном и закреплена тестом. В `Object3D` 0.185.1 метод `updateMatrix()` собирает локальную матрицу и **безусловно** ставит `matrixWorldNeedsUpdate = true`; `updateMatrixWorld()` при `matrixAutoUpdate === true` (умолчание) зовёт `updateMatrix()` на каждом узле, поэтому условие пересчёта мировой матрицы истинно всегда, а `force` уходит вниз по всему поддереву. Обход запускает сам рендерер (`WebGLRenderer.js`, строка 1635). Числа на дереве из 781 узла при движении одного узла (поддерево 31): движок — 1 локальная матрица и 31 мировая; three по умолчанию — 781 и 781; three с `matrixAutoUpdate = false` — снова 1 и 31. Обход во всех трёх случаях полный: флаг экономит умножения, а не проход.

**2026-07-31 — `runVirtual` был неидемпотентен, и это поймал тест детерминизма.** Стенд сбрасывал часы цикла, но не состояние симуляции, поэтому второй прогон продолжал первый и давал зеркальные позиции. Требование §1.3.2 «кадр — функция своего номера» распространяется и на состояние мира, а не только на время. Приём: любая функция прогона виртуальным временем обязана сбрасывать **всё** состояние, от которого зависит кадр.

**2026-07-31 — при alpha = 0 на экране предыдущий шаг, а не текущий.** 61 кадр ровно по шагу дают 60 шагов и alpha ≈ 0, а отрисовка интерполирует между предыдущим и текущим шагом (решение Р2.3), то есть показывает состояние после 59 шагов. Расчётные ожидания в сценариях слоя 2 нужно строить от `steps - 1`; в главе 3 это стоило одного падения теста.

**2026-07-31 — масштаб узла достаётся потомкам, отсюда правило «меш отдельно, точка привязки отдельно».** В стенде главы 3 наивная иерархия «планета → луна» растягивала орбиту луны масштабом планеты. Сцена перестроена на десять узлов с отдельными узлами-пустышками под орбиты и привязки. Приём общий и понадобится в главах о моделях и анимации.

**2026-07-31 — заход по главе 3 завершён.** Глава `chapters/03-scene-graph.md`: 4 части, 15 параграфов, **37 заданий** (9/10/10/8), пять решений журнала **Р3.1–Р3.5** (математика из three, свой слой смысла; ориентация кватернионом и порядок углов `YXZ`; один обход за кадр с распространением грязи вниз; узел не даёт менять себя мимо флага; камера как узел графа с явной системой координат). Код состояния — `code/chapters/03-scene-graph/`: четыре новых модуля движка (`math`, `rotation`, `scene-graph`, `camera`), 47 новых тестов слоя 3, 6 сценариев слоя 2, измерение стоимости обхода; полный прогон каталога — 93 теста, по всей книге — 143, все зелёные. Обход не выделяет память: 10 000 обходов подряд дали 0 сборок мусора. Сверочный скрипт захода (`scratchpad/verify_ch03.mjs`, не коммитится) даёт 0 проблем при 45 листингах, все дословные. Контур не изменился: Chrome 151.0.7922.71, three 0.185.1.

**2026-08-01 — смещение страниц E1-II измерено: +17, как и у тома I.** PDF-страница 544 несёт книжную 527 (начало раздела 16.2). Проверено прямым чтением страниц, а не оглавлением. Оглавление тома II, в отличие от T4, номера страниц даёт верные — извлечение `pdftotext -layout` первых двадцати страниц читается как есть. Полный набор смещений на эту дату: E1-I и E1-II = книга + 17, T5 = + 9, T1 = + 21, T3 = + 15, T4 = + 12 (у T4 оглавление врёт, разделы искать поиском по страницам).

**2026-08-01 — побитовые операции JavaScript тридцатидвухразрядные и знаковые.** Дескриптор сущности собирается как `(generation << 20) | index`, и поколение от 2048 попадает в знаковый разряд: `2048 << 20` даёт отрицательное число. Лечится беззнаковым сдвигом `>>> 0`. Проверено прогоном и закреплено тестом, который утверждает обе половины факта — что сырой сдвиг отрицателен и что дескриптор после исправления положителен. Следствие для будущих глав: любая упаковка полей в одно число в этом контуре требует `>>> 0` на выходе.

**2026-08-01 — круг поколений замыкается и оживляет древний дескриптор; тест поймал ошибку автора.** Первая редакция теста утверждала, что после 4096 смен жильца место свободно и старый дескриптор не считается живым. Прогон опроверг: `isAlive` сверяет только «освоено ли место» и «совпало ли поколение», поэтому дескриптор с нулевым поколением проходит проверку на **пустом** месте, у которого поколение только что обернулось в ноль. Тест переписан под измеренное поведение, граница приёма названа в главе прямым текстом, в реестре заведён счётчик `stats.wrapped`. Приём общий: правило «никаких догадок» работает и против автора главы.

**2026-08-01 — измерение раскладки данных: первый замер мерял геттер, а не данные.** Проход по хранилищу читал `store.size` на каждой итерации, то есть звал геттер; результат — «сплошные массивы вчетверо-впятеро медленнее массива объектов», вывод, противоположный обоим источникам. После выноса длины и массива из цикла картина перевернулась. Приём: в измерительном цикле не должно оставаться ни вызовов, ни свойств-геттеров, иначе меряется их цена.

**2026-08-01 — что раскладка данных даёт в этом контуре на самом деле.** Замеры разведены так, чтобы за раз менялось одно (`harness/layout-cost.mjs`, 100 000 элементов, 200 проходов, четыре прогона). Сплошные массивы чисел против массива объектов — 1,26–1,87 при равном числе элементов. Тот же массив и тот же проход, но с чтением второго поля и проверкой всегда истинного флага — **3,24–4,48**. Проход по 100 000 с проверкой против прохода по 50 000 без неё — 2,78–5,36 (меняются два условия сразу, на слагаемые не раскладывается). Вывод для книги: в JavaScript главный выигрыш плотных хранилищ даёт не близость данных, а исчезновение условия из цикла; совет источников остаётся в силе, но причина у него здесь другая, и числа E2 («в пятьдесят раз») к нашему контуру не переносятся. Детерминированная часть измерения: 10 000 циклов «удалить компонент — вернуть компонент» создают 1024 объекта (все при сборке хранилища) и ноль сборок мусора.

**2026-08-01 — необъяснённое наблюдение, вынесено в текст главы как есть.** В окна двух проходов с ветвлением по полю объекта попадают сборки мусора (12 и 4), хотя циклы ничего не выделяют; в окна остальных пяти замеров — ноль. Воспроизведено в отдельном процессе и после паузы, дающей сборщику доесть мусор постройки данных. Объяснения не найдено. На выводы главы не влияет: все опорные утверждения опираются на отношения времён и на счётчик созданных объектов.

**2026-08-01 — заход по главе 4 завершён.** Глава `chapters/04-world-model.md`: 4 части, 15 параграфов, **35 заданий** (9/8/10/8), пять решений журнала **Р4.1–Р4.5** (сущность — число, а не объект; индекс и поколение в одном дескрипторе; плотный массив с разрежённой картой и удалением обменом с последним; система — проход по хранилищу с явным порядком; очередь событий постоянной ёмкости с границей партии). Код состояния — `code/chapters/04-world-model/`: пять новых модулей движка (`entity`, `component-store`, `vector-store`, `world`, `events`), 56 новых тестов слоя 3, 5 сценариев слоя 2, измерение раскладки данных; полный прогон каталога — 148 тестов, по всей книге — **291**, всё зелёное. Прогон слоя 2 на 61 кадре: 12 заведённых тел, 5 смертей, 7 живых, все три хранилища ходят строем с реестром, событий отправлено 77 (72 урона + 5 смертей), доставлено 75, осталось в очереди 2, отброшено 0, пик очереди 4, обходов графа 61 при 35 узлах, вызовов отрисовки 7. Сверочный скрипт захода (`scratchpad/verify_ch04.mjs`, не коммитится) даёт 0 проблем при 41 листинге, все дословные. Контур не изменился: Chrome 151.0.7922.71, three 0.185.1, Node 24.15.0.

**2026-08-01 — указание владельца: один заход — один коммит.** Заход по главе 4 разошёлся на три коммита (глава с кодом, очистка `code/`, Serena-память), и владелец потребовал впредь делать один. Раздел 9 переписан. Сквошить уже сделанные три не стали: между ними стоят коммиты параллельных сессий (asm-книга и C→Rust), и сведение моих трёх в один потребовало бы переписать и чужие. Следствие правила, замеченное при его записи: очистка `code/` переезжает в **начало следующего** захода — добавить и удалить код одним коммитом нельзя, иначе он не попадает в историю и приём `git show <коммит>:…/code/…` перестаёт работать.

**2026-08-01 — сборка ядра находит то, чего не видно в модулях: пять пропущенных сбросов.** Заход по главе 5 (контрольная точка 1). Четыре пробела нашлись при написании кода и очевидны задним числом: часам не хватало `resetClock`, аккумулятору — `resetFixedStep`, циклу — `reset()` (номер кадра, счётчик шагов, отметка времени), контракту хранилища — `clear()`. Пятый нашёл тест: `engine.reset()` обнулял счётчики графа, но не возвращал узлам грязные флаги, и второй прогон одного сценария пересчитывал 0 матриц вместо 2. Все четырнадцать тестов главы 3 при этом были зелёными — сам граф работал правильно, ошибка была чисто сборочной. Приём общий: **тест «два прогона дают один отчёт» должен сравнивать весь отчёт, включая служебные счётчики**; именно на них ловится утечка состояния между прогонами.

**2026-08-01 — «ноль аллокаций» проверяется тождеством объектов, а не сборщиком мусора.** Гипотеза, что дробное число в поле обычного объекта стоит аллокации (HeapNumber), **проверена и отвергнута**: замер на 2 000 000 итераций (`scratchpad/heapnum.mjs`, не коммитится) показал 0,0–0,1 Б на итерацию для целых, дробных, накопления и `Float64Array` — в оптимизированном цикле разницы нет. Настоящая проверка критерия — детерминированная: множество ссылок на объект `FrameInfo` за 201 кадр × 4 фазы содержит ровно один элемент; события приходят из запаса очереди; мировые матрицы узлов — те же объекты до и после прогона. Измерение `harness/hot-loop.mjs` подтверждает снаружи: 9 800 / 49 800 / 199 800 измеренных кадров дают 1 / 1 / 2 сборки мусора и живую кучу 10 586 / 10 609 / 10 609 КиБ при старте с 10 516 КиБ — двадцатикратный рост числа кадров не меняет ни того, ни другого.

**2026-08-01 — эталонный кадр: допуск не понадобился, открытый пункт 5 программы закрыт.** Первый эталон книги снят: `harness/reference/checkpoint-01-core.png`, 640×360, 2268 байт, паспорт рядом в `.json` (браузер, three, число кадров, шаг, счётчики слоя 1). В пределах одной патч-версии Chrome кадр воспроизводится **побайтово** — 0 различающихся пикселей из 230 400, поэтому допуск на слое 2 остаётся нулевым. Проверка проверки: сдвиг камеры на 0,01 единицы (при расстоянии 11) даёт 40 различающихся пикселей с максимальным расхождением канала 223 — то есть допуск «десяток пикселей» пропустил бы реальное изменение картинки. Механика сверки: сначала сравнение байтов, при расхождении — раскодирование обоих PNG в уже открытой вкладке (`createImageBitmap` + `OffscreenCanvas`) и подсчёт пикселей; сравнивать хеш файла нельзя (§1.3.2). Первый прогон снимает эталон и **падает** с инструкцией — иначе он был бы зелёным, ничего не сверив. Дрейф патч-версии браузера — отдельный ассерт с командой `REFERENCE=refresh` и обязанностью записи в журнал.

**2026-08-01 — `renderer.info.render.frame` не сбрасывается ничем.** Тест детерминизма поймал: во втором прогоне 122 вместо 61. Это счётчик вызовов `render()` за всю жизнь рендерера; в 0.185.1 `info.reset()` обнуляет вызовы, треугольники, точки и линии, но не его (J3 плюс прогон). Движку он не принадлежит, поэтому в сверку с эталоном не входит, а тест утверждает про него ровно то, что есть: `second.info.frame === first.info.frame + FRAMES`.

**2026-08-01 — заход по главе 5 завершён (контрольная точка 1).** Глава `chapters/05-checkpoint-core.md`: 3 части, 11 параграфов, **25 заданий** (9/9/7), пять решений журнала **Р5.1–Р5.5** (точка входа перечисляет поверхность поимённо; конфигурация — данные, проверенные один раз, отказ вместо умолчания; системы ядра расставляет само ядро и не даёт их снять; эталон сверяется по пикселям, версия браузера пишется рядом; рендер не входит в ядро). Код состояния — `code/chapters/05-checkpoint-core/`: три новых модуля (`config`, `engine`, `index`) плюс мост `src/render/view.ts`, четыре доделки в модулях глав 2–4, **30 новых тестов** (14 на сборку, 7 архитектурных, 3 критерия КТ1, 3 на дисциплину кадра, 3 сценария слоя 2), полный прогон каталога — 173 теста, по всей книге — **464**, всё зелёное. Снимок движка `engine/checkpoint-01/` — самостоятельный проект: свои `package.json` и `tsconfig.json`, `npm install && npm test` даёт те же 173 теста, `tsc --noEmit` чист. Отступление от конвенции 3.2 программы, зафиксировано здесь: в снимок вошли не только `src/`, `tests/` и README, но и `harness/`, `web/index.html`, `package.json`, `tsconfig.json` — без них README не мог бы честно назвать команды прогона. Сверочный скрипт захода (`scratchpad/verify_ch05.mjs`, не коммитится) даёт 0 проблем при 31 листинге, все дословные. Контур не изменился: Chrome 151.0.7922.71, three 0.185.1, Node 24.15.0.

**2026-08-01 — указание владельца: код рабочего места не хранится, хранятся только контрольные точки.** Разбор по итогам захода по главе 5. Исходное решение 2026-07-30 требовало двух несовместимых вещей сразу — «каталог очищается при закрытии главы» и «проект остаётся доступен в коммите захода», — что выполнимо только при двух коммитах на заход; после указания «один заход — один коммит» агент сдвинул очистку в начало следующего захода. Владелец отменил этот порядок: **очистка в конце захода, содержимое `code/` в репозиторий не попадает вовсе**. Довод владельца: код рабочего места — леса, и хранить в истории проект четырёх предыдущих глав при каждом заходе незачем; хранится тот код, где это имеет смысл, то есть снимки контрольных точек. Следствия: `code/*` закрыт `.gitignore` (отслеживается только `code/README.md`); точка входа в следующий заход — последний снимок `engine/checkpoint-NN/` плюс листинги глав, написанных после него; коммиты `163d732`, `fc623d7`, `290dc18`, `7c32c3f` с кодом глав 1–4 остаются в истории как след прежнего порядка и как способ достать код глав 1–4 при необходимости.

**Открытый пункт (не закрывается агентом).** Ручная приёмка визуальной части по списку из п. 4.4. Для контрольной точки 1 к нему добавляется эталонный кадр `harness/reference/checkpoint-01-core.png`: агент подтверждает его воспроизводимость, но не то, что на нём изображено именно то, что задумано.
