# engine-book-progress

Указатель для быстрого входа в заход по книге «Свой 3D-движок для веб-игр на three.js» (`books/three-based-custom-engine/`). Источник истины — трекер (раздел 8) и журнал (раздел 10) в `custom-three-based-engine-book.md` + git-история.

Состояние на 2026-07-30 (заход «глава 2» завершён, коммиты `163d732` и `fc623d7`):

- **Этапы 0, 1, 2 пройдены**, программа утверждена владельцем целиком. Отчёт этапа 0 — `spike-report.md`, реестр **37** источников — `sources.md`, программа (24 главы, 86 частей, 5 КТ) — `outline.md`.
- **Идёт этап 3.** Закрыты **глава 1** (3 части, 28 заданий, Р1.1–Р1.4) и **глава 2** `chapters/02-frame-loop.md` (4 части, 13 параграфов, **31 задание** 8/9/7/7, решения **Р2.1–Р2.5**).
- **Следующая клетка: глава 3 `03-scene-graph.md`** (4 части: векторы и матрицы / повороты и кватернионы / иерархия и мировые матрицы / цепочка пространств). Порядок — строго по номерам глав.
- **Оба вопроса владельца от 2026-07-28 закрыты** (см. ниже). Открытых вопросов к владельцу нет.

**Код книги теперь в репозитории — `code/` (решение владельца 2026-07-30):**

- Один проект: `code/package.json` + `code/tsconfig.json` + один `node_modules` (не коммитится). Состояния движка — по главам: `code/chapters/NN-<slug>/` с полной копией проекта читателя (`src/`, `tests/`, `harness/`, `web/`). Оболочка `code/shell/` собирает все состояния в одну страницу с шапкой-переключателем глав.
- Команды из корня `code/`: `npm run typecheck`, `npm test` (гоняет `node --test` в каждом каталоге главы), `npm run build`, `npm run dev` (порт 5173). Одна глава — `cd chapters/02-frame-loop && node --test`.
- **Граница, которую нельзя размывать:** в каталог главы попадает только то, что напечатано в главе. Реализаций заданий там нет — они пишутся на этапе 5 в `solutions/`. Правило записано в `code/README.md`.
- Не коммитятся: `node_modules/`, `code/web/`, `chapters/*/web/bundle.js*`, `chapters/*/harness/out/` (правила в корневом `.gitignore`).
- **Имена тестов — латиницей** (решение владельца 2026-07-30); глава 1 переведена и переснята.

**Приём захода этапа 3** (отработан на главах 1–2):

- Сначала сверка источников (pdftotext по закрытым изданиям, WebFetch по открытым), потом код и прогоны, и только потом текст. Числа в тексте — из того же прогона, что заявлен.
- **Сверочный скрипт захода** (`scratchpad/verify_chNN.mjs`, не коммитится, пишется заново): ID заданий подряд и 6–15 на часть; шесть обязательных элементов в каждом параграфе плюс ≥2 разобранных примера; блоки «Источники части N»; формат решений `Р{глава}.{номер}` со всеми шестью строками; **каждый листинг — дословный непрерывный фрагмент файла из `code/chapters/NN-*/`**; посторонние символы (в белый список нужны Δ, π, знак ударения, ✔/✖/ℹ); баланс ограждений; версия three в главе. В главе 2 дал 0 проблем при 25 листингах.
- Ссылки на разделы закрытых изданий проверять `pdftotext` **до** написания. Смещение страниц GEA т. I: PDF = книга + 17 (проверено ещё раз).

**Технические факты главы 2 (детали — журнал 10):**

- **Контур:** Chrome обновился 150.0.7871.187 → **151.0.7922.71**; все числа главы 1 воспроизвелись, включая SHA-256 снимка `8741646730365f7c` и 1456 байт — эталон переснимать не пришлось.
- **rAF в headless ≈165–166 кадров/с** (два окна по секунде): дисплея нет, синхронизировать не с чем.
- **Фоновую вкладку в контуре получить нельзя:** `bringToFront` оставляет страницу `visible`; `Emulation.setVisibilityState` в Chrome 151 отсутствует; `Page.setWebLifecycleState('frozen')` кадры не останавливает. Факт остановки кадров держится на W4 + ручная приёмка (задание Г2.Ч1.З8), ограничение контура закреплено тестом слоя 2.
- **Плавающая точка:** сравнение аккумулятора с шагом без допуска съедает шаг (61 кадр по 16,666… мс → 59 шагов). Введён `EPSILON_MS = 1e-9`. Помнить в главах о физике и анимации.
- **Измерение аллокаций:** записи `PerformanceObserver` типа `'gc'` приходят асинхронно — считать по окнам с отметками времени; V8 почти не создаёт несбегающий объект (10 сборок против 188 у сбегающего в кольцо живых; пул — 0 сборок, 64 объекта). Источник I5 (`node:perf_hooks`) добавлен в реестр.
- **Playwright:** `browser.newPage()` создаёт неявный контекст, вторую страницу в него добавить нельзя (`Please use browser.newContext()`).
- **three 0.185.1 (J3):** `WebGLAnimation` зовёт функцию кадра и **потом** запрашивает следующий кадр у `self`; `setAnimationLoop` попутно ведёт XR-ветку. `Clock` — только дельта; `Timer` (`src/core/Timer.js`) — масштаб времени и реакция на `visibilitychange`, но ни фиксированного шага, ни фаз.
- Полный прогон книги: глава 1 — 6 тестов, глава 2 — 44 теста (40 слоя 3 за ~148 мс + 4 браузерных сценария), всё зелёное.

**Что закрыли этапы 0–2** (кратко; подробности — `spike-report.md`, `sources.md`, `outline.md`):

- Версии: three 0.185.1, TypeScript 7.0.2, esbuild 0.28.1, Playwright 1.62.0, Node 24.15.0, системный Chrome. Эталонный контур — `--use-angle=swiftshader --enable-unsafe-swiftshader`.
- Правило 5.2: утверждение об API three.js = теория + первоисточник проекта + прогон с указанием версии. J1/J2/J3/J4 — **один** источник; пара J2 + W7 независимой не является; арбитр — исходный код плюс прогон.
- `node --test` не видит `.spec.` → все тесты книги `*.test.mjs`. Код движка — только стираемый синтаксис. Кадр сверять по пикселям, а не по хешу файла. Абсолютные тайминги в критерии приёмки не входят.
- Издания: T3 — 4-е изд., E5 — 1-е изд. Локальные копии в `extra-sources/` (в git не попадают), чтение PDF требует poppler.

Конвенции репозитория — `mem:core`, окружение — `mem:tech_stack`, команды — `mem:suggested_commands`, завершение захода — `mem:task_completion`.
