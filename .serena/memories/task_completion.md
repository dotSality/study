# Task completion (завершение захода книги)

1. Весь код захода прогнан (эталон MinGW GCC `-std=c17 -Wall -Wextra`; главы ≥7 — ещё ASan в WSL; Rust — cargo build && cargo test). Вывод сверён с текстом.
2. Самопроверка по чеклисту §6 `C-and-Rust-learning.md` (структура §3, нет забеганий вперёд, ID/сложность заданий, ≥2 источника на часть, нет маркеров догадок).
3. Обновить трекер §7 (клетка захода) и журнал §10 (новые платформенные факты/договорённости).
4. `/sc:save`: обновить Serena-память `mem:c-rust-book-progress` (последняя закрытая клетка, договорённости, на чём остановились).
5. Commit: `book(c-rust): ch NN <slug> — <chapter|revision|solutions|check>`, тело 1–3 строки.
6. Сборки/бинарники из scratchpad не коммитить — в репо только markdown. Сверочные node-скрипты захода тоже **не коммитим** (каталог `books/c-and-rust-book/tests/` в книге C→Rust пуст и не отслеживается — в отличие от книги по линалгебре; проверено 2026-07-27).
7. **Убрать за собой — обязательный шаг захода, выполняется агентом без напоминания** (правило журнала §10 от 2026-07-05: «сборки и бинарники после захода удаляются»). Удалить каталог стенда в scratchpad (`rm -rf .../scratchpad/chNN`) **и** его копию в WSL (`wsl -d Ubuntu-24.04 -u root bash -c 'rm -rf /root/chNN /root/*.sh'`) — стенд восстановим из листингов решений, они сверены скриптом. **Не удалять** `/root/n3220.pdf` и `/root/n3220.txt` — это рабочая копия стандарта C (см. `mem:tech_stack`). Замечание владельца 2026-07-27: спрашивать разрешения на эту уборку не нужно.
