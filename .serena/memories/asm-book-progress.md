# asm-book-progress

Самоучитель по ассемблеру x86-64 (`books/asm-book/`) — третья книга репо, ведётся по общему Плану. Эта память — только указатель; **источник истины один: `books/asm-book/Asm-learning.md`** (операционный документ, FR-7).

Состояние на **2026-08-19** (заход 12c, виндовая машина, контур L).

- **Этапы 0, 1, 2 закрыты**: требования, `sources.md` (25 источников), `outline.md` — 9 глав / 28 частей, **v1.31**.
- **Заходы 1–11 закрыты на обоих контурах (`[x]`)**; двухконтурны главы 1–7 целиком и часть 8.Ч1. Объёмы: 1632, 1948, 2072, 2170 (МП-1), 3527 (МП-2), 4591 (МП-3), 6554 (МП-4), **6113** (глава 8 — дописана целиком).
- **Часть 8.Ч2 написана тремя заходами (12a/12b/12c), все три на контуре L, все три клетки `[L]`.**
- **12c выполнен 2026-08-19** — чекпоинты 4–6, практика (13 заданий), «Источники части», резюме главы 8: **2108 строк**. Открыт **третий** долг по M на 13 пунктов.
- **Следующая клетка — заход 13, глава 9 «Мост в язык C».** Ограничение «глава 9 только после 8.Ч2» снято; действующих ограничений порядка в столбце «Текст» больше нет.

**Долг §9.4 состоит из трёх таблиц (12a — 12, 12b — 12, 12c — 13; всего 37 пунктов).** Маковская сессия гасит их подряд, **в порядке 12a → 12b → 12c**: 12a спрашивает про числа, 12b — про листинги, куда эти числа вписаны, 12c — про готовую игру, собранную из этих листингов.

**Что дал заход 12c (факты — `Asm-learning.md` §9.2 пп. 205–217).** Две программы: `move.s` (587 строк — правила игры при шаге по клавише) и `snake.s` (779 строк — готовая игра, приведена в книге **целиком**, по образцу `vm.s`). Плюс `sigtest.sh` (9 строк) в тексте. Часть 8.Ч2 вводит четыре новые функции libc сверх 12b: `clock_gettime`, `fwrite`, `atexit`, `signal`. В `sources.md` строка S7 дополнена семью страницами POSIX, включая раздел **2.4 «Signal Concepts»** (список async-signal-safe: `system` и `printf` в нём отсутствуют).

**Проектные решения чекпоинтов 4–6:**

1. **Чекпоинт 4 получил собственную программу `move.s`** (в плане было «в snake.s») — ради воспроизводимости: пока игра ходит только по клавише, каждая партия сверяется дословно. Часы появляются только в чекпоинте 5.
2. **Все правила игры — таблица решений по одному байту клетки назначения** (пусто / еда / стена / тело). Координаты не проверяются нигде: границы стережёт стена внутри массива.
3. **Единственное исключение — клетка хвоста:** войти в неё можно, она освободится на этом же шаге. Исключение потребовало правки `step` из 12b (см. ниже).
4. **Тайминг по дедлайну**, `дедлайн += период` (не «сейчас + период»); `poll` получает **остаток** до дедлайна. Наблюдаемое следствие: число шагов задано геометрией поля, а не машиной.
5. **Еда — собственный ЛКГ, зерно в листинге** (12345). Партия — свойство листинга, а не окружения.
6. **Три уровня уборки:** явный `rawoff` (потому что после него `printf`), `atexit` (много веток выхода), обработчик сигнала, поднимающий флаг (сигнал — не нормальное завершение). Предел назван вслух: `SIGKILL` не перехватывается.

**Найденный дефект (свой, из 12b).** `step` гасил клетку хвоста **после** записи головы — а при входе головы на клетку хвоста это одна и та же клетка, и голова стиралась. Ошибка молчит, код 0, на поле змейка без головы при верном кольце. Исправлено двумя инструкциями и меткой; опыт с их удалением стоит в §8.2.4.

**Измерено на L:** ЛКГ — 8 инструкций / 46 байт, с зерном 12345 даёт 235318264, 569910583, 1901863042 → клетки еды (5,4), (4,10), (3,3); младший бит чередуется 0,1,0,1. `now` — 16 инструкций. Партия без клавиш — 10 шагов до стены, наивная схема при 4 клавишах внутри первого кадра — 12 шагов и другая строка гибели. Три пути гибели: SIGTERM с обработчиком — код 0 и `icanon echo`; без обработчика — 143 и `-icanon -echo`; SIGKILL — 137 и `-icanon -echo`. valgrind: 3/3 (2520 байт) без файла рекордов, 4/4 (6616) с ним — разница ровно 4096, буфер потока `fopen`. Импорты: `move.s` 8, `snake.s` 15. Правок при загрузке — по 3, как у `field`.

**Уроки захода 12c:**

1. **Воспроизводимость интерактивной игры — проектное решение, а не свойство.** Четыре решения ради сверки: зерно в листинге, свой генератор, шаг по дедлайну, подача клавиш **посередине между тиками** (при периоде 150 мс расписание 0,08 / 0,22 / 0,97 / 1,84, запас ±70 мс). Каждая цитируемая партия прогнана по пять раз и совпала по md5.
2. **Сверку надо вести в обратную сторону.** `verify.py` вынимает `snake.s`, `play.py` и `sigtest.sh` **из markdown главы**, собирает в пустом каталоге, порождает три сборки для опытов и прогоняет все 16 цитируемых команд: **41 проверка, 0 расхождений**. Это доказывает то, что нужно читателю, а не то, что удобно автору. Скрипт в гит не идёт — воссоздаётся по §9.5.
3. **Дефект нашёлся при написании опыта, а не при отладке.** Правило «убери одну строку» заставило проверить случай, до которого обычная игра не доходит.
4. **Цитировать только те команды, чей вывод зависит от книги.** К номеру процесса valgrind добавилась метка времени `ls -l` — заменена на `wc -c`.
5. **Два дефекта измерителя дали ложный отказ, а не ложное «совпало»** — впервые за девять заходов. Причина: обе проверки строились на точном равенстве. Нормализацию применять к **обеим** сторонам сравнения.

**Как работать дальше**:

- Заход = глава 9 по протоколу `Asm-learning.md` §5.2; прогон всего кода обязателен (FR-5), при одном доступном контуре — §5.3 (статус `[L]`/`[M]` + долг §9.4).
- **Вопросы, которые может решить владелец, задавать в той же сессии, где они возникли.**
- **Постановки заданий проверять наравне с текстом** — улов восемь погашений подряд.
- **Учётные документы расходятся с книгой быстрее, чем портится книга**, и расходятся обе учётные строки (§1 `Asm-learning.md` и шапка `outline.md`). Сверять взаимно; §1, §7, §9 и §5.1, §4, §8 обновлять в конце захода вместе.
- **Свои учётные правки коммитить в том же заходе** — иначе их снесёт слиянием чужой сессии.
- **Объём:** двенадцатое превышение подряд (часть 8.2 — 3433 строки при норме 550–900 на часть). Вопрос у ревизии Р2.

**Слияние двух сессий одного дня** (инциденты 2026-08-07 и 2026-08-09): обе сессии правят одни и те же учётные разделы. Расходятся всегда: §1 и §7 `Asm-learning.md`, §9.2 (обе стороны нумеруют новые пункты с одного числа — младшую по времени серию перенумеровать и поправить ссылки; серия захода 10 заняла **129–142**, погашения 2026-08-12 — **143–158**, захода 11 — **159–168**, погашения 2026-08-15 — **169–178**, захода 12a — **179–192**, захода 12b — **193–204**, захода 12c — **205–217**), §9.4 (одна закрывает долг, другая открывает) и §8 `outline.md` (обе заводят одинаковый номер версии). Разрешать содержательно.

**Приёмы стендов.** Виндовая машина: WSL-дистрибутив зовётся **`Ubuntu-24.04`** (не `Ubuntu`), пользователь **`max`, не root** — стенд в `~/asmNN`, `/root` недоступен. **Переменные оболочки в `wsl -d … -- bash -lc '…'` из Git Bash приходят пустыми** (цикл `for f in ...; do` там же не работает), а heredoc и обратные кавычки ломаются — писать `.sh`/`.py`/`.s` в scratchpad, копировать **хардкоженым** путём `/mnt/c/...`, затем `sed -i 's/\r$//'`, всю логику с переменными, циклами и регулярками держать в файле скрипта. Экранирование `\x03` через два уровня кавычек не выживает — расписание нажатий передавать из файла-скрипта в одинарных кавычках. Путь `/mnt/c/...` в аргументе требует `MSYS_NO_PATHCONV=1`. `set -e` в проверочных однострочниках не ставить. Сессии `gdb` — в pty питоновским драйвером; интерактивные программы — драйвером `play.py` (расписание отсчитывается **от первого вывода программы**), сигналы им шлёт фоновый `pkill`, а сама программа остаётся на переднем плане. `strace` в WSL1 нет. Мак (контур M): сборка `clang -arch x86_64`, сообщения ассемблера через `as -arch x86_64`; `lldb` в пакетном режиме — `lldb -b -o 'команда' … ./prog`; оболочка zsh не делит слова при подстановке переменной; `readelf`/`timeout`/`pdftotext` отсутствуют (правки при загрузке — `dyld_info -fixups`, сегменты — `otool -lv`), BSD `awk` не знает `strtonum`, BSD `/usr/bin/time` не знает `-f`, BSD `od` форматирует `-An -tx1` иначе GNU, BSD `script` имеет другой синтаксис, `leaks(1)` под Rosetta медленный и часто не завершается.

**Осторожно с коммитами:** владелец параллельно ведёт сессии по другим книгам и на другой машине в одном репозитории. Коммитить только формой `git commit <явные пути> -m …`; Serena-памяти в `.serena/memories/` отслеживаются и идут **тем же** коммитом (`mem:task_completion` пп. 6, 6a, 6f).

Конвенции соседней книги — `mem:conventions`, окружение — `mem:tech_stack`, завершение захода — `mem:task_completion`, соседняя книга — `mem:c-rust-book-progress`.
