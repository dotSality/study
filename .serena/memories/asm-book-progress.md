# asm-book-progress

Самоучитель по ассемблеру x86-64 (`books/asm-book/`) — третья книга репо, ведётся по общему Плану. Эта память — только указатель; **источник истины теперь один: `books/asm-book/Asm-learning.md`** (операционный документ, FR-7, создан 2026-07-31).

Состояние на 2026-07-31 (заход «FR-7»):

- **Этапы 0, 1, 2 закрыты**: `asm-book-requirements.md` (решения владельца Р1–Р6, FR/NFR, исторические протоколы прогонов §7.1–7.2), `sources.md` (23 источника), `outline.md` — 9 глав / 28 частей, **v1.3**.
- **Этап FR-7 закрыт**: `Asm-learning.md` — жёсткие правила захода (§2) и оформления кода книги (§2.1), шаблоны главы (§3) и решений (§4), протокол захода (§5), чеклист ревизии (§6), **трекер по 13 заходам / 28 частям (§7 — где искать первую незакрытую клетку)**, журнал окружения и платформенных фактов (§9), шаблон промпта запуска (§10). Коммит `fcee633`.
- **Следующая клетка: заход 1 — глава 1 «Машина, число, инструменты»** (`chapters/01-machine-and-tools.md`, части Ч1–Ч3). Ни одной главы не написано, каталогов `chapters/` и `solutions/` нет.
- **Приёмку программы владелец делегировал агенту** (2026-07-30: «я ничего не смыслю в ассемблере, исправляй всё, что ошибочно») — отдельного утверждения ждать не нужно, но и правки вносить теперь тоже агенту.

**Что важно знать перед следующим заходом** (детали — в `Asm-learning.md`, не здесь):

- **Контуры на разных машинах**: L — WSL1 на виндовой машине, M — мак. FR-5 требует прогона каждой программы на обоих, поэтому в процесс введено правило захода при одном доступном контуре: клетка закрывается статусом `[L]`/`[M]`, непрогнанное пишется в «долг по прогону» (§9.4), первый заход на второй машине начинается с погашения долга (§5).
- **Три действующих ограничения порядка**: **В14** (проба `.note.GNU-stack` на маке) — до сдачи главы 1; **В13** (добор реестра: valgrind + S7 как арбитр по libc) — до захода 7 (глава 6); глава 9 — только после 8.Ч2. Открытый список машинозависимых действий — `Asm-learning.md` §9.3.
- **Решение этого захода, которого не было в программе**: ревизия разбита на **Р1 после главы 5** (гейт на ABI — на нём стоит вся вторая половина книги) и **Р2 сквозную** после главы 9. Программа задавала только число заходов.
- **Журнал §7.1–7.2 требований заморожен на месте, а не перенесён**: на его пункты ссылается `outline.md` в 16 местах формой «требования §7.2 п. N». Новые прогоны и договорённости — в `Asm-learning.md` §9.

**Осторожно с коммитами:** владелец параллельно ведёт сессии по другим книгам в одном рабочем дереве (2026-07-31 прямо во время этого захода чужая сессия закоммитила `ef61b18` и `754f7a4`). Коммитить только формой `git commit <явные пути> -m …`; новый файл сначала `git add <путь>`, иначе partial commit не примет его как неизвестный git (`mem:task_completion` пп. 6, 6a).

Конвенции соседней книги — `mem:conventions`, окружение — `mem:tech_stack`, завершение захода — `mem:task_completion`, соседняя книга — `mem:c-rust-book-progress`.
