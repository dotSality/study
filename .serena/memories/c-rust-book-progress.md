# c-rust-book-progress

Состояние на 2026-07-16 (заход главы 18 завершён):

- Том I: главы 1–13 написаны, ревизия тома I выполнена (2026-07-11). Решения (этап 5) не начаты.
- Том II: главы 14–18 написаны и верифицированы. **Глава 18 `18-error-handling` (panic!/Result/?/собственные ошибки/From; зеркало обработки ошибок гл. 7/10 тома I) закрыта 2026-07-16**: 2 части, 6 параграфов, 20 заданий, 32 программы на эталоне rustc 1.97 (Windows gnu) + механическая перекомпиляция всех 19 полных листингов из готового текста. Детали — журнал §10 `C-and-Rust-learning.md`, запись 2026-07-16.
- **Критично для верификации: голый `rustc` = издание 2015.** Эталон курса — **`rustc --edition 2024 файл.rs`** (в 2015 `panic!("{t}")` не форматирует). Главы 14–17 прогонялись голым rustc — их конструкции издание-нейтральны, но при ревизии тома II поправить формулировку «rustc файл.rs» (§14.1.1/введения) на форму с флагом.
- Окружение тома II: rustup на Windows, host `x86_64-pc-windows-gnu`, rustc/cargo 1.97.0 stable; rustc доступен код-агенту прямо в Git Bash; WSL rustc 1.75 — запасной контур.
- **Следующая клетка: глава 19 `19-generics-traits` (этап 3).** Параллельно допустимы решения тома I (этап 5, главы 1→13 по порядку).
- Введено к концу гл. 18 (можно свободно использовать дальше): всё из гл. 14–17 + panic!/размотка/RUST_BACKTRACE, Result/unwrap/expect/unwrap_or/must_use, оператор ?, main -> Result<(), E: Debug>, eprintln!, File::open/create/metadata, fs::read_to_string, io::Error/ErrorKind (non_exhaustive → обязательный `_`), собственные error-enum с данными, **impl From<X> for Y как рецепт** (первый трейт курса; механика — гл. 19), char::to_digit/is_whitespace, str::is_empty, String::clear, E0277 (два вида: ? вне Result-fn; ? без From). НЕ введено: трейты/дженерики/лайфтаймы/Display/parse (гл. 19), модули/use систематически (гл. 20), замыкания/итераторы/map_err/ok_or (гл. 21), Box/deref (гл. 22).
- Нюансы 1.97/Windows для будущих глав: паника → exit 101 (наблюдение); main->Result Err → «Error: {Debug}» + exit 1; io::Error Display англ. от ОС («The system cannot find the file specified. (os error 2)»); Debug File — handle + \\?\-путь; E0277 у ? в main()->() подсказывает Box<dyn Error> (якорь для гл. 22); dead_code «field 0 is never read» на error-enum, чей payload читают только Debug/Termination — читать поля match-ем.
- Верификация источников: R4 (O'Reilly) стабильно 403 WebFetch-у — цитировать по outline, покрывать R1/R9/R7/R2. R6 error-handling URL: `error-handling/{panics,result,try-operator,try-conversions}.html` — актуальны на 2026-07-16. R7: `?` в справочнике называется «The try propagation expression» (expressions/operator-expr.html).

Источник истины — трекер §7 и журнал §10 в `books/c-and-rust-book/C-and-Rust-learning.md` + git-история; эта память — только указатель для быстрого входа в заход.
