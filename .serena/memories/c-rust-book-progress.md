# c-rust-book-progress

Состояние на 2026-07-19 (заход главы 24 завершён):

- Том I: главы 1–13 написаны, ревизия тома I выполнена (2026-07-11). Решения (этап 5) не начаты.
- Том II: главы 14–24 написаны и верифицированы. **Глава 24 `24-unsafe-ffi` (unsafe Rust и FFI) закрыта 2026-07-19**: 2 части, 7 параграфов (3+4), 18 заданий (9+9), ~65 программ/сборок двумя компиляторами (rustc 1.97 edition 2024 + MinGW GCC 15.2), включая смешанные бинарники в обе стороны (C↔Rust staticlib), cargo-проект с build.rs+cc, 2 Miri-отчёта. Механическая сверка: 29 rust-блоков + 5 C-блоков. Детали — журнал §10 `C-and-Rust-learning.md`, запись «2026-07-19 (2)».
- Эталон верификации: `rustc --edition 2024 файл.rs`, cargo для проектов (rustc/cargo 1.97.0 stable, host `x86_64-pc-windows-gnu`); C — MinGW GCC 15.2 `-std=c17 -Wall -Wextra`. **Новое в окружении (заход 24): nightly-тулчейн с Miri** (`rustup toolchain install nightly --profile minimal --component miri`; rustc 1.99.0-nightly, miri 0.1.0; запуск `cargo +nightly miri run`).
- **Следующая клетка: глава 25 `25-capstone` (этап 3): итоговые консольные проекты** — Ч1 CLI-утилита мини-grep (R1 гл. 12) + эскиз на C, Ч2 многопоточный проект (пул потоков, R1 гл. 21). Проектные чекпоинты вместо классических параграфов допустимы (§3 программы). Параллельно допустимы решения тома I (этап 5, главы 1→13 по порядку).
- Введено к концу гл. 24 (можно свободно использовать): всё из гл. 14–23 + unsafe-блоки/unsafe fn/SAFETY-конвенция, сырые указатели (*const/*mut, &raw, add, is_null, from_raw_parts_mut), static mut, union, unsafe impl Send, UnsafeCell, Miri, extern "C"/safe fn/#[link], c_*-алиасы, #[repr(C)]/offset_of!, CString/CStr, b'x'-литералы, #[unsafe(no_mangle)], staticlib, Box::into_raw/from_raw, коллбэки extern "C" fn, build.rs + cc. НЕ введено: атомики (упомянуты «родней» в гл. 23), "C-unwind"/catch_unwind (помечены «за рамками»), bindgen/cbindgen (обзорно).
- Ключевые нюансы 1.97/2024 из захода 24 (пригодятся в ревизии): static_mut_refs — deny (println! от static mut — ошибка); unsafe_op_in_unsafe_fn — warn; «extern blocks must be unsafe»; #[no_mangle] без unsafe(...) — ошибка; null-deref в dev — вставленная проверка (abort), в release −O — мусор+exit 0; ловушка точного захвата ломает unsafe impl Send (замыкание захватывает поле *mut); repr(Rust) переупорядочивает поля ({i8,i32,i8}: 8 байт против 12 у repr(C)); native-static-libs: -lkernel32 -lntdll -luserenv -lws2_32 -ldbghelp; паника через extern "C" — abort 0xC0000409.
- Верификация источников (2026-07-19): R1 гл. 20.1, R7 (unsafety/behavior-considered-undefined/external-blocks/type-layout), R8 (what-unsafe-does/ffi), R9 (std::ffi), R6 (unsafe-rust, android/interoperability/with-c) — URL живые. R4/R5 печатные (O'Reilly 403 стабильно) — по outline.

Источник истины — трекер §7 и журнал §10 в `books/c-and-rust-book/C-and-Rust-learning.md` + git-история; эта память — только указатель для быстрого входа в заход.
