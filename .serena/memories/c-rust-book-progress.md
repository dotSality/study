# c-rust-book-progress

Состояние на 2026-07-19 (заход главы 25 завершён):

- Том I: главы 1–13 написаны, ревизия тома I выполнена (2026-07-11). Решения (этап 5) не начаты.
- Том II: главы 14–25 написаны и верифицированы. **Глава 25 `25-capstone` (итоговые проекты) закрыта 2026-07-19** — первая глава в формате проектных чекпоинтов: Ч1 minigrep (4 стадии эволюции, lib+bin, 3 юнит-теста, C-эскиз 27 строк с ASan-прогоном и фактом потери совпадения на границе буфера fgets), Ч2 ThreadPool+pargrep (⛔ while let подтверждён прогоном, Drop-протокол, path-зависимость на minigrep, бенч 2.04 с → 368 мс на 8 потоках) + ретроспектива курса. 20 заданий (10+10). Мех. сверка 27 блоков + скриптовая сверка перекрёстных ссылок (68 ссылок §N.M.k, 0 битых). Детали — журнал §10, записи «2026-07-19 (3)».
- Эталон верификации: `rustc --edition 2024 файл.rs` / cargo (rustc 1.97.0 stable, host `x86_64-pc-windows-gnu`); C — MinGW GCC 15.2 `-std=c17 -Wall -Wextra`, санитайзеры/valgrind — WSL1 Ubuntu-24.04. Nightly с Miri установлен (заход 24).
- **Следующая клетка: глава 26 `26-gui-rust-egui` (этап 3, финальная глава): GUI на Rust (egui/eframe)** — обзорная глава (единственный спец-источник R11, оговорено в outline и sources), Ч1 immediate mode + eframe + первое окно/виджеты, Ч2 контрольный проект — GUI-обёртка мини-grep **поверх библиотеки minigrep из гл. 25** (переиспользование crate, фоновый поиск через канал из гл. 23). Понадобится `cargo add eframe` (первая внешняя зависимость реального размера; компиляция долгая — закладывать время). GUI-правило §2.3: агент проверяет компиляцию и smoke-запуск, функциональную проверку интерфейса делает владелец (фиксировать как открытый пункт отчёта). Параллельно допустимы решения тома I (этап 5, главы 1→13).
- Введено к концу гл. 25 (можно использовать в гл. 26): всё из гл. 14–24 + `process::exit`, `env::var`, `str::contains`/`lines`/`to_lowercase`, `is_ok`, `Option::take`, `Vec::drain`, `Instant`, `fs::write`; библиотека minigrep (Config/run/search/search_case_insensitive) и ThreadPool. Match guard (`Ok(n) if ...`) НЕ вводился нигде — избегать или вводить явно. `sort` НЕ вводился (в гл. 25 упомянут в задании со ссылкой R9).
- Инфраструктурные факты захода 25 (детали в §10): wsl.exe из Git Bash теряет кавычки/коды возврата — прогоны WSL через скрипт-файл + лог внутри WSL ФС; `MSYS_NO_PATHCONV=1` против конвертации путей; MinGW разворачивает argv[0] в полный путь. Приём «скриптовая сверка §-ссылок» стоит повторить на ревизии тома II.

Источник истины — трекер §7 и журнал §10 в `books/c-and-rust-book/C-and-Rust-learning.md` + git-история; эта память — только указатель для быстрого входа в заход.
