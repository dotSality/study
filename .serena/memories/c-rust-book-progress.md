# c-rust-book-progress

Состояние на 2026-07-30 (заход «решения главы 20» завершён):

- **Этапы 3 и 4 (главы 1–26 + ревизии обоих томов) ЗАВЕРШЕНЫ.** Идёт **этап 5 — решения**: **том I закрыт целиком** (главы 1–13), в томе II закрыты главы 14 `rust-start`, 15 `ownership`, 16 `structs-enums`, 17 `collections-strings`, 18 `error-handling`, 19 `generics-traits` (2026-07-29) и 20 `modules-cargo-tests` (2026-07-30). Файл `solutions/20-modules-cargo-tests.md`: 20 заданий (10 + 10), 6 вторых вариантов, стенд из **26 cargo-пакетов** в двух каталогах (`p1`/`p2`) плюс одиночные файлы `m*` и 2 программы на C.
- **Следующая клетка: решения главы 21** (`21-functional`). Дальше строго по порядку 22→26; затем сквозная финальная ревизия и итоговый отчёт этапа 6. Один файл решений за заход.
- **Приём захода этапа 5** (отработан на главах 1–20): условия копируются из главы дословно (сверяется скриптом), формат §4 — условие → пошаговое решение → выделенный **Ответ** → **вторые варианты после «Ответа»** (порядок Ответ → `**Вариант N (имя подхода).**` → `*Сравнение.*`) → где уместно разбор типичной неверной попытки (неверные, но собирающиеся версии реально прогоняются, вывод дословный). Доля вторых вариантов ≈22–30 %: гл. 1 — 5/22, 2 — 8/36, 3 — 6/24, 4 — 7/32, 5 — 5/22, 6 — 7/30, 7 — 5/20, 8 — 5/20, 9 — 7/30, 10 — 5/20, 11 — 5/20, 12 — 4/18, 13 — 4/17, 14 — 5/20, 15 — 7/29, 16 — 5/20, 17 — 6/20, 18 — 6/20, 19 — 9/30, 20 — 6/20. Текст русский, код — язык главы, комментарии в коде русские, **печатаемые строки латиницей** (исключение — если кириллица задана условием или самой главой; в гл. 20 таких строк 29).
- **Эталон тома II:** `rustc/cargo 1.97.0` stable, host `x86_64-pc-windows-gnu`, издание 2024; для главы 20 добавились `rustfmt 1.9.0-stable` и `clippy 0.1.97` из той же поставки. Одиночные файлы — **`rustc --edition 2024`** (голый rustc собирает в издании 2015 — ловушка), релиз — `rustc --edition 2024 -O`; проекты — `cargo new/build/run/test/doc/fmt/clippy/clean`. **rustc/cargo не видны на PATH инструмента Bash** — прогоны вести bash-скриптом с `export PATH="/c/Users/Max/.cargo/bin:$PATH"`, сам скрипт запускать через `tr -d '\r'`; файлы `.rs` с кириллицей писать инструментом Write (UTF-8). **WSL из инструмента Bash с путём `/mnt/c/...` ломается конверсией путей MSYS — вызывать через инструмент PowerShell.** Реестр crates.io доступен: `cargo add rand` и `cargo add <пакет> --path ...` работают.
- **Приём сборки стенда тома II:** каталог на часть главы, один bash-скрипт на каталог, вывод в `log_*.txt`, читается одним `cat`. **Имена файлов стенда попадают в сообщения компилятора** — разводить их префиксами по частям (гл. 15: `o*`/`b*`/`s*`; 16: `s*`/`m*`; 17: `v*`/`h*`; 18: `p*`/`d*`; 19: `g*`/`t*`/`l*`; 20: `m*` + пакеты). **Глава про пакеты (20) требует другого стенда: не файлы, а проекты** — каждое состояние проекта (до/после переезда модуля в файл, до/после порчи `expected`, до/после `cargo fmt`) живёт в отдельном каталоге, иначе листинг перестаёт быть куском реального файла; `cp -r` сохраняет имя пакета из `Cargo.toml`, поэтому выводы копий совпадают с выводами главы. Файлы, которые предстоит переписать инструментом Write, надо **удалить скриптом заранее** (Write не пишет поверх непрочитанного файла). Сценарии с кодами выхода печатать в лог строкой `[exit code: $?]`.
- **Сверочные скрипты захода** (node, писать заново под главу; последние — `verify_ch20.js` + `check_listings_ch20.js`, **не коммитятся и удаляются после захода**): (1) ID глава↔решения (множество и порядок), дословность условий (нормализация пробелов), число `**Вариант N` == число `*Сравнение.*` и доля, «Ответ» в каждом задании и до вариантов, забегания вперёд (поиск в **коде** решений конструкций последующих глав), маркеры догадок, баланс ограждений, кириллица в печатаемых строках (список печатается для ручной сверки), перекрёстные ссылки `§N.M(.k)` против реальных заголовков глав, концы строк. (2) `check_listings` сверяет, что каждый листинг — **непрерывный фрагмент** названного файла стенда, и что **каждая строка блоков вывода найдена в логах прогона**; файл к листингу определяется как «последнее упоминание пути перед блоком» — в главе 20 пути в тексте даны от корня стенда (`p2/textstat/src/stats.rs`), иначе `src/stats.rs` неоднозначен между шестью копиями проекта. Блоки внутри `**Условие.**` из сверки исключать (там код главы, не стенда); сокращения `...` в цитатах сверять регуляркой с `.*` на месте многоточия; абстрактные схемы размечать явно и в тексте, и в белом списке скрипта. Грабли: в JS `\w` и `\b` не работают с кириллицей — границы задавать классом `[^а-яёА-ЯЁ]`; «вероятность» ловится маркером «вероятно»; в шаблонной строке JS нельзя писать ``` внутри `${...}`-литерала. **Концы строк: файлы глав и `C-and-Rust-learning.md` — CRLF, файлы решений — LF** (проверено node: у `19-…` и `20-…` ноль CRLF); правки операционного файла делать node-скриптом, вставляющим `\r\n`, и проверять, что одиночных LF осталось 0.
- **Полезные факты гл. 20 (журнал §10, запись 2026-07-30):** реконструкция проекта `textstat` дала **те же хеши тестовых бинарников**, что в главе (хеш зависит от имени и версии пакета, а не от содержимого) — выводы главы сверяются дословно; `use crate::net;` рядом с `mod net` — **E0255**; приватное поле даёт **E0616** при чтении и **E0451** при построении литерала (вторую видно только убрав первую); повторный `mod` файла библиотеки в `main.rs` — **E0308** с `note: multiple different versions of crate …`, оба типа из одного файла; переезд модуля в файл не двигает его по дереву (все три починки §20.1.3 продолжают работать); C-контур: 10 строк → 1073 после `gcc -E`, `struct Point {` дважды без стража; workspace держит `Cargo.lock` и `target/` **только в корне**, `cargo run` без `-p` работает при одном бинарнике, удаление зависимости — **E0433**; `cargo add rand` лочит 8 пакетов (в `cargo tree` под Windows видно 6, `libc` и `r-efi` платформенные); clippy выдаёт **`manual_clamp`** с готовой заменой `x.clamp(0, 10)`, `approx_constant` — `deny`, `len_zero` — `warn`; `todo!()` под `#[should_panic(expected=…)]` даёт `panic did not contain expected string` (честный «красный»), а полное отсутствие реализации — E0425 и ошибку сборки; Result-тест при провале печатает одну строку `Error: "<Debug>"`; doc-тест назван по строке **открывающей ограды**, провал — `Test executable failed (exit status: 101)` в `doctest_bundle_2024.rs`; `cargo fmt -- --check` печатает **цветной** diff, для цитаты нужен `--color=never`; снятый `#[cfg(test)]` даёт единственный симптом `unused import: super::*`.
- **Полезные факты глав 14–19** — в журнале §10 (записи 2026-07-27…2026-07-29): мономорфизация измерена через `--emit=llvm-ir`; `mismatched_lifetime_syntaxes` в 1.97; E0515 вместо E0106 при попытке починить `'static`; минимальный набор derive для ключа `HashMap` (`Eq, Hash, PartialEq`) и его абляции; `+` у строк забирает буфер левого операнда; `realloc` реально переносит блок (ASan `heap-use-after-free`); `main -> Result` даёт код 1 против 101 у паники; cargo пересобирает по mtime.
- **Открытые пункты для сквозной ревизии (этап 6):**
  - гл. 9: условие Г9.Ч3.З8 обещает порядок печати «`a: 3`, `bb: 2`, `ccc: 1` — в порядке корзин», фактический порядок корзин — `bb`(0), `a`(1), `ccc`(3).
  - гл. 13 (три расхождения, подробно — в конце `solutions/13-ub-and-bridge.md`): (1) у `oob.c` §13.1.2 нет «чистого» запуска — правка `argc + 2`; (2) условие Г13.Ч1.З8 неверно утверждает несовместимость ASan и UBSan (несовместимы ASan и TSan); (3) номера строк в отчётах UBSan §13.1.4 — для файлов без первой строки-комментария.
  - гл. 14: §14.1.1 приводит вывод `rustup show` сокращённо.
  - гл. 15, 16, 18 и 19: расхождений по существу нет, только ручные переносы длинных строк `note:`/`help:` (оговорены в преамбулах решений).
  - **гл. 16 ↔ 17:** §16.2.2 обещает «передавать разбор по ссылке научимся на коллекциях (глава 17)», но глава 17 разбор по ссылке не вводит — в §17.1.2 только обход `&v`. Приём предъявлен во втором варианте Г17.Ч1.З8 с оговоркой и ссылкой на R7.
  - **гл. 20:** §20.1.3, ⛔-пример 4 — вывод E0433 приведён без блока `help: consider importing this module through its public re-export` (`use crate::words;`), который rustc 1.97 печатает; в решении Г20.Ч1.З5 вывод дан полностью.
- Машинозависимые числа в решениях — с оговоркой «на эталонной машине» (для Rust это ещё и служебный номер потока в панике, числовой `handle` в отладочной печати `File`, хеш-суффиксы тестовых бинарников). Программы с вводом прогоняются конвейером: `printf 'hi\n' | ./prog.exe`.
- Эталон тома I: C — MinGW GCC 15.2 `-std=c17 -Wall -Wextra` (Git Bash полный путь `/c/Users/Max/scoop/apps/gcc/current/bin/gcc.exe`); make 4.4.1 и ar 2.45.1 — на PATH из Git Bash; санитайзеры/valgrind — WSL1 (gcc 13.3, valgrind 3.22; `wsl -d Ubuntu-24.04 -u root bash ...`); nightly+Miri установлен; в registry закешированы rand 0.10.2 и eframe/egui 0.35.0.
- **Прогон GUI-программ (гл. 12 и 26):** служебный водитель PowerShell + P/Invoke (`drv.ps1`) шлёт окнам сообщения системы — `BM_CLICK`, `WM_LBUTTONDOWN`/`UP`, `WM_CHAR`, `WM_SYSCOMMAND`+`SC_CLOSE`. Грабли: `SendMessage(BM_CLICK)` зависает при модальном `MessageBox` (нужен `PostMessage`); содержимое `EDIT` чужого процесса читается через `WM_GETTEXT`; `$null` в string-параметрах P/Invoke — `[NullString]::Value`.
- **Открытый пункт владельца (не закрывается агентом): ручная приёмка GUI глав 12 и 26.**
- 2026-07-22, договорённость владельца: **отдельного документа про вариативность решений не существует и не будет** — работаем по §4 п. 5 и чеклисту §6.

Источник истины — трекер §7 и журнал §10 в `books/c-and-rust-book/C-and-Rust-learning.md` + git-история; эта память — только указатель для быстрого входа в заход. Конвенции — `mem:conventions`, окружение — `mem:tech_stack`, команды — `mem:suggested_commands`, завершение захода — `mem:task_completion`.
