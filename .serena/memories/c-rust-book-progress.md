# c-rust-book-progress

Состояние на 2026-07-26 (заход «решения главы 11» завершён):

- **Этапы 3 и 4 (главы 1–26 + ревизии обоих томов) ЗАВЕРШЕНЫ.** Идёт **этап 5 — решения**: закрыты главы 1 (2026-07-22), 2–4 (2026-07-23), 5–9 (2026-07-24), 10 и 11 (2026-07-26). Последний файл — `solutions/11-preprocessor-build.md`: 20 заданий, 5 вторых вариантов, 6 разборов неверных подходов, 14 намеренно падающих сборок предъявлены дословно.
- **Следующая клетка: решения главы 12** (`12-gui-c-win32`) — **GUI-глава на Win32, обзорная, контрольная работа тома I**; ожидается стенд с оконным приложением и линковкой `-luser32 -lgdi32` (механика `-l` отработана в решениях гл. 11). Дальше строго по порядку 13→26; затем сквозная финальная ревизия и итоговый отчёт этапа 6. Один файл решений за заход. Для глав с кучей дополнительно прогонять ASan/Valgrind в WSL1 (`wsl -d Ubuntu-24.04 -u root bash -c '...'` — запускать из PowerShell, не из Git Bash; скрипты писать Write + `tr -d '\r'`).
- **Приём захода этапа 5** (отработан на главах 1–11): условия копируются из главы дословно (сверяется скриптом), формат §4 — условие → пошаговое решение → выделенный **Ответ** → **вторые варианты после «Ответа»** (порядок Ответ → `**Вариант N` → `*Сравнение.*`) → где уместно разбор типичной неверной попытки (неверные, но не-UB версии реально прогоняются, вывод дословный). Доля вторых вариантов ≈22–25 %: гл. 1 — 5/22, 2 — 8/36, 3 — 6/24, 4 — 7/32, 5 — 5/22, 6 — 7/30, 7 — 5/20, 8 — 5/20, 9 — 7/30, 10 — 5/20, 11 — 5/20. Текст русский, код — язык главы, комментарии в коде русские, **печатаемые строки латиницей** (в гл. 11 из-за этого пришлось убрать кавычки и из русского комментария — скрипт ловит кириллицу внутри `"…"` в C-блоках).
- **Приём сборки стенда:** гл. 9 — файлы-«прелюдии» + `cat prelude body > prog.c`; гл. 10 — по программе на задание в `src/`, драйвер компилирует каждую в свой каталог и пишет stdout/stderr раздельно; гл. 11 — каталог на задание (`p1/z1…p1/z10`, `p2/z1…p2/z10`), общие модули (`vec.h/.c`, `stack.h/.c`) копируются из `/tmp`, Makefile-ы создаются `printf`-ом (гарантированные табуляции), драйвер печатает `$ команда` + раздельные stdout/stderr + `[exit N]`.
- **Сверочный скрипт захода** (node, писать заново под главу; последний — `verify_ch11.js`): извлечь из главы `**ГN.ЧM.Зk.**` с телом условия (собирать строки **до пустой строки** — условия бывают многострочными, как Г11.Ч2.З4) и из решений `### ГN.ЧM.Зk` + `**Условие.**`; сравнить множества ID, тексты и порядок секций; число `**Вариант N` == число `*Сравнение.*`; наличие `**Решение.**`/`**Ответ:**` и порядок «Ответ до вариантов»; поиск «глава N»/«§N.» с N > текущей и маркеров догадок (маркеры — с лукбихайндом `(?<![а-яёa-z])`, иначе «вероятность» ловится как «вероятно»); баланс ограждений кода; кириллица в печатаемых строках C-кода. **Обязательно вычёркивать ссылки на главы источников** (`C2 (гл. 22)`, `C1 (прил. B1)`). Гл. 11: 20/20, всё чисто.
- **Открытый пункт для сквозной ревизии (этап 6):** условие Г9.Ч3.З8 обещает порядок печати «`a: 3`, `bb: 2`, `ccc: 1` — в порядке корзин», а фактический порядок корзин — `bb`(0), `a`(1), `ccc`(3) (счётчики верны). В решении оговорено с пересчётом хешей; формулировку главы поправить при финальной ревизии.
- **Полезные факты гл. 11 (журнал §10, запись 2026-07-26):** GNU Make 4.4.1 на Windows сравнивает отметки времени **с точностью до секунды** — протоколы с `touch` требуют паузы ≥ 1 с (на Linux make 4.3 — наносекунды); `gcc -E hello.c | wc -l` = 1057 на MinGW против 547 на glibc; `-E -P` печатает и весь `stdio.h`, свой код — в хвосте; в MinGW `stderr` = `(__acrt_iob_func(2))`; забытый пререквизит заголовка + переставленные поля `len`/`cap` дают устойчивое `len=8 cap=5` (UB, но воспроизводимо); заголовок без стража ломается только на определении типа (макросы с тем же текстом замены переопределяются молча); `-std=c23` на GCC 15.2 → `__STDC_VERSION__` = 202311.
- Машинозависимые числа в решениях — с оговоркой «на эталонной машине». Программы с вводом прогоняются конвейером: `printf 'hi\n' | ./prog.exe`.
- Эталон верификации: C — MinGW GCC 15.2 `-std=c17 -Wall -Wextra` (Git Bash полный путь `/c/Users/Max/scoop/apps/gcc/current/bin/gcc.exe`); make 4.4.1 и ar 2.45.1 — на PATH из Git Bash (`export PATH="/c/Users/Max/scoop/apps/gcc/current/bin:$PATH"`); Rust — rustc/cargo 1.97.0 stable gnu, одиночные файлы `rustc --edition 2024`; санитайзеры/valgrind — WSL1 (gcc 13.3, valgrind 3.22; ASan и LSan там реально работают — проверено контрольными программами); nightly+Miri установлен; в registry закешированы rand 0.10.2 и eframe/egui 0.35.0.
- **Открытый пункт владельца (не закрывается агентом): ручная приёмка GUI глав 12 и 26.**
- 2026-07-22, договорённость владельца: **отдельного документа про вариативность решений не существует и не будет** — работаем по §4 п. 5 и чеклисту §6, новых критериев не вводим.

Источник истины — трекер §7 и журнал §10 в `books/c-and-rust-book/C-and-Rust-learning.md` + git-история; эта память — только указатель для быстрого входа в заход. Конвенции — `mem:conventions`, окружение — `mem:tech_stack`, команды — `mem:suggested_commands`, завершение захода — `mem:task_completion`.
