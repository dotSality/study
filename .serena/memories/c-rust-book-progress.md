# c-rust-book-progress

Состояние на 2026-07-20 (заход главы 26 завершён):

- **Этап 3 (написание глав) ЗАВЕРШЁН: все 26 глав написаны.** Том I: главы 1–13 + ревизия тома I (2026-07-11). Том II: главы 14–26. Решения (этап 5) не начаты.
- **Глава 26 `26-gui-rust-egui` (GUI на Rust; обзорная, финальная) закрыта 2026-07-20**: Ч1 — immediate mode + eframe 0.35.0 (3 параграфа: парадигма vs Win32 гл. 12, первое окно, виджеты/счётчик-зеркало §12.2.1), Ч2 — 4 чекпоинта (GUI-обёртка minigrep из гл. 25 — библиотека не изменена, path-зависимость; синхронный поиск → замер заморозки (WM_CLOSE 2723 мс vs 191 мс) → фоновый поток + канал + try_recv + request_repaint → release + windows_subsystem). 17 заданий (8+9). Мех. сверка: 28 блоков diff-идентичны прогнанным, §-ссылки скриптом 126/0 битых. Детали — журнал §10, записи «2026-07-20».
- **Следующая клетка: РЕВИЗИЯ ТОМА II (главы 14–26, этап 4а)** — по чеклисту §6; повторить приём скриптовой сверки §-ссылок (гл. 25/26); напоминание захода 18: заменить «rustc файл.rs» в §14.1.1/введениях на «rustc --edition 2024»; закрыть открытые пункты владельца: ручная приёмка GUI гл. 12 и гл. 26. Параллельно допустимы решения тома I (этап 5, главы 1→13 по порядку).
- Эталон верификации: rustc/cargo 1.97.0 stable gnu, `rustc --edition 2024`; C — MinGW GCC 15.2 `-std=c17 -Wall -Wextra`; санитайзеры/valgrind — WSL1; nightly+Miri установлен.
- Окружение GUI (гл. 26, важно для решений/ревизии): eframe 0.35 — метод `App::ui(&mut Ui, …)` (НЕ update), панели `Panel::top(id).show(ui, …)`; gnu-тулчейн требует `.cargo/config.toml` с `rustflags = ["-L", "<MinGW>\x86_64-w64-mingw32\lib"]` (rust-mingw без libshlwapi.a); два одноимённых пакета в общем CARGO_TARGET_DIR коллидируют отпечатками; факты по egui сверять по исходникам в `~/.cargo/registry/src/`, не по WebFetch-пересказам. GUI-автоматизация: фильтровать ConsoleWindowClass, PrintWindow(PW_RENDERFULLCONTENT), SendInput INPUT=40 байт, перед вводом проверять GetForegroundWindow.
- Введено в гл. 26 сверх прежнего: Default/derive(Default), Option::is_some, try_recv/TryRecvError, .cargo/config.toml, egui/eframe API, windows_subsystem. `str::trim` НЕ вводился (в задании Ч1.З3 дан с пометкой R9).

Источник истины — трекер §7 и журнал §10 в `books/c-and-rust-book/C-and-Rust-learning.md` + git-история; эта память — только указатель для быстрого входа в заход.
