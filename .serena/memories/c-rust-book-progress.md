# c-rust-book-progress

Состояние на 2026-07-18 (заход главы 19 завершён):

- Том I: главы 1–13 написаны, ревизия тома I выполнена (2026-07-11). Решения (этап 5) не начаты.
- Том II: главы 14–19 написаны и верифицированы. **Глава 19 `19-generics-traits` (обобщённые типы/трейты/времена жизни; закрывает рецепты From/Display/derive/parse/E0106 из гл. 14–18) закрыта 2026-07-18**: 3 части, 8 параграфов, 30 заданий, 44 программы на эталоне + механическая перекомпиляция 21 листинга из готового текста. Детали — журнал §10 `C-and-Rust-learning.md`, запись 2026-07-18.
- **Критично для верификации: голый `rustc` = издание 2015.** Эталон курса — **`rustc --edition 2024 файл.rs`** (rustc/cargo 1.97.0 stable, host `x86_64-pc-windows-gnu`, доступен код-агенту прямо в Git Bash; WSL rustc 1.75 — запасной контур). Главы 14–17 прогонялись голым rustc — при ревизии тома II поправить «rustc файл.rs» в §14.1.1/введениях на форму с флагом.
- **Следующая клетка: глава 20 `20-modules-cargo-tests` (этап 3).** Параллельно допустимы решения тома I (этап 5, главы 1→13 по порядку).
- Введено к концу гл. 19 (можно свободно использовать дальше): всё из гл. 14–18 + обобщённые функции/структуры/enum/методы (`impl<T>`, `impl Point<f64>`), ограничения (`T: A + B`, `where`), turbofish `::<>`, мономорфизация, трейты (объявление, `impl Trait for Тип`, тела по умолчанию, orphan rule/newtype), `&impl Trait`/`-> impl Trait` (один тип!), условные и blanket-реализации, derive как impl (`PartialEq`/`==`, `Eq` (нет у f64 — NaN), `Hash`, `PartialOrd` — супертрейт PartialEq, лексикографический), ручной `Display` (`use std::fmt`, `write!`, `Formatter`) + автоматический `to_string`, `From` (определение прозрачно), `FromStr`+`parse::<T>()` (+ ассоциированный `type Err` — только знакомство), лайфтаймы (`'a` в функциях/структурах `Excerpt<'a>`, три правила elision по R1/R7, `'static`). НЕ введено: `dyn Trait`/`Box` (гл. 22), замыкания/итераторы/`Iterator`/`Into` (гл. 21), модули/use системно/тесты (гл. 20), `TryFrom`/`Borrow` (за рамками, упомянуты справочно).
- Нюансы 1.97 для будущих глав: «cannot find type `T`» = **E0425** (не E0412); E0369 у generic-тела без вызовов (проверка в определении); orphan — E0117 с советом newtype; два типа под `impl Trait` — E0308 с help `Box<dyn ...>`; ключ HashMap без derive — E0277×2+E0599 разом; чужой лайфтайм из метода — «lifetime may not live long enough» без E-кода; `ParseIntError` kinds Empty/InvalidDigit/PosOverflow.
- Верификация источников: R1 актуальная редакция — `Tweet` → `SocialPost` (гл. 10.2). R4 (O'Reilly) стабильно 403 WebFetch-у — цитировать по outline, покрывать R1/R7/R9; R5 печатное — справочно. URL-структуры R2 (generics/gen_fn, scope/lifetime/*), R6 (generics/*) актуальны на 2026-07-18.

Источник истины — трекер §7 и журнал §10 в `books/c-and-rust-book/C-and-Rust-learning.md` + git-история; эта память — только указатель для быстрого входа в заход.
