# c-rust-book-progress

Состояние на 2026-07-27 (заход «решения главы 15» завершён):

- **Этапы 3 и 4 (главы 1–26 + ревизии обоих томов) ЗАВЕРШЕНЫ.** Идёт **этап 5 — решения**: **том I закрыт целиком** (главы 1–13) и **открыт том II**: главы 14 `rust-start` и 15 `ownership` — 2026-07-27. Файл `solutions/15-ownership.md`: 29 заданий (10 + 10 + 9), 7 вторых вариантов, стенд из 67 файлов в трёх каталогах (`p1`/`p2`/`p3`) + 3 программы на C.
- **Следующая клетка: решения главы 16** (`16-structs-enums`). Дальше строго по порядку 17→26; затем сквозная финальная ревизия и итоговый отчёт этапа 6. Один файл решений за заход.
- **Приём захода этапа 5** (отработан на главах 1–15): условия копируются из главы дословно (сверяется скриптом), формат §4 — условие → пошаговое решение → выделенный **Ответ** → **вторые варианты после «Ответа»** (порядок Ответ → `**Вариант N (имя подхода).**` → `*Сравнение.*`) → где уместно разбор типичной неверной попытки (неверные, но собирающиеся версии реально прогоняются, вывод дословный). Доля вторых вариантов ≈22–25 %: гл. 1 — 5/22, 2 — 8/36, 3 — 6/24, 4 — 7/32, 5 — 5/22, 6 — 7/30, 7 — 5/20, 8 — 5/20, 9 — 7/30, 10 — 5/20, 11 — 5/20, 12 — 4/18, 13 — 4/17, 14 — 5/20, 15 — 7/29. Текст русский, код — язык главы, комментарии в коде русские, **печатаемые строки латиницей** (исключение допускается, только если кириллица задана самим условием главы — так было в Г15.Ч1.З3).
- **Эталон тома II:** `rustc/cargo 1.97.0` stable, host `x86_64-pc-windows-gnu`, издание 2024. Одиночные файлы — **`rustc --edition 2024`** (голый rustc собирает в издании 2015 — ловушка), релиз — `rustc --edition 2024 -O`; проекты — `cargo new/build/run/check/clean`, релиз `cargo build --release`. **rustc/cargo не видны на PATH инструмента Bash** — прогоны вести bash-скриптом с `export PATH="/c/Users/Max/.cargo/bin:$PATH"`, сам скрипт запускать через `tr -d '\r'`; файлы `.rs` с кириллицей в комментариях писать инструментом Write (UTF-8). **WSL из инструмента Bash с путём `/mnt/c/...` ломается конверсией путей MSYS — вызывать через инструмент PowerShell.**
- **Приём сборки стенда тома II:** каталог на часть главы, один bash-скрипт на каталог, вывод в `log_*.txt`, читается одним `cat`. **Имена файлов стенда попадают в сообщения компилятора** — разводить их префиксами по частям (в гл. 15: `o*`/`b*`/`s*`), иначе одноимённые файлы из разных каталогов сталкиваются в тексте решений.
- **Сверочные скрипты захода** (node, писать заново под главу; последние — `tests/verify_ch15.js` + `tests/check_listings_ch15.js`, **не коммитятся**): (1) ID глава↔решения (множество и порядок), дословность условий (нормализация пробелов — условия могут содержать листинг), число `**Вариант N` == число `*Сравнение.*`, «Ответ» в каждом задании и до вариантов, забегания вперёд (поиск в **коде** решений конструкций последующих глав), маркеры догадок, баланс ограждений, кириллица в печатаемых строках. (2) `check_listings` сверяет каждую содержательную строку листингов с файлами стенда **и текстом главы** (листинги внутри блоков «**Условие.**» пропускаются). Грабли: в JS `\w` не матчит кириллицу — писать `[а-яё]`; **файлы глав в CRLF, файлы решений в LF** — в нормализации строки первым делом снимать `\r`, иначе `//`-комментарии не срезаются и дают ложные расхождения; в негативном лукахеде для `'static` скобка ставится сразу после апострофа (`&'(?!static\b)[a-z]+`).
- **Полезные факты гл. 15 (журнал §10, запись 2026-07-27 (4)):** `drop(x)` для `Copy`-типа даёт предупреждение `dropping_copy_types` («does nothing»); починка E0106 через `'static` не проходит — сменяется на **E0515** `cannot return reference to local variable`; E0382 различает `borrow of moved value` (через `println!`) и `use of moved value` (передача во владение); `&a[2..7]` с константными границами компилируется и паникует, а константное `a[7]` — ошибка сборки (`unconditional_panic` умеет индексацию, но не диапазоны); `realloc` реально переносит блок (адреса до/после разные) — опора под запретом «читатель при живом писателе»; два `char *` на один блок + `strcat` собираются без предупреждений и печатают одно и то же через оба имени, ASan даёт `heap-buffer-overflow` «0 bytes after 6-byte region».
- **Полезные факты гл. 14 (журнал §10, запись 2026-07-27 (3)):** cargo пересобирает **по mtime** (`touch` без правки содержимого вызывает `Compiling`), dep-info `target/debug/<имя>.d` — правило в формате make; `cargo new` не создаёт `Cargo.lock`; после `clean` + `check` в `target/debug/` нет exe; размеры одной программы: debug 4 871 636 против release 1 166 352 байта; **профилем управляются только проверки переполнения** — выход за границы и деление на ноль паникуют и в релизе.
- **Открытые пункты для сквозной ревизии (этап 6):**
  - гл. 9: условие Г9.Ч3.З8 обещает порядок печати «`a: 3`, `bb: 2`, `ccc: 1` — в порядке корзин», фактический порядок корзин — `bb`(0), `a`(1), `ccc`(3) (счётчики верны; в решении оговорено).
  - гл. 13 (три расхождения, подробно — в конце `solutions/13-ub-and-bridge.md` и в журнале §10): (1) у `oob.c` §13.1.2 **нет** «чистого» запуска — правка `argc + 2`; (2) условие Г13.Ч1.З8 неверно утверждает несовместимость ASan и UBSan (несовместимы ASan и TSan); (3) номера строк в отчётах UBSan §13.1.4 — для файлов без первой строки-комментария.
  - гл. 14: §14.1.1 приводит вывод `rustup show` **сокращённо** — rustup 1.29 печатает ещё блок `active toolchain`.
  - гл. 15: расхождений по существу нет; в §15.1.1 (пример 2), §15.1.2 (пример 2) и §15.2.3 (пример 1) длинные строки `note:`/`help:` перенесены вручную, а строки готовой правки опущены — вопрос оформления, оговорён в преамбуле решений.
- Машинозависимые числа в решениях — с оговоркой «на эталонной машине» (для Rust это ещё и служебный номер потока в панике: `thread 'main' (12336)`). Программы с вводом прогоняются конвейером: `printf 'hi\n' | ./prog.exe`.
- Эталон тома I: C — MinGW GCC 15.2 `-std=c17 -Wall -Wextra` (Git Bash полный путь `/c/Users/Max/scoop/apps/gcc/current/bin/gcc.exe`); make 4.4.1 и ar 2.45.1 — на PATH из Git Bash (`export PATH="/c/Users/Max/scoop/apps/gcc/current/bin:$PATH"`); санитайзеры/valgrind — WSL1 (gcc 13.3, valgrind 3.22; `wsl -d Ubuntu-24.04 -u root bash ...`); nightly+Miri установлен; в registry закешированы rand 0.10.2 и eframe/egui 0.35.0.
- **Прогон GUI-программ (гл. 12 и 26):** служебный водитель PowerShell + P/Invoke (`drv.ps1`) шлёт окнам те же сообщения, что и система — `BM_CLICK`, `WM_LBUTTONDOWN`/`UP`, `WM_CHAR` посимвольно, `WM_SYSCOMMAND`+`SC_CLOSE` вместо «крестика». Грабли: `SendMessage(BM_CLICK)` **зависает** при модальном `MessageBox` (нужен `PostMessage`); содержимое `EDIT` чужого процесса читается только через `WM_GETTEXT`; `$null` в string-параметрах P/Invoke — `[NullString]::Value`.
- **Открытый пункт владельца (не закрывается агентом): ручная приёмка GUI глав 12 и 26** — прокрутка, живой ввод с клавиатуры, поведение на очень больших файлах, внешний вид.
- 2026-07-22, договорённость владельца: **отдельного документа про вариативность решений не существует и не будет** — работаем по §4 п. 5 и чеклисту §6, новых критериев не вводим.

Источник истины — трекер §7 и журнал §10 в `books/c-and-rust-book/C-and-Rust-learning.md` + git-история; эта память — только указатель для быстрого входа в заход. Конвенции — `mem:conventions`, окружение — `mem:tech_stack`, команды — `mem:suggested_commands`, завершение захода — `mem:task_completion`.
