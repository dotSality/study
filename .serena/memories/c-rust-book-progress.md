# c-rust-book-progress

Состояние на 2026-07-12 (заход главы 15 завершён):

- Том I: главы 1–13 написаны, ревизия тома I выполнена (2026-07-11).
- Том II: главы 14–15 написаны и верифицированы. **Глава 15 `15-ownership` (владение/заимствование/срезы) закрыта 2026-07-12**: 3 части, 29 заданий, 46 прогонов на эталоне rustc 1.97 (Windows gnu). Детали захода — журнал §10 `C-and-Rust-learning.md`, запись 2026-07-12.
- Окружение тома II: rustup на Windows, host `x86_64-pc-windows-gnu`, rustc/cargo **1.97.0 stable**, издание 2024; WSL rustc 1.75 — запасной контур. rustc доступен код-агенту прямо в Git Bash (`rustc file.rs`).
- **Следующая клетка: глава 16 `16-structs-enums` (этап 3).** Параллельно допустимы решения тома I (этап 5, главы 1→13 по порядку).
- Нюансы 1.97 для будущих глав: (1) видимое статически переполнение — отказ компиляции (deny(arithmetic_overflow)), рантайм-демо прятать за вызов функции; (2) формат паники включает номер потока `thread 'main' (NNNN)`; (3) срез с константными границами вне диапазона (`&a[2..7]`) — НЕ статический отказ, а рантайм-паника (в отличие от константного индекса `a[7]`); (4) E0382 даёт машинные подсказки `+ .clone()` и note «to borrow instead».
- Верификация R4 (Programming Rust, O'Reilly): страница отдаёт 403 WebFetch-у — цитировать по формулировке outline, факты покрывать R1/R6/R7/R9.

Источник истины — трекер §7 и журнал §10 в `books/c-and-rust-book/C-and-Rust-learning.md` + git-история; эта память — только указатель для быстрого входа в заход.