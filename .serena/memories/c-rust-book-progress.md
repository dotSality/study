# c-rust-book-progress

Состояние на 2026-07-27 (заход «решения главы 14» завершён):

- **Этапы 3 и 4 (главы 1–26 + ревизии обоих томов) ЗАВЕРШЕНЫ.** Идёт **этап 5 — решения**: **том I закрыт целиком** (главы 1–13, последняя — 13 `ub-and-bridge`, 2026-07-27) и **открыт том II**: глава 14 `rust-start` — 2026-07-27. Файл `solutions/14-rust-start.md`: 20 заданий (8 + 12) + разминка без номера (перезапуск витрин §13.2), 5 вторых вариантов, стенд из 47 файлов (три cargo-проекта + одиночные `.rs` + 2 контрольных `.c`).
- **Следующая клетка: решения главы 15** (`15-ownership`) — центральная глава тома II (владение/заимствование/срезы, 29 заданий). Дальше строго по порядку 16→26; затем сквозная финальная ревизия и итоговый отчёт этапа 6. Один файл решений за заход.
- **Приём захода этапа 5** (отработан на главах 1–14): условия копируются из главы дословно (сверяется скриптом), формат §4 — условие → пошаговое решение → выделенный **Ответ** → **вторые варианты после «Ответа»** (порядок Ответ → `**Вариант N` → `*Сравнение.*`) → где уместно разбор типичной неверной попытки (неверные, но собирающиеся версии реально прогоняются, вывод дословный). Доля вторых вариантов ≈22–25 %: гл. 1 — 5/22, 2 — 8/36, 3 — 6/24, 4 — 7/32, 5 — 5/22, 6 — 7/30, 7 — 5/20, 8 — 5/20, 9 — 7/30, 10 — 5/20, 11 — 5/20, 12 — 4/18, 13 — 4/17, 14 — 5/20. Текст русский, код — язык главы, комментарии в коде русские, **печатаемые строки латиницей**.
- **Эталон тома II:** `rustc/cargo 1.97.0` stable, host `x86_64-pc-windows-gnu`, издание 2024. Одиночные файлы — **`rustc --edition 2024`** (голый rustc собирает в издании 2015 — ловушка), релиз — `rustc --edition 2024 -O`; проекты — `cargo new/build/run/check/clean`, релиз `cargo build --release`. **rustc/cargo не видны на PATH инструмента Bash** — прогоны вести bash-скриптом с `export PATH="/c/Users/Max/.cargo/bin:$PATH"`, сам скрипт запускать через `tr -d '\r'`; файлы `.rs` с кириллицей в комментариях писать инструментом Write (UTF-8).
- **Приём сборки стенда тома II (гл. 14):** каталоги `p1` (cargo-проекты и протоколы инструментов), `p2` (одиночные `.rs` + `.c`), `warm` (витрины гл. 13); один bash-скрипт на каталог, вывод в `log_*.txt`, читается одним `cat`. Для протоколов cargo (что вызывает пересборку, check vs build) — последовательность команд в том же скрипте.
- **Сверочные скрипты захода** (node, писать заново под главу; последние — `tests/verify_ch14.js` + `tests/check_listings_ch14.js`, **не коммитятся**): (1) ID глава↔решения (множество и порядок), дословность условий (нормализация пробелов — условия могут содержать листинг), число `**Вариант N` == число `*Сравнение.*`, «Ответ» в каждом задании и до вариантов, забегания вперёд (для тома II — поиск в **коде** решений конструкций `match`/`Vec`/`Option`/`struct`/`enum`/`impl`/`trait`/`.iter()`/`&mut`/`unwrap` с исключением для витрин гл. 13), маркеры догадок, баланс ограждений, кириллица в печатаемых строках. (2) `check_listings` сверяет каждую содержательную строку листингов с файлами стенда **и текстом главы** (листинги внутри блоков «**Условие.**» пропускаются). Полезно: в JS `\w` не матчит кириллицу — в регулярках писать `[а-яё]`.
- **Полезные факты гл. 14 (журнал §10, запись 2026-07-27 (3)):** cargo пересобирает **по mtime** (`touch` без правки содержимого вызывает `Compiling`), dep-info `target/debug/<имя>.d` — правило в формате make; `cargo new` не создаёт `Cargo.lock` (появляется после первой сборки); после `clean` + `check` в `target/debug/` нет exe (только `build`/`deps`/`examples`/`incremental`); размеры одной программы: debug 4 871 636 против release 1 166 352 байта; **профилем управляются только проверки переполнения** — выход за границы и деление на ноль паникуют и в релизе; rustc 1.97 добавил к E0382 готовую правку `.clone()`, к E0106 — второй `help`; `rustup override set nightly` разводит пометки `(default)`/`(active)` и печатает `active because: directory override`.
- **Открытые пункты для сквозной ревизии (этап 6):**
  - гл. 9: условие Г9.Ч3.З8 обещает порядок печати «`a: 3`, `bb: 2`, `ccc: 1` — в порядке корзин», фактический порядок корзин — `bb`(0), `a`(1), `ccc`(3) (счётчики верны; в решении оговорено).
  - гл. 13 (три расхождения, подробно — в конце `solutions/13-ub-and-bridge.md` и в журнале §10): (1) у `oob.c` §13.1.2 **нет** «чистого» запуска (`i = argc + 3`, `argc >= 1` ⇒ `i >= 4`; допустимы 0…3) — правка `argc + 2`; (2) условие Г13.Ч1.З8 неверно утверждает несовместимость ASan и UBSan — на gcc 13.3 они совместимы, несовместимы ASan и TSan; (3) номера строк в отчётах UBSan §13.1.4 — для файлов без первой строки-комментария.
  - гл. 14: §14.1.1 приводит вывод `rustup show` **сокращённо** — rustup 1.29 печатает ещё блок `active toolchain` (имя, `active because`, `installed targets`).
- Машинозависимые числа в решениях — с оговоркой «на эталонной машине» (для Rust это ещё и служебный номер потока в панике: `thread 'main' (12336)`). Программы с вводом прогоняются конвейером: `printf 'hi\n' | ./prog.exe`.
- Эталон тома I: C — MinGW GCC 15.2 `-std=c17 -Wall -Wextra` (Git Bash полный путь `/c/Users/Max/scoop/apps/gcc/current/bin/gcc.exe`); make 4.4.1 и ar 2.45.1 — на PATH из Git Bash (`export PATH="/c/Users/Max/scoop/apps/gcc/current/bin:$PATH"`); санитайзеры/valgrind — WSL1 (gcc 13.3, valgrind 3.22; `wsl -d Ubuntu-24.04 -u root bash -c '...'`); nightly+Miri установлен; в registry закешированы rand 0.10.2 и eframe/egui 0.35.0.
- **Прогон GUI-программ (гл. 12 и 26):** служебный водитель PowerShell + P/Invoke (`drv.ps1`) шлёт окнам те же сообщения, что и система — `BM_CLICK`, `WM_LBUTTONDOWN`/`UP`, `WM_CHAR` посимвольно, `WM_SYSCOMMAND`+`SC_CLOSE` вместо «крестика». Грабли: `SendMessage(BM_CLICK)` **зависает** при модальном `MessageBox` (нужен `PostMessage`); содержимое `EDIT` чужого процесса читается только через `WM_GETTEXT`; `$null` в string-параметрах P/Invoke — `[NullString]::Value`.
- **Открытый пункт владельца (не закрывается агентом): ручная приёмка GUI глав 12 и 26** — прокрутка, живой ввод с клавиатуры, поведение на очень больших файлах, внешний вид.
- 2026-07-22, договорённость владельца: **отдельного документа про вариативность решений не существует и не будет** — работаем по §4 п. 5 и чеклисту §6, новых критериев не вводим.

Источник истины — трекер §7 и журнал §10 в `books/c-and-rust-book/C-and-Rust-learning.md` + git-история; эта память — только указатель для быстрого входа в заход. Конвенции — `mem:conventions`, окружение — `mem:tech_stack`, команды — `mem:suggested_commands`, завершение захода — `mem:task_completion`.
