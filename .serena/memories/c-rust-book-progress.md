# c-rust-book-progress

Состояние на 2026-07-27 (заход «решения главы 12» завершён):

- **Этапы 3 и 4 (главы 1–26 + ревизии обоих томов) ЗАВЕРШЕНЫ.** Идёт **этап 5 — решения**: закрыты главы 1 (2026-07-22), 2–4 (2026-07-23), 5–9 (2026-07-24), 10 и 11 (2026-07-26), 12 (2026-07-27). Последний файл — `solutions/12-gui-c-win32.md`: 18 заданий, 4 вторых варианта, 5 разборов неверных подходов, 37 сборок стенда без предупреждений.
- **Следующая клетка: решения главы 13** (`13-ub-and-bridge`) — финал тома I: неопределённое поведение, витрины Rust (только чтение). Ожидается много прогонов с санитайзерами и разными уровнями оптимизации в WSL, плюс `rustc --edition 2024` для витрин §13.2. Дальше строго по порядку 14→26 (том II, Rust); затем сквозная финальная ревизия и итоговый отчёт этапа 6. Один файл решений за заход.
- **Приём захода этапа 5** (отработан на главах 1–12): условия копируются из главы дословно (сверяется скриптом), формат §4 — условие → пошаговое решение → выделенный **Ответ** → **вторые варианты после «Ответа»** (порядок Ответ → `**Вариант N` → `*Сравнение.*`) → где уместно разбор типичной неверной попытки (неверные, но не-UB версии реально прогоняются, вывод дословный). Доля вторых вариантов ≈22–25 %: гл. 1 — 5/22, 2 — 8/36, 3 — 6/24, 4 — 7/32, 5 — 5/22, 6 — 7/30, 7 — 5/20, 8 — 5/20, 9 — 7/30, 10 — 5/20, 11 — 5/20, 12 — 4/18. Текст русский, код — язык главы, комментарии в коде русские, **печатаемые строки латиницей**.
- **Приём сборки стенда:** гл. 9 — файлы-«прелюдии» + `cat prelude body > prog.c`; гл. 10 — по программе на задание в `src/`, драйвер пишет stdout/stderr раздельно; гл. 11 — каталог на задание, Makefile-ы через `printf`; гл. 12 — каталог на задание (`p1/z1…z8`, `p2/z1…z10`), общий скрипт `rebuild_all.sh` пересобирает весь стенд и считает предупреждения.
- **Прогон GUI-программ (гл. 12 и 26):** служебный водитель PowerShell + P/Invoke (`drv.ps1`) шлёт окнам те же сообщения, что и система — `BM_CLICK`, `WM_LBUTTONDOWN`/`UP`, `WM_CHAR` посимвольно, `WM_SYSCOMMAND`+`SC_CLOSE` вместо «крестика» — и читает результат (`GetWindowText`, `WM_GETTEXT`, `GetPixel`, `GetWindowRect`). Грабли: `SendMessage(BM_CLICK)` **зависает**, если обработчик открывает модальный `MessageBox` (нужен `PostMessage`); содержимое `EDIT` чужого процесса читается только через `WM_GETTEXT` (у `STATIC`/`BUTTON` текст = заголовок окна, читается напрямую); `$null` в string-параметрах P/Invoke надо передавать как `[NullString]::Value`.
- **Сверочные скрипты захода** (node, писать заново под главу; последние — `verify_ch12.js` + `check_listings.js`): (1) извлечь из главы `**ГN.ЧM.Зk.**` с телом до пустой строки и из решений `### ГN.ЧM.Зk` + `**Условие.**`, сравнить множества, тексты и порядок; число `**Вариант N` == число `*Сравнение.*`; «Ответ» до вариантов; поиск «глава N»/«§N.» с N > текущей и маркеров догадок (лукбихайнд `(?<![а-яёa-z])`); баланс ограждений; кириллица в печатаемых строках C; ссылки вида `C11 (гл. …)` вычёркивать. (2) **Новое и полезное:** `check_listings.js` сверяет каждую содержательную строку C-листингов решений с файлами стенда — ловит расхождение текста с реально собиравшимся исходником (в гл. 12 поймал два).
- **Открытые пункты для сквозной ревизии (этап 6):** условие Г9.Ч3.З8 обещает порядок печати «`a: 3`, `bb: 2`, `ccc: 1` — в порядке корзин», а фактический порядок корзин — `bb`(0), `a`(1), `ccc`(3) (счётчики верны; в решении оговорено с пересчётом хешей).
- **Полезные факты гл. 12 (журнал §10, запись 2026-07-27):** `objdump -x … | grep Subsystem` даёт три строки (две — версия подсистемы); CUI-сборка порождает лишнее окно класса `ConsoleWindowClass` с заголовком-путём к exe; `CreateWindowEx(…,480,320)` → клиентская область 464×281 (рамка 16×39); `COLOR_WINDOW`=0xFFFFFF, `COLOR_BTNFACE`=0xF0F0F0; диалог `MessageBox` — класс `#32770`, ID кнопок равны кодам возврата (6/7/2), `MB_YESNO` игнорирует закрытие; два ребёнка с одним ID компилируются молча, но два `case` с одним значением дают `error: duplicate case value`; `SetWindowText` при `ES_MULTILINE` не порождает `EN_CHANGE`, а фокус даёт `EN_SETFOCUS`=0x0100; текстовый режим накапливает `\r` (18→19→20→21→22 байта), чтение CRLF в `"r"` даёт `ftell`=18 против `fread`=17.
- Машинозависимые числа в решениях — с оговоркой «на эталонной машине». Программы с вводом прогоняются конвейером: `printf 'hi\n' | ./prog.exe`.
- Эталон верификации: C — MinGW GCC 15.2 `-std=c17 -Wall -Wextra` (Git Bash полный путь `/c/Users/Max/scoop/apps/gcc/current/bin/gcc.exe`); make 4.4.1 и ar 2.45.1 — на PATH из Git Bash (`export PATH="/c/Users/Max/scoop/apps/gcc/current/bin:$PATH"`); Rust — rustc/cargo 1.97.0 stable gnu, одиночные файлы `rustc --edition 2024`; санитайзеры/valgrind — WSL1 (gcc 13.3, valgrind 3.22; запускать `wsl -d Ubuntu-24.04 -u root bash -c '...'` из PowerShell, скрипты писать Write + `tr -d '\r'`, потоки разводить в файлы — иначе вывод перемешивается); nightly+Miri установлен; в registry закешированы rand 0.10.2 и eframe/egui 0.35.0.
- **Открытый пункт владельца (не закрывается агентом): ручная приёмка GUI глав 12 и 26** — прокрутка, живой ввод с клавиатуры, поведение на очень больших файлах, внешний вид.
- 2026-07-22, договорённость владельца: **отдельного документа про вариативность решений не существует и не будет** — работаем по §4 п. 5 и чеклисту §6, новых критериев не вводим.

Источник истины — трекер §7 и журнал §10 в `books/c-and-rust-book/C-and-Rust-learning.md` + git-история; эта память — только указатель для быстрого входа в заход. Конвенции — `mem:conventions`, окружение — `mem:tech_stack`, команды — `mem:suggested_commands`, завершение захода — `mem:task_completion`.
