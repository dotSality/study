# c-rust-book-progress

Состояние на 2026-07-23 (заход «решения главы 2» завершён):

- **Этапы 3 и 4 (главы 1–26 + ревизии обоих томов) ЗАВЕРШЕНЫ.** Идёт **этап 5 — решения**: закрыты глава 1 (`solutions/01-intro-and-tools.md`, 2026-07-22) и глава 2 (`solutions/02-basics.md`, 2026-07-23, 36 заданий, 8 вторых вариантов).
- **Следующая клетка: решения главы 3** (`03-control-flow`), дальше строго по порядку 4→26; затем сквозная финальная ревизия и итоговый отчёт этапа 6. Один файл решений за заход.
- **Приём захода этапа 5** (отработан на главах 1–2): условия копируются из главы дословно (сверяется скриптом), формат §4 — условие → пошаговое решение → выделенный **Ответ** → где уместно разбор типичной неверной попытки; вторые варианты по §4 п. 5 с абзацем «Сравнение» (глава 1 — 5 из 22, глава 2 — 8 из 36). Текст русский, код — язык главы (том I — C, том II — Rust), комментарии в коде русские, печатаемые строки латиницей. Весь код прогоняется в scratchpad, в файл идут дословные выводы и сообщения компилятора.
- **Сверочный скрипт захода** (node, писать заново под главу): извлечь из главы строки `**ГN.ЧM.Зk.**` с телом условия и из решений заголовки `### ГN.ЧM.Зk` + блоки `**Условие.**`; сравнить множества ID и нормализованные тексты условий; проверить, что у каждого `**Вариант N (…).**` есть `*Сравнение.*`; поискать маркеры догадок. Для главы 2 полезным оказался ещё один пункт: **regex-поиск упоминаний «глава N»/«§N.» с N > текущей** — так ловятся забегания вперёд (в главе 2 нашлось `%s` в варианте Г2.Ч3.З9 — материал главы 5, переписано на подписи внутри строки формата).
- **Забегания вперёд проверять и в вариантах**: глава 1 — склейка строковых литералов (§11.1.2) убрана из Г1.Ч2.З10; глава 2 — `%s` убран из варианта Г2.Ч3.З9. Ссылки-указатели («полный механизм — глава 6») допустимы, если материал не используется.
- Машинозависимые числа (объём ОЗУ, размеры файлов, значение неинициализированной переменной, результат знакового переполнения) в решениях приводятся с оговоркой «на эталонной машине».
- Программы с вводом прогоняются конвейером: `printf '7 4\n' | ./prog.exe` (в решениях так и показано — воспроизводимо и не зависит от клавиатуры).
- Эталон верификации: C — MinGW GCC 15.2 `-std=c17 -Wall -Wextra` (в Git Bash полный путь `/c/Users/Max/scoop/apps/gcc/current/bin/gcc.exe`); Rust — rustc/cargo 1.97.0 stable gnu, одиночные файлы **`rustc --edition 2024`**; санитайзеры/valgrind — WSL1; nightly+Miri установлен; в registry закешированы rand 0.10.2 и eframe/egui 0.35.0.
- **Открытый пункт владельца (не закрывается агентом): ручная приёмка GUI глав 12 и 26.**
- 2026-07-22, договорённость владельца: **отдельного документа про вариативность решений не существует и не будет** — задумывался как `c-and-rust-tasks-expand.md`, но написан не был; все упоминания удалены из §1, §4, §6, §11 (та доработка нужна была линалгебра-книге, потому что варианты добавлялись к уже завершённому тексту; здесь вариативность закладывается сразу). Работаем по §4 п. 5 и чеклисту §6, новых критериев не вводим.

Источник истины — трекер §7 и журнал §10 в `books/c-and-rust-book/C-and-Rust-learning.md` + git-история; эта память — только указатель для быстрого входа в заход. Конвенции — `mem:conventions`, окружение — `mem:tech_stack`, команды — `mem:suggested_commands`, завершение захода — `mem:task_completion`.
