# c-rust-book-progress

Состояние на 2026-07-24 (заход «решения главы 9» завершён):

- **Этапы 3 и 4 (главы 1–26 + ревизии обоих томов) ЗАВЕРШЕНЫ.** Идёт **этап 5 — решения**: закрыты главы 1 (2026-07-22), 2–4 (2026-07-23), 5–8 (2026-07-24) и 9 (`solutions/09-data-structures.md`, 2026-07-24: 30 заданий, 7 вторых вариантов, 4 разбора намеренно сломанных версий; 31 корректная программа + 4 демонстрации ошибок прогнаны под ASan в WSL — у корректных чисто).
- **Следующая клетка: решения главы 10** (`10-files-io`), дальше строго по порядку 11→26; затем сквозная финальная ревизия и итоговый отчёт этапа 6. Один файл решений за заход. Для глав с кучей код решений дополнительно прогонять ASan/Valgrind в WSL1 (`wsl -d Ubuntu-24.04 -u root bash -c '...'` — запускать из PowerShell, не из Git Bash; скрипты писать Write + `tr -d '\r'`).
- **Приём захода этапа 5** (отработан на главах 1–9): условия копируются из главы дословно (сверяется скриптом), формат §4 — условие → пошаговое решение → выделенный **Ответ** → **вторые варианты после «Ответа»** (порядок Ответ → `**Вариант N`  → `*Сравнение.*`; в первой редакции файла гл. 9 «Ответ» стоял после вариантов — исправлено, проверка добавлена в скрипт) → где уместно разбор типичной неверной попытки (неверные, но не-UB версии реально прогоняются, вывод дословный). Доля вторых вариантов ≈22–25 %: гл. 1 — 5/22, 2 — 8/36, 3 — 6/24, 4 — 7/32, 5 — 5/22, 6 — 7/30, 7 — 5/20, 8 — 5/20, 9 — 7/30. Текст русский, код — язык главы, комментарии в коде русские, **печатаемые строки латиницей**. Весь код прогоняется в scratchpad, в файл идут дословные выводы и сообщения компилятора. UB-версии — только сборка; отчёты ASan/Valgrind — реальный вывод с сокращениями как в главе (пути до имён файлов, PID → `==...==`, служебные кадры → `…`).
- **Приём сборки стенда (гл. 9, экономит время):** общие определения главы вынести в файлы-«прелюдии» (`pre_vec.c`, `pre_list.c`, `pre_stack.c`, `pre_queue.c`, `pre_table.c`, `pre_bst.c`), тела решений писать отдельно и склеивать `cat prelude body > prog.c` — в книге листинг подаётся с комментарием-ссылкой `/* … — как в §9.1.2 */`, как это делает сама глава.
- **Сверочный скрипт захода** (node, писать заново под главу; для гл. 9 — `verify_ch9.js`): извлечь из главы `**ГN.ЧM.Зk.**` с телом условия и из решений `### ГN.ЧM.Зk` + `**Условие.**`…`**Решение.**`; сравнить множества ID, тексты и порядок секций; число `**Вариант N` == число `*Сравнение.*`; наличие `**Решение.**`/`**Ответ:**` и порядок «Ответ до вариантов»; поиск «глава N»/«§N.» с N > текущей и маркеров догадок. **Разбор строго построчный** (regex с `m` режет многострочные условия). Гл. 9: 30/30, условия дословны, 7 ↔ 7, единственная ссылка вперёд — «гл. 10» в Ч2.З9.
- **Открытый пункт для сквозной ревизии (этап 6):** условие Г9.Ч3.З8 обещает порядок печати «`a: 3`, `bb: 2`, `ccc: 1` — в порядке корзин», а фактический порядок корзин — `bb`(0), `a`(1), `ccc`(3) (счётчики верны). В решении оговорено с пересчётом хешей; формулировку главы поправить при финальной ревизии.
- **Полезные факты гл. 9 (журнал §10, запись 2026-07-24 (5)):** обратный порядок перецепки в `insert_after` ASan **не ловит** (нет ошибочных доступов) — только утечка на выходе, Valgrind: `64 (32 direct, 32 indirect) bytes definitely lost`; `push_front` по значению собирается без предупреждений и рапортует об успехе (ловится лишь как `32 byte(s) leaked in 2 allocation(s)`); хеши K&R при `NBUCKETS = 8`: `"ab"`→1, `"a"`→1, `"bb"`→0, `"ccc"`→3.
- Машинозависимые числа в решениях — с оговоркой «на эталонной машине». Программы с вводом прогоняются конвейером: `printf 'hi\n' | ./prog.exe`.
- Эталон верификации: C — MinGW GCC 15.2 `-std=c17 -Wall -Wextra` (Git Bash полный путь `/c/Users/Max/scoop/apps/gcc/current/bin/gcc.exe`); Rust — rustc/cargo 1.97.0 stable gnu, одиночные файлы `rustc --edition 2024`; санитайзеры/valgrind — WSL1 (gcc 13.3, valgrind 3.22); nightly+Miri установлен; в registry закешированы rand 0.10.2 и eframe/egui 0.35.0.
- **Открытый пункт владельца (не закрывается агентом): ручная приёмка GUI глав 12 и 26.**
- 2026-07-22, договорённость владельца: **отдельного документа про вариативность решений не существует и не будет** — работаем по §4 п. 5 и чеклисту §6, новых критериев не вводим.

Источник истины — трекер §7 и журнал §10 в `books/c-and-rust-book/C-and-Rust-learning.md` + git-история; эта память — только указатель для быстрого входа в заход. Конвенции — `mem:conventions`, окружение — `mem:tech_stack`, команды — `mem:suggested_commands`, завершение захода — `mem:task_completion`.
