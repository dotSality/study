# c-rust-book-progress

Состояние на 2026-07-23 (заход «решения главы 4» завершён):

- **Этапы 3 и 4 (главы 1–26 + ревизии обоих томов) ЗАВЕРШЕНЫ.** Идёт **этап 5 — решения**: закрыты главы 1 (`solutions/01-intro-and-tools.md`, 2026-07-22), 2 (`solutions/02-basics.md`, 2026-07-23, 36 заданий, 8 вторых вариантов), 3 (`solutions/03-control-flow.md`, 2026-07-23, 24 задания, 6 вторых вариантов) и 4 (`solutions/04-functions.md`, 2026-07-23, 32 задания, 7 вторых вариантов).
- **Следующая клетка: решения главы 5** (`05-arrays-strings`), дальше строго по порядку 6→26; затем сквозная финальная ревизия и итоговый отчёт этапа 6. Один файл решений за заход.
- **Приём захода этапа 5** (отработан на главах 1–4): условия копируются из главы дословно (сверяется скриптом), формат §4 — условие → пошаговое решение → выделенный **Ответ** → где уместно разбор типичной неверной попытки; вторые варианты по §4 п. 5 с абзацем «Сравнение» (гл. 1 — 5 из 22, гл. 2 — 8 из 36, гл. 3 — 6 из 24, гл. 4 — 7 из 32, ≈22–25 %). Текст русский, код — язык главы (том I — C, том II — Rust), комментарии в коде русские, печатаемые строки латиницей. Весь код прогоняется в scratchpad, в файл идут дословные выводы и сообщения компилятора.
- **Сверочный скрипт захода** (node, писать заново под главу; для гл. 4 — `verify_ch4.js`): извлечь из главы строки `**ГN.ЧM.Зk.**` с телом условия (включая код-блоки) и из решений заголовки `### ГN.ЧM.Зk` + блоки `**Условие.**`; нормализовать пробелы и сравнить множества ID и тексты условий; проверить, что число `**Вариант N` == число `*Сравнение.*` в каждом разделе; regex-поиск упоминаний «глава N»/«§N.» с N > текущей (забегания). Для гл. 4 всё прошло: 32/32 ID, условия дословны, 7 вариантов ↔ 7 «Сравнение»; forward-упоминания — только указатели на главу 6 (указатели) и главу 13 (UB), материал не используется — допустимо.
- **Забегания вперёд** проверять и в вариантах. Ссылки-указатели («механизм — глава 6», «UB — глава 13») допустимы, если материал не используется. В коде гл. 4 использованы только printf/scanf, ветвления/циклы, функции/`static`/рекурсия, скалярные типы — без массивов (гл. 5), указателей (гл. 6), `%s`/строк.
- Машинозависимые числа (объём ОЗУ, размеры файлов, значение неинициализированной переменной, результат знакового переполнения/UB) в решениях — с оговоркой «на эталонной машине». Пример гл. 4: сломанная `max2(3,9)` без `return` вернула `3` (UB).
- **Полезные факты гл. 4 (MinGW GCC 15.2):** отсутствие `return` в non-void → `warning: control reaches end of non-void function [-Wreturn-type]`; вызов необъявленной функции под `-std=c17` → `error: implicit declaration of function ... [-Wimplicit-function-declaration]`; бесконечная рекурсия ловится статически при `-Wall`: `warning: infinite recursion detected [-Winfinite-recursion]`; краш от переполнения стека под Git Bash/MSYS — exit 127 без вывода (в книге описываю качественно, без кода-числа). Аргументы `printf` на эталоне вычисляются справа налево (`g(),g(),g()` со `static` → `3 2 1`).
- Программы с вводом прогоняются конвейером: `printf '1 2 17 91 97\n' | ./prog.exe`.
- Эталон верификации: C — MinGW GCC 15.2 `-std=c17 -Wall -Wextra` (Git Bash полный путь `/c/Users/Max/scoop/apps/gcc/current/bin/gcc.exe`); Rust — rustc/cargo 1.97.0 stable gnu, одиночные файлы `rustc --edition 2024`; санитайзеры/valgrind — WSL1; nightly+Miri установлен; в registry закешированы rand 0.10.2 и eframe/egui 0.35.0.
- **Открытый пункт владельца (не закрывается агентом): ручная приёмка GUI глав 12 и 26.**
- 2026-07-22, договорённость владельца: **отдельного документа про вариативность решений не существует и не будет** — работаем по §4 п. 5 и чеклисту §6, новых критериев не вводим.

Источник истины — трекер §7 и журнал §10 в `books/c-and-rust-book/C-and-Rust-learning.md` + git-история; эта память — только указатель для быстрого входа в заход. Конвенции — `mem:conventions`, окружение — `mem:tech_stack`, команды — `mem:suggested_commands`, завершение захода — `mem:task_completion`.
