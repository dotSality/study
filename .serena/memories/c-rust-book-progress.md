# c-rust-book-progress

Состояние на 2026-07-26 (заход «решения главы 10» завершён):

- **Этапы 3 и 4 (главы 1–26 + ревизии обоих томов) ЗАВЕРШЕНЫ.** Идёт **этап 5 — решения**: закрыты главы 1 (2026-07-22), 2–4 (2026-07-23), 5–9 (2026-07-24) и 10 (`solutions/10-files-io.md`, 2026-07-26: 20 заданий, 5 вторых вариантов, 6 разборов неверных подходов; 31 программа на MinGW GCC без предупреждений, ASan на Linux чист у всех корректных).
- **Следующая клетка: решения главы 11** (`11-preprocessor-build`), дальше строго по порядку 12→26; затем сквозная финальная ревизия и итоговый отчёт этапа 6. Один файл решений за заход. Для глав с кучей код решений дополнительно прогонять ASan/Valgrind в WSL1 (`wsl -d Ubuntu-24.04 -u root bash -c '...'` — запускать из PowerShell, не из Git Bash; скрипты писать Write + `tr -d '\r'`).
- **Приём захода этапа 5** (отработан на главах 1–10): условия копируются из главы дословно (сверяется скриптом), формат §4 — условие → пошаговое решение → выделенный **Ответ** → **вторые варианты после «Ответа»** (порядок Ответ → `**Вариант N` → `*Сравнение.*`) → где уместно разбор типичной неверной попытки (неверные, но не-UB версии реально прогоняются, вывод дословный). Доля вторых вариантов ≈22–25 %: гл. 1 — 5/22, 2 — 8/36, 3 — 6/24, 4 — 7/32, 5 — 5/22, 6 — 7/30, 7 — 5/20, 8 — 5/20, 9 — 7/30, 10 — 5/20. Текст русский, код — язык главы, комментарии в коде русские, **печатаемые строки латиницей**. Весь код прогоняется в scratchpad, в файл идут дословные выводы и сообщения компилятора. UB-версии — только сборка; отчёты ASan/Valgrind — реальный вывод с сокращениями как в главе (пути до имён файлов, PID → `==...==`, служебные кадры → `…`).
- **Приём сборки стенда:** гл. 9 — файлы-«прелюдии» + `cat prelude body > prog.c`; гл. 10 — по программе на задание в `src/`, драйвер-скрипт компилирует каждую в **свой** каталог `run/<имя>` (программы главы создают файлы в текущей папке) и пишет stdout/stderr **раздельно** (в заданиях про `stderr` это принципиально). В книге повторяющиеся определения подаются комментарием-ссылкой (`/* Vec, vec_init … — как в §9.1.1 */`).
- **Сверочный скрипт захода** (node, писать заново под главу; последний — `verify_ch10.js`): извлечь из главы `**ГN.ЧM.Зk.**` с телом условия и из решений `### ГN.ЧM.Зk` + `**Условие.**`…`**Решение.**`; сравнить множества ID, тексты и порядок секций; число `**Вариант N` == число `*Сравнение.*`; наличие `**Решение.**`/`**Ответ:**` и порядок «Ответ до вариантов»; поиск «глава N»/«§N.» с N > текущей и маркеров догадок; баланс ограждений кода. **Разбор строго построчный** (regex с `m` режет многострочные условия). **Обязательно вычёркивать ссылки на главы источников** (`C2 (гл. 22)`, `C1 (прил. B1)`) — иначе ложные срабатывания в блоке «Источники». Гл. 10: 20/20, всё чисто.
- **Открытый пункт для сквозной ревизии (этап 6):** условие Г9.Ч3.З8 обещает порядок печати «`a: 3`, `bb: 2`, `ccc: 1` — в порядке корзин», а фактический порядок корзин — `bb`(0), `a`(1), `ccc`(3) (счётчики верны). В решении оговорено с пересчётом хешей; формулировку главы поправить при финальной ревизии.
- **Полезные факты гл. 10 (журнал §10, запись 2026-07-26):** массив `int` с байтом `0x0a`, записанный без `"b"`, на Windows растёт 12→13 байт и читается как `2573 5120 7680` (на Linux дефекта нет); `-funsigned-char` даёт `-Wtype-limits` на `while ((char_c = fgetc(f)) != EOF)`, а знаковая сборка молчит, теряя все данные; `fopen` каталога как файла → `Permission denied`; частичный элемент `fread` в буфер не переносится (байты-маркеры уцелели), возврат неотличим от «файл ровно из N записей»; `fwrite(&Vec, sizeof, …)` пишет 16 байт с адресом, разным между запусками; текстовое сохранение вектора — 23 байта на Windows против 17 на Linux.
- Машинозависимые числа в решениях — с оговоркой «на эталонной машине». Программы с вводом прогоняются конвейером: `printf 'hi\n' | ./prog.exe`.
- Эталон верификации: C — MinGW GCC 15.2 `-std=c17 -Wall -Wextra` (Git Bash полный путь `/c/Users/Max/scoop/apps/gcc/current/bin/gcc.exe`); Rust — rustc/cargo 1.97.0 stable gnu, одиночные файлы `rustc --edition 2024`; санитайзеры/valgrind — WSL1 (gcc 13.3, valgrind 3.22); nightly+Miri установлен; в registry закешированы rand 0.10.2 и eframe/egui 0.35.0.
- **Открытый пункт владельца (не закрывается агентом): ручная приёмка GUI глав 12 и 26.**
- 2026-07-22, договорённость владельца: **отдельного документа про вариативность решений не существует и не будет** — работаем по §4 п. 5 и чеклисту §6, новых критериев не вводим.

Источник истины — трекер §7 и журнал §10 в `books/c-and-rust-book/C-and-Rust-learning.md` + git-история; эта память — только указатель для быстрого входа в заход. Конвенции — `mem:conventions`, окружение — `mem:tech_stack`, команды — `mem:suggested_commands`, завершение захода — `mem:task_completion`.
