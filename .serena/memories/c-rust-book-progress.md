# c-rust-book-progress

Состояние на 2026-07-31 (заход «решения главы 24» завершён):

- **Этапы 3 и 4 (главы 1–26 + ревизии обоих томов) ЗАВЕРШЕНЫ.** Идёт **этап 5 — решения**: **том I закрыт целиком** (главы 1–13), в томе II закрыты главы 14–23 и **24 `unsafe-ffi`** (2026-07-31). Файл `solutions/24-unsafe-ffi.md`: 18 заданий (9 + 9), 5 вторых вариантов (28 %), 141 КБ; стенд двухкомпиляторный — одиночные файлы `u*`/`f*`, шесть статических библиотек в подкаталогах (`clamp`, `point3`, `mylib`, `imax`, `divlib`, `stats`) и два cargo-пакета под Miri.
- **Следующая клетка: решения главы 25** (`25-capstone`). Дальше 26; затем сквозная финальная ревизия и итоговый отчёт этапа 6. Один файл решений за заход. Внимание: главы 25 и 26 — проектные (чекпоинты вместо параграфов), заданий там меньше, но код крупнее.
- **Приём захода этапа 5** (отработан на главах 1–24): условия копируются из главы дословно, формат §4 — условие → пошаговое решение → выделенный **Ответ** → **вторые варианты после «Ответа»** (`**Вариант N (имя подхода).**` → `*Сравнение.*`) → где уместно разбор типичной неверной попытки (неверные версии реально прогоняются, вывод дословный). Доля вторых вариантов ≈22–30 %: гл. 20–23 — по 6/20, гл. 24 — 5/18. Текст русский, код — язык главы, комментарии русские, **печатаемые строки латиницей** (в гл. 24 кириллица осталась только в двух строках, заданных условием Г24.Ч1.З5).
- **Условия не переписываются руками:** секции пишутся в scratchpad с плейсхолдерами `{{COND:Г24.ЧM.Зk}}`, `assemble.js` подставляет условия из главы. **Извлечение обязано быть многострочным** (переписано в заходе 24): условия глав содержат блоки кода, построчная регулярка прошлых заходов их обрезает. Правило: резать от строки `**ID.**` до следующего `**ID.**` / `**Источники части` / заголовка.
- **Эталон тома II:** `rustc/cargo 1.97.0` stable, host `x86_64-pc-windows-gnu`, издание 2024; одиночные файлы — `rustc --edition 2024`. C-сторона: MinGW GCC 15.2 `-std=c17 -Wall -Wextra`, `ar`/`nm` оттуда же. **rustc/cargo не видны на PATH инструмента Bash** — `export PATH="/c/Users/Max/.cargo/bin:$PATH"`; gcc — только полным путём `/c/Users/Max/scoop/apps/gcc/current/bin/gcc.exe`. Файлы с кириллицей писать инструментом Write. Miri стоит в nightly, запускается только через cargo (`cargo +nightly miri run`), поэтому под него заводятся отдельные мини-пакеты.
- **Смешанная сборка (гл. 24, проверено):** `rustc --edition 2024 --crate-type=staticlib lib.rs -o libX.a` даёт архив ~15 МБ (внутри вся std); список системных библиотек берётся у rustc — `--print native-static-libs` печатает `-lkernel32 -lntdll -luserenv -lws2_32 -ldbghelp` (ключу нужен `-o`). C-клиент: `gcc … main.c -L . -lX <syslibs>`. Обратное направление: `#[link(name="X", kind="static")]` либо `-l static=X` в командной строке rustc.
- **Полезные факты гл. 24:** Miri на выходе за границу винит **арифметику** (`in-bounds pointer arithmetic failed`), а не чтение — подтверждает «сам `add` за границу уже UB»; потерянный параметр в extern-блоке линкуется молча и делает два одинаковых вызова `c_add(2)` разными (2 и 3); `abort` на границе уносит несброшенный stdout C-стороны (exit 127); `Option<&mut T>` в `extern "C"` проходит без `improper_ctypes` и позволяет объявить экспорт **без** `unsafe`; `strlen` от `String::as_ptr()` дал 6 при `len == 5`; `CString::new` с внутренним нулём → `Err(NulError(2, [...]))`.
- **Сверочные скрипты захода** (node, писать заново под главу; последние — `verify_ch24.js` + `check_listings_ch24.js`, **не коммитятся и удаляются после захода**): ID глава↔решения, дословность условий, «Вариант N» == «Сравнение» и доля, «Ответ» в каждом задании и до вариантов, забегания вперёд (для гл. 25 искать `egui`/`eframe`; для 26 — конструкции 25: `process::exit`, `Instant`, `read_to_string`, пул потоков), маркеры догадок, баланс ограждений, кириллица в печатаемых строках, ссылки `§N.M(.k)` против реальных заголовков (**ссылки в стандарт C вида `C5 §7.24.6.1` изымать перед проверкой**), концы строк, существование файлов стенда. `check_listings` сверяет непрерывность листингов и строки выводов по логу полного перепрогона; уточнения гл. 24: **блоки кода внутри «Условия» проверять по стенду нельзя** (их источник — глава), волатильные величины (адреса `0x…`, номер потока в скобках) нормализовать перед сверкой.
- **Грабли захода 24 (важно):** в JS-строке журнальной записи `"ab\0cd"` превращается в **настоящий NUL-байт** — операционный файл становится «бинарным» для git (`w/-text`), и `git diff` показывает переписанным весь файл. Проверять после правки: `d.count(bytes([0])) == 0`. Патчи через python-heredoc с обратными косыми рвутся молча (двойная косая схлопывается) — писать скрипт файлом инструментом Write, а не heredoc'ом.
- **Концы строк — уточнено:** в репозитории действует `.gitattributes` (`text=auto eol=lf`), поэтому **в индексе всё LF**; рабочая копия операционного файла — CRLF (`git ls-files --eol` даёт `i/lf w/crlf`), и git сам нормализует при коммите, предупреждая «CRLF will be replaced by LF». Главы 21–24 и файлы решений в рабочей копии — LF. Правки операционного файла делать node-скриптом, вставляющим `\r\n`, и проверять, что одиночных LF осталось 0.
- **Открытые пункты для сквозной ревизии (этап 6):**
  - гл. 9: условие Г9.Ч3.З8 обещает порядок печати «`a: 3`, `bb: 2`, `ccc: 1` — в порядке корзин», фактический порядок корзин — `bb`(0), `a`(1), `ccc`(3).
  - гл. 13 (три расхождения, подробно — в конце `solutions/13-ub-and-bridge.md`): (1) у `oob.c` §13.1.2 нет «чистого» запуска; (2) условие Г13.Ч1.З8 неверно утверждает несовместимость ASan и UBSan; (3) номера строк в отчётах UBSan §13.1.4.
  - гл. 14: §14.1.1 приводит вывод `rustup show` сокращённо.
  - **гл. 16 ↔ 17:** §16.2.2 обещает «передавать разбор по ссылке научимся на коллекциях (глава 17)», но глава 17 этого не вводит — приём предъявлен во втором варианте Г17.Ч1.З8.
  - **гл. 20:** §20.1.3, ⛔-пример 4 — вывод E0433 приведён без блока `help: consider importing this module through its public re-export`.
  - **гл. 21:** §21.1.1 про `add_one_v3(30u32)` — «иначе компилятору не из чего вывести тип `x`»: без суффикса программа собирается (умолчание `i32`); §21.2.2, пример 1 — при `()` из `map` rustc 1.97 печатает **два** предупреждения.
  - **гл. 22:** §22.1.1 объясняет печать `{b}` прозрачностью `Deref` — на деле за это отвечает `impl Display for Box<T>`.
  - **гл. 23:** таблица §23.2.3 ставит прочерк в столбце Sync у `Sender<T>`/`Receiver<T>`, тогда как на 1.97 `Sender<T>: Sync` есть, а у `Receiver<T>` нет.
  - **гл. 24 (новое, разобрано в разделе «Уточнения» файла решений):** (1) условие Г24.Ч2.З1 просит обосновать пометку `safe` у `labs`, но C5 §7.24.6.1 объявляет поведение неопределённым при непредставимом результате — то есть на `c_long::MIN`; (2) §24.2.3 приводит вывод паники на границе сокращённо — реальный содержит `note: run with RUST_BACKTRACE=1` и весь блок `stack backtrace:`; (3) терминология: §16.2.3 — «niche-оптимизация», §24.2.2 — «нишевая оптимизация».
- Машинозависимые числа в решениях — с оговоркой «на эталонной машине». Санитайзеры/valgrind — WSL1; **WSL из инструмента Bash с путём `/mnt/c/...` ломается конверсией путей MSYS — вызывать через инструмент PowerShell** (так же читается рабочая копия стандарта C `/root/n3220.txt`).
- **Прогон GUI-программ (гл. 12 и 26):** служебный водитель PowerShell + P/Invoke (`drv.ps1`). **Открытый пункт владельца (не закрывается агентом): ручная приёмка GUI глав 12 и 26.**
- 2026-07-22, договорённость владельца: **отдельного документа про вариативность решений не существует и не будет** — работаем по §4 п. 5 и чеклисту §6.

Источник истины — трекер §7 и журнал §10 в `books/c-and-rust-book/C-and-Rust-learning.md` + git-история; эта память — только указатель для быстрого входа в заход. Конвенции — `mem:conventions`, окружение — `mem:tech_stack`, команды — `mem:suggested_commands`, завершение захода — `mem:task_completion`.
