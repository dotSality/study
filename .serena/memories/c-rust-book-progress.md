# c-rust-book-progress

Состояние на 2026-07-30 (заход «решения главы 21» завершён):

- **Этапы 3 и 4 (главы 1–26 + ревизии обоих томов) ЗАВЕРШЕНЫ.** Идёт **этап 5 — решения**: **том I закрыт целиком** (главы 1–13), в томе II закрыты главы 14 `rust-start`, 15 `ownership`, 16 `structs-enums`, 17 `collections-strings`, 18 `error-handling`, 19 `generics-traits` (2026-07-29), 20 `modules-cargo-tests` и 21 `functional` (обе 2026-07-30). Файл `solutions/21-functional.md`: 20 заданий (10 + 10), 6 вторых вариантов, стенд из одиночных файлов `c*`/`i*` и **6 cargo-пакетов** плюс две копии проекта `textstat` из §20.1.4 (`p2/before`, `p2/after`).
- **Следующая клетка: решения главы 22** (`22-smart-pointers`). Дальше строго по порядку 23→26; затем сквозная финальная ревизия и итоговый отчёт этапа 6. Один файл решений за заход.
- **Приём захода этапа 5** (отработан на главах 1–21): условия копируются из главы дословно (сверяется скриптом), формат §4 — условие → пошаговое решение → выделенный **Ответ** → **вторые варианты после «Ответа»** (порядок Ответ → `**Вариант N (имя подхода).**` → `*Сравнение.*`) → где уместно разбор типичной неверной попытки (неверные, но собирающиеся версии реально прогоняются, вывод дословный). Доля вторых вариантов ≈22–30 %: гл. 1 — 5/22, 2 — 8/36, 3 — 6/24, 4 — 7/32, 5 — 5/22, 6 — 7/30, 7 — 5/20, 8 — 5/20, 9 — 7/30, 10 — 5/20, 11 — 5/20, 12 — 4/18, 13 — 4/17, 14 — 5/20, 15 — 7/29, 16 — 5/20, 17 — 6/20, 18 — 6/20, 19 — 9/30, 20 — 6/20, 21 — 6/20. Текст русский, код — язык главы, комментарии в коде русские, **печатаемые строки латиницей** (исключение — если кириллица задана условием или самой главой; в гл. 21 таких строк 16).
- **Эталон тома II:** `rustc/cargo 1.97.0` stable, host `x86_64-pc-windows-gnu`, издание 2024. Одиночные файлы — **`rustc --edition 2024`** (голый rustc собирает в издании 2015 — ловушка), релиз — `rustc --edition 2024 -O`; проекты — `cargo new/build/run/test/doc/fmt/clippy/clean`. **rustc/cargo не видны на PATH инструмента Bash** — прогоны вести bash-скриптом с `export PATH="/c/Users/Max/.cargo/bin:$PATH"`, сам скрипт запускать через `tr -d '\r'`; файлы `.rs` с кириллицей писать инструментом Write **или генератором на node** (в гл. 21 весь стенд создавался одним `gen_p*.js` с `String.raw`-литералами — 2 вызова Write вместо 45; в шаблонах не должно быть backtick и `${`). **WSL из инструмента Bash с путём `/mnt/c/...` ломается конверсией путей MSYS — вызывать через инструмент PowerShell.** Реестр crates.io доступен.
- **Приём сборки стенда тома II:** каталог на часть главы, один bash-скрипт на каталог, вывод в `log_*.txt`, читается одним `cat`. **Имена файлов стенда попадают в сообщения компилятора** — разводить их префиксами по частям (гл. 15: `o*`/`b*`/`s*`; 16: `s*`/`m*`; 17: `v*`/`h*`; 18: `p*`/`d*`; 19: `g*`/`t*`/`l*`; 20: `m*` + пакеты; 21: `c*` замыкания / `i*` итераторы). Состояния проекта «до/после» — отдельными каталогами с **одинаковым именем пакета** (тогда отчёты `cargo test` сравнимы построчно и совпадают хеши тестовых бинарников). **Грабли прогонщика (гл. 21):** при ошибке сборки старый `.exe` остаётся и запускается — в функции запуска `rm -f "${f%.rs}.exe"` **до** `rustc`, иначе в лог попадёт вывод прошлой версии. **Волатильные строки:** `Finished ... in X.XXs` и порядок строк `test ... ok` меняются от прогона к прогону — после любой пересборки лог перегенерировать целиком и сверять цитаты с последним.
- **Сверочные скрипты захода** (node, писать заново под главу; последние — `verify_ch21.js` + `check_listings_ch21.js`, **не коммитятся и удаляются после захода**): (1) ID глава↔решения (множество и порядок), дословность условий (нормализация пробелов), число `**Вариант N` == число `*Сравнение.*` и доля, «Ответ» в каждом задании и до вариантов, забегания вперёд (поиск в **коде** решений конструкций последующих глав), маркеры догадок, баланс ограждений, кириллица в печатаемых строках (список печатается для ручной сверки), перекрёстные ссылки `§N.M(.k)` против реальных заголовков глав, концы строк. (2) `check_listings` сверяет, что каждый листинг — **непрерывный фрагмент** какого-либо файла стенда, и что **каждая строка блоков вывода найдена в логах**; адрес листинга — упоминание пути **не дальше 400 символов** до блока (иначе сводки кода в «Ответе» ложно привязываются к последнему названному файлу), строки `$ ...` (приглашение оболочки) из сверки исключать, сокращения `...` сверять регуляркой с `.*`. Грабли: в JS `\w` и `\b` не работают с кириллицей — границы задавать классом `[^а-яёА-ЯЁ]`. **Концы строк: операционный файл `C-and-Rust-learning.md` и главы тома I — CRLF, но `chapters/21-functional.md` — LF (не универсально!), файлы решений — LF**; правки операционного файла делать node-скриптом, вставляющим `\r\n`, и проверять, что одиночных LF осталось 0.
- **Полезные факты гл. 21 (журнал §10, запись 2026-07-30 (2)):** литерал без суффикса даёт замыканию **умолчание `i32`** — `|x| x + 1` с `add_one_v3(30)` собирается (формулировка §21.1.1 неточна, см. открытые пункты); настоящая граница вывода — **E0284** у ни разу не вызванного замыкания; `Fn()` это `Fn() -> ()`, поэтому `|| v.len()` под `probe_fn` даёт **E0308** (не про трейт) с подсказкой про точку с запятой, а `Vec::len` не `#[must_use]`; изменяющий **метод** под `Fn` — **E0596** (присваивание — E0594, как в главе); `f(f(2))` для `FnMut` — **E0499**; **`sort`/`sort_by` стабильны**, поэтому «сначала второстепенный ключ, потом главный» = двухключевой компаратор, а `sort_by_key` с ключом-кортежем `(-(score as i64), name)` компилируется; `sort_by_key` на 3 элементах зовёт ключ 6 раз; `map` с замыканием, возвращающим `()`, даёт **два** предупреждения (`unused Map` + линт `map_unit_fn` с советом `for_each`); реконструкция `textstat` дала **тот же хеш** `textstat-471d3f27ef1b1f7e.exe`, что в §20.2.1.
- **Полезные факты глав 14–20** — в журнале §10 (записи 2026-07-27…2026-07-30): мономорфизация через `--emit=llvm-ir`; E0515 вместо E0106; минимальный derive для ключа `HashMap`; `realloc` и ASan `heap-use-after-free`; `main -> Result` даёт код 1 против 101 у паники; в гл. 20 — E0255/E0616/E0451, workspace, clippy `manual_clamp`, doc-тесты, `cargo fmt --check --color=never`.
- **Открытые пункты для сквозной ревизии (этап 6):**
  - гл. 9: условие Г9.Ч3.З8 обещает порядок печати «`a: 3`, `bb: 2`, `ccc: 1` — в порядке корзин», фактический порядок корзин — `bb`(0), `a`(1), `ccc`(3).
  - гл. 13 (три расхождения, подробно — в конце `solutions/13-ub-and-bridge.md`): (1) у `oob.c` §13.1.2 нет «чистого» запуска — правка `argc + 2`; (2) условие Г13.Ч1.З8 неверно утверждает несовместимость ASan и UBSan (несовместимы ASan и TSan); (3) номера строк в отчётах UBSan §13.1.4 — для файлов без первой строки-комментария.
  - гл. 14: §14.1.1 приводит вывод `rustup show` сокращённо.
  - гл. 15, 16, 18 и 19: расхождений по существу нет, только ручные переносы длинных строк `note:`/`help:` (оговорены в преамбулах решений).
  - **гл. 16 ↔ 17:** §16.2.2 обещает «передавать разбор по ссылке научимся на коллекциях (глава 17)», но глава 17 разбор по ссылке не вводит — приём предъявлен во втором варианте Г17.Ч1.З8 с оговоркой и ссылкой на R7.
  - **гл. 20:** §20.1.3, ⛔-пример 4 — вывод E0433 приведён без блока `help: consider importing this module through its public re-export`, который rustc 1.97 печатает; в решении Г20.Ч1.З5 вывод дан полностью.
  - **гл. 21 (новое):** §21.1.1 утверждает про `add_one_v3(30u32)` — «иначе компилятору не из чего вывести тип `x`»; проверено запуском: без суффикса программа собирается и печатает 31 (умолчание `i32`, §14.2.2). Суффикс выравнивает типы трёх записей примера, а не спасает вывод; настоящий случай «выводить не из чего» — E0284 у невызванного замыкания. Разобрано в решении Г21.Ч1.З1.
  - **гл. 21, мелочь:** §21.2.2, пример 1 показывает одно предупреждение на цепочке без потребителя; если замыкание `map` возвращает `()` (как в задании Г21.Ч2.З4), rustc 1.97 печатает **два** — добавляется линт `map_unit_fn`. Разбор — в решении Г21.Ч2.З4.
- Машинозависимые числа в решениях — с оговоркой «на эталонной машине». Программы с вводом прогоняются конвейером: `printf 'hi\n' | ./prog.exe`.
- Эталон тома I: C — MinGW GCC 15.2 `-std=c17 -Wall -Wextra` (Git Bash полный путь `/c/Users/Max/scoop/apps/gcc/current/bin/gcc.exe`); make 4.4.1 и ar 2.45.1 — на PATH из Git Bash; санитайзеры/valgrind — WSL1 (gcc 13.3, valgrind 3.22; `wsl -d Ubuntu-24.04 -u root bash ...`); nightly+Miri установлен; в registry закешированы rand 0.10.2 и eframe/egui 0.35.0.
- **Прогон GUI-программ (гл. 12 и 26):** служебный водитель PowerShell + P/Invoke (`drv.ps1`) шлёт окнам сообщения системы — `BM_CLICK`, `WM_LBUTTONDOWN`/`UP`, `WM_CHAR`, `WM_SYSCOMMAND`+`SC_CLOSE`. Грабли: `SendMessage(BM_CLICK)` зависает при модальном `MessageBox` (нужен `PostMessage`); содержимое `EDIT` чужого процесса читается через `WM_GETTEXT`; `$null` в string-параметрах P/Invoke — `[NullString]::Value`.
- **Открытый пункт владельца (не закрывается агентом): ручная приёмка GUI глав 12 и 26.**
- 2026-07-22, договорённость владельца: **отдельного документа про вариативность решений не существует и не будет** — работаем по §4 п. 5 и чеклисту §6.

Источник истины — трекер §7 и журнал §10 в `books/c-and-rust-book/C-and-Rust-learning.md` + git-история; эта память — только указатель для быстрого входа в заход. Конвенции — `mem:conventions`, окружение — `mem:tech_stack`, команды — `mem:suggested_commands`, завершение захода — `mem:task_completion`.
