# c-rust-book-progress

Состояние на 2026-07-27 (заход «решения главы 13» завершён):

- **Этапы 3 и 4 (главы 1–26 + ревизии обоих томов) ЗАВЕРШЕНЫ.** Идёт **этап 5 — решения**: **том I закрыт целиком** — главы 1 (2026-07-22), 2–4 (2026-07-23), 5–9 (2026-07-24), 10 и 11 (2026-07-26), 12 и 13 (2026-07-27). Последний файл — `solutions/13-ub-and-bridge.md`: 17 заданий, 4 вторых варианта, стенд из 28 файлов C + 12 витрин Rust.
- **Следующая клетка: решения главы 14** (`14-rust-start`) — начало тома II в этапе 5. Эталон тома II: `rustc/cargo 1.97.0` (Windows gnu), одиночные файлы — **`rustc --edition 2024`** (голый rustc собирает в издании 2015 — ловушка, зафиксирована в журнале), проекты — `cargo build && cargo test`. Дальше строго по порядку 15→26; затем сквозная финальная ревизия и итоговый отчёт этапа 6. Один файл решений за заход.
- **Приём захода этапа 5** (отработан на главах 1–13): условия копируются из главы дословно (сверяется скриптом), формат §4 — условие → пошаговое решение → выделенный **Ответ** → **вторые варианты после «Ответа»** (порядок Ответ → `**Вариант N` → `*Сравнение.*`) → где уместно разбор типичной неверной попытки (неверные, но не-UB версии реально прогоняются, вывод дословный). Доля вторых вариантов ≈22–25 %: гл. 1 — 5/22, 2 — 8/36, 3 — 6/24, 4 — 7/32, 5 — 5/22, 6 — 7/30, 7 — 5/20, 8 — 5/20, 9 — 7/30, 10 — 5/20, 11 — 5/20, 12 — 4/18, 13 — 4/17. Текст русский, код — язык главы, комментарии в коде русские, **печатаемые строки латиницей**.
- **Приём сборки стенда:** гл. 9 — файлы-«прелюдии» + `cat prelude body > prog.c`; гл. 10 — по программе на задание в `src/`, драйвер пишет stdout/stderr раздельно; гл. 11 — каталог на задание, Makefile-ы через `printf`; гл. 12 — каталог на задание + `rebuild_all.sh`; гл. 13 — каталог `z1…z9` + `rust/`, три bash-скрипта для WSL (`wsl1/2/3.sh`), потоки в файлы, вывод собирается в `log*.txt` и читается одним `cat` (иначе PowerShell теряет начало вывода).
- **Прогон GUI-программ (гл. 12 и 26):** служебный водитель PowerShell + P/Invoke (`drv.ps1`) шлёт окнам те же сообщения, что и система — `BM_CLICK`, `WM_LBUTTONDOWN`/`UP`, `WM_CHAR` посимвольно, `WM_SYSCOMMAND`+`SC_CLOSE` вместо «крестика» — и читает результат (`GetWindowText`, `WM_GETTEXT`, `GetPixel`, `GetWindowRect`). Грабли: `SendMessage(BM_CLICK)` **зависает**, если обработчик открывает модальный `MessageBox` (нужен `PostMessage`); содержимое `EDIT` чужого процесса читается только через `WM_GETTEXT`; `$null` в string-параметрах P/Invoke надо передавать как `[NullString]::Value`.
- **Сверочные скрипты захода** (node, писать заново под главу; последние — `tests/verify_ch13.js` + `tests/check_listings_ch13.js`): (1) ID глава↔решения (множество и порядок), дословность условий, число `**Вариант N` == число `*Сравнение.*`, «Ответ» до вариантов и в каждом задании, забегания вперёд, маркеры догадок, баланс ограждений, кириллица в печатаемых строках C. (2) `check_listings` сверяет каждую содержательную строку C-листингов с файлами стенда; в версии гл. 13 добавлен пропуск листингов внутри блоков «**Условие.**» (они — дословная копия главы). Полезно: в JS `\w` не матчит кириллицу — в регулярках писать `[а-яё]`.
- **Открытые пункты для сквозной ревизии (этап 6):**
  - гл. 9: условие Г9.Ч3.З8 обещает порядок печати «`a: 3`, `bb: 2`, `ccc: 1` — в порядке корзин», фактический порядок корзин — `bb`(0), `a`(1), `ccc`(3) (счётчики верны; в решении оговорено).
  - гл. 13 (три расхождения, подробно — в конце `solutions/13-ub-and-bridge.md` и в журнале §10): (1) у `oob.c` §13.1.2 **нет** «чистого» запуска (`i = argc + 3`, `argc >= 1` ⇒ `i >= 4`; допустимы 0…3) — затронуты §13.1.4, «типичные ошибки» §13.1.2 и условие Г13.Ч1.З6; правка — `argc + 2`; (2) условие Г13.Ч1.З8 неверно утверждает несовместимость ASan и UBSan — на gcc 13.3 они совместимы, несовместимы ASan и TSan (`cc1: error: '-fsanitize=thread' is incompatible with '-fsanitize=address'`); (3) номера строк в отчётах UBSan §13.1.4 — для файлов без первой строки-комментария (проверено, столбцы совпадают всегда).
- **Полезные факты гл. 13 (журнал §10, запись 2026-07-27 (2)):** `1 << n` при n=35 из переменной даёт на x86 **8** (маска 5 бит) без предупреждений, константное `1 << 35` — **0** плюс `-Wshift-count-overflow`; UBSan **не видит** выход за границы через параметр `const int *` (ASan видит: `stack-buffer-overflow` с разметкой кадра), а промах внутри запаса ёмкости вектора (`data[5]` при `cap == 8`) не видят **оба** — только Memcheck по неинициализированному чтению; TSan сериализует выполнение так, что `race.c` печатает правильные 2 000 000 и всё равно даёт два отчёта `data race`; LeakSanitizer в ASan на Linux включён по умолчанию; целочисленные повышения (N3220 §6.3.1.1): `unsigned char x = 255; x + 1` == **256** типа `int`; порядок вычисления операндов в Rust задан слева направо (R7 «Expressions», подтверждено прогоном).
- Машинозависимые числа в решениях — с оговоркой «на эталонной машине». Программы с вводом прогоняются конвейером: `printf 'hi\n' | ./prog.exe`.
- Эталон верификации: C — MinGW GCC 15.2 `-std=c17 -Wall -Wextra` (Git Bash полный путь `/c/Users/Max/scoop/apps/gcc/current/bin/gcc.exe`; `timeout N ./prog.exe` доступен — нужен для программ, которые UB превращает в вечный цикл); make 4.4.1 и ar 2.45.1 — на PATH из Git Bash (`export PATH="/c/Users/Max/scoop/apps/gcc/current/bin:$PATH"`); Rust — rustc/cargo 1.97.0 stable gnu, одиночные файлы `rustc --edition 2024`; санитайзеры/valgrind — WSL1 (gcc 13.3, valgrind 3.22; запускать `wsl -d Ubuntu-24.04 -u root bash -c '...'` из PowerShell, скрипты писать Write + `tr -d '\r'`, потоки разводить в файлы); nightly+Miri установлен; в registry закешированы rand 0.10.2 и eframe/egui 0.35.0.
- **Открытый пункт владельца (не закрывается агентом): ручная приёмка GUI глав 12 и 26** — прокрутка, живой ввод с клавиатуры, поведение на очень больших файлах, внешний вид.
- 2026-07-22, договорённость владельца: **отдельного документа про вариативность решений не существует и не будет** — работаем по §4 п. 5 и чеклисту §6, новых критериев не вводим.

Источник истины — трекер §7 и журнал §10 в `books/c-and-rust-book/C-and-Rust-learning.md` + git-история; эта память — только указатель для быстрого входа в заход. Конвенции — `mem:conventions`, окружение — `mem:tech_stack`, команды — `mem:suggested_commands`, завершение захода — `mem:task_completion`.
