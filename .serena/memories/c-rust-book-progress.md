# c-rust-book-progress

Состояние на 2026-07-22 (заход «решения главы 1» завершён):

- **Этапы 3 и 4 (главы 1–26 + ревизии обоих томов) ЗАВЕРШЕНЫ.** Идёт **этап 5 — решения**: закрыта глава 1 (`solutions/01-intro-and-tools.md`, 2026-07-22).
- **Следующая клетка: решения главы 2** (`02-basics`), дальше строго по порядку 3→26; затем сквозная финальная ревизия и итоговый отчёт этапа 6. Один файл решений за заход.
- **Приём захода этапа 5** (отработан на главе 1): условия копируются из главы дословно (сверяется скриптом), формат §4 — условие → пошаговое решение → выделенный **Ответ** → где уместно разбор типичной неверной попытки; вторые варианты по §4 п. 5 с абзацем «Сравнение» (в главе 1 их 5 из 22 заданий). Текст русский, код — язык главы (том I — C, том II — Rust), комментарии в коде русские, печатаемые строки латиницей. Весь код прогоняется в scratchpad, в файл идут дословные выводы и сообщения компилятора.
- **Сверочный скрипт захода** (node, писать заново под главу): извлечь из главы строки `**ГN.ЧM.Зk.**` с телом условия и из решений заголовки `### ГN.ЧM.Зk` + блоки `**Условие.**`; сравнить множества ID и нормализованные тексты условий; проверить, что у каждого `**Вариант N (…).**` есть `*Сравнение.*`; поискать маркеры догадок. Для главы 1: 22/22 ID, условия дословны, 5 вариантов — все с «Сравнение».
- **Забегания вперёд проверять и в вариантах**: склейка соседних строковых литералов — материал §11.1.2, из решения Г1.Ч2.З10 убрана.
- Машинозависимые числа (объём ОЗУ, размеры файлов) в решениях приводятся с оговоркой «на эталонной машине».
- Эталон верификации: C — MinGW GCC 15.2 `-std=c17 -Wall -Wextra` (в Git Bash полный путь `/c/Users/Max/scoop/apps/gcc/current/bin/gcc.exe`); Rust — rustc/cargo 1.97.0 stable gnu, одиночные файлы **`rustc --edition 2024`**; санитайзеры/valgrind — WSL1; nightly+Miri установлен; в registry закешированы rand 0.10.2 и eframe/egui 0.35.0.
- **Открытый пункт владельца (не закрывается агентом): ручная приёмка GUI глав 12 и 26.**
- 2026-07-22, договорённость: отдельного документа про вариативность решений (`c-and-rust-tasks-expand.md`, упомянут в §4) **не будет** — новых критериев не вводим, работаем по §4 п. 5 и чеклисту §6; ссылку на него владелец убирает сам.

Источник истины — трекер §7 и журнал §10 в `books/c-and-rust-book/C-and-Rust-learning.md` + git-история; эта память — только указатель для быстрого входа в заход. Конвенции — `mem:conventions`, окружение — `mem:tech_stack`, команды — `mem:suggested_commands`, завершение захода — `mem:task_completion`.
