# c-rust-book-progress

Состояние на 2026-08-01 (заход «решения главы 25» завершён):

- **Этапы 3 и 4 (главы 1–26 + ревизии обоих томов) ЗАВЕРШЕНЫ.** Идёт **этап 5 — решения**: том I закрыт целиком (главы 1–13), в томе II закрыты главы 14–24 и **25 `capstone`** (2026-08-01). Файл `solutions/25-capstone.md`: 20 заданий (10 + 10), 166 КБ, 7 вторых вариантов (35 %); стенд — 25 cargo-пакетов и 146 файлов исходников, включая 8 файлов данных по 33 МБ.
- **Следующая клетка: решения главы 26** (`26-gui-rust-egui`, 17 заданий) — **последняя клетка этапа 5**. Дальше сквозная финальная ревизия и итоговый отчёт этапа 6. Глава 26 проектная и GUI-шная: прогон окон — служебным водителем PowerShell + P/Invoke (`drv.ps1`, приём главы 12), ручная приёмка интерфейса — открытый пункт владельца, агентом не закрывается. Забегания вперёд в главе 26 искать нечего (она последняя), зато проверять использование только материала глав 1–25.
- **Приём захода этапа 5** (отработан на главах 1–25): условия копируются из главы дословно **сборкой** — секции пишутся в scratchpad с плейсхолдерами `{{COND:ГN.ЧM.Зk}}`, `assemble.js` подставляет условия из главы (извлечение многострочное: от строки `**ID.**` до следующего `**ID.**` / `**Источники части` / заголовка; метка `**ID.**` снимается — иначе в сборнике удвоится). Формат §4: условие → пошаговое решение → выделенный **Ответ** → вторые варианты после «Ответа» (`**Вариант N (имя подхода).**` → `*Сравнение.*`) → где уместно разбор типичной неверной попытки (неверные версии реально прогоняются, вывод дословный). Доля вторых вариантов: гл. 20–23 — 30 %, гл. 24 — 28 %, гл. 25 — 35 % (проектная глава, почти каждое задание допускает второй подход).
- **Стенд проектных глав — набор проектов, а не файлов** (приём главы 20, подтверждён на 25): каждое состояние пакета живёт в отдельном каталоге (`v1_draft`/`v2_lib`/`v3_tests`/`minigrep`, `pool_v2`/`pool_whilelet`/`pool_v3`/`pargrep`), иначе листинг перестаёт быть куском реального файла. Имя пакета в копиях сохранять — тогда выводы совпадают с главой построчно. Копии для вариантов делать `cp -r` + `rm` заменяемого файла (инструмент Write не перезаписывает непрочитанные файлы).
- **Эталон тома II:** `rustc/cargo 1.97.0` stable, host `x86_64-pc-windows-gnu`, издание 2024; одиночные файлы — `rustc --edition 2024`; замеры — только `--release`. C-сторона: MinGW GCC 15.2 `-std=c17 -Wall -Wextra`. **rustc/cargo не видны на PATH инструмента Bash** — `export PATH="/c/Users/Max/.cargo/bin:$PATH"`; gcc — только полным путём `/c/Users/Max/scoop/apps/gcc/current/bin/gcc.exe`. Файлы с кириллицей писать инструментом Write. Машина: 12 ядер / 24 логических процессора.
- **Полезные факты гл. 25:** хеш тестового бинарника зависит только от имени и версии пакета (два разных `lib.rs` дали `minigrep-2382e24421bae8bf`) — независимое подтверждение находки гл. 20; переезд `String` без копии виден по `as_ptr()` (совпадает с исходным `args[1]`), а `clone` выдаёт себя ёмкостью, равной длине; `-> impl Iterator` в издании 2024 захватывает **все** времена жизни, поэтому ленивый поиск заимствует запрос (E0597 с note про правила 2024), подсказка `use<'a>` открывает второй барьер E0700, лечит только копия запроса внутрь итератора; `Receiver` не `Sync` → `Arc<Receiver>` не `Send` (E0277), `rx.clone()` — E0599; `Vec<&str>` в канал — E0597 «lifetime `'1` appears in the type of `tx`»; `unwrap_err()` требует `Debug` у **успешного** типа (E0277 на `ThreadPool`), лечится `.err().unwrap()`; `process::exit` пропускает `Drop` пула (нет строк «ждём воркера»).
- **Замеры пула (гл. 25, эталонная машина):** 8 файлов × 33 МБ, релиз, медианы трёх прогонов — 1 поток 2.47 с, 2 — 1.22 с, 4 — 699 мс, 8 — 405 мс (6.10×), 16 — 413 мс (прироста нет: задач всего 8). Сумма времён задач **не сохраняется**: 2.07 с на одном потоке против 3.00 с на восьми (каждая задача 255 → 370 мс из-за конкуренции за память).
- **Грабли прогонщика (накоплено):** конвейер `./prog.exe | head` закрывает stdout досрочно — собирать в файл; зависающие программы снимать `timeout` (код 124); при ошибке сборки старый `.exe` остаётся и запускается — удалять до `rustc`; в JS-строке `"ab\0cd"` даёт настоящий NUL и делает операционный файл бинарным для git; патчи через python-heredoc с косыми рвутся молча — писать скрипт файлом; пробник, печатающий края буфера, обязан заменять неASCII-байты на `?`, иначе в лог попадает половинка UTF-8-символа и листинг не сверить.
- **Сверочные скрипты захода** (node, писать заново под главу; последние — `verify_ch25.js` + `check_listings_ch25.js`, **не коммитятся и удаляются после захода**): ID глава↔решения, дословность условий, «Вариант N» == «Сравнение» и доля, «Ответ» в каждом задании и до вариантов, забегания вперёд, маркеры догадок, баланс ограждений, ссылки `§N.M(.k)` против реальных заголовков (**ссылки в стандарт C вида `C5 §7.24.6.1` изымать перед проверкой**), концы строк, существование файлов стенда. `check_listings` требует, чтобы каждый листинг был **непрерывным** фрагментом названного файла, а каждая строка блока вывода находилась в логах; голые числа (`echo $?`, `wc`) сверять со служебными строками протокола `--- exit=N`. Проверка ловит именно транскрипционные ошибки — в заходе 25 поймала выдуманный итог `wc -l`, сокращённый путь замыкания в E0700 и листинг `main.rs`, подписанный адресом `lib.rs`.
- **Концы строк:** в репозитории `.gitattributes` (`text=auto eol=lf`) — в индексе всё LF; рабочая копия операционного файла CRLF (`git ls-files --eol` → `i/lf w/crlf`). Правки операционного файла делать node-скриптом, вставляющим `\r\n`, и проверять, что одиночных LF осталось 0. Файлы решений и главы тома II — LF; `chapters/25-capstone.md` — CRLF.
- **Открытые пункты для сквозной ревизии (этап 6):**
  - гл. 9: условие Г9.Ч3.З8 обещает порядок печати «`a: 3`, `bb: 2`, `ccc: 1` — в порядке корзин», фактический порядок корзин — `bb`(0), `a`(1), `ccc`(3).
  - гл. 13 (три расхождения, подробно — в конце `solutions/13-ub-and-bridge.md`): (1) у `oob.c` §13.1.2 нет «чистого» запуска; (2) условие Г13.Ч1.З8 неверно утверждает несовместимость ASan и UBSan; (3) номера строк в отчётах UBSan §13.1.4.
  - гл. 14: §14.1.1 приводит вывод `rustup show` сокращённо.
  - **гл. 16 ↔ 17:** §16.2.2 обещает «передавать разбор по ссылке научимся на коллекциях (глава 17)», но глава 17 этого не вводит — приём предъявлен во втором варианте Г17.Ч1.З8.
  - **гл. 20:** §20.1.3, ⛔-пример 4 — вывод E0433 приведён без блока `help: consider importing this module through its public re-export`.
  - **гл. 21:** §21.1.1 про `add_one_v3(30u32)` — «иначе компилятору не из чего вывести тип `x`»: без суффикса программа собирается (умолчание `i32`); §21.2.2, пример 1 — при `()` из `map` rustc 1.97 печатает **два** предупреждения.
  - **гл. 22:** §22.1.1 объясняет печать `{b}` прозрачностью `Deref` — на деле за это отвечает `impl Display for Box<T>`.
  - **гл. 23:** таблица §23.2.3 ставит прочерк в столбце Sync у `Sender<T>`/`Receiver<T>`, тогда как на 1.97 `Sender<T>: Sync` есть, а у `Receiver<T>` нет.
  - **гл. 24:** (1) условие Г24.Ч2.З1 просит обосновать пометку `safe` у `labs`, но C5 §7.24.6.1 объявляет поведение неопределённым при непредставимом результате; (2) §24.2.3 приводит вывод паники на границе сокращённо; (3) терминология: §16.2.3 — «niche-оптимизация», §24.2.2 — «нишевая оптимизация».
  - **гл. 25 (новое):** условие Г25.Ч1.З7 обещает, что C-версия «напечатает больше строк, чем в файле есть (одна физическая строка выведется как две)» — невоспроизводимо: удваиваются **совпадения** (два `fputs` на одну строку), а число переводов строки вырасти не может; реальный артефакт обратного знака — склейка соседних строк. Разобрано в решении Г25.Ч1.З7 и в разделе «Уточнения» файла решений.
- Машинозависимые числа в решениях — с оговоркой «на эталонной машине». Санитайзеры/valgrind — WSL1; **WSL из инструмента Bash с путём `/mnt/c/...` ломается конверсией путей MSYS — вызывать через инструмент PowerShell** (так же читается рабочая копия стандарта C `/root/n3220.txt`).
- **Прогон GUI-программ (гл. 12 и 26):** служебный водитель PowerShell + P/Invoke (`drv.ps1`). **Открытый пункт владельца (не закрывается агентом): ручная приёмка GUI глав 12 и 26.**
- 2026-07-22, договорённость владельца: **отдельного документа про вариативность решений не существует и не будет** — работаем по §4 п. 5 и чеклисту §6.

Источник истины — трекер §7 и журнал §10 в `books/c-and-rust-book/C-and-Rust-learning.md` + git-история; эта память — только указатель для быстрого входа в заход. Конвенции — `mem:conventions`, окружение — `mem:tech_stack`, команды — `mem:suggested_commands`, завершение захода — `mem:task_completion`.
