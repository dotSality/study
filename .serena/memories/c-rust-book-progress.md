# c-rust-book-progress

Состояние на 2026-07-24 (заход «решения главы 6» завершён):

- **Этапы 3 и 4 (главы 1–26 + ревизии обоих томов) ЗАВЕРШЕНЫ.** Идёт **этап 5 — решения**: закрыты главы 1 (`solutions/01-intro-and-tools.md`, 2026-07-22), 2 (2026-07-23, 36 заданий, 8 вторых вариантов), 3 (2026-07-23, 24 задания, 6 вариантов), 4 (2026-07-23, 32 задания, 7 вариантов), 5 (2026-07-24, 22 задания, 5 вариантов, 5 разборов) и 6 (`solutions/06-pointers.md`, 2026-07-24, 30 заданий, 7 вторых вариантов, 4 разбора неверных попыток).
- **Следующая клетка: решения главы 7** (`07-memory`), дальше строго по порядку 8→26; затем сквозная финальная ревизия и итоговый отчёт этапа 6. Один файл решений за заход. ВАЖНО для гл. ≥7: код решений дополнительно прогонять ASan в WSL1 (`wsl -d Ubuntu-24.04 -u root bash -c '...'`; скрипты писать Write + `tr -d '\r'`).
- **Приём захода этапа 5** (отработан на главах 1–6): условия копируются из главы дословно (сверяется скриптом), формат §4 — условие → пошаговое решение → выделенный **Ответ** → где уместно разбор типичной неверной попытки (неверные, но не-UB версии реально прогоняются, вывод дословный); вторые варианты по §4 п. 5 с абзацем «Сравнение» (гл. 1 — 5/22, гл. 2 — 8/36, гл. 3 — 6/24, гл. 4 — 7/32, гл. 5 — 5/22, гл. 6 — 7/30, ≈22–25 %). Текст русский, код — язык главы, комментарии в коде русские, печатаемые строки латиницей. Весь код прогоняется в scratchpad, в файл идут дословные выводы и сообщения компилятора. UB-версии — только сборка, их вывод в текст не выносится; намеренные ошибки компиляции (⛔) — сборка с дословной цитатой сообщения.
- **Сверочный скрипт захода** (node, писать заново под главу; для гл. 6 — `verify_ch6.js`): извлечь из главы `**ГN.ЧM.Зk.**` с телом условия и из решений `### ГN.ЧM.Зk` + блоки `**Условие.**`; нормализовать пробелы, сравнить множества ID и тексты; число `**Вариант N` == число `*Сравнение.*` в каждом разделе; regex-поиск «глава N»/«§N.» с N > текущей. Для гл. 6 всё прошло: 30/30, условия дословны, 7 ↔ 7; forward — только главы 10 и 13 (в самой главе те же указатели вперёд), допустимо.
- **Полезные факты гл. 6 (MinGW GCC 15.2):** `int *p; *p = 42;` даёт `-Wuninitialized` **уже при `-O0`**; несовпадение `int`/`int *` (забытый `&` в вызове, `*pp = 50`) — теперь **ошибка** `[-Wint-conversion]` с подсказкой «possible fix: take the address with '&'»; `p = 0;` по параметру — `-Wunused-but-set-parameter`; запись в строковый литерал компилируется без предупреждений; ошибка записи через `const int *const r` цитируется как `'*(const int *)r'`. Адреса `%p` — «на эталонной машине», невоспроизводимы.
- Машинозависимые числа в решениях — с оговоркой «на эталонной машине». Программы с вводом прогоняются конвейером: `printf 'hi\n' | ./prog.exe`.
- Эталон верификации: C — MinGW GCC 15.2 `-std=c17 -Wall -Wextra` (Git Bash полный путь `/c/Users/Max/scoop/apps/gcc/current/bin/gcc.exe`); Rust — rustc/cargo 1.97.0 stable gnu, одиночные файлы `rustc --edition 2024`; санитайзеры/valgrind — WSL1; nightly+Miri установлен; в registry закешированы rand 0.10.2 и eframe/egui 0.35.0.
- **Открытый пункт владельца (не закрывается агентом): ручная приёмка GUI глав 12 и 26.**
- 2026-07-22, договорённость владельца: **отдельного документа про вариативность решений не существует и не будет** — работаем по §4 п. 5 и чеклисту §6, новых критериев не вводим.

Источник истины — трекер §7 и журнал §10 в `books/c-and-rust-book/C-and-Rust-learning.md` + git-история; эта память — только указатель для быстрого входа в заход. Конвенции — `mem:conventions`, окружение — `mem:tech_stack`, команды — `mem:suggested_commands`, завершение захода — `mem:task_completion`.
