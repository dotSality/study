# c-rust-book-progress

Состояние на 2026-07-28 (заход «решения главы 17» завершён):

- **Этапы 3 и 4 (главы 1–26 + ревизии обоих томов) ЗАВЕРШЕНЫ.** Идёт **этап 5 — решения**: **том I закрыт целиком** (главы 1–13), в томе II закрыты главы 14 `rust-start`, 15 `ownership` (2026-07-27), 16 `structs-enums` и 17 `collections-strings` (2026-07-28). Файл `solutions/17-collections-strings.md`: 20 заданий (10 + 10), 6 вторых вариантов, стенд из 41 файла в двух каталогах (`p1`/`p2`) + 2 программы на C.
- **Следующая клетка: решения главы 18** (`18-error-handling`). Дальше строго по порядку 19→26; затем сквозная финальная ревизия и итоговый отчёт этапа 6. Один файл решений за заход.
- **Приём захода этапа 5** (отработан на главах 1–17): условия копируются из главы дословно (сверяется скриптом), формат §4 — условие → пошаговое решение → выделенный **Ответ** → **вторые варианты после «Ответа»** (порядок Ответ → `**Вариант N (имя подхода).**` → `*Сравнение.*`) → где уместно разбор типичной неверной попытки (неверные, но собирающиеся версии реально прогоняются, вывод дословный). Доля вторых вариантов ≈22–30 %: гл. 1 — 5/22, 2 — 8/36, 3 — 6/24, 4 — 7/32, 5 — 5/22, 6 — 7/30, 7 — 5/20, 8 — 5/20, 9 — 7/30, 10 — 5/20, 11 — 5/20, 12 — 4/18, 13 — 4/17, 14 — 5/20, 15 — 7/29, 16 — 5/20, 17 — 6/20. Текст русский, код — язык главы, комментарии в коде русские, **печатаемые строки латиницей** (исключение — если кириллица задана условием или самой главой; в гл. 17 это данные «привет»/«абракадабра»/русские тексты счётчиков и три строки-зеркала §17.1.4 «байт/символов»).
- **Эталон тома II:** `rustc/cargo 1.97.0` stable, host `x86_64-pc-windows-gnu`, издание 2024. Одиночные файлы — **`rustc --edition 2024`** (голый rustc собирает в издании 2015 — ловушка), релиз — `rustc --edition 2024 -O`; проекты — `cargo new/build/run/check/clean`. **rustc/cargo не видны на PATH инструмента Bash** — прогоны вести bash-скриптом с `export PATH="/c/Users/Max/.cargo/bin:$PATH"`, сам скрипт запускать через `tr -d '\r'`; файлы `.rs` с кириллицей писать инструментом Write (UTF-8). **WSL из инструмента Bash с путём `/mnt/c/...` ломается конверсией путей MSYS — вызывать через инструмент PowerShell** (и туда же перенаправлять отчёт ASan в файл лога стенда, иначе его нечем сверять скриптом).
- **Приём сборки стенда тома II:** каталог на часть главы, один bash-скрипт на каталог, вывод в `log_*.txt`, читается одним `cat`. **Имена файлов стенда попадают в сообщения компилятора** — разводить их префиксами по частям (гл. 15: `o*`/`b*`/`s*`; гл. 16: `s*`/`m*`; гл. 17: `v*`/`h*`).
- **Сверочные скрипты захода** (node, писать заново под главу; последние — `tests/verify_ch17.js` + `tests/check_listings_ch17.js`, **не коммитятся и удаляются после захода**): (1) ID глава↔решения (множество и порядок), дословность условий (нормализация пробелов), число `**Вариант N` == число `*Сравнение.*`, «Ответ» в каждом задании и до вариантов, забегания вперёд (поиск в **коде** решений конструкций последующих глав), маркеры догадок, баланс ограждений, кириллица в печатаемых строках (по белому списку данных), перекрёстные ссылки `§N.M(.k)` против реальных заголовков глав — в исключения добавить ссылки на стандарт C (`N3220 §7.24.3.7`). (2) `check_listings` сверяет, что каждый листинг — **непрерывный фрагмент** названного файла стенда, и (с гл. 17) что **каждая строка блоков вывода найдена в логах прогона** — эта вторая проверка ловит опечатки в приведённых выводах; файл к листингу определяется как «последнее упоминание \`имя.rs\` перед блоком», поэтому в тексте перед листингом варианта не должно упоминаться имя другого файла. Грабли: в JS `\w` и `\b` не работают с кириллицей — границы задавать классом `[^а-яёА-ЯЁ]`; **файлы глав и `C-and-Rust-learning.md` в CRLF, файлы решений в LF** — правки программного файла делать node-скриптом, вставляющим `\r\n` (проверять: одиночных LF должно остаться 0).
- **Полезные факты гл. 17 (журнал §10, запись 2026-07-28 (2)):** `Vec::new()` + 6 `push` → ёмкость 0 → 4 → 8; `+` у строк **забирает буфер** левого операнда (`with_capacity(32)` → у результата тот же `cap = 32`), `s1.clone() + &s2` → `cap = 14`, `format!` → `cap = 16`; E0382 на сложении несёт `note: calling this operator moves the left-hand side`; `s1 + s2` — **E0308** `expected &str, found String`; хвост E0277 на `s[0]` включает `SliceIndex<ByteStr>`; чтобы `vec_push` из §9.1.1 позвал `realloc`, надо довести `len` до `cap` (иначе висячий указатель не воспроизводится) — под ASan `heap-use-after-free`, по C5 (N3220 §7.24.3.7) `realloc` освобождает старый объект, так что UB даже при совпадении адресов; порядок обхода `HashMap` разный в каждом запуске, ничья счётчиков не детерминирована; поиски на слово: `entry` — 1, `get`+`insert` — 2, `contains_key`+`get`+`insert` — 3; `insert` вместо `entry` в группировке молча вытесняет накопленные списки.
- **Полезные факты гл. 16 (журнал §10, запись 2026-07-28):** `enum` без `Copy` в цикле `s = s.next()` собирается; перемещение элемента массива — **E0508**; возврат поля `String` из метода с `&self` — **E0507**; перепутанные формы образцов — **E0532**; `_ => ()` среди рукавов типа `String` — **E0308**; `_ => todo!()` переносит отказ в рантайм (код 101); `..s1` отслеживается пополевно; `size_of::<Meters>()` == 8; сторона C: `enum color c = 42;` не даёт сообщений с `-Wall -Wextra`, `-Wswitch` предупреждает и собирает.
- **Полезные факты гл. 15 (журнал §10, 2026-07-27 (4)):** `drop(x)` для `Copy`-типа — предупреждение `dropping_copy_types`; починка E0106 через `'static` сменяется на **E0515**; E0382 различает `borrow of moved value` и `use of moved value`; `&a[2..7]` с константными границами компилируется и паникует, а константное `a[7]` — ошибка сборки; `realloc` реально переносит блок; два `char *` на один блок + `strcat` → ASan `heap-buffer-overflow`.
- **Полезные факты гл. 14 (журнал §10, 2026-07-27 (3)):** cargo пересобирает по mtime; `cargo new` не создаёт `Cargo.lock`; debug 4 871 636 против release 1 166 352 байта; профилем управляются только проверки переполнения.
- **Открытые пункты для сквозной ревизии (этап 6):**
  - гл. 9: условие Г9.Ч3.З8 обещает порядок печати «`a: 3`, `bb: 2`, `ccc: 1` — в порядке корзин», фактический порядок корзин — `bb`(0), `a`(1), `ccc`(3).
  - гл. 13 (три расхождения, подробно — в конце `solutions/13-ub-and-bridge.md`): (1) у `oob.c` §13.1.2 нет «чистого» запуска — правка `argc + 2`; (2) условие Г13.Ч1.З8 неверно утверждает несовместимость ASan и UBSan (несовместимы ASan и TSan); (3) номера строк в отчётах UBSan §13.1.4 — для файлов без первой строки-комментария.
  - гл. 14: §14.1.1 приводит вывод `rustup show` сокращённо.
  - гл. 15 и 16: расхождений по существу нет, только ручные переносы длинных строк `note:`/`help:` (оговорены в преамбулах решений).
  - **гл. 16 ↔ 17 (новое, 2026-07-28):** §16.2.2 обещает «передавать разбор по ссылке научимся на коллекциях (глава 17)», но глава 17 разбор по ссылке (`match` над `&Value`, режимы привязки по умолчанию) **не вводит** — в §17.1.2 только обход `&v`. В решениях приём предъявлен во втором варианте Г17.Ч1.З8 с оговоркой и ссылкой на R7.
- Машинозависимые числа в решениях — с оговоркой «на эталонной машине» (для Rust это ещё и служебный номер потока в панике). Программы с вводом прогоняются конвейером: `printf 'hi\n' | ./prog.exe`.
- Эталон тома I: C — MinGW GCC 15.2 `-std=c17 -Wall -Wextra` (Git Bash полный путь `/c/Users/Max/scoop/apps/gcc/current/bin/gcc.exe`); make 4.4.1 и ar 2.45.1 — на PATH из Git Bash; санитайзеры/valgrind — WSL1 (gcc 13.3, valgrind 3.22; `wsl -d Ubuntu-24.04 -u root bash ...`); nightly+Miri установлен; в registry закешированы rand 0.10.2 и eframe/egui 0.35.0.
- **Прогон GUI-программ (гл. 12 и 26):** служебный водитель PowerShell + P/Invoke (`drv.ps1`) шлёт окнам сообщения системы — `BM_CLICK`, `WM_LBUTTONDOWN`/`UP`, `WM_CHAR`, `WM_SYSCOMMAND`+`SC_CLOSE`. Грабли: `SendMessage(BM_CLICK)` зависает при модальном `MessageBox` (нужен `PostMessage`); содержимое `EDIT` чужого процесса читается через `WM_GETTEXT`; `$null` в string-параметрах P/Invoke — `[NullString]::Value`.
- **Открытый пункт владельца (не закрывается агентом): ручная приёмка GUI глав 12 и 26.**
- 2026-07-22, договорённость владельца: **отдельного документа про вариативность решений не существует и не будет** — работаем по §4 п. 5 и чеклисту §6.

Источник истины — трекер §7 и журнал §10 в `books/c-and-rust-book/C-and-Rust-learning.md` + git-история; эта память — только указатель для быстрого входа в заход. Конвенции — `mem:conventions`, окружение — `mem:tech_stack`, команды — `mem:suggested_commands`, завершение захода — `mem:task_completion`.
