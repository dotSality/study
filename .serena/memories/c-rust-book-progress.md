# c-rust-book-progress

Состояние на 2026-07-31 (заход «решения главы 23» завершён):

- **Этапы 3 и 4 (главы 1–26 + ревизии обоих томов) ЗАВЕРШЕНЫ.** Идёт **этап 5 — решения**: **том I закрыт целиком** (главы 1–13), в томе II закрыты главы 14 `rust-start`, 15 `ownership`, 16 `structs-enums`, 17 `collections-strings`, 18 `error-handling`, 19 `generics-traits`, 20 `modules-cargo-tests`, 21 `functional`, 22 `smart-pointers` и 23 `concurrency` (2026-07-31). Файл `solutions/23-concurrency.md`: 20 заданий (10 + 10), 6 вторых вариантов (30 %), 192 КБ; стенд из одиночных файлов `t*`/`m*` и **7 cargo-пакетов**.
- **Следующая клетка: решения главы 24** (`24-unsafe-ffi`). Дальше 25→26; затем сквозная финальная ревизия и итоговый отчёт этапа 6. Один файл решений за заход.
- **Приём захода этапа 5** (отработан на главах 1–23): условия копируются из главы дословно, формат §4 — условие → пошаговое решение → выделенный **Ответ** → **вторые варианты после «Ответа»** (порядок Ответ → `**Вариант N (имя подхода).**` → `*Сравнение.*`) → где уместно разбор типичной неверной попытки (неверные версии реально прогоняются, вывод дословный). Доля вторых вариантов ≈22–30 %: гл. 19 — 9/30, 20 — 6/20, 21 — 6/20, 22 — 6/20, 23 — 6/20. Текст русский, код — язык главы, комментарии в коде русские, **печатаемые строки латиницей** (исключение — кириллица, заданная условием или взятая из главы; в гл. 23 таких заданий девять, список — в преамбуле файла решений).
- **Условия не переписываются руками** (приём с гл. 22): секции пишутся в scratchpad с плейсхолдерами `{{COND:Г23.ЧM.Зk}}`, `assemble.js` склеивает их и подставляет условия, вырезанные регуляркой из главы (`^\*\*(ID)\.\*\*[ ]*(?:\*\(маркер\)\*[ ]*)?(текст)$`). Дословность гарантируется сборкой; `verify` оставлен как страховка.
- **Эталон тома II:** `rustc/cargo 1.97.0` stable, host `x86_64-pc-windows-gnu`, издание 2024. Одиночные файлы — **`rustc --edition 2024`**; проекты — `cargo new/build/run/test`. **rustc/cargo не видны на PATH инструмента Bash** — прогоны вести bash-скриптом с `export PATH="/c/Users/Max/.cargo/bin:$PATH"`. Файлы `.rs` с кириллицей писать инструментом Write; **`cargo new --lib` создаёт `src/lib.rs`, и Write откажется его перезаписывать без чтения — заглушки удалять до записи**. Исходники `std` доступны локально (`~/.rustup/toolchains/nightly-*/lib/rustlib/src/rust/library/std/`) — годятся как источник R9 (так найдена причина `!Send` у `MutexGuard`).
- **Специфика многопоточной главы (23):** вывод недетерминирован, поэтому (1) прогоны повторяются пачками, а вместо «вывода» приводится **распределение исходов** (300 прогонов дали 3 легальных вывода из 4 предсказанных; 60 прогонов без `join` — 24/20/16 по числу выживших строк); (2) зависающие программы снимаются `timeout 5` (в протоколе `--- run exit=124 ---`); (3) **конвейер `./prog.exe | head -3` ломает опыт** — `head` закрывает stdout, потоки паникуют `failed printing to stdout: The pipe is being closed. (os error 232)`; писать вывод в файл и резать уже файл; (4) логическая гонка воспроизводится детерминированно расширением окна (пауза между проверкой и списанием) и оформляется `#[ignore]`-тестом.
- **Полезные факты гл. 23 (журнал §10, запись 2026-07-31):** `Sender<T>` — **Sync**, `Receiver<T>` — **не Sync** (прочерк в таблице §23.2.3 — открытый пункт этапа 6); `Send` у концов канала наследуется от груза (`Sender<Rc<i32>>` не Send). Debug-печать `Mutex` **не блокирует**: при занятом замке `data: "<locked>"`. `MutexGuard` не Send из-за требования pthreads освобождать мьютекс в том же потоке (формулировка — в исходниках std), но Sync при Sync-содержимом. `Arc::strong_count` после `join` = 1 (в §22.2.2 было 3): клоны умерли вместе с потоками.
- **Сверочные скрипты захода** (node, писать заново под главу; последние — `verify_ch23.js` + `check_listings_ch23.js`, **не коммитятся и удаляются после захода**): ID глава↔решения, дословность условий, число `**Вариант N` == число `*Сравнение.*` и доля, «Ответ» в каждом задании и до вариантов, забегания вперёд (для гл. 24 искать в коде решений `egui`/`eframe` и конструкции гл. 25–26), маркеры догадок, баланс ограждений, кириллица в печатаемых строках, **перекрёстные ссылки `§N.M(.k)` против реальных заголовков глав** (поймала `§7.2.3` вместо `§7.2.1`), концы строк, существование упомянутых файлов стенда. `check_listings` сверяет, что каждый листинг — непрерывный фрагмент файла стенда и что каждая строка блоков вывода найдена в логах; схемы помечаются в тексте фразой «не вывод программы» и пропускаются. Грабли: в JS `\w`/`\b` не работают с кириллицей; тройные бэктики нельзя класть в template literal.
- **Концы строк:** операционный файл `C-and-Rust-learning.md` и главы тома I — CRLF; главы 21–23 — LF (не универсально!), файлы решений — LF. Правки операционного файла делать node-скриптом, вставляющим `\r\n`, и проверять, что одиночных LF осталось 0.
- **Открытые пункты для сквозной ревизии (этап 6):**
  - гл. 9: условие Г9.Ч3.З8 обещает порядок печати «`a: 3`, `bb: 2`, `ccc: 1` — в порядке корзин», фактический порядок корзин — `bb`(0), `a`(1), `ccc`(3).
  - гл. 13 (три расхождения, подробно — в конце `solutions/13-ub-and-bridge.md`): (1) у `oob.c` §13.1.2 нет «чистого» запуска; (2) условие Г13.Ч1.З8 неверно утверждает несовместимость ASan и UBSan; (3) номера строк в отчётах UBSan §13.1.4.
  - гл. 14: §14.1.1 приводит вывод `rustup show` сокращённо.
  - **гл. 16 ↔ 17:** §16.2.2 обещает «передавать разбор по ссылке научимся на коллекциях (глава 17)», но глава 17 этого не вводит — приём предъявлен во втором варианте Г17.Ч1.З8.
  - **гл. 20:** §20.1.3, ⛔-пример 4 — вывод E0433 приведён без блока `help: consider importing this module through its public re-export`.
  - **гл. 21:** §21.1.1 про `add_one_v3(30u32)` — «иначе компилятору не из чего вывести тип `x`»: без суффикса программа собирается (умолчание `i32`); §21.2.2, пример 1 — при `()` из `map` rustc 1.97 печатает **два** предупреждения.
  - **гл. 22:** §22.1.1 объясняет печать `{b}` прозрачностью `Deref` — на деле за это отвечает `impl Display for Box<T>` (проверено E0277 на обёртке с одним `Deref`).
  - **гл. 23 (новое):** таблица §23.2.3 ставит прочерк в столбце Sync у `Sender<T>`/`Receiver<T>`, тогда как на 1.97 `Sender<T>: Sync` есть, а у `Receiver<T>` нет; подпись под таблицей обещает, что «каждая клетка подтверждена этой программой». Разобрано в решении Г23.Ч2.З6 и в разделе «Уточнения к тексту главы» в конце файла решений.
- Машинозависимые числа в решениях — с оговоркой «на эталонной машине». Эталон тома I: C — MinGW GCC 15.2 `-std=c17 -Wall -Wextra`; санитайзеры/valgrind — WSL1; **WSL из инструмента Bash с путём `/mnt/c/...` ломается конверсией путей MSYS — вызывать через инструмент PowerShell.**
- **Прогон GUI-программ (гл. 12 и 26):** служебный водитель PowerShell + P/Invoke (`drv.ps1`) шлёт окнам сообщения системы. **Открытый пункт владельца (не закрывается агентом): ручная приёмка GUI глав 12 и 26.**
- 2026-07-22, договорённость владельца: **отдельного документа про вариативность решений не существует и не будет** — работаем по §4 п. 5 и чеклисту §6.

Источник истины — трекер §7 и журнал §10 в `books/c-and-rust-book/C-and-Rust-learning.md` + git-история; эта память — только указатель для быстрого входа в заход. Конвенции — `mem:conventions`, окружение — `mem:tech_stack`, команды — `mem:suggested_commands`, завершение захода — `mem:task_completion`.
